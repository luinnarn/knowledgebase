export default function GraphPage() {
  return (
    <section style={{ padding: 'var(--sp-6)' }}>
      <h1>Knowledge Graph</h1>
      <p>Interactive graph lands in a later task.</p>
    </section>
  )
}

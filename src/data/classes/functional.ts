import type { JavaClass } from '../../types/content'
import { jc } from './util'

export const classes: JavaClass[] = [
  jc({
    fqcn: 'java.util.stream.Stream',
    module: 'java.base',
    kind: 'interface',
    since: '1.8',
    summary: 'A lazy pipeline of elements: intermediate operations (map/filter/flatMap) describe, one terminal operation (collect/reduce/count…) executes. Single-use; never mutates its source.',
    declaration: 'public interface Stream<T> extends BaseStream<T, Stream<T>>',
    methods: [
      { signature: 'Stream<R> map(Function<T,R>) / Stream<T> filter(Predicate<T>)', desc: 'The transform/select pair.' },
      { signature: 'Stream<R> flatMap(Function<T, Stream<R>>)', desc: '1→many + flatten.' },
      { signature: 'Stream<T> sorted() / distinct() / limit(n) / skip(n)', desc: 'Stateful shapers.' },
      { signature: 'Stream<T> takeWhile / dropWhile(Predicate<T>)', desc: 'Cut ordered streams at a condition (Java 9).' },
      { signature: '<R,A> R collect(Collector<T,A,R>)', desc: 'Mutable reduction — the main terminal.' },
      { signature: 'Optional<T> reduce(BinaryOperator<T>) / T reduce(T, BinaryOperator<T>)', desc: 'Fold with an associative op.' },
      { signature: 'boolean anyMatch/allMatch/noneMatch(Predicate<T>)', desc: 'Short-circuit predicates.' },
      { signature: 'Optional<T> findFirst() / findAny()', desc: 'Short-circuit retrieval.' },
      { signature: 'List<T> toList()', desc: 'Unmodifiable list (Java 16) — the everyday terminal.' },
      { signature: 'static <T> Stream<T> of(T...) / iterate(seed, f) / generate(s)', desc: 'Sources.' },
    ],
    pitfalls: [
      'A stream can be consumed once — a second terminal throws IllegalStateException.',
      'Side effects in map/filter break under parallel() — keep pipeline functions pure.',
    ],
    related: ['topic:stream-pipeline', 'java.util.stream.Collectors', 'java.util.stream.IntStream'],
  }),
  jc({
    fqcn: 'java.util.stream.IntStream',
    module: 'java.base',
    kind: 'interface',
    since: '1.8',
    summary: 'Primitive int pipeline: no boxing, plus numeric terminals (sum, average, summaryStatistics) and range() as the modern indexed loop.',
    declaration: 'public interface IntStream extends BaseStream<Integer, IntStream>',
    methods: [
      { signature: 'static IntStream range(int start, int end)', desc: 'Half-open [start, end) — the for-loop replacement.' },
      { signature: 'static IntStream rangeClosed(int start, int end)', desc: 'Inclusive end.' },
      { signature: 'int sum() / OptionalDouble average() / OptionalInt max()', desc: 'Built-in numeric terminals.' },
      { signature: 'IntSummaryStatistics summaryStatistics()', desc: 'Count/sum/min/max/avg in one pass.' },
      { signature: 'Stream<Integer> boxed()', desc: 'Bridge to the object stream (allocates!).' },
      { signature: 'IntStream mapToInt … / mapToObj(IntFunction<R>)', desc: 'Bridges in and out.' },
    ],
    related: ['topic:primitive-streams', 'java.util.stream.Stream'],
  }),
  jc({
    fqcn: 'java.util.stream.Collectors',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'The standard Collector recipes: containers (toList/toSet/toMap), strings (joining), statistics, and the grouping/partitioning family with composable downstreams.',
    declaration: 'public final class Collectors',
    methods: [
      { signature: 'static Collector toList() / toSet() / toUnmodifiableList()', desc: 'Container terminals.' },
      { signature: 'static Collector toMap(keyFn, valueFn[, mergeFn])', desc: 'Throws on duplicate keys without a merge function!' },
      { signature: 'static Collector groupingBy(classifier[, downstream])', desc: 'SQL GROUP BY — Map<K, List<T>> or aggregated.' },
      { signature: 'static Collector partitioningBy(predicate[, downstream])', desc: 'Boolean split, both keys guaranteed.' },
      { signature: 'static Collector joining(delim, prefix, suffix)', desc: 'Delimited strings.' },
      { signature: 'static Collector counting() / summingInt(…) / averagingDouble(…)', desc: 'Downstream aggregates.' },
      { signature: 'static Collector mapping(fn, downstream) / filtering(p, downstream)', desc: 'Adapt elements before a downstream.' },
      { signature: 'static Collector teeing(c1, c2, merger)', desc: 'Two collectors, one pass (Java 12).' },
    ],
    example: {
      code: 'Map<String, Long> byDept = staff.stream()\n        .collect(groupingBy(Employee::dept, counting()));',
    },
    related: ['topic:collectors', 'java.util.stream.Stream'],
  }),
  jc({
    fqcn: 'java.util.function.Function',
    module: 'java.base',
    kind: 'interface',
    since: '1.8',
    summary: 'T → R transformation, the most general functional shape. Composes with andThen/compose; identity() is the no-op.',
    declaration: '@FunctionalInterface\npublic interface Function<T, R>',
    methods: [
      { signature: 'R apply(T t)', desc: 'The transformation.' },
      { signature: 'default <V> Function<T,V> andThen(Function<R,V> after)', desc: 'this first, then after.' },
      { signature: 'default <V> Function<V,R> compose(Function<V,T> before)', desc: 'before first, then this.' },
      { signature: 'static <T> Function<T,T> identity()', desc: 'x → x (useful in toMap).' },
    ],
    related: ['topic:functional-interfaces', 'java.util.function.BiFunction'],
  }),
  jc({
    fqcn: 'java.util.function.BiFunction',
    module: 'java.base',
    kind: 'interface',
    since: '1.8',
    summary: '(T, U) → R — the two-argument Function, seen in Map.merge and compute. BinaryOperator<T> is its same-type specialization.',
    declaration: '@FunctionalInterface\npublic interface BiFunction<T, U, R>',
    methods: [{ signature: 'R apply(T t, U u)', desc: 'The transformation.' }],
    related: ['java.util.function.Function', 'topic:functional-interfaces'],
  }),
  jc({
    fqcn: 'java.util.function.Predicate',
    module: 'java.base',
    kind: 'interface',
    since: '1.8',
    summary: 'T → boolean test, the filter shape. Combines logically with and/or/negate — validation rules as data.',
    declaration: '@FunctionalInterface\npublic interface Predicate<T>',
    methods: [
      { signature: 'boolean test(T t)', desc: 'The test.' },
      { signature: 'default Predicate<T> and(p) / or(p) / negate()', desc: 'Boolean algebra over predicates.' },
      { signature: 'static <T> Predicate<T> not(Predicate<T>)', desc: 'Readable negation for method refs (Java 11).' },
      { signature: 'static <T> Predicate<T> isEqual(Object target)', desc: 'Equality test.' },
    ],
    related: ['topic:functional-interfaces', 'java.util.stream.Stream'],
  }),
  jc({
    fqcn: 'java.util.function.Consumer',
    module: 'java.base',
    kind: 'interface',
    since: '1.8',
    summary: 'T → void side-effecting action: forEach bodies, callbacks, builders. Chain with andThen.',
    declaration: '@FunctionalInterface\npublic interface Consumer<T>',
    methods: [
      { signature: 'void accept(T t)', desc: 'The action.' },
      { signature: 'default Consumer<T> andThen(Consumer<T> after)', desc: 'Sequential composition.' },
    ],
    related: ['topic:functional-interfaces', 'java.lang.Iterable'],
  }),
  jc({
    fqcn: 'java.util.function.Supplier',
    module: 'java.base',
    kind: 'interface',
    since: '1.8',
    summary: '() → T lazy producer: deferred construction, defaults (orElseGet), factories, and memoization building block.',
    declaration: '@FunctionalInterface\npublic interface Supplier<T>',
    methods: [{ signature: 'T get()', desc: 'Produce the value — evaluated only when called (the whole point).' }],
    related: ['topic:functional-interfaces', 'java.util.Optional'],
  }),
  jc({
    fqcn: 'java.util.function.UnaryOperator',
    module: 'java.base',
    kind: 'interface',
    since: '1.8',
    summary: 'T → T same-type transform — Function specialized for in-kind updates (List.replaceAll, AtomicReference.updateAndGet).',
    declaration: '@FunctionalInterface\npublic interface UnaryOperator<T> extends Function<T, T>',
    methods: [{ signature: 'static <T> UnaryOperator<T> identity()', desc: 'x → x.' }],
    related: ['java.util.function.Function', 'topic:functional-interfaces'],
  }),
  jc({
    fqcn: 'java.util.function.BinaryOperator',
    module: 'java.base',
    kind: 'interface',
    since: '1.8',
    summary: '(T, T) → T combiner — the reduce/merge shape. minBy/maxBy build comparator-based combiners.',
    declaration: '@FunctionalInterface\npublic interface BinaryOperator<T> extends BiFunction<T, T, T>',
    methods: [
      { signature: 'static <T> BinaryOperator<T> minBy/maxBy(Comparator<T>)', desc: 'Pick-one combiners.' },
    ],
    related: ['java.util.stream.Stream', 'topic:functional-interfaces'],
  }),
]

import type { JavaClass } from '../../types/content'
import { jc } from './util'

export const classes: JavaClass[] = [
  jc({
    fqcn: 'java.io.InputStream',
    module: 'java.base',
    kind: 'class',
    since: '1.0',
    summary: 'The abstract byte source: read() returns 0–255 or −1 at end. Everything binary flows through it, usually wrapped in a BufferedInputStream.',
    declaration: 'public abstract class InputStream implements Closeable',
    methods: [
      { signature: 'int read()', desc: 'One byte (as int) or −1 at end of stream.' },
      { signature: 'int read(byte[] b)', desc: 'May fill FEWER bytes than requested — loop or use readNBytes.' },
      { signature: 'byte[] readAllBytes()', desc: 'Slurp the rest (small payloads).' },
      { signature: 'long transferTo(OutputStream out)', desc: 'Pipe everything downstream (Java 9).' },
      { signature: 'static InputStream nullInputStream()', desc: 'An empty stream for tests/defaults.' },
    ],
    related: ['java.io.OutputStream', 'topic:io-streams', 'java.io.BufferedReader'],
  }),
  jc({
    fqcn: 'java.io.OutputStream',
    module: 'java.base',
    kind: 'class',
    since: '1.0',
    summary: 'The abstract byte sink. Buffer it, write, flush before checking results, close via try-with-resources (which flushes).',
    declaration: 'public abstract class OutputStream implements Closeable, Flushable',
    methods: [
      { signature: 'void write(int b) / write(byte[] b, int off, int len)', desc: 'Byte and block writes.' },
      { signature: 'void flush()', desc: 'Push buffered bytes down the chain.' },
    ],
    related: ['java.io.InputStream', 'topic:io-streams'],
  }),
  jc({
    fqcn: 'java.io.Reader',
    module: 'java.base',
    kind: 'class',
    since: '1.1',
    summary: 'The abstract character source — bytes decoded through a Charset. InputStreamReader bridges from bytes; BufferedReader adds buffering and readLine.',
    declaration: 'public abstract class Reader implements Readable, Closeable',
    methods: [
      { signature: 'int read()', desc: 'One char (as int) or −1.' },
      { signature: 'int read(char[] cbuf)', desc: 'Block read.' },
    ],
    related: ['java.io.BufferedReader', 'topic:readers-writers'],
  }),
  jc({
    fqcn: 'java.io.Writer',
    module: 'java.base',
    kind: 'class',
    since: '1.1',
    summary: 'The abstract character sink. OutputStreamWriter encodes to bytes; PrintWriter adds println/printf conveniences.',
    declaration: 'public abstract class Writer implements Appendable, Closeable, Flushable',
    methods: [
      { signature: 'void write(String str)', desc: 'The common case.' },
      { signature: 'Writer append(CharSequence csq)', desc: 'Chaining-friendly.' },
    ],
    related: ['java.io.PrintWriter', 'topic:readers-writers'],
  }),
  jc({
    fqcn: 'java.io.BufferedReader',
    module: 'java.base',
    kind: 'class',
    since: '1.1',
    summary: 'Buffered character reading plus readLine() and the lazy lines() stream — the standard way to consume text line by line.',
    declaration: 'public class BufferedReader extends Reader',
    methods: [
      { signature: 'String readLine()', desc: 'Next line without its terminator; null at end.' },
      { signature: 'Stream<String> lines()', desc: 'Lazy line stream — close the reader (try-with-resources).' },
    ],
    related: ['java.nio.file.Files', 'topic:readers-writers'],
  }),
  jc({
    fqcn: 'java.io.PrintWriter',
    module: 'java.base',
    kind: 'class',
    since: '1.1',
    summary: 'Convenient formatted text output: print/println/printf. Swallows I/O errors by design (checkError() to ask) — fine for console, deliberate choice elsewhere.',
    declaration: 'public class PrintWriter extends Writer',
    methods: [
      { signature: 'void println(Object x)', desc: 'Value + platform line separator.' },
      { signature: 'PrintWriter printf(String format, Object... args)', desc: 'Formatted output.' },
      { signature: 'boolean checkError()', desc: 'The only way to learn of write failures.' },
    ],
    related: ['java.io.Writer', 'topic:readers-writers'],
  }),
  jc({
    fqcn: 'java.util.Scanner',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'Tokenizing text parser over any source: nextInt/nextLine/hasNext, regex-delimited. The standard beginner input tool with one famous newline gotcha.',
    declaration: 'public final class Scanner implements Iterator<String>, Closeable',
    methods: [
      { signature: 'boolean hasNextInt() / int nextInt()', desc: 'Typed token pairs — test then read.' },
      { signature: 'String nextLine()', desc: 'Rest of the current line.' },
      { signature: 'Scanner useDelimiter(String pattern)', desc: 'Custom token boundaries.' },
    ],
    pitfalls: ['nextInt() leaves the newline unconsumed — the following nextLine() returns "".'],
    related: ['java.io.BufferedReader', 'topic:readers-writers'],
  }),
  jc({
    fqcn: 'java.nio.file.Path',
    module: 'java.base',
    kind: 'interface',
    since: '1.7',
    summary: 'An immutable filesystem location with path algebra: resolve (join), relativize (invert), normalize (clean). The modern replacement for File\'s path half.',
    declaration: 'public interface Path extends Comparable<Path>, Iterable<Path>, Watchable',
    methods: [
      { signature: 'static Path of(String first, String... more)', desc: 'Create from segments.' },
      { signature: 'Path resolve(String other)', desc: 'Join paths.' },
      { signature: 'Path relativize(Path other)', desc: 'The inverse: this → other as a relative path.' },
      { signature: 'Path normalize()', desc: 'Remove . and .. segments.' },
      { signature: 'Path getParent() / getFileName()', desc: 'Structural accessors.' },
    ],
    related: ['java.nio.file.Files', 'topic:files-paths-nio'],
  }),
  jc({
    fqcn: 'java.nio.file.Files',
    module: 'java.base',
    kind: 'class',
    since: '1.7',
    summary: 'Every file operation as a static method: read/write whole files, copy/move with options, walk trees, create/delete, query metadata. The workhorse of java.nio.file.',
    declaration: 'public final class Files',
    methods: [
      { signature: 'static String readString(Path) / static Path writeString(Path, CharSequence)', desc: 'Whole-file text I/O (UTF-8 default).' },
      { signature: 'static List<String> readAllLines(Path) / Stream<String> lines(Path)', desc: 'Eager / lazy line access (close the stream!).' },
      { signature: 'static Path copy(Path, Path, CopyOption...) / move(...)', desc: 'REPLACE_EXISTING, ATOMIC_MOVE options.' },
      { signature: 'static Stream<Path> walk(Path) / list(Path)', desc: 'Recursive / single-level listing — resource-holding streams.' },
      { signature: 'static boolean exists(Path) / isRegularFile / isDirectory', desc: 'Probes.' },
      { signature: 'static Path createDirectories(Path)', desc: 'mkdir -p.' },
      { signature: 'static BufferedReader newBufferedReader(Path, Charset)', desc: 'Reader with explicit encoding.' },
    ],
    pitfalls: ['walk/list/lines hold OS directory/file handles — always try-with-resources, or descriptors leak under load.'],
    related: ['java.nio.file.Path', 'topic:files-paths-nio'],
  }),
  jc({
    fqcn: 'java.nio.ByteBuffer',
    module: 'java.base',
    kind: 'class',
    since: '1.4',
    summary: 'The NIO byte container with the position/limit/capacity state machine: fill → flip() → drain → clear. Heap or direct; typed getters/putters with configurable byte order.',
    declaration: 'public abstract class ByteBuffer extends Buffer\n        implements Comparable<ByteBuffer>',
    methods: [
      { signature: 'static ByteBuffer allocate(int) / allocateDirect(int)', desc: 'Heap vs off-heap (direct: no copy for I/O, pricier to create).' },
      { signature: 'Buffer flip()', desc: 'Switch from writing to reading — the forgotten call behind half of NIO bugs.' },
      { signature: 'Buffer clear() / compact()', desc: 'Reset for refilling (clear discards; compact keeps unread bytes).' },
      { signature: 'int getInt() / putInt(int)', desc: 'Typed access at the position (all primitives).' },
      { signature: 'ByteBuffer order(ByteOrder bo)', desc: 'Endianness — default BIG_ENDIAN; most native formats are little.' },
    ],
    related: ['java.nio.channels.FileChannel', 'topic:binary-data-buffers'],
  }),
  jc({
    fqcn: 'java.nio.channels.FileChannel',
    module: 'java.base',
    kind: 'class',
    since: '1.4',
    summary: 'Block-oriented file access: positional (thread-safe) reads/writes, memory mapping, locking, and zero-copy transferTo — the substrate of high-performance file I/O.',
    declaration: 'public abstract class FileChannel extends AbstractInterruptibleChannel\n        implements SeekableByteChannel, GatheringByteChannel, ScatteringByteChannel',
    methods: [
      { signature: 'static FileChannel open(Path, OpenOption...)', desc: 'READ/WRITE/CREATE/APPEND options.' },
      { signature: 'int read(ByteBuffer dst, long position)', desc: 'Positional read — no seek, thread-safe.' },
      { signature: 'MappedByteBuffer map(MapMode, long pos, long size)', desc: 'Memory-map a region.' },
      { signature: 'long transferTo(long pos, long count, WritableByteChannel)', desc: 'Zero-copy file → socket.' },
      { signature: 'FileLock lock()', desc: 'Advisory cross-process file locking.' },
    ],
    related: ['java.nio.ByteBuffer', 'topic:binary-data-buffers'],
  }),
  jc({
    fqcn: 'java.nio.charset.StandardCharsets',
    module: 'java.base',
    kind: 'class',
    since: '1.7',
    summary: 'Constants for the guaranteed charsets — UTF_8 above all. Passing them explicitly at every text/byte boundary is encoding hygiene.',
    declaration: 'public final class StandardCharsets',
    points: ['UTF_8, US_ASCII, ISO_8859_1, UTF_16 and variants — always present on every JVM'],
    related: ['topic:readers-writers', 'java.lang.String'],
  }),
  jc({
    fqcn: 'java.net.Socket',
    module: 'java.base',
    kind: 'class',
    since: '1.0',
    summary: 'A TCP client connection: connect, then read/write its streams. Production rule: connect and read timeouts on every socket, no exceptions.',
    declaration: 'public class Socket implements Closeable',
    methods: [
      { signature: 'void connect(SocketAddress endpoint, int timeoutMs)', desc: 'Bounded connection attempt.' },
      { signature: 'void setSoTimeout(int timeoutMs)', desc: 'Bound each read — otherwise a silent peer hangs you forever.' },
      { signature: 'InputStream getInputStream() / OutputStream getOutputStream()', desc: 'The byte streams.' },
    ],
    related: ['java.net.ServerSocket', 'topic:sockets-networking'],
  }),
  jc({
    fqcn: 'java.net.ServerSocket',
    module: 'java.base',
    kind: 'class',
    since: '1.0',
    summary: 'The TCP listener: bind a port, accept() connections in a loop, hand each Socket to a worker — one virtual thread per connection in modern designs.',
    declaration: 'public class ServerSocket implements Closeable',
    methods: [
      { signature: 'ServerSocket(int port)', desc: 'Bind (0 = ephemeral).' },
      { signature: 'Socket accept()', desc: 'Block until a client connects.' },
    ],
    related: ['java.net.Socket', 'topic:sockets-networking', 'topic:virtual-threads'],
  }),
  jc({
    fqcn: 'java.net.URI',
    module: 'java.base',
    kind: 'class',
    since: '1.4',
    summary: 'Parsed, immutable resource identifier — scheme, host, path, query — with proper escaping rules. The type HttpClient and modern APIs want.',
    declaration: 'public final class URI implements Comparable<URI>, Serializable',
    methods: [
      { signature: 'static URI create(String str)', desc: 'Parse (unchecked exception on syntax errors).' },
      { signature: 'String getHost() / getPath() / getQuery()', desc: 'Components.' },
      { signature: 'URI resolve(String str)', desc: 'Relative reference resolution.' },
    ],
    related: ['java.net.http.HttpRequest', 'topic:http-client'],
  }),
  jc({
    fqcn: 'java.net.http.HttpClient',
    module: 'java.net.http',
    kind: 'class',
    since: '11',
    summary: 'The modern built-in HTTP client: HTTP/2, connection pooling, sync send or async sendAsync via CompletableFuture. Build once, share application-wide.',
    declaration: 'public abstract class HttpClient implements AutoCloseable',
    methods: [
      { signature: 'static HttpClient.Builder newBuilder()', desc: 'connectTimeout, followRedirects, executor, proxy…' },
      { signature: '<T> HttpResponse<T> send(HttpRequest, BodyHandler<T>)', desc: 'Blocking exchange.' },
      { signature: '<T> CompletableFuture<HttpResponse<T>> sendAsync(…)', desc: 'Non-blocking exchange.' },
    ],
    pitfalls: ['A new client per request forfeits pooling and leaks threads — share one instance (it is thread-safe).'],
    related: ['java.net.http.HttpRequest', 'topic:http-client', 'java.util.concurrent.CompletableFuture'],
  }),
  jc({
    fqcn: 'java.net.http.HttpRequest',
    module: 'java.net.http',
    kind: 'class',
    since: '11',
    summary: 'An immutable HTTP request assembled by builder: method, URI, headers, timeout, and a BodyPublisher for uploads.',
    declaration: 'public abstract class HttpRequest',
    methods: [
      { signature: 'static Builder newBuilder(URI uri)', desc: 'GET by default.' },
      { signature: 'Builder POST(BodyPublisher body) / header(String, String) / timeout(Duration)', desc: 'The usual assembly line.' },
      { signature: 'static BodyPublishers.ofString(String) / ofFile(Path)', desc: 'Upload sources.' },
    ],
    related: ['java.net.http.HttpClient', 'java.net.http.HttpResponse'],
  }),
  jc({
    fqcn: 'java.net.http.HttpResponse',
    module: 'java.net.http',
    kind: 'interface',
    since: '11',
    summary: 'The exchange result: statusCode(), headers(), and a body of whatever type the BodyHandler produced. Non-2xx does not throw — check the code.',
    declaration: 'public interface HttpResponse<T>',
    methods: [
      { signature: 'int statusCode()', desc: 'Check it — 404 arrives as a normal response.' },
      { signature: 'T body()', desc: 'String, Path, InputStream, Stream<String>… per handler.' },
      { signature: 'static BodyHandlers.ofString() / ofFile(Path) / ofInputStream()', desc: 'The standard handlers.' },
    ],
    related: ['java.net.http.HttpClient', 'topic:http-client'],
  }),
  jc({
    fqcn: 'java.util.regex.Pattern',
    module: 'java.base',
    kind: 'class',
    since: '1.4',
    summary: 'A compiled regular expression — compile once as a static final constant, then hand out Matchers. Flags, named groups, and stream-friendly splitting included.',
    declaration: 'public final class Pattern implements Serializable',
    methods: [
      { signature: 'static Pattern compile(String regex, int flags)', desc: 'CASE_INSENSITIVE, MULTILINE, DOTALL…' },
      { signature: 'Matcher matcher(CharSequence input)', desc: 'The matching engine (not thread-safe; Pattern is).' },
      { signature: 'String[] split(CharSequence) / Stream<String> splitAsStream(…)', desc: 'Regex splitting.' },
      { signature: 'static String quote(String s)', desc: 'Escape a literal for embedding in a regex.' },
      { signature: 'Predicate<String> asMatchPredicate()', desc: 'Whole-string predicate for filter chains.' },
    ],
    related: ['java.util.regex.Matcher', 'topic:regex'],
  }),
  jc({
    fqcn: 'java.util.regex.Matcher',
    module: 'java.base',
    kind: 'class',
    since: '1.4',
    summary: 'The stateful matching engine: matches() (whole input) vs find() (next occurrence), group access by index or name, and replace operations.',
    declaration: 'public final class Matcher implements MatchResult',
    methods: [
      { signature: 'boolean matches()', desc: 'Entire input must match.' },
      { signature: 'boolean find()', desc: 'Advance to the next match — loop it.' },
      { signature: 'String group(int n) / group(String name)', desc: 'Captured text (group 0 = whole match).' },
      { signature: 'String replaceAll(String replacement)', desc: '$1 references groups; Matcher.quoteReplacement for literals.' },
    ],
    related: ['java.util.regex.Pattern', 'topic:regex'],
  }),
]

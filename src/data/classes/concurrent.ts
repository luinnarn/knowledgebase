import type { JavaClass } from '../../types/content'
import { jc } from './util'

export const classes: JavaClass[] = [
  jc({
    fqcn: 'java.util.concurrent.ExecutorService',
    module: 'java.base',
    kind: 'interface',
    since: '1.5',
    summary: 'Task-execution service: submit Callables/Runnables, get Futures back, and shut down cleanly. The abstraction that separates what runs from how it runs (EJ 80).',
    declaration: 'public interface ExecutorService extends Executor, AutoCloseable',
    methods: [
      { signature: '<T> Future<T> submit(Callable<T> task)', desc: 'Run and get a result handle.' },
      { signature: '<T> List<Future<T>> invokeAll(Collection<Callable<T>>)', desc: 'Run all, wait for all.' },
      { signature: 'void shutdown() / List<Runnable> shutdownNow()', desc: 'Graceful stop-accepting / interrupt-and-drain.' },
      { signature: 'boolean awaitTermination(long, TimeUnit)', desc: 'Wait for in-flight tasks after shutdown.' },
      { signature: 'void close()', desc: 'AutoCloseable (Java 19): shutdown + await — try-with-resources pools.' },
    ],
    pitfalls: ['Forgetting shutdown leaks non-daemon threads and keeps the JVM alive.'],
    related: ['java.util.concurrent.Executors', 'topic:executors-thread-pools', 'java.util.concurrent.Future'],
  }),
  jc({
    fqcn: 'java.util.concurrent.Executors',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'Factory methods for standard executor configurations — fixed pools, scheduled pools, single-thread serializers, and the Java 21 virtual-thread-per-task executor.',
    declaration: 'public class Executors',
    methods: [
      { signature: 'static ExecutorService newFixedThreadPool(int n)', desc: 'N threads, UNBOUNDED queue — mind overload behavior.' },
      { signature: 'static ExecutorService newVirtualThreadPerTaskExecutor()', desc: 'One cheap virtual thread per task (Java 21) — the I/O-workload default.' },
      { signature: 'static ScheduledExecutorService newScheduledThreadPool(int n)', desc: 'Delayed and periodic tasks.' },
      { signature: 'static ExecutorService newSingleThreadExecutor()', desc: 'Serialized execution — an actor-lite.' },
      { signature: 'static ExecutorService newCachedThreadPool()', desc: 'Unbounded thread growth — dangerous under load; largely superseded.' },
    ],
    related: ['java.util.concurrent.ExecutorService', 'topic:virtual-threads', 'java.util.concurrent.ThreadPoolExecutor'],
  }),
  jc({
    fqcn: 'java.util.concurrent.ThreadPoolExecutor',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'The configurable pool behind the factories: core/max size, keep-alive, work queue, thread factory, and rejection policy — every production pool decision in one constructor.',
    declaration: 'public class ThreadPoolExecutor extends AbstractExecutorService',
    methods: [
      { signature: 'ThreadPoolExecutor(core, max, keepAlive, unit, queue, factory, handler)', desc: 'The full policy constructor.' },
      { signature: 'int getActiveCount() / long getCompletedTaskCount()', desc: 'Monitoring hooks.' },
      { signature: 'CallerRunsPolicy / AbortPolicy / DiscardPolicy', desc: 'Rejection handlers — CallerRuns gives natural backpressure.' },
    ],
    points: ['Bound the queue + choose a rejection policy, or overload becomes an OOM instead of a 503'],
    related: ['topic:executors-thread-pools', 'java.util.concurrent.ArrayBlockingQueue'],
  }),
  jc({
    fqcn: 'java.util.concurrent.ScheduledExecutorService',
    module: 'java.base',
    kind: 'interface',
    since: '1.5',
    summary: 'Delayed and periodic execution: schedule one-shots, fixed-rate (clock-aligned) or fixed-delay (gap-aligned) repeating tasks. The modern Timer.',
    declaration: 'public interface ScheduledExecutorService extends ExecutorService',
    methods: [
      { signature: 'ScheduledFuture<?> schedule(Runnable, delay, unit)', desc: 'One-shot after a delay.' },
      { signature: 'scheduleAtFixedRate(task, initial, period, unit)', desc: 'Every period, regardless of task duration (skips pile up).' },
      { signature: 'scheduleWithFixedDelay(task, initial, delay, unit)', desc: 'Delay measured from each completion.' },
    ],
    pitfalls: ['A periodic task that throws is silently cancelled forever — wrap the body in try/catch.'],
    related: ['java.util.concurrent.ExecutorService', 'topic:executors-thread-pools'],
  }),
  jc({
    fqcn: 'java.util.concurrent.Future',
    module: 'java.base',
    kind: 'interface',
    since: '1.5',
    summary: 'A handle to an asynchronous result: blocking get(), cancellation, and state inspection. CompletableFuture extends it with composition.',
    declaration: 'public interface Future<V>',
    methods: [
      { signature: 'V get() / V get(long timeout, TimeUnit)', desc: 'Block for the result; task failures arrive wrapped in ExecutionException.' },
      { signature: 'boolean cancel(boolean mayInterruptIfRunning)', desc: 'Best-effort cancellation.' },
      { signature: 'boolean isDone() / isCancelled()', desc: 'State checks.' },
      { signature: 'V resultNow() / Throwable exceptionNow()', desc: 'Non-blocking accessors for completed futures (Java 19).' },
    ],
    related: ['java.util.concurrent.CompletableFuture', 'topic:executors-thread-pools'],
  }),
  jc({
    fqcn: 'java.util.concurrent.CompletableFuture',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'A composable promise: chain (thenApply/thenCompose), combine (thenCombine/allOf), recover (exceptionally), and time-bound (orTimeout) asynchronous work.',
    declaration: 'public class CompletableFuture<T>\n        implements Future<T>, CompletionStage<T>',
    methods: [
      { signature: 'static CompletableFuture<T> supplyAsync(Supplier<T>, Executor)', desc: 'Start async work — ALWAYS pass your executor for blocking tasks.' },
      { signature: 'CompletableFuture<U> thenApply(Function<T,U>)', desc: 'Map the result.' },
      { signature: 'CompletableFuture<U> thenCompose(Function<T, CompletableFuture<U>>)', desc: 'FlatMap — chain dependent async calls.' },
      { signature: 'CompletableFuture<V> thenCombine(other, BiFunction)', desc: 'Zip two independent futures.' },
      { signature: 'static CompletableFuture<Void> allOf/anyOf(CompletableFuture<?>...)', desc: 'Fan-in.' },
      { signature: 'CompletableFuture<T> exceptionally(Function<Throwable,T>)', desc: 'Fallback on failure.' },
      { signature: 'CompletableFuture<T> orTimeout(long, TimeUnit)', desc: 'Fail with TimeoutException after the deadline (Java 9).' },
      { signature: 'T join()', desc: 'get() without checked exceptions (throws CompletionException).' },
    ],
    pitfalls: [
      'Default executor is the common ForkJoinPool — blocking I/O there starves parallel streams JVM-wide.',
      'A future whose failure nobody observes fails silently — every chain needs a terminal consumer.',
    ],
    related: ['topic:completable-future', 'java.util.concurrent.ExecutorService', 'java.net.http.HttpClient'],
  }),
  jc({
    fqcn: 'java.util.concurrent.ConcurrentHashMap',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'The concurrent Map: lock-free reads, per-bin write locking, and atomic compound operations (computeIfAbsent, merge) that replace client-side locking entirely.',
    declaration: 'public class ConcurrentHashMap<K, V> extends AbstractMap<K, V>\n        implements ConcurrentMap<K, V>, Serializable',
    methods: [
      { signature: 'V computeIfAbsent(K, Function<K,V>)', desc: 'Atomic get-or-create (the mapping function runs under the bin lock — keep it short, no map reentry).' },
      { signature: 'V merge(K, V, BiFunction<V,V,V>)', desc: 'Atomic insert-or-combine.' },
      { signature: 'V putIfAbsent(K, V) / boolean replace(K, V old, V new)', desc: 'Atomic conditional writes.' },
      { signature: 'void forEach(long parallelismThreshold, BiConsumer)', desc: 'Bulk parallel operations.' },
      { signature: 'static <K> KeySetView<K,Boolean> newKeySet()', desc: 'A concurrent Set.' },
    ],
    pitfalls: [
      'Null keys and values are forbidden by design.',
      'size()/isEmpty() are estimates under concurrent mutation — don\'t build logic on exact counts.',
    ],
    related: ['topic:concurrent-collections', 'java.util.HashMap', 'java.util.concurrent.atomic.LongAdder'],
  }),
  jc({
    fqcn: 'java.util.concurrent.ConcurrentSkipListMap',
    module: 'java.base',
    kind: 'class',
    since: '1.6',
    summary: 'The concurrent sorted map: a lock-free skip list giving TreeMap\'s NavigableMap API (floor/ceiling/ranges) under concurrency at O(log n).',
    declaration: 'public class ConcurrentSkipListMap<K, V> extends AbstractMap<K, V>\n        implements ConcurrentNavigableMap<K, V>, Cloneable, Serializable',
    related: ['java.util.TreeMap', 'topic:concurrent-collections', 'topic:sorted-collections'],
  }),
  jc({
    fqcn: 'java.util.concurrent.CopyOnWriteArrayList',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'List that copies its backing array on every write: iteration is lock-free over an immutable snapshot. Ideal for read-mostly listener registries; pathological for write-heavy use.',
    declaration: 'public class CopyOnWriteArrayList<E>\n        implements List<E>, RandomAccess, Cloneable, Serializable',
    points: [
      'Iterators never throw ConcurrentModificationException — they see the snapshot at creation',
      'Every add/remove is O(n) allocation + copy',
    ],
    related: ['topic:concurrent-collections', 'java.util.ArrayList'],
  }),
  jc({
    fqcn: 'java.util.concurrent.BlockingQueue',
    module: 'java.base',
    kind: 'interface',
    since: '1.5',
    summary: 'Queue with blocking put/take — the producer-consumer conveyor with built-in backpressure and safe publication. Bounded implementations are the overload valve.',
    declaration: 'public interface BlockingQueue<E> extends Queue<E>',
    methods: [
      { signature: 'void put(E e)', desc: 'Block until space (bounded queues).' },
      { signature: 'E take()', desc: 'Block until an element arrives.' },
      { signature: 'boolean offer(E, long timeout, TimeUnit)', desc: 'Bounded-wait insert — the load-shedding hook.' },
      { signature: 'E poll(long timeout, TimeUnit)', desc: 'Bounded-wait removal.' },
      { signature: 'int drainTo(Collection<E>)', desc: 'Bulk transfer for batch consumers.' },
    ],
    related: ['java.util.concurrent.ArrayBlockingQueue', 'topic:concurrent-collections', 'topic:scalability-patterns'],
  }),
  jc({
    fqcn: 'java.util.concurrent.ArrayBlockingQueue',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'Bounded array-backed BlockingQueue — fixed capacity set at construction, optional fairness. The standard explicit-backpressure work queue.',
    declaration: 'public class ArrayBlockingQueue<E> extends AbstractQueue<E>\n        implements BlockingQueue<E>, Serializable',
    related: ['java.util.concurrent.BlockingQueue', 'java.util.concurrent.LinkedBlockingQueue'],
  }),
  jc({
    fqcn: 'java.util.concurrent.LinkedBlockingQueue',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'Linked BlockingQueue, optionally bounded (unbounded by default — beware), with separate head/tail locks for higher producer/consumer throughput.',
    declaration: 'public class LinkedBlockingQueue<E> extends AbstractQueue<E>\n        implements BlockingQueue<E>, Serializable',
    pitfalls: ['The no-arg constructor is UNBOUNDED — under overload it buffers until OOM. Pass a capacity.'],
    related: ['java.util.concurrent.ArrayBlockingQueue', 'topic:scalability-patterns'],
  }),
  jc({
    fqcn: 'java.util.concurrent.CountDownLatch',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'One-shot gate: await() blocks until countDown() has been called N times. Start gates, completion gates, and "wait for N services to boot".',
    declaration: 'public class CountDownLatch',
    methods: [
      { signature: 'CountDownLatch(int count)', desc: 'The number of events to wait for.' },
      { signature: 'void await() / boolean await(timeout, unit)', desc: 'Block until zero.' },
      { signature: 'void countDown()', desc: 'Record one event — never blocks.' },
    ],
    points: ['Single-use: it cannot be reset (CyclicBarrier and Phaser can)'],
    related: ['topic:synchronizers', 'java.util.concurrent.CyclicBarrier', 'java.util.concurrent.Semaphore'],
  }),
  jc({
    fqcn: 'java.util.concurrent.CyclicBarrier',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'Reusable rendezvous for N parties: each await() blocks until all arrive, an optional barrier action runs, and a new generation begins — iterative simulations\' heartbeat.',
    declaration: 'public class CyclicBarrier',
    methods: [
      { signature: 'CyclicBarrier(int parties, Runnable barrierAction)', desc: 'Action runs once per generation, by the last arriver.' },
      { signature: 'int await()', desc: 'Wait for the others; broken barriers throw BrokenBarrierException.' },
    ],
    related: ['topic:synchronizers', 'java.util.concurrent.CountDownLatch'],
  }),
  jc({
    fqcn: 'java.util.concurrent.Semaphore',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'A permit counter bounding concurrent access: acquire/release around any scarce resource. With virtual threads, the replacement for pool-size-as-limit.',
    declaration: 'public class Semaphore implements Serializable',
    methods: [
      { signature: 'Semaphore(int permits, boolean fair)', desc: 'Fairness prevents starvation at some throughput cost.' },
      { signature: 'void acquire() / void release()', desc: 'Take and return a permit (release in finally!).' },
      { signature: 'boolean tryAcquire(long timeout, TimeUnit)', desc: 'Bounded wait — the load-shedding form.' },
    ],
    related: ['topic:synchronizers', 'topic:virtual-threads'],
  }),
  jc({
    fqcn: 'java.util.concurrent.locks.ReentrantLock',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'Explicit mutual-exclusion lock with the powers synchronized lacks: tryLock, timed and interruptible acquisition, fairness, and multiple Conditions.',
    declaration: 'public class ReentrantLock implements Lock, Serializable',
    methods: [
      { signature: 'void lock() / void unlock()', desc: 'unlock() in finally, always.' },
      { signature: 'boolean tryLock() / tryLock(long, TimeUnit)', desc: 'Deadlock-avoiding acquisition.' },
      { signature: 'void lockInterruptibly()', desc: 'Cancellable waiting.' },
      { signature: 'Condition newCondition()', desc: 'await/signal wait-sets, multiple per lock.' },
    ],
    related: ['topic:locks-synchronization', 'java.util.concurrent.locks.ReentrantReadWriteLock'],
  }),
  jc({
    fqcn: 'java.util.concurrent.locks.ReentrantReadWriteLock',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'A lock pair: shared read lock (many concurrent readers) and exclusive write lock. Pays off for read-mostly data with non-trivial critical sections.',
    declaration: 'public class ReentrantReadWriteLock implements ReadWriteLock, Serializable',
    methods: [
      { signature: 'Lock readLock() / Lock writeLock()', desc: 'The pair; write excludes everything.' },
    ],
    points: ['Under heavy read traffic consider StampedLock\'s optimistic reads for another gear'],
    related: ['java.util.concurrent.locks.StampedLock', 'topic:locks-synchronization'],
  }),
  jc({
    fqcn: 'java.util.concurrent.locks.StampedLock',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'Three-mode lock with optimistic reads: tryOptimisticRead + validate lets read-mostly hot paths skip locking entirely. Powerful, non-reentrant, expert-tier.',
    declaration: 'public class StampedLock implements Serializable',
    methods: [
      { signature: 'long tryOptimisticRead() / boolean validate(long stamp)', desc: 'Lock-free read, then check nothing changed.' },
      { signature: 'long readLock() / writeLock(); unlockRead/Write(stamp)', desc: 'Pessimistic fallbacks.' },
    ],
    pitfalls: ['Not reentrant and no Conditions — misuse deadlocks in ways ReentrantLock would not.'],
    related: ['java.util.concurrent.locks.ReentrantReadWriteLock', 'topic:locks-synchronization'],
  }),
  jc({
    fqcn: 'java.util.concurrent.atomic.AtomicInteger',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'Lock-free int with atomic read-modify-write (CAS): counters, sequence numbers, state flags — volatile semantics plus atomicity.',
    declaration: 'public class AtomicInteger extends Number implements Serializable',
    methods: [
      { signature: 'int incrementAndGet() / getAndIncrement()', desc: 'Atomic ++ (pre/post).' },
      { signature: 'boolean compareAndSet(int expect, int update)', desc: 'The CAS primitive.' },
      { signature: 'int updateAndGet(IntUnaryOperator)', desc: 'Atomic functional update (pure function — may retry).' },
      { signature: 'int addAndGet(int delta)', desc: 'Atomic +=.' },
    ],
    related: ['topic:atomics-nonblocking', 'java.util.concurrent.atomic.LongAdder'],
  }),
  jc({
    fqcn: 'java.util.concurrent.atomic.AtomicLong',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'The 64-bit atomic counter. For write-hot counters under contention, LongAdder scales better; AtomicLong wins when you need exact reads between writes.',
    declaration: 'public class AtomicLong extends Number implements Serializable',
    related: ['java.util.concurrent.atomic.LongAdder', 'topic:atomics-nonblocking'],
  }),
  jc({
    fqcn: 'java.util.concurrent.atomic.AtomicReference',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'Atomic object reference: CAS-swap immutable snapshots to update multi-field state without locks — the heart of nonblocking data structures.',
    declaration: 'public class AtomicReference<V> implements Serializable',
    methods: [
      { signature: 'boolean compareAndSet(V expect, V update)', desc: 'CAS on the reference.' },
      { signature: 'V updateAndGet(UnaryOperator<V>)', desc: 'Atomic functional swap.' },
      { signature: 'V getAndSet(V newValue)', desc: 'Swap and return old.' },
    ],
    example: {
      code: 'AtomicReference<Range> range = new AtomicReference<>(new Range(0, 10));\nrange.updateAndGet(r -> r.withUpper(20));   // multi-field invariant in ONE CAS',
    },
    related: ['topic:atomics-nonblocking', 'topic:immutability-class-design'],
  }),
  jc({
    fqcn: 'java.util.concurrent.atomic.LongAdder',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'Contention-striped counter: increments hit per-thread cells, sum() aggregates. Beats AtomicLong decisively for hot write-mostly counters (metrics, hit counts).',
    declaration: 'public class LongAdder extends Striped64 implements Serializable',
    methods: [
      { signature: 'void increment() / void add(long x)', desc: 'Nearly contention-free writes.' },
      { signature: 'long sum()', desc: 'Aggregate — weakly consistent under concurrent writes (fine for stats).' },
    ],
    related: ['java.util.concurrent.atomic.AtomicLong', 'topic:concurrent-performance'],
  }),
  jc({
    fqcn: 'java.util.concurrent.ForkJoinPool',
    module: 'java.base',
    kind: 'class',
    since: '1.7',
    summary: 'Work-stealing pool for divide-and-conquer tasks; its commonPool() runs parallel streams and default CompletableFuture stages — which is why blocking it hurts everything.',
    declaration: 'public class ForkJoinPool extends AbstractExecutorService',
    methods: [
      { signature: 'static ForkJoinPool commonPool()', desc: 'The shared JVM-wide pool (~cores−1 threads).' },
      { signature: '<T> T invoke(ForkJoinTask<T> task)', desc: 'Run a recursive task to completion.' },
    ],
    points: ['CPU-bound, non-blocking tasks only; blocking work belongs in explicit executors or virtual threads'],
    related: ['topic:parallel-streams', 'java.util.concurrent.CompletableFuture', 'topic:executors-thread-pools'],
  }),
  jc({
    fqcn: 'java.util.concurrent.TimeUnit',
    module: 'java.base',
    kind: 'enum',
    since: '1.5',
    summary: 'Time granularity enum (NANOSECONDS…DAYS) used across concurrency APIs for timeouts, plus conversion and sleep helpers.',
    declaration: 'public enum TimeUnit',
    methods: [
      { signature: 'long convert(Duration duration)', desc: 'Duration → this unit (Java 11).' },
      { signature: 'long toMillis(long duration)', desc: 'Conversions between units.' },
      { signature: 'void sleep(long timeout)', desc: 'Unit-safe Thread.sleep.' },
    ],
    related: ['java.time.Duration', 'topic:executors-thread-pools'],
  }),
]

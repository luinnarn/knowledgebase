import type { JavaClass } from '../../types/content'
import { jc } from './util'

export const classes: JavaClass[] = [
  jc({
    fqcn: 'java.time.LocalDate',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'A calendar date without time or zone (2026-07-07): birthdays, due dates, calendar math. Immutable; every "mutation" returns a new date.',
    declaration: 'public final class LocalDate\n        implements Temporal, TemporalAdjuster, ChronoLocalDate, Serializable',
    methods: [
      { signature: 'static LocalDate now() / of(int y, int m, int d) / parse(CharSequence)', desc: 'Creation (parse expects ISO yyyy-MM-dd).' },
      { signature: 'LocalDate plusDays/plusMonths/plusYears(long)', desc: 'Calendar arithmetic (end-of-month aware).' },
      { signature: 'LocalDate with(TemporalAdjuster adj)', desc: 'e.g. TemporalAdjusters.next(MONDAY), lastDayOfMonth().' },
      { signature: 'DayOfWeek getDayOfWeek() / int lengthOfMonth()', desc: 'Calendar queries.' },
      { signature: 'boolean isBefore/isAfter(ChronoLocalDate)', desc: 'Comparisons.' },
      { signature: 'LocalDateTime atTime(LocalTime) / atStartOfDay()', desc: 'Combine with a time.' },
    ],
    related: ['java.time.LocalDateTime', 'topic:date-time-api', 'java.time.Period'],
  }),
  jc({
    fqcn: 'java.time.LocalTime',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'A wall-clock time without date or zone (09:30): opening hours, alarms, schedules — resolved against a zone only when they become real moments.',
    declaration: 'public final class LocalTime\n        implements Temporal, TemporalAdjuster, Comparable<LocalTime>, Serializable',
    methods: [
      { signature: 'static LocalTime of(int hour, int minute)', desc: 'Creation.' },
      { signature: 'LocalTime plusHours/plusMinutes(long)', desc: 'Wrap-around arithmetic.' },
    ],
    related: ['java.time.LocalDate', 'topic:date-time-api'],
  }),
  jc({
    fqcn: 'java.time.LocalDateTime',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'Date + time without zone — a wall-clock reading, NOT a moment. Use for zone-independent local semantics; convert via atZone/toInstant for real timestamps.',
    declaration: 'public final class LocalDateTime\n        implements Temporal, TemporalAdjuster, ChronoLocalDateTime<LocalDate>, Serializable',
    methods: [
      { signature: 'static LocalDateTime now() / of(LocalDate, LocalTime)', desc: 'Creation.' },
      { signature: 'ZonedDateTime atZone(ZoneId zone)', desc: 'Attach a zone → an actual moment.' },
    ],
    pitfalls: ['Persisting event timestamps as LocalDateTime loses the zone — ambiguous twice a year. Store Instant.'],
    related: ['java.time.Instant', 'java.time.ZonedDateTime', 'topic:date-time-api'],
  }),
  jc({
    fqcn: 'java.time.ZonedDateTime',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'A full moment with zone rules attached: DST-aware arithmetic for human-facing scheduling ("9:30 in Belgrade, next Tuesday too").',
    declaration: 'public final class ZonedDateTime\n        implements Temporal, ChronoZonedDateTime<LocalDate>, Serializable',
    methods: [
      { signature: 'static ZonedDateTime now(ZoneId zone) / of(LocalDateTime, ZoneId)', desc: 'Creation.' },
      { signature: 'ZonedDateTime plusDays(long) vs plus(Duration)', desc: 'Calendar day (DST-aware) vs exact elapsed time — deliberately different.' },
      { signature: 'ZonedDateTime withZoneSameInstant(ZoneId)', desc: 'Same moment, another zone\'s clock.' },
      { signature: 'Instant toInstant()', desc: 'Down to machine time.' },
    ],
    related: ['java.time.Instant', 'java.time.ZoneId', 'topic:date-time-api'],
  }),
  jc({
    fqcn: 'java.time.Instant',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'A point on the UTC timeline (epoch seconds + nanos) — THE type for event timestamps, logs, and persistence. Zone-free by definition.',
    declaration: 'public final class Instant\n        implements Temporal, TemporalAdjuster, Comparable<Instant>, Serializable',
    methods: [
      { signature: 'static Instant now() / now(Clock clock)', desc: 'Current moment (inject Clock for testability).' },
      { signature: 'static Instant ofEpochMilli(long) / long toEpochMilli()', desc: 'Legacy epoch-millis bridge.' },
      { signature: 'Instant plus(Duration) / isBefore(Instant)', desc: 'Machine-time arithmetic and comparison.' },
      { signature: 'ZonedDateTime atZone(ZoneId)', desc: 'Up to human calendar terms for display.' },
    ],
    related: ['java.time.Duration', 'java.time.Clock', 'topic:date-time-api'],
  }),
  jc({
    fqcn: 'java.time.Duration',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'A machine-time span in seconds/nanos: timeouts, elapsed measurement, exact arithmetic. Period is its calendar-unit sibling.',
    declaration: 'public final class Duration\n        implements TemporalAmount, Comparable<Duration>, Serializable',
    methods: [
      { signature: 'static Duration ofSeconds/ofMillis/ofMinutes(long)', desc: 'Creation.' },
      { signature: 'static Duration between(Temporal start, Temporal end)', desc: 'Elapsed between two instants.' },
      { signature: 'long toMillis() / toSeconds()', desc: 'Conversions for legacy APIs.' },
      { signature: 'Duration plus(Duration) / multipliedBy(long)', desc: 'Arithmetic.' },
    ],
    related: ['java.time.Instant', 'java.time.Period', 'java.util.concurrent.TimeUnit'],
  }),
  jc({
    fqcn: 'java.time.Period',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'A calendar-unit span (years, months, days): "3 months" regardless of their length. Duration measures exact time; Period measures human calendar distance.',
    declaration: 'public final class Period implements ChronoPeriod, Serializable',
    methods: [
      { signature: 'static Period between(LocalDate start, LocalDate end)', desc: 'Calendar distance (y/m/d components).' },
      { signature: 'static Period ofMonths(int) / ofYears(int)', desc: 'Creation.' },
    ],
    related: ['java.time.Duration', 'java.time.LocalDate'],
  }),
  jc({
    fqcn: 'java.time.ZoneId',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'A time-zone identifier ("Europe/Belgrade") carrying the full IANA rule history — offsets, DST transitions, past reforms. Prefer region ids over fixed offsets.',
    declaration: 'public abstract class ZoneId implements Serializable',
    methods: [
      { signature: 'static ZoneId of(String zoneId)', desc: '"Europe/Belgrade", "America/New_York", "UTC".' },
      { signature: 'static ZoneId systemDefault()', desc: 'The JVM\'s zone — an input, never an assumption.' },
    ],
    pitfalls: ['Fixed offsets ("+01:00") ignore DST — a Belgrade meeting stored with an offset drifts an hour every spring.'],
    related: ['java.time.ZonedDateTime', 'topic:date-time-api'],
  }),
  jc({
    fqcn: 'java.time.format.DateTimeFormatter',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'Immutable, thread-safe date/time formatting and parsing — pattern-based, localized, or ISO constants. The safe successor to SimpleDateFormat.',
    declaration: 'public final class DateTimeFormatter',
    methods: [
      { signature: 'static DateTimeFormatter ofPattern(String pattern)', desc: '"dd.MM.yyyy HH:mm" — y year, M month, d day, H hour, m minute, s second.' },
      { signature: 'DateTimeFormatter withLocale(Locale) / withZone(ZoneId)', desc: 'Localization and zone attachment.' },
      { signature: 'ISO_INSTANT / ISO_LOCAL_DATE …', desc: 'Standard machine formats as constants.' },
      { signature: 'String format(TemporalAccessor) / parse via LocalDate.parse(text, fmt)', desc: 'Both directions.' },
    ],
    points: ['Share as static final constants — unlike SimpleDateFormat, it is thread-safe'],
    related: ['topic:date-time-api', 'topic:internationalization'],
  }),
  jc({
    fqcn: 'java.time.Clock',
    module: 'java.base',
    kind: 'class',
    since: '1.8',
    summary: 'An injectable time source: fixed, offset, or system clocks. Passing a Clock instead of calling now() makes time-dependent logic deterministic under test.',
    declaration: 'public abstract class Clock implements InstantSource',
    methods: [
      { signature: 'static Clock systemUTC() / systemDefaultZone()', desc: 'Production clocks.' },
      { signature: 'static Clock fixed(Instant fixed, ZoneId zone)', desc: 'Frozen time for tests.' },
      { signature: 'Instant instant()', desc: 'The current reading.' },
    ],
    related: ['java.time.Instant', 'topic:date-time-api'],
  }),
  jc({
    fqcn: 'java.math.BigDecimal',
    module: 'java.base',
    kind: 'class',
    since: '1.1',
    summary: 'Arbitrary-precision decimal arithmetic — the money type. Construct from String, divide with explicit scale and RoundingMode, compare with compareTo.',
    declaration: 'public class BigDecimal extends Number implements Comparable<BigDecimal>',
    methods: [
      { signature: 'BigDecimal(String val) / static BigDecimal valueOf(double)', desc: 'Exact creation (the double CONSTRUCTOR copies binary error — avoid).' },
      { signature: 'BigDecimal add/subtract/multiply(BigDecimal)', desc: 'Exact arithmetic.' },
      { signature: 'BigDecimal divide(BigDecimal, int scale, RoundingMode)', desc: 'Division MUST specify rounding — 1/3 never terminates.' },
      { signature: 'BigDecimal setScale(int, RoundingMode)', desc: 'Normalize decimal places (money: 2, HALF_UP or HALF_EVEN).' },
      { signature: 'int compareTo(BigDecimal)', desc: 'Numeric comparison — equals() distinguishes 2.0 from 2.00!' },
    ],
    pitfalls: [
      'equals is scale-sensitive: 2.0 ≠ 2.00 — use compareTo for numeric equality (and beware in HashSet/TreeSet).',
      'new BigDecimal(0.1) is 0.1000000000000000055… — String constructor or valueOf only.',
    ],
    related: ['topic:numbers-math', 'java.math.BigInteger'],
  }),
  jc({
    fqcn: 'java.math.BigInteger',
    module: 'java.base',
    kind: 'class',
    since: '1.1',
    summary: 'Arbitrary-size integers: cryptography, combinatorics, ids beyond 64 bits. Immutable, operator-free arithmetic via methods.',
    declaration: 'public class BigInteger extends Number implements Comparable<BigInteger>',
    methods: [
      { signature: 'static BigInteger valueOf(long) / BigInteger(String val)', desc: 'Creation.' },
      { signature: 'BigInteger add/multiply/pow/mod(…)', desc: 'Arithmetic.' },
      { signature: 'static BigInteger probablePrime(int bitLength, Random rnd)', desc: 'Crypto-grade prime generation.' },
    ],
    related: ['java.math.BigDecimal', 'topic:numbers-math'],
  }),
  jc({
    fqcn: 'java.util.UUID',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: '128-bit universally unique identifier. randomUUID() (v4) is the standard for distributed ids; string round-trips via toString/fromString.',
    declaration: 'public final class UUID implements Serializable, Comparable<UUID>',
    methods: [
      { signature: 'static UUID randomUUID()', desc: 'Cryptographically random v4 — the everyday factory.' },
      { signature: 'static UUID fromString(String name)', desc: 'Parse the 8-4-4-4-12 form.' },
    ],
    related: ['java.security.SecureRandom', 'topic:security-basics'],
  }),
  jc({
    fqcn: 'java.util.Random',
    module: 'java.base',
    kind: 'class',
    since: '1.0',
    summary: 'Seedable pseudo-random generator — reproducible sequences for tests and simulations. For concurrent code use ThreadLocalRandom; for security use SecureRandom.',
    declaration: 'public class Random implements RandomGenerator, Serializable',
    methods: [
      { signature: 'Random(long seed)', desc: 'Deterministic sequence — reproducible tests.' },
      { signature: 'int nextInt(int origin, int bound)', desc: 'Uniform in [origin, bound).' },
      { signature: 'IntStream ints(long size, int origin, int bound)', desc: 'Random streams.' },
    ],
    pitfalls: ['Never for secrets — its 48-bit state is trivially predictable. SecureRandom for anything attackable.'],
    related: ['java.util.concurrent.ThreadLocalRandom', 'java.security.SecureRandom'],
  }),
  jc({
    fqcn: 'java.util.concurrent.ThreadLocalRandom',
    module: 'java.base',
    kind: 'class',
    since: '1.7',
    summary: 'Per-thread random generator without contention: the default choice inside concurrent code and parallel streams. Access via current(), never share, never seed.',
    declaration: 'public class ThreadLocalRandom extends Random',
    methods: [
      { signature: 'static ThreadLocalRandom current()', desc: 'The calling thread\'s generator — always call at use site.' },
      { signature: 'int nextInt(int origin, int bound)', desc: 'Range sampling.' },
    ],
    related: ['java.util.Random', 'topic:concurrent-performance'],
  }),
  jc({
    fqcn: 'java.security.SecureRandom',
    module: 'java.base',
    kind: 'class',
    since: '1.1',
    summary: 'Cryptographically strong randomness sourced from OS entropy: tokens, salts, IVs, keys. Slower than Random — and the only acceptable choice for security.',
    declaration: 'public class SecureRandom extends Random',
    methods: [
      { signature: 'static SecureRandom getInstanceStrong()', desc: 'The strongest configured algorithm.' },
      { signature: 'void nextBytes(byte[] bytes)', desc: 'Fill with entropy — the salt/IV workhorse.' },
    ],
    related: ['topic:security-basics', 'java.util.UUID'],
  }),
  jc({
    fqcn: 'java.lang.ProcessBuilder',
    module: 'java.base',
    kind: 'class',
    since: '1.5',
    summary: 'Spawn and wire OS processes: command, environment, working directory, and stream redirection — the safe, structured way to shell out.',
    declaration: 'public final class ProcessBuilder',
    methods: [
      { signature: 'ProcessBuilder(String... command)', desc: 'Argument list — no shell parsing, no injection.' },
      { signature: 'ProcessBuilder redirectErrorStream(boolean)', desc: 'Merge stderr into stdout — avoids deadlocked pipes.' },
      { signature: 'Process start()', desc: 'Launch.' },
      { signature: 'ProcessBuilder inheritIO()', desc: 'Reuse the parent\'s console.' },
    ],
    pitfalls: ['Not draining a process\'s output pipe fills the OS buffer and deadlocks the child — read it or redirect it.'],
    related: ['java.lang.Runtime', 'topic:program-anatomy'],
  }),
  jc({
    fqcn: 'java.util.Locale',
    module: 'java.base',
    kind: 'class',
    since: '1.1',
    summary: 'Language + region (+ script) identifier driving all locale-sensitive behavior: formatting, collation, resource-bundle selection. Machine-facing strings use Locale.ROOT.',
    declaration: 'public final class Locale implements Cloneable, Serializable',
    methods: [
      { signature: 'static Locale forLanguageTag(String tag)', desc: 'BCP 47: "sr-Latn-RS", "en-US".' },
      { signature: 'static Locale getDefault()', desc: 'The JVM default — an input to respect, not to assume.' },
      { signature: 'ROOT / US / GERMANY …', desc: 'Constants; ROOT = locale-neutral for protocols and logs.' },
    ],
    related: ['topic:internationalization', 'java.time.format.DateTimeFormatter'],
  }),
  jc({
    fqcn: 'java.util.ServiceLoader',
    module: 'java.base',
    kind: 'class',
    since: '1.6',
    summary: 'Discovers interface implementations registered via module `provides` clauses or META-INF/services files — plugin architecture without compile-time coupling.',
    declaration: 'public final class ServiceLoader<S> implements Iterable<S>',
    methods: [
      { signature: 'static <S> ServiceLoader<S> load(Class<S> service)', desc: 'Find all providers on the class/module path.' },
      { signature: 'Optional<S> findFirst()', desc: 'The first provider, if any.' },
      { signature: 'Stream<Provider<S>> stream()', desc: 'Inspect provider types before instantiating.' },
    ],
    related: ['topic:modules-jpms', 'topic:class-loading'],
  }),
]

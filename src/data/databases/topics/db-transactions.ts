import type { Topic } from '../../../types/content'

export const dbTransactionsTopics: Topic[] = [
  {
    id: 'transactions-and-acid', domainId: 'db-transactions', title: 'Transactions and ACID',
    summary: 'A transaction is an application-chosen unit of work with an atomic commit outcome. ACID describes useful boundaries, but the database cannot invent business consistency rules and durability is always relative to the acknowledged failure model.',
    keyPoints: [
      { text: 'Atomicity is all-or-abort, not concurrency isolation.', detail: 'After failure, none of an aborted transaction’s effects remain; atomicity does not mean each statement is indivisible to observers.' },
      { text: 'Consistency is shared responsibility.', detail: 'Constraints and the engine preserve declared rules; the application must choose a valid transition and include every dependent write.' },
      { text: 'Isolation constrains concurrent histories.', detail: 'The chosen level determines which intermediate observations or serialization failures are possible; “transactional” alone does not imply serializable.' },
      { text: 'Durability has a failure boundary.', detail: 'A commit acknowledgement may rely on WAL, flush settings, storage guarantees, and replicas. It does not by itself promise survival of site loss or operator error.' },
      { text: 'Transaction boundaries are semantic.', detail: 'Place the boundary around one invariant-preserving state transition, not around an arbitrary repository call or HTTP request.' },
      { text: 'Abort is a normal outcome.', detail: 'Constraint violations, deadlocks, conflicts, cancellation, and uncertain connections require explicit rollback and outcome handling.' },
    ],
    blocks: [
      { kind: 'subheading', text: 'One transfer, one boundary' },
      { kind: 'code', language: 'text', title: 'Starting state, transition, and invariant', code: 'Start: account A = 100; account B = 40; invariant: A + B = 140.\nT1: debit A by 30; credit B by 30; COMMIT.\nCommitted result: A = 70; B = 70; invariant still 140.\nCrash after debit: recovery must expose either start or committed result, never A = 70, B = 40.' },
      { kind: 'table', caption: 'What ACID does—and does not—assign', headers: ['Property', 'Engine mechanism', 'Application obligation'], rows: [['Atomicity', 'Undo/redo and atomic commit decision', 'Put all required writes in one boundary'], ['Consistency', 'Declared constraints and correct recovery', 'Declare and preserve business invariants'], ['Isolation', 'Locks, versions, validation, aborts', 'Choose level and handle allowed outcomes'], ['Durability', 'Log/flush/replication policy', 'Define required failure scope and backups']] },
      { kind: 'paragraph', text: 'A successful commit says the database accepted the transition under its configured guarantees. It does not prove that transferring 30 was authorized, that an email was sent exactly once, or that a replica in another region has persisted it.' },
      { kind: 'pitfall', title: 'Treating every request as one giant transaction', text: 'Waiting for user input or a remote service while holding a transaction expands lock/version retention and creates an outcome gap. Keep database work short and use an outbox or idempotent workflow for external effects.' },
      { kind: 'bestPractice', title: 'Write the invariant before the SQL', text: 'State the valid starting state, the transition, the commit point, and the failures the acknowledgement must survive; then test crash, rollback, and concurrency paths.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 17 — Transactions' }, { book: 'ddia-2', chapter: 'Ch. 8 — Transactions: The Meaning of ACID' }, { book: 'database-internals', chapter: 'Ch. 5 — Transaction Processing and Recovery' }],
    related: ['histories-schedules-and-serializability', 'write-ahead-logging-and-checkpoints', 'application-concurrency-and-transaction-boundaries'],
  },
  {
    id: 'histories-schedules-and-serializability', domainId: 'db-transactions', title: 'Histories, Schedules, and Serializability',
    summary: 'A history orders reads, writes, commits, and aborts from transactions. Serializability asks whether committed effects and observations are equivalent to some serial order; conflict graphs provide a practical sufficient test, while view serializability is broader.',
    keyPoints: [
      { text: 'Notation makes interleavings reviewable.', detail: '`r1(X)` and `w1(X)` are T1 reads/writes; `c1` and `a1` are commit/abort. Program order inside each transaction is preserved.' },
      { text: 'Conflicts require same item, different transactions, and a write.', detail: 'Read–read pairs commute; read–write, write–read, and write–write pairs create precedence edges.' },
      { text: 'An acyclic precedence graph proves conflict serializability.', detail: 'A topological ordering supplies an equivalent serial order; a cycle proves no conflict-equivalent serial order exists.' },
      { text: 'View serializability is more general.', detail: 'It preserves who reads whose value and final writers, and can admit blind-write schedules rejected by conflict testing; deciding it is harder.' },
      { text: 'Recoverability is a separate axis.', detail: 'Cascadeless/strict schedules prevent committed transactions from depending on aborted data; serializability alone is not a recovery policy.' },
    ],
    blocks: [
      { kind: 'code', language: 'text', title: 'A lost-update counter history', code: 'Start: counter X = 0. T1 and T2 each mean “increment X once.”\nH = r1(X=0), r2(X=0), w1(X=1), c1, w2(X=1), c2\nEdges: T1 → T2 from r1(X) before w2(X) (and w1(X) before w2(X)); T2 → T1 from r2(X) before w1(X).\nThe cycle T1 → T2 → T1 proves H is not conflict-serializable. Both transactions commit, but final X = 1 violates the invariant X = number of committed increments = 2. T2 reads the initial committed value, and its write follows c1, so the history contains no dirty read or dirty write.' },
      { kind: 'diagram', title: 'Lost-update precedence cycle', code: 'flowchart LR\n  T1["T1: increment X once"] -->|"r1(X) before w2(X); w1(X) before w2(X)"| T2["T2: increment X once"]\n  T2 -->|"r2(X) before w1(X)"| T1' },
      { kind: 'table', caption: 'Schedule properties answer different questions', headers: ['Property', 'Question'], rows: [['Serial', 'Did transactions run without interleaving?'], ['Conflict-serializable', 'Can conflicting operations be reordered to a serial history?'], ['View-serializable', 'Are read-from relations and final writes equivalent to a serial history?'], ['Strict', 'Are uncommitted writes neither read nor overwritten?'], ['Strict serializable', 'Is it serializable and consistent with real-time order?']] },
      { kind: 'paragraph', text: 'The trace loses one committed increment without exposing uncommitted data: both transactions derive the same replacement value from X=0, and T2 overwrites X only after T1 commits. The precedence cycle rules out either serial order, each of which would finish at X=2. A graph is an analysis model, not a claim that an engine literally constructs this graph for every execution.' },
      { kind: 'pitfall', title: 'Equating serializable with serial execution', text: 'A serializable engine may interleave and run transactions in parallel, then block or abort enough work to ensure an equivalent serial outcome.' },
      { kind: 'bestPractice', title: 'Capture the smallest decisive history', text: 'Name initial values, reads, writes, commit/abort events, and invariant; derive every graph edge from a concrete conflicting pair.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 17 — Transactions: Schedules and Serializability' }, { book: 'berenson-isolation', chapter: '§2.1 — Serializability Concepts' }, { book: 'ddia-2', chapter: 'Ch. 8 — Transactions: Serializability' }],
    related: ['transactions-and-acid', 'isolation-levels-and-anomalies', 'serializable-transactions'],
  },
  {
    id: 'isolation-levels-and-anomalies', domainId: 'db-transactions', title: 'Isolation Levels and Anomalies',
    summary: 'Isolation levels are contracts over histories, not a simple speed ladder. Standards name selected phenomena, research exposes additional anomalies, and vendor implementations may prevent more—or different—behaviors than the same label suggests.',
    keyPoints: [
      { text: 'Dirty write and dirty read are distinct.', detail: 'A dirty write overwrites uncommitted data; a dirty read observes it. Robust transactional engines normally prevent dirty writes even at weak read isolation.' },
      { text: 'Nonrepeatable reads concern one row; phantoms concern a predicate.', detail: 'A repeated lookup can change, while a repeated range or aggregate can gain or lose qualifying rows.' },
      { text: 'Lost update is a read–modify–write failure.', detail: 'Two actors derive writes from the same old value and one result disappears unless locking, atomic DML, conflict detection, or validation intervenes.' },
      { text: 'Read skew and write skew cross rows.', detail: 'A consistent snapshot can prevent fractured reads yet still permit disjoint writes that jointly violate a predicate invariant.' },
      { text: 'Names do not fully specify implementations.', detail: 'Ask about snapshot timing, lost-update detection, predicate protection, and serialization failure behavior on the actual engine.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Anomaly matrix (✓ means the mechanism targets it; vendor details still apply)', headers: ['Anomaly', 'Minimal history', 'RC', 'RR / snapshot-style', 'Serializable'], rows: [['Dirty write', 'w1(X), w2(X) before c1', '✓', '✓', '✓'], ['Dirty read', 'w1(X), r2(X) before c1', '✓', '✓', '✓'], ['Nonrepeatable/fuzzy read', 'r1(X), w2(X)c2, r1(X)', '—', 'usually ✓', '✓'], ['Phantom', 'predicate r1(P), insert2(P)c2, r1(P)', '—', 'varies', '✓'], ['Lost update', 'r1(X=0), r2(X=0), w1(X=1)c1, w2(X=1)c2', 'varies', 'varies', '✓/abort'], ['Read skew', 'r1(X), w2(X,Y)c2, r1(Y)', '—', 'snapshot ✓', '✓'], ['Write skew', 'r1(P), r2(P), disjoint w1/w2', '—', '— under SI', '✓/abort'], ['Serialization anomaly', 'cycle in dependency graph', '—', '—', '✓/abort']] },
      { kind: 'code', language: 'text', title: 'Lost update trace', code: 'Start: inventory.on_hand = 10; invariant on_hand equals prior stock minus all committed sales.\nT1 reads 10; T2 reads 10; T1 writes 9 and commits; T2 writes 8 and commits.\nFinal = 8, but three units were sold, so expected 7: T1’s decrement was lost.' },
      { kind: 'paragraph', text: 'The SQL standard’s classic dirty/nonrepeatable/phantom framing is not a complete taxonomy. Berenson et al. show why lock- and history-based definitions matter; modern MVCC systems add snapshot and dependency behavior that cannot be inferred from a label.' },
      { kind: 'note', title: 'A level is necessary, not sufficient', text: 'Even serializable transactions cannot repair an incorrectly stated invariant, missing row, external side effect, or stale value read before the transaction began.' },
      { kind: 'pitfall', title: 'Publishing a universal checkmark chart', text: 'A chart that maps every vendor’s `REPEATABLE READ` to one behavior is misleading. Treat this matrix as concepts, then verify each engine and configuration.' },
      { kind: 'bestPractice', title: 'Test histories, not marketing names', text: 'Build two-session tests for the application’s exact invariant and record whether the engine blocks, returns a version, commits both, or aborts one.' },
    ],
    refs: [{ book: 'berenson-isolation', chapter: '§2.2 — ANSI SQL Isolation Levels; §3 — Analyzing ANSI SQL Isolation Levels; §4 — Other Isolation Types' }, { book: 'database-system-concepts', chapter: 'Ch. 17–18 — Transactions and Concurrency Control' }, { book: 'ddia-2', chapter: 'Ch. 8 — Weak Isolation Levels' }, { book: 'sql-standard', chapter: 'SQL/Foundation — Transaction isolation levels' }],
    related: ['histories-schedules-and-serializability', 'read-committed-and-repeatable-read', 'snapshot-isolation-and-write-skew'],
  },
  {
    id: 'read-committed-and-repeatable-read', domainId: 'db-transactions', title: 'Read Committed and Repeatable Read',
    summary: 'Read Committed normally chooses a fresh committed view per statement; Repeatable Read aims for stability across a transaction, but locking, snapshot timing, predicate handling, and even supported names differ materially across engines.',
    keyPoints: [
      { text: 'Read Committed prevents dirty reads, not stale decisions.', detail: 'Two statements can see different committed states, and a read followed by a write can still race.' },
      { text: 'Repeatable Read is not one algorithm.', detail: 'It may retain read locks, use a transaction snapshot, add next-key locks for locking reads, or be absent as a named vendor level.' },
      { text: 'Plain and locking reads differ.', detail: '`SELECT FOR UPDATE` deliberately participates in write locking and may observe or protect a different state from a nonlocking snapshot read.' },
      { text: 'Snapshot scope matters.', detail: 'Statement snapshots prevent dirty/fractured results within one query; transaction snapshots stabilize multiple queries.' },
      { text: 'Writes still need a concurrency strategy.', detail: 'Use atomic conditional DML, row locks, version checks, or serializable execution for decisions whose premise must remain true.' },
    ],
    blocks: [
      { kind: 'code', language: 'text', title: 'Fuzzy read under Read Committed', code: 'Start: order 42 status=PENDING; invariant: one transaction’s approval decision uses one stated order state.\nT1 reads PENDING. T2 writes CANCELLED and commits. T1 reads CANCELLED.\nBoth values were committed, but T1 did not observe a stable premise.' },
      { kind: 'table', caption: 'Current vendor behavior—verify configuration and access path', headers: ['Engine / level', 'Typical read view', 'Important qualification'], rows: [['PostgreSQL 18 RC', 'Statement snapshot', 'Each command may see a newer commit'], ['PostgreSQL 18 RR', 'Transaction snapshot', 'Snapshot isolation; serialization-style update conflicts, no phantoms in docs table'], ['MySQL 8.4 InnoDB RC', 'Fresh snapshot per consistent read', 'Gap locking reduced; locking reads differ'], ['MySQL 8.4 InnoDB RR', 'Snapshot from first consistent read', 'Default; next-key locks for locking/index scans'], ['SQL Server 2025 RC', 'Locking RC or statement row versions', '`READ_COMMITTED_SNAPSHOT` changes mechanism'], ['SQL Server 2025 RR', 'Shared locks retained to commit', 'New predicate rows can still appear'], ['Oracle 26ai RC', 'Statement-level read consistency', 'Default; no named RR level'], ['SQLite', 'Serialized writes; snapshot isolation in WAL', '`read_uncommitted` matters only with shared cache; one writer']] },
      { kind: 'paragraph', text: 'The table deliberately does not collapse equal names. PostgreSQL Repeatable Read is snapshot-based; SQL Server Repeatable Read is lock-based and does not protect new qualifying rows; Oracle exposes Read Committed and Serializable rather than a separately named Repeatable Read.' },
      { kind: 'pitfall', title: 'Mixing snapshot and current reads', text: 'In engines with MVCC, a nonlocking read and a later locking read can intentionally follow different visibility rules. Document which observation guards the write.' },
      { kind: 'bestPractice', title: 'Pin and probe the deployed configuration', text: 'Record engine/version, isolation, database options, storage engine, query form, and indexes; rerun the two-session trace after upgrades.' },
    ],
    refs: [{ book: 'postgresql-docs', chapter: '13.2 — Transaction Isolation' }, { book: 'mysql-docs', chapter: '17.7.2.1–17.7.2.3 — Isolation Levels and Consistent Reads' }, { book: 'sqlserver-docs', chapter: 'Transaction locking and row versioning guide' }, { book: 'oracle-docs', chapter: 'Database Concepts — Data Concurrency and Consistency' }, { book: 'sqlite-docs', chapter: 'Isolation In SQLite' }],
    related: ['isolation-levels-and-anomalies', 'snapshot-isolation-and-write-skew', 'locking-and-two-phase-locking'],
  },
  {
    id: 'snapshot-isolation-and-write-skew', domainId: 'db-transactions', title: 'Snapshot Isolation and Write Skew',
    summary: 'Snapshot isolation gives a transaction a consistent historical view and usually rejects concurrent writes to the same item. It can nevertheless admit write skew when transactions read a shared predicate and update disjoint rows.',
    keyPoints: [
      { text: 'A snapshot is a visibility rule.', detail: 'Reads select versions committed before the snapshot and normally include the transaction’s own writes.' },
      { text: 'First-committer/updater-wins is row-conflict protection.', detail: 'It prevents many lost updates but does not detect every dependency cycle across different rows.' },
      { text: 'Write skew preserves rows but breaks predicates.', detail: 'Each row can satisfy its local constraint while the set-level invariant fails.' },
      { text: 'Phantoms can supply the stale premise.', detail: 'A decision based on “no matching row exists” needs predicate protection, a materialized conflict, or serializable validation.' },
      { text: 'Fixes encode the conflict.', detail: 'Serializable isolation, locking all relevant rows/ranges, or updating a shared guard row can force one transaction to wait or abort.' },
    ],
    blocks: [
      { kind: 'code', language: 'text', title: 'Canonical disjoint-row write skew', code: 'Start: doctors(A,on_call=true), (B,on_call=true). Invariant: COUNT(on_call) >= 1.\nT1 snapshot: count=2. T2 snapshot: count=2.\nT1 writes only A=false. T2 writes only B=false.\nT1 commits; T2 commits (no same-row write conflict).\nFinal count=0: invariant violated.' },
      { kind: 'diagram', title: 'The dangerous structure', code: 'sequenceDiagram\n  participant T1\n  participant DB\n  participant T2\n  T1->>DB: read A,B = on\n  T2->>DB: read A,B = on\n  T1->>DB: write A = off\n  T2->>DB: write B = off\n  T1->>DB: commit\n  T2->>DB: commit under plain SI\n  Note over DB: zero doctors on call' },
      { kind: 'table', caption: 'Ways to preserve the invariant', headers: ['Technique', 'How it creates safety', 'Trade-off'], rows: [['Serializable/SSI', 'Detects dependency structure and aborts', 'Transaction retry'], ['Lock relevant rows/range', 'Makes decisions wait for concurrent writers', 'Blocking; predicate/index details'], ['Guard row', 'Both transactions update one shared conflict', 'Hotspot'], ['Declarative constraint', 'Commit cannot store invalid state', 'Cross-row assertions often unavailable']] },
      { kind: 'paragraph', text: 'Snapshot isolation is stronger than statement-level Read Committed for read skew and often lost updates, yet it is not serializability. “MVCC” identifies version storage, not the complete isolation contract.' },
      { kind: 'pitfall', title: 'Locking only the row being changed', text: 'T1 and T2 change different doctors, so those locks never conflict. Lock the rows or predicate that justify the decision, or use serializable validation.' },
      { kind: 'bestPractice', title: 'Search for multi-object decisions', text: 'Audit uniqueness checks, quotas, schedules, inventory totals, and “at least one” rules; write the disjoint-write trace before selecting an isolation level.' },
    ],
    refs: [{ book: 'ddia-2', chapter: 'Ch. 8 — Snapshot Isolation and Repeatable Read; Write Skew and Phantoms' }, { book: 'berenson-isolation', chapter: '§4.2 — Snapshot Isolation' }, { book: 'postgresql-ssi', chapter: '§2.1 — Snapshot Isolation; §3.3 — SSI' }, { book: 'database-system-concepts', chapter: 'Ch. 18 — Multiversion Concurrency Control' }],
    related: ['mvcc', 'serializable-transactions', 'application-concurrency-and-transaction-boundaries'],
  },
  {
    id: 'serializable-transactions', domainId: 'db-transactions', title: 'Serializable Transactions',
    summary: 'Serializable isolation permits only outcomes equivalent to a serial execution. Engines achieve it through serial execution, strict two-phase locking, SSI or other validation, trading blocking or aborts for a tractable correctness model.',
    keyPoints: [
      { text: 'Serializable is an outcome guarantee.', detail: 'It does not require transactions to execute literally one at a time, and does not necessarily preserve external real-time order.' },
      { text: 'Strict 2PL prevents cycles by blocking.', detail: 'Read/write and predicate locks are held through commit, giving recoverable serializable histories but allowing deadlocks.' },
      { text: 'SSI detects dangerous dependencies.', detail: 'Transactions read snapshots optimistically; the engine tracks read/write dependencies and aborts transactions that could complete a serialization cycle.' },
      { text: 'Aborts are part of the API.', detail: 'Serialization failure means the whole transaction’s premise is invalid. Retry from the beginning with fresh reads, subject to idempotency and a bound.' },
      { text: 'Serializable does not cover the outside world.', detail: 'Values read before BEGIN and remote calls are not magically placed in the database serial order.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Serializable implementation families', headers: ['Family', 'Primary response to conflict', 'Operational consequence'], rows: [['Serial execution', 'Queue work', 'No concurrency inside partition; transactions must be short'], ['Strict 2PL', 'Wait on incompatible locks', 'Deadlocks and lock footprint'], ['SSI / optimistic validation', 'Run then abort dangerous structure', 'More concurrency; retry pressure'], ['Deterministic ordering', 'Preorder operations', 'Needs known read/write set or coordination']] },
      { kind: 'code', language: 'text', title: 'On-call trace under serializable', code: 'Start: A=true, B=true; invariant at least one on call.\nT1 and T2 read both rows; T1 proposes A=false; T2 proposes B=false.\nSerializable outcome: one order may commit; the other must wait and see the new state or abort.\nBoth may not commit with final A=false, B=false.' },
      { kind: 'paragraph', text: 'PostgreSQL Serializable uses SSI over snapshots and reports serialization failures. Oracle Serializable uses transaction-level read consistency and rejects certain conflicting updates. SQL Server Serializable primarily uses key-range locking. Equal labels, different mechanisms and failure surfaces.' },
      { kind: 'note', title: 'SSI is not plain SI', text: 'The extra dependency tracking and abort rule are what close write-skew cycles; merely storing multiple versions does not.' },
      { kind: 'pitfall', title: 'Retrying only the failed statement', text: 'The earlier reads established a stale premise. Roll back and rerun the complete transaction function, with external effects protected from duplication.' },
      { kind: 'bestPractice', title: 'Make retryable units explicit', text: 'Keep inputs immutable, move remote calls outside, classify retryable error codes, cap attempts and latency, and observe abort rate by transaction type.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 18 — Concurrency Control' }, { book: 'ddia-2', chapter: 'Ch. 8 — Serializability' }, { book: 'postgresql-ssi', chapter: '§3.3 — SSI; §5.2 — Detecting Conflicts' }, { book: 'postgresql-docs', chapter: '13.2.3 — Serializable Isolation Level' }],
    related: ['snapshot-isolation-and-write-skew', 'locking-and-two-phase-locking', 'deadlocks-contention-and-retries'],
  },
  {
    id: 'locking-and-two-phase-locking', domainId: 'db-transactions', title: 'Locking and Two-Phase Locking',
    summary: 'Locks coordinate access to logical resources at row, key, range, page, or table granularity. Two-phase locking grows then shrinks a lock set; strict variants retain write locks through completion, while predicate protection is essential for serializable searches.',
    keyPoints: [
      { text: 'Shared and exclusive modes express compatibility.', detail: 'Many readers may share; an exclusive writer conflicts. Update modes can reduce conversion deadlocks in some engines.' },
      { text: 'Intention locks make hierarchy safe.', detail: '`IS`, `IX`, and `SIX` announce lower-level locks so table locks can test compatibility without scanning every row lock.' },
      { text: 'Granularity trades concurrency for overhead.', detail: 'Fine locks permit more overlap; escalation or coarse locking reduces bookkeeping but increases unrelated blocking.' },
      { text: '2PL has growing and shrinking phases.', detail: 'After releasing any lock, basic 2PL acquires no new locks. Strict 2PL commonly holds exclusive locks until commit/abort.' },
      { text: 'Rows do not represent absent rows.', detail: 'Predicate locks or index key-range/next-key locks protect “all rows matching P,” including gaps where a phantom could be inserted.' },
      { text: '2PL is not two-phase commit.', detail: '2PL is concurrency control; 2PC coordinates one atomic decision across participants.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Simplified compatibility', headers: ['Held / requested', 'S', 'X'], rows: [['S', 'compatible', 'wait'], ['X', 'wait', 'wait']] },
      { kind: 'diagram', title: 'Lock hierarchy and intention', code: 'flowchart TD\n  DB["Database"] --> T["inventory table: IX"]\n  T --> P["index page"]\n  P --> K["product 42 key: X"]\n  T --> R["predicate/key range: S-range"]' },
      { kind: 'code', language: 'text', title: 'Phantom without predicate protection', code: 'Start: no open order for customer 7; invariant at most one open order/customer.\nT1 searches and finds none. T2 searches and finds none.\nT1 inserts order 91; T2 inserts order 92; both commit.\nRow locks on existing rows lock nothing; a UNIQUE constraint or protected key range is required.' },
      { kind: 'paragraph', text: 'An index changes which keys and gaps can be locked and how many resources a query touches. A full scan, missing index, or changed plan can broaden lock footprint; correctness must rely on the engine’s isolation contract, not a guessed row-lock plan.' },
      { kind: 'pitfall', title: 'Assuming SELECT FOR UPDATE locks a business predicate', text: 'It locks what the engine identifies under vendor semantics. Empty results, joins, aggregates, and nonindexed predicates need special scrutiny.' },
      { kind: 'bestPractice', title: 'Lock deliberately and briefly', text: 'Access objects in a stable order, index selective predicates, inspect actual locks/waits, and prefer declarative uniqueness when it exactly expresses the invariant.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 18 — Lock-Based Protocols' }, { book: 'database-internals', chapter: 'Ch. 5 — Concurrency Control' }, { book: 'mysql-docs', chapter: '17.7.1 — InnoDB Locking' }, { book: 'sqlserver-docs', chapter: 'Transaction locking and row versioning guide — Lock modes and granularity' }],
    related: ['serializable-transactions', 'deadlocks-contention-and-retries', 'index-mental-model'],
  },
  {
    id: 'mvcc', domainId: 'db-transactions', title: 'Multiversion Concurrency Control',
    summary: 'MVCC retains multiple logical versions so readers can choose a version visible to their snapshot while writers create successors. Visibility metadata, update conflict rules, and garbage collection—not “MVCC” alone—determine isolation and cost.',
    keyPoints: [
      { text: 'Updates create version chains.', detail: 'A version carries creator/deleter or time/sequence metadata; physical representation may be in-row, undo, or a separate version store.' },
      { text: 'A snapshot defines visibility.', detail: 'The reader considers commit status and relative transaction IDs/timestamps, excludes in-progress/aborted versions, and normally sees its own changes.' },
      { text: 'Readers and writers often avoid blocking each other.', detail: 'Writers still conflict with writers, and schema, predicate, or maintenance operations can still block.' },
      { text: 'Garbage collection follows the oldest needed view.', detail: 'Vacuum/purge/version-store cleanup cannot reclaim a version visible to any active snapshot or required recovery horizon.' },
      { text: 'Long transactions create retention debt.', detail: 'They lengthen chains, consume undo/version space, delay cleanup, and can turn a read-only report into an operational problem.' },
      { text: 'MVCC is not an isolation level.', detail: 'It can implement statement snapshots, snapshot isolation, or serializable schemes with added validation/predicate protection.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Versions, snapshot, and reclamation', code: 'flowchart LR\n  V3["v3: stock=7, committed 30"] --> V2["v2: stock=8, committed 20"] --> V1["v1: stock=10, committed 10"]\n  S["snapshot at 25"] --> V2\n  O["oldest active snapshot=15"] --> G["GC horizon"]\n  G -. "v1 still potentially visible" .-> V1' },
      { kind: 'code', language: 'text', title: 'Visibility example', code: 'Start versions: v1 stock=10 committed at 10; v2 stock=8 committed at 20.\nT1 snapshot time=15 reads v1=10. T2 snapshot time=25 reads v2=8.\nA new v3 committed at 30 is invisible to both snapshots.\nCleanup cannot remove v1 while T1 may still read it.' },
      { kind: 'table', caption: 'Physical strategies vary', headers: ['Strategy', 'Version location', 'Cleanup concern'], rows: [['Append/new tuple', 'Heap/index-visible chain', 'Dead tuples and index cleanup'], ['Undo-based', 'Current row plus undo records', 'Purge history after old snapshots'], ['Separate version store', 'External store (for example tempdb-based)', 'Store growth and long readers']] },
      { kind: 'paragraph', text: 'The visibility algorithm must also account for aborted and in-progress creators. A timestamp alone is insufficient unless the system’s commit protocol makes that timestamp authoritative.' },
      { kind: 'pitfall', title: 'Calling MVCC nonblocking', text: 'Write conflicts, uniqueness checks, foreign keys, DDL, buffer pressure, and cleanup all create waits or aborts. MVCC mainly changes read/write interaction.' },
      { kind: 'bestPractice', title: 'Monitor age, not just count', text: 'Track oldest transaction/snapshot, version or undo bytes, cleanup lag, dead versions, and transaction duration; cancel abandoned sessions safely.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 18 — Multiversion Concurrency Control' }, { book: 'ddia-2', chapter: 'Ch. 8 — Multiversion Concurrency Control' }, { book: 'database-internals', chapter: 'Ch. 5 — Multiversion Concurrency Control' }, { book: 'postgresql-internals', chapter: 'Ch. 5 — Row Versions and Snapshots' }],
    related: ['snapshot-isolation-and-write-skew', 'write-ahead-logging-and-checkpoints', 'pages-records-and-file-layout'],
  },
  {
    id: 'deadlocks-contention-and-retries', domainId: 'db-transactions', title: 'Deadlocks, Contention, and Retries',
    summary: 'A deadlock is a cycle of waits; ordinary contention is a queue without a cycle. Engines detect cycles or use timeouts and sacrifice work, so applications need short, consistently ordered transactions and bounded, transaction-level retries.',
    keyPoints: [
      { text: 'A wait-for cycle cannot resolve itself.', detail: 'If T1 waits for T2 and T2 waits for T1, one must abort or release resources; the engine normally selects a victim.' },
      { text: 'Timeout is not proof of deadlock.', detail: 'It bounds waiting and can break undetected cycles, but a slow holder or overloaded system can trigger the same symptom.' },
      { text: 'Deadlocks are normal but rates are diagnostic.', detail: 'Correct code must tolerate occasional victims; frequent cycles indicate inconsistent ordering, broad scans, or oversized boundaries.' },
      { text: 'Retry the whole invalidated unit.', detail: 'Rollback, reacquire a connection if needed, reread state, and replay only operations designed for repetition.' },
      { text: 'Backoff needs jitter and bounds.', detail: 'Exponential delay with randomness reduces synchronized collisions; maximum attempts, deadline, and cancellation prevent retry storms.' },
      { text: 'Idempotency is not automatic.', detail: 'Emails, charges, random IDs, and sequence allocation may already have effects even when database work aborts or the commit response is lost.' },
    ],
    blocks: [
      { kind: 'code', language: 'text', title: 'Two-account deadlock', code: 'Start: A=100, B=100; invariant total=200.\nT1 locks A, then requests B. T2 locks B, then requests A.\nWait-for edges T1→T2 and T2→T1 form a cycle.\nEngine aborts one victim; survivor commits; victim must restart from its first read.' },
      { kind: 'diagram', title: 'Wait-for graph', code: 'flowchart LR\n  T1["T1 holds A"] -->|"waits for B"| T2["T2 holds B"]\n  T2 -->|"waits for A"| T1' },
      { kind: 'code', language: 'text', title: 'Bounded retry policy', code: 'deadline = now + 2s; maxAttempts = 4\nfor attempt in 1..maxAttempts:\n  begin; run complete transaction from fresh reads; commit; return\n  on classified deadlock/serialization failure:\n    rollback; if deadline exceeded: fail\n    sleep(random(0, min(25ms * 2^(attempt-1), 250ms)))\nNever retry unknown errors or duplicate external effects blindly.' },
      { kind: 'table', caption: 'Reduce contention at its source', headers: ['Symptom', 'Practice'], rows: [['Opposite lock order', 'Sort keys and access tables consistently'], ['Many locks', 'Add selective indexes; shrink scanned set'], ['Long holders', 'Remove remote/user waits; commit promptly'], ['Hot row', 'Partition, batch, or redesign shared counter'], ['Retry storm', 'Jitter, cap, admission control, observability']] },
      { kind: 'pitfall', title: 'Catching every SQLException and retrying', text: 'Syntax, authorization, constraint, connection, and ambiguous-commit failures need different handling. Retry only classified transient outcomes and preserve an idempotency key.' },
      { kind: 'bestPractice', title: 'Observe both victim and blocker', text: 'Capture transaction name, attempt, SQL state/vendor code, wait graph, held duration, query plan, and success after retry without logging sensitive parameters.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 18 — Deadlock Handling' }, { book: 'ddia-2', chapter: 'Ch. 8 — Handling errors and aborts; Two-Phase Locking' }, { book: 'mysql-docs', chapter: '17.7.5 — Deadlocks in InnoDB' }, { book: 'sqlserver-docs', chapter: 'Deadlocks guide' }],
    related: ['locking-and-two-phase-locking', 'serializable-transactions', 'application-concurrency-and-transaction-boundaries'],
  },
  {
    id: 'application-concurrency-and-transaction-boundaries', domainId: 'db-transactions', title: 'Application Concurrency and Transaction Boundaries',
    summary: 'Applications preserve invariants by combining short database transactions with atomic DML, pessimistic locks or optimistic version checks. Savepoints scope partial database rollback; idempotency and outbox-style boundaries address retries and remote effects, while 2PC has availability and operability costs.',
    keyPoints: [
      { text: 'Optimistic checks turn stale writes into zero-row outcomes.', detail: 'Update with the version read and increment it atomically; a zero row count means reread, merge, or report conflict—not unconditional retry.' },
      { text: 'Pessimistic locks protect a decision window.', detail: 'Use locking reads when contention is expected and waiting is preferable, but keep the transaction short and lock the full invariant scope.' },
      { text: 'Savepoints are partial database rollback markers.', detail: 'They do not commit independently, release every lock uniformly across vendors, or compensate a remote side effect.' },
      { text: 'Remote calls do not belong inside ordinary DB transactions.', detail: 'They extend locks and leave ambiguous cross-system outcomes; record durable intent/outbox state and deliver idempotently.' },
      { text: 'Idempotency keys name one logical command.', detail: 'A unique key plus stored result lets repeats converge, including after an unknown commit response.' },
      { text: '2PC coordinates atomic commit but can block and amplify failure.', detail: 'Prepared participants retain resources while awaiting the coordinator; heterogeneous XA adds operational recovery and heuristic-outcome complexity.' },
    ],
    blocks: [
      { kind: 'code', language: 'sql', title: 'Optimistic inventory reservation', code: 'UPDATE inventory\nSET reserved = reserved + :qty, version = version + 1\nWHERE product_id = :id\n  AND version = :seen_version\n  AND on_hand - reserved >= :qty;\n-- row_count = 1: committed candidate; 0: stale version or insufficient stock—reread.' },
      { kind: 'code', language: 'text', title: 'Concurrent version-check trace', code: 'Start: product 42 on_hand=10, reserved=0, version=7; invariant reserved<=on_hand.\nT1 and T2 read version 7. T1 reserves 6: update succeeds, version=8.\nT2 tries to reserve 6 with version=7: row_count=0.\nT2 rereads available=4 and rejects; invariant preserved.' },
      { kind: 'diagram', title: 'Database-to-remote boundary', code: 'flowchart LR\n  R["request + idempotency key"] --> T["short DB transaction"]\n  T --> D["business rows"]\n  T --> O["outbox row"]\n  O --> W["relay after commit"]\n  W --> E["remote idempotent endpoint"]' },
      { kind: 'table', caption: 'Boundary tools are not substitutes', headers: ['Tool', 'Protects', 'Does not provide'], rows: [['Savepoint', 'Partial rollback within one transaction', 'Independent durability or remote undo'], ['Version column', 'One object from stale overwrite', 'Cross-row predicate serialization'], ['Idempotency key', 'Duplicate logical command', 'Isolation from concurrent different commands'], ['Outbox', 'Atomic business state + message intent', 'Instant delivery or exactly-once consumer effect'], ['2PC/XA', 'Atomic decision across enlisted participants', 'Nonblocking operation or simple recovery']] },
      { kind: 'pitfall', title: 'Holding locks while calling payment', text: 'Latency and retries now hold database resources, and a lost response still leaves the payment outcome uncertain. Persist a state machine and reconcile using stable operation IDs.' },
      { kind: 'bestPractice', title: 'Make the transaction function replayable', text: 'Pass immutable command input, perform all reads inside the attempt, emit external work as durable intent, classify conflicts, and return the stored result for a repeated idempotency key.' },
    ],
    refs: [{ book: 'ddia-2', chapter: 'Ch. 8 — Distributed Transactions; Exactly-Once Message Processing Revisited' }, { book: 'database-system-concepts', chapter: 'Ch. 17 — Transaction Definition in SQL; Ch. 19 — Recovery System' }, { book: 'mysql-docs', chapter: '15.3 — Transactional and Locking Statements; XA Transactions' }, { book: 'postgresql-docs', chapter: 'SAVEPOINT; PREPARE TRANSACTION' }],
    related: ['transactions-and-acid', 'deadlocks-contention-and-retries', 'application-transaction-design'],
  },
]

import type { Topic } from '../../../types/content'

export const dbInternalsTopics: Topic[] = [
  {
    id: 'database-system-architecture', domainId: 'db-internals', title: 'Database System Architecture',
    summary: 'A DBMS is a set of cooperating control planes: SQL is parsed and bound into a logical statement, rewritten and optimized into a physical plan, executed against transactional storage, and supported by logging, cache, and maintenance services. The boundaries vary by engine; the data and durability invariants do not.',
    keyPoints: [
      { text: 'The catalog gives names physical meaning.', detail: 'Binding resolves identifiers, types, privileges, functions, and dependencies against a transactional metadata snapshot before optimization.' },
      { text: 'Rewrite and optimization answer different questions.', detail: 'Rewriting preserves semantics while exposing a better logical form; optimization chooses operators, access paths, orders, and resource strategies.' },
      { text: 'Execution crosses concurrency and storage boundaries.', detail: 'Operators request tuples or batches while locks/MVCC, buffer management, file access, and WAL preserve visibility and recovery rules.' },
      { text: 'Background work is part of foreground correctness.', detail: 'Checkpointing, flushing, reclamation, statistics, and log recycling bound recovery time and keep reusable space and estimates current.' },
      { text: 'Component boundaries are not universal APIs.', detail: 'Some engines fuse rewrite with planning, use push or vectorized execution, or embed storage; the diagram is a responsibility map, not a mandated process topology.' },
      { text: 'Queues reveal overload location.', detail: 'CPU saturation, lock waits, buffer misses, WAL flushes, I/O queues, and maintenance debt create different latency shapes and remedies.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Responsibility and data-flow map', code: 'flowchart TD\n  SQL["SQL + parameters"] --> PAR["parser"]\n  PAR --> CAT["binder / catalog"]\n  CAT --> RW["rewriter"]\n  RW --> OPT["optimizer"]\n  OPT --> EX["executor"]\n  EX --> CC["locks / MVCC"]\n  EX --> ST["storage access methods"]\n  ST --> BP["buffer pool"]\n  BP --> FS["pages / files / devices"]\n  EX --> WAL["WAL / commit path"]\n  BG["checkpoint, vacuum, stats, log recycling"] --> BP\n  BG --> WAL\n  CAT --> OPT\n  CC --> ST' },
      { kind: 'table', caption: 'Boundary contracts', headers: ['Stage', 'Consumes', 'Produces / invariant'], rows: [['Parse', 'SQL text', 'Syntax tree; no object identity yet'], ['Bind/catalog', 'Tree + metadata snapshot', 'Resolved and typed statement'], ['Rewrite/optimize', 'Logical semantics + statistics', 'Equivalent executable strategy'], ['Execute', 'Plan + transaction context', 'Rows/effects under visibility rules'], ['Storage/recovery', 'Page requests + log records', 'Durable, recoverable state']] },
      { kind: 'note', title: 'PostgreSQL 14 mechanics', text: 'PostgreSQL uses a postmaster and per-connection backend processes plus auxiliary processes. Its parser, analyzer/rewriter, planner, and executor feed shared buffers, WAL, checkpointer/background writer, and autovacuum. That concrete process model is PostgreSQL-specific.' },
      { kind: 'pitfall', title: 'Diagnosing every slow query as storage', text: 'A query can wait before touching a data page: parsing storms, catalog locks, plan search, lock queues, WAL flush, or maintenance contention require different evidence.' },
      { kind: 'bestPractice', title: 'Trace one request across boundaries', text: 'Correlate plan nodes, actual rows, waits, buffer hits/reads, temporary I/O, WAL volume, and maintenance state; optimize the first constrained boundary rather than the most familiar component.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 1 — Introduction' }, { book: 'postgresql-internals', chapter: 'Ch. 15 — Query Execution Stages' }, { book: 'cmu-15445', chapter: 'Course Notes — Query Planning & Optimization' }],
    related: ['query-lifecycle-and-iterator-execution', 'buffer-pools-and-caching', 'write-ahead-logging-and-checkpoints'],
  },
  {
    id: 'pages-records-and-file-layout', domainId: 'db-internals', title: 'Pages, Records, and File Layout',
    summary: 'Storage engines move fixed-size pages but store changing-size records. A slotted page separates stable logical slots from movable record bytes, so compaction can recover contiguous free space without invalidating every record identifier.',
    keyPoints: [
      { text: 'The page is the core transfer and recovery unit.', detail: 'Files are divided into fixed-size blocks/pages with headers, checksums or LSNs, record metadata, and payload; exact sizes and fields are engine choices.' },
      { text: 'A record identifier names page plus slot.', detail: 'The slot can remain stable when bytes move inside the page; deletion can mark a slot unused or redirect it rather than renumbering all records.' },
      { text: 'Variable-width rows grow from the opposite end.', detail: 'Slot entries grow forward while tuple bytes grow backward, leaving a free-space region that is compacted when fragmented.' },
      { text: 'Logical free space differs from contiguous free space.', detail: 'Deleted fragments may total enough bytes yet still require compaction before a large record fits.' },
      { text: 'Oversized values need indirection.', detail: 'Engines may use overflow pages, chained fragments, external value stores, or row forwarding; each adds reads and update complexity.' },
      { text: 'Allocation metadata avoids scanning every page.', detail: 'Free-space maps, extent maps, segment headers, and visibility metadata steer insertion and maintenance but must remain conservative under crashes.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Slotted-page invariant', code: 'flowchart LR\n  subgraph P["page N"]\n    H["header: page LSN, bounds"] --> S["slot 0 → offset/length\nslot 1 → redirect\nslot 2 → unused"]\n    S --> F["contiguous free space"]\n    F --> R["record bytes grow backward\n[varlen row 2][row 0]"]\n  end\n  RID["RID = (page N, slot 0)"] --> S\n  C["compact record bytes"] --> R\n  C -. "RID unchanged" .-> RID' },
      { kind: 'table', caption: 'Layout mechanisms and consequences', headers: ['Mechanism', 'Preserves', 'Cost / failure symptom'], rows: [['Slot directory', 'Stable intra-page identity', 'Header overhead; damaged offsets corrupt interpretation'], ['Compaction', 'Reusable contiguous space', 'CPU/latches and extra dirtying'], ['Overflow/external value', 'Bounded base-page row', 'Pointer chasing and orphan cleanup'], ['Free-space map', 'Fast insertion target', 'Stale hints cause retries, not wrong data'], ['Checksum/page LSN', 'Detection/recovery ordering', 'Write overhead; torn/corrupt page becomes visible']] },
      { kind: 'note', title: 'PostgreSQL 14 mechanics', text: 'A PostgreSQL heap tuple identifier (CTID) is block number plus line pointer. Heap pages have a header, item identifier array, free space, and tuple area; large attributes may be compressed or moved to a per-table TOAST relation. CTIDs can change after an update or table rewrite and are not application identity.' },
      { kind: 'pitfall', title: 'Treating a physical locator as a permanent key', text: 'Updates, page splits, forwarding, compaction across pages, clustering, and rewrites can change locators. Persist logical keys outside storage-engine internals.' },
      { kind: 'bestPractice', title: 'Measure width and locality distributions', text: 'Track row-width percentiles, overflow frequency, page fill, fragmentation, and random reads. Average width hides the values that trigger forwarding, splits, or external fetches.' },
    ],
    refs: [{ book: 'database-internals', chapter: 'Ch. 3 — File Formats' }, { book: 'database-system-concepts', chapter: 'Ch. 13 — Data Storage Structures' }, { book: 'postgresql-internals', chapter: 'Ch. 2 — Pages and Tuples' }, { book: 'cmu-15445', chapter: 'Course Notes — Database Storage' }],
    related: ['heap-clustered-and-index-organized-storage', 'buffer-pools-and-caching', 'space-reclamation-and-maintenance'],
  },
  {
    id: 'heap-clustered-and-index-organized-storage', domainId: 'db-internals', title: 'Heap, Clustered, and Index-Organized Storage',
    summary: 'Physical organization decides where a base row lives and what secondary indexes point to. Heaps favor unconstrained placement, clustered layouts colocate a chosen key range, and index-organized tables make the primary search tree the row store.',
    keyPoints: [
      { text: 'Heap placement minimizes ordering obligations.', detail: 'An insert can target a page with space, but ordered access normally needs an index and matching rows may be scattered.' },
      { text: 'Clustering turns key proximity into page proximity.', detail: 'Range reads benefit while maintaining order can cause splits, moves, overflow, or periodic reorganization.' },
      { text: 'Index-organized leaves are the base records.', detail: 'A primary-key lookup ends at the row; wide rows reduce fan-out and increase tree height and split work.' },
      { text: 'Secondary-index payload follows base identity.', detail: 'It may store a stable row locator, primary key, or logical identifier; primary-key width and row movement therefore change secondary lookup cost.' },
      { text: 'Only one physical order can dominate.', detail: 'A table may have many access paths but cannot be simultaneously clustered for unrelated keys without replicas or derived structures.' },
      { text: 'Terminology is engine-specific.', detail: '“Clustered index,” “index-organized,” and one-time physical CLUSTER operations are not interchangeable guarantees.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Where the order row lives', headers: ['Organization', 'Base-row location', 'Secondary entry commonly points to', 'Workload fit'], rows: [['Heap', 'Any page with space', 'RID / tuple locator', 'Mixed writes; many access paths'], ['Clustered table/index', 'Pages ordered near clustering key', 'Clustering key or row locator', 'Frequent ranges on one key'], ['Index-organized', 'Primary-tree leaf', 'Primary key', 'Primary-key reads; narrow stable key']] },
      { kind: 'diagram', title: 'Secondary lookup implications', code: 'flowchart TD\n  S["secondary key: email"] --> SH["heap: email → RID"]\n  SH --> HP["heap page row"]\n  S --> SI["index-organized: email → primary key"]\n  SI --> PK["primary tree lookup → row leaf"]\n  U["primary-key update"] --> SI\n  M["heap row movement"] --> SH' },
      { kind: 'note', title: 'PostgreSQL 14 mechanics', text: 'Ordinary PostgreSQL tables are heaps and secondary indexes contain heap TIDs. `CLUSTER` rewrites a table once using an index; later writes do not preserve that order automatically. HOT updates can avoid new index entries only when indexed columns are unchanged and page-local space permits.' },
      { kind: 'pitfall', title: 'Calling any primary-key index clustered', text: 'A uniqueness constraint says nothing about base-row placement. Confirm what leaf entries contain, whether order is maintained, and how secondary lookups reach the row.' },
      { kind: 'bestPractice', title: 'Choose organization from dominant locality', text: 'Model lookup hops, range pages, key width, update/move frequency, split amplification, and secondary-index bytes under a representative workload.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 13 — Data Storage Structures' }, { book: 'database-internals', chapter: 'Ch. 2 — B-Tree Basics' }, { book: 'postgresql-internals', chapter: 'Ch. 2 — Pages and Tuples; Ch. 4 — Page Pruning and HOT Updates' }],
    related: ['pages-records-and-file-layout', 'b-tree-indexes', 'covering-and-index-only-scans'],
  },
  {
    id: 'buffer-pools-and-caching', domainId: 'db-internals', title: 'Buffer Pools and Caching',
    summary: 'A buffer pool maps persistent page identifiers to memory frames, coordinates concurrent users, and schedules dirty-page writes. Pinning protects active frames from eviction; latches protect in-memory structures; WAL ordering makes eventual dirty-page flush safe.',
    keyPoints: [
      { text: 'A page table separates identity from frame address.', detail: 'Lookup finds a resident frame or reserves a victim, reads the requested page, then publishes the mapping under synchronization.' },
      { text: 'Pins and latches solve different hazards.', detail: 'A pin prevents replacement while a caller uses a frame; a latch protects short critical updates to frame/page memory.' },
      { text: 'Dirty does not mean immediately durable.', detail: 'A modified frame may be written later, but its relevant WAL must reach durable storage before the data page.' },
      { text: 'Replacement approximates future reuse.', detail: 'Clock, LRU variants, scan resistance, and admission policies trade bookkeeping for hit rate; pinned frames are ineligible.' },
      { text: 'Foreground misses can inherit eviction work.', detail: 'No free victim, dirty writeback, WAL flush, or storage latency can turn a logical page request into a stall.' },
      { text: 'The OS cache may duplicate database pages.', detail: 'Buffered file I/O can hold the same bytes in DB frames and kernel cache; direct I/O or coordinated sizing changes the trade-off.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Lookup, pin, dirty, and eviction flow', code: 'stateDiagram-v2\n  [*] --> Lookup\n  Lookup --> PinnedClean: hit + pin\n  Lookup --> Loading: miss + reserve victim\n  Loading --> PinnedClean: read + publish\n  PinnedClean --> PinnedDirty: modify\n  PinnedClean --> UnpinnedClean: unpin\n  PinnedDirty --> UnpinnedDirty: unpin\n  UnpinnedDirty --> Writeback: select victim / background flush\n  Writeback --> UnpinnedClean: flush WAL through pageLSN, write page\n  UnpinnedClean --> Loading: remove mapping, reuse frame' },
      { kind: 'table', caption: 'State constraints', headers: ['State', 'Eligible as victim?', 'Required transition before reuse'], rows: [['Pinned clean/dirty', 'No', 'All users unpin'], ['Unpinned clean', 'Yes', 'Remove mapping before frame reuse'], ['Unpinned dirty', 'Yes', 'Select victim; flush WAL through pageLSN as needed; write page; then reuse'], ['I/O in progress', 'No', 'Waiters observe completion/error consistently']] },
      { kind: 'note', title: 'PostgreSQL 14 mechanics', text: 'PostgreSQL shared buffers use buffer tags, pins, content locks, usage counts, and a clock-sweep replacement strategy. The OS page cache may also cache relation files; PostgreSQL intentionally relies on both rather than promising one exclusive cache.' },
      { kind: 'pitfall', title: 'Maximizing hit ratio in isolation', text: 'A high ratio can hide repeated scans, and a lower ratio can be healthy for deliberate streaming. Dirty backlog, latency, useful bytes, working-set churn, and OS-cache effects matter.' },
      { kind: 'bestPractice', title: 'Separate demand reads from writeback pressure', text: 'Observe hit/miss latency, pinned-frame exhaustion, eviction scans, dirty age, checkpoint writes, WAL waits, and kernel I/O together.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 13 — Data Storage Structures; §13.5 Database Buffer' }, { book: 'database-internals', chapter: 'Ch. 5 — Transaction Processing and Recovery; “Buffer Management”' }, { book: 'postgresql-internals', chapter: 'Ch. 8 — Buffer Cache' }, { book: 'cmu-15445', chapter: 'Course Notes — Buffer Pools' }],
    related: ['pages-records-and-file-layout', 'write-ahead-logging-and-checkpoints', 'explain-and-performance-methodology'],
  },
  {
    id: 'write-ahead-logging-and-checkpoints', domainId: 'db-internals', title: 'Write-Ahead Logging and Checkpoints',
    summary: 'Write-ahead logging decouples transaction durability from random data-page writes: log records describing a change become durable before the changed page, and commit acknowledgment waits for the commit record’s required durability. Checkpoints bound recovery work without necessarily making every page clean.',
    keyPoints: [
      { text: 'WAL-before-data is the ordering invariant.', detail: 'Before a dirty page with page LSN x reaches persistent storage, the log must be durable through x.' },
      { text: 'Commit durability is a log boundary.', detail: 'Under durable commit, acknowledgment follows persistence of the commit record, not flushing every page touched by the transaction.' },
      { text: 'LSNs order log positions and page progress.', detail: 'Log records carry LSNs; a page LSN states the newest logged update reflected in that page and supports idempotent redo tests.' },
      { text: 'Group commit amortizes synchronization.', detail: 'Many transactions can share a durable log flush while preserving each commit boundary.' },
      { text: 'A sharp checkpoint is disruptive.', detail: 'Stopping updates and flushing all dirty pages simplifies recovery but creates a latency and I/O wall.' },
      { text: 'A fuzzy checkpoint records a moving frontier.', detail: 'Updates continue while dirty-page and active-transaction state is captured; recovery may begin earlier and redo selectively.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'WAL and fuzzy-checkpoint ordering', code: 'sequenceDiagram\n  participant T as transaction\n  participant B as buffer pool\n  participant L as WAL device\n  participant D as data device\n  T->>B: update page; assign LSN 420\n  T->>L: append update + commit\n  L-->>T: durable through commit (ack allowed)\n  Note over B,L: checkpoint records active txns / dirty-page frontier while work continues\n  B->>L: ensure WAL durable through pageLSN 420\n  B->>D: write page later\n  Note over L,D: data page must never outrun its WAL' },
      { kind: 'table', caption: 'Checkpoint models', headers: ['Model', 'Concurrent updates', 'Recovery knowledge', 'Operational consequence'], rows: [['Sharp/quiescent', 'Stopped or tightly constrained', 'All prior pages known durable', 'Simple restart; large pause/write burst'], ['Fuzzy', 'Continue', 'Checkpoint plus transaction/dirty-page metadata', 'More recovery logic; smoother foreground work']] },
      { kind: 'note', title: 'PostgreSQL 14 mechanics', text: 'PostgreSQL WAL records are addressed by LSNs. Checkpoints write a checkpoint record and drive dirty-buffer writes over time; full-page images protect against torn pages after the first change following a checkpoint. `synchronous_commit` changes when a client is told a commit is durable, not WAL-before-data correctness.' },
      { kind: 'pitfall', title: 'Equating checkpoint with backup or commit', text: 'A checkpoint is a recovery-progress marker, not a transaction boundary or independent backup. Aggressive checkpoints can increase full-page-image WAL and saturate write I/O.' },
      { kind: 'bestPractice', title: 'Tune the whole durability path', text: 'Measure commit flush latency and batching, WAL generation, checkpoint distance/duration, dirty backlog, full-page-image volume, and restart objectives together.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 19 — Recovery System; §19.5 Buffer Management' }, { book: 'database-internals', chapter: 'Ch. 5 — Transaction Processing and Recovery; “Recovery,” “Log Semantics,” and “Steal and Force Policies”' }, { book: 'postgresql-internals', chapter: 'Ch. 9 — Write-Ahead Log' }, { book: 'aries', chapter: '§1.1 Logging, Failures, and Recovery Methods; §5.4 Checkpoints' }],
    related: ['aries-and-crash-recovery', 'buffer-pools-and-caching', 'transactions-and-acid'],
  },
  {
    id: 'aries-and-crash-recovery', domainId: 'db-internals', title: 'ARIES and Crash Recovery',
    summary: 'ARIES recovers a steal/no-force system in three conceptual passes: analysis reconstructs transaction and dirty-page state, redo repeats history to restore the exact pre-crash state, and undo rolls back loser transactions using compensation log records that make rollback itself restartable.',
    keyPoints: [
      { text: 'Steal requires undo.', detail: 'A page containing an uncommitted update may reach disk, so recovery must remove effects of transactions that never committed.' },
      { text: 'No-force requires redo.', detail: 'Commit need not flush changed pages, so committed effects absent from data files must be reapplied from WAL.' },
      { text: 'Analysis rebuilds recovery tables.', detail: 'Starting from checkpoint information, it identifies winners/losers, transaction lastLSNs, and dirty pages with recLSNs.' },
      { text: 'Redo repeats history.', detail: 'It replays necessary logged actions, including loser work, using page LSN checks to avoid reapplying effects already present.' },
      { text: 'Restart undo takes losers back to transaction begin.', detail: 'Recovery follows each loser transaction’s log chain backward until it is completely undone; it does not stop at a savepoint.' },
      { text: 'CLRs make undo idempotent and restartable.', detail: 'A compensation log record describes an undo already performed and points recovery toward the next action; a crash during undo does not undo the undo.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'ARIES restart passes', code: 'flowchart LR\n  C["last checkpoint"] --> A["ANALYSIS\ntransaction table\ndirty page table"]\n  A --> R["REDO from smallest recLSN\nrepeat history\npageLSN test"]\n  R --> U["UNDO losers backward\nwrite CLRs + END"]\n  U --> OK["consistent committed state"]\n  CR["crash during undo"] --> A2["restart"]\n  A2 --> R2["redo CLRs"]\n  R2 --> U2["continue via undoNextLSN"]' },
      { kind: 'table', caption: 'Why each record exists', headers: ['Metadata', 'Recovery question'], rows: [['prevLSN / lastLSN', 'What is the previous action for this transaction?'], ['pageID + pageLSN', 'Is this page action already reflected?'], ['recLSN', 'How early might redo for this dirty page be needed?'], ['CLR + undoNextLSN', 'Which undo completed, and where should undo continue?'], ['commit/end status', 'Winner, loser, or fully completed?']] },
      { kind: 'note', title: 'Savepoints are normal-processing rollback boundaries', text: 'A transaction may request a partial rollback to a savepoint while it is running. ARIES logs that undo with CLRs; if a crash follows, restart redoes those CLRs and uses undoNextLSN to skip work already undone, while still taking every loser transaction back to its begin.' },
      { kind: 'note', title: 'Algorithm family, not universal engine script', text: 'ARIES is a precise recovery design, but engines may use physical, physiological, or logical logging; shadow paging; append-only structures; different checkpoint metadata; or MVCC-specific cleanup. PostgreSQL WAL recovery is ARIES-influenced but is not a literal implementation of every ARIES table and pass.' },
      { kind: 'pitfall', title: 'Skipping loser updates during redo', text: 'Undo expects the historical state and logged chains to be reconstructed. “Redo only committed transactions” breaks repeating history and complicates partial rollback.' },
      { kind: 'bestPractice', title: 'Test crashes at ordering boundaries', text: 'Inject failure before/after log append, WAL flush, page write, commit, checkpoint, CLR, and undo completion; restart repeatedly and verify atomicity plus idempotence.' },
    ],
    refs: [{ book: 'aries', chapter: '§3 Overview of ARIES; §5.2 Total or Partial Rollbacks; §6 Restart Processing' }, { book: 'database-system-concepts', chapter: 'Ch. 19 — Recovery System; §19.9 ARIES' }, { book: 'database-internals', chapter: 'Ch. 5 — Transaction Processing and Recovery; “Recovery” and “ARIES”' }, { book: 'cmu-15445', chapter: 'Course Notes — Database Recovery' }],
    related: ['write-ahead-logging-and-checkpoints', 'transactions-and-acid', 'mvcc'],
  },
  {
    id: 'query-lifecycle-and-iterator-execution', domainId: 'db-internals', title: 'Query Lifecycle and Iterator Execution',
    summary: 'A query moves from syntax through name/type binding, semantic rewriting and physical planning into an executor tree. In the classic pull iterator model, each parent asks a child for the next tuple, so access, filtering, joining, grouping, and projection compose without materializing every intermediate.',
    keyPoints: [
      { text: 'Parsing is not semantic validation.', detail: 'The parser recognizes grammar; binding resolves names, scopes, overloads, types, privileges, and parameter coercions.' },
      { text: 'Rewrites must preserve observable semantics.', detail: 'Predicate movement, subquery decorrelation, constant folding, and view expansion are constrained by NULLs, outer joins, volatility, ordering, and security.' },
      { text: 'Planning chooses executable properties.', detail: 'Access paths, join order/algorithm, distribution, ordering, materialization, memory, and parallelism turn algebra into a physical tree.' },
      { text: 'Pull iterators compose open/next/close.', detail: 'A root demand propagates downward; pipelined nodes return incrementally while blocking nodes such as sort consume input first.' },
      { text: 'Rescan and parameterization multiply work.', detail: 'A nested-loop inner subtree may execute once per outer row; per-loop instrumentation must be read with loop counts.' },
      { text: 'Vectorized and push engines change granularity.', detail: 'Batches reduce dispatch overhead and improve CPU locality; push pipelines reverse control. Neither changes SQL semantics.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Lifecycle and pull-based operator tree', code: 'flowchart TD\n  SQL["SQL"] --> P["parse"] --> B["bind / type"] --> W["rewrite"] --> O["cost plan"]\n  O --> A["Aggregate (blocking/hash state)"]\n  A --> J["Nested Loop / Hash Join"]\n  J --> F["Filter orders"]\n  F --> S["Index or table scan"]\n  J --> I["items lookup"]\n  CLIENT["client asks root for next"] -.-> A\n  A -. "next() demand" .-> J\n  J -. "next() demand" .-> F' },
      { kind: 'table', caption: 'Operator execution properties', headers: ['Operator', 'Pipeline behavior', 'Key consequence'], rows: [['Scan/filter/project', 'Usually streaming', 'Early selectivity reduces upstream work'], ['Nested loop', 'Streaming but rescans inner', 'Tiny estimate miss can multiply cost'], ['Hash join/aggregate', 'Build/state then output', 'Memory and spill depend on actual width/rows'], ['Sort', 'Blocking', 'Startup latency and external runs'], ['Materialize', 'Stores reusable result', 'Avoids rescan at memory/temp-I/O cost']] },
      { kind: 'note', title: 'PostgreSQL 14 mechanics', text: 'PostgreSQL parses, analyzes/rewrites, plans, and initializes a plan-state tree. `ExecProcNode`-style calls pull tuples through nodes; nodes may materialize, sort, hash, run in parallel, or be rescanned. This is a PostgreSQL implementation of a broader iterator family.' },
      { kind: 'pitfall', title: 'Reading a plan as a chronological list', text: 'A plan is a demand tree. Startup/total time, loops, rows removed, blocking nodes, and child dependencies determine when and how often work occurs.' },
      { kind: 'bestPractice', title: 'Reconcile estimates with execution flow', text: 'Walk leaves to root, multiply per-loop work, identify first cardinality divergence and blocking/spill boundaries, then connect them to buffers and waits.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 15 — Query Processing' }, { book: 'database-system-concepts', chapter: 'Ch. 16 — Query Optimization' }, { book: 'postgresql-internals', chapter: 'Ch. 15 — Query Execution Stages' }, { book: 'cmu-15445', chapter: 'Course Notes — Query Execution' }],
    related: ['database-system-architecture', 'cost-based-query-planning', 'join-sort-and-aggregation-algorithms'],
  },
  {
    id: 'row-column-and-compressed-storage', domainId: 'db-internals', title: 'Row, Column, and Compressed Storage',
    summary: 'Row stores colocate attributes for one entity; column stores colocate values of one attribute across many entities. Encoding and compression exploit physical regularity, while late materialization delays row reconstruction so analytical operators carry only needed columns and compact position/selection vectors.',
    keyPoints: [
      { text: 'Row layout favors whole-record locality.', detail: 'Point lookup, insert, and update touch few pages when most columns of a small number of rows are needed.' },
      { text: 'Column layout favors projection and scans.', detail: 'Analytics can read only referenced columns and process homogeneous arrays with better cache/SIMD behavior.' },
      { text: 'Compression is part of execution economics.', detail: 'Dictionary, run-length, bit packing, delta, frame-of-reference, and general codecs trade CPU for fewer bytes and can permit operations on encoded data.' },
      { text: 'Late materialization avoids premature width.', detail: 'Filters and aggregates operate on columns plus row/position identifiers; full tuples are assembled only when required.' },
      { text: 'Updates complicate columnar locality.', detail: 'Mutable analytics systems use deltas, delete vectors, write-optimized stores, or immutable segments later merged into compressed form.' },
      { text: 'Hybrid formats choose a unit of locality.', detail: 'Column groups, PAX-like pages, row groups, and replicas can balance point access, scan pruning, compression, and write cost.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Same four orders, different locality', code: 'flowchart LR\n  subgraph ROW["row pages"]\n    R1["7 | PAID | 42.00"]\n    R2["8 | NEW | 9.00"]\n    R3["9 | PAID | 15.00"]\n  end\n  subgraph COL["column segments"]\n    C1["id: 7,8,9"]\n    C2["status: D0,D1,D0 (dictionary)"]\n    C3["amount: 42,9,15 (bit-packed/delta)"]\n  end\n  C2 --> SV["selection vector: positions 0,2"]\n  SV --> C3\n  C3 --> LM["late materialize selected result"]' },
      { kind: 'table', caption: 'Physical-fit comparison', headers: ['Need', 'Row-oriented', 'Column-oriented'], rows: [['One order, most fields', 'Few localized reads', 'Gather from several segments'], ['Sum amount for paid orders', 'Reads unused columns', 'Reads status + amount; encoded scan'], ['Single-row update', 'Localized in-place/version write', 'Delta/delete vector or segment rewrite'], ['Compression', 'Mixed values limit runs', 'Homogeneous values expose dictionaries/runs/deltas']] },
      { kind: 'note', title: 'Storage labels are not workload guarantees', text: 'A row engine can have columnar extensions and vectorized nodes; a columnar warehouse can maintain row/write buffers. Page/segment size, pruning metadata, sort order, codec, and merge policy often matter more than the label.' },
      { kind: 'pitfall', title: 'Maximizing compression ratio', text: 'A denser codec may cost more decode CPU, prevent predicate evaluation on encoded values, enlarge update deltas, or increase read amplification. Optimize query cost and operability, not ratio alone.' },
      { kind: 'bestPractice', title: 'Benchmark the physical pipeline', text: 'Measure bytes read, columns projected, rows/segments pruned, decode CPU, vector width, materialization width, update delta, compaction, and concurrency on realistic distributions.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 13 — Data Storage Structures; Column-Oriented Storage' }, { book: 'database-internals', chapter: 'Ch. 3 — File Formats' }, { book: 'cmu-15445', chapter: 'Course Notes — Storage Models & Data Representation' }, { book: 'ddia-2', chapter: 'Ch. 4 — Storage and Retrieval' }],
    related: ['pages-records-and-file-layout', 'join-sort-and-aggregation-algorithms', 'cost-based-query-planning'],
  },
  {
    id: 'space-reclamation-and-maintenance', domainId: 'db-internals', title: 'Space Reclamation and Maintenance',
    summary: 'Deletes and updates usually make bytes logically obsolete before they become physically reusable. Reclamation must prove that no active snapshot or recovery/replication consumer needs an old version, then prune, compact, vacuum, merge, or rewrite while controlling bloat and foreground interference.',
    keyPoints: [
      { text: 'Logical death precedes physical reuse.', detail: 'MVCC delete/update marks a version obsolete; visibility horizons decide when readers can no longer observe it.' },
      { text: 'Page pruning and file shrinking are different.', detail: 'A vacuum may make internal space reusable without returning blocks to the operating system; truncation or rewrite has stronger prerequisites and cost.' },
      { text: 'Long snapshots pin history.', detail: 'Old transactions, replicas, prepared transactions, or logical decoding can hold a global horizon and retain dead versions and logs.' },
      { text: 'Bloat amplifies unrelated work.', detail: 'More pages mean larger scans, cache churn, index depth/garbage, backups, WAL, and maintenance cycles.' },
      { text: 'Compaction moves bytes and consumes resources.', detail: 'Page compaction, segment merge, index cleanup, and table rewrite require CPU/I/O, latches/locks, WAL, and temporary capacity.' },
      { text: 'Maintenance must outrun garbage creation.', detail: 'Thresholds, worker capacity, throttling, and per-object exceptions should be based on dead-byte rate and horizon age, not a fixed calendar alone.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Version-to-reusable-space lifecycle', code: 'flowchart LR\n  LIVE["live version"] --> DEAD["update/delete: dead to new snapshots"]\n  DEAD --> WAIT["retained by oldest snapshot / replica / recovery horizon"]\n  WAIT --> SAFE["globally reclaimable"]\n  SAFE --> PRUNE["prune index/page references"]\n  PRUNE --> REUSE["compact + free space reusable"]\n  REUSE --> SHRINK["optional truncate/rewrite returns file space"]\n  LONG["long transaction"] -. "holds horizon" .-> WAIT\n  LOAD["foreground writes"] --> DEAD' },
      { kind: 'table', caption: 'Maintenance levels', headers: ['Action', 'Reclaims', 'Usually does not guarantee', 'Risk'], rows: [['Prune/garbage collect', 'Dead record/version references', 'Smaller file', 'CPU/latch contention'], ['Vacuum/merge', 'Reusable pages/segments and metadata', 'OS space return', 'I/O/WAL backlog'], ['Truncate tail', 'Empty trailing blocks', 'Internal-hole removal', 'Lock/horizon constraints'], ['Rewrite/repack', 'Dense new physical copy', 'Cheap online operation', 'Space, WAL, lock and replica load']] },
      { kind: 'note', title: 'PostgreSQL 14 mechanics', text: 'PostgreSQL autovacuum identifies dead heap tuples using visibility horizons, prunes pages, updates the free-space/visibility maps, cleans indexes, and protects transaction-ID wraparound. Ordinary `VACUUM` mainly enables reuse; `VACUUM FULL` rewrites and takes a stronger lock. Long transactions and replication slots can delay cleanup.' },
      { kind: 'pitfall', title: 'Killing maintenance to reduce load', text: 'Deferral converts current load into larger tables/indexes, worse cache locality, longer future vacuum, wraparound risk, and a more disruptive catch-up event.' },
      { kind: 'bestPractice', title: 'Operate from horizon age and debt', text: 'Alert on oldest snapshot/transaction and retained WAL, dead tuples/bytes, table and index growth, cleanup throughput, freeze age, and worker saturation; tune hot objects independently.' },
    ],
    refs: [{ book: 'database-internals', chapter: 'Ch. 4 — Implementing B-Trees; “Vacuum and Maintenance” and “Page Defragmentation”' }, { book: 'postgresql-internals', chapter: 'Ch. 5 — Vacuum and Autovacuum; Ch. 7 — Rebuilding Tables and Indexes' }, { book: 'cmu-15445', chapter: 'Course Notes — Multi-Version Concurrency Control' }],
    related: ['mvcc', 'pages-records-and-file-layout', 'buffer-pools-and-caching'],
  },
]

import type { Topic } from '../../../types/content'

export const dbOperationsTopics: Topic[] = [
  {
    id: 'roles-privileges-and-least-authority', domainId: 'db-operations', title: 'Roles, Privileges, and Least Authority',
    summary: 'Least authority gives each human, service, and automation identity only the operations it needs, for only as long as it needs them, while separating login, membership, ownership, and emergency elevation so access remains reviewable and revocable.',
    keyPoints: [
      { text: 'Identity, role membership, ownership, and object privileges are separate controls.', detail: 'A login authenticates a principal; group roles collect capabilities; owners can alter or drop their objects and usually grant access; privileges authorize particular operations.' },
      { text: 'Applications should not own their schema.', detail: 'A non-login owner role controls DDL, a migration identity can assume it under change control, and runtime roles receive only required DML and routine execution.' },
      { text: 'Future objects need an explicit grant policy.', detail: 'Default privileges are set by the role that will create objects and are scoped by object kind and schema; changing another administrator’s defaults does not repair existing objects.' },
      { text: 'Escalation paths are part of the model.', detail: 'Role administration, ownership, SECURITY DEFINER routines, unsafe search paths, extension installation, replication capabilities, and credential stores can all bypass an apparently narrow GRANT list.' },
      { text: 'Access expires unless renewed.', detail: 'Inventory direct and inherited grants, owners, dormant logins, shared identities, break-glass use, and privilege changes; review against current service ownership and job duties.' },
      { text: 'Emergency access needs stronger evidence, not a permanent superuser.', detail: 'Use time-bounded approval, separate credentials, session recording or audit events, immediate alerting, and a post-use review.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Ownership and grant model', code: 'flowchart TD\n  H["human SSO/login"] -->|temporary membership| M["migration role"]\n  M -->|SET ROLE under change control| O["commerce_owner NOLOGIN"]\n  A["orders_app LOGIN"] --> R["orders_runtime NOLOGIN"]\n  R -->|USAGE| S["commerce schema"]\n  R -->|SELECT/INSERT/UPDATE selected columns| T["orders + order_items"]\n  O -->|owns| S\n  O -->|default privileges for future objects| R\n  B["break_glass NOLOGIN"] -. "approved, expiring elevation" .-> O' },
      { kind: 'code', language: 'sql', title: 'PostgreSQL: separate owner, runtime, and migration identities', code: "CREATE ROLE commerce_owner NOLOGIN;\nCREATE ROLE orders_runtime NOLOGIN;\nCREATE ROLE orders_app LOGIN NOINHERIT;\nGRANT orders_runtime TO orders_app;\n\nCREATE SCHEMA commerce AUTHORIZATION commerce_owner;\nGRANT USAGE ON SCHEMA commerce TO orders_runtime;\nGRANT SELECT, INSERT ON commerce.orders TO orders_runtime;\nGRANT SELECT, INSERT ON commerce.order_items TO orders_runtime;\nREVOKE CREATE ON SCHEMA public FROM PUBLIC;\n\n-- Run as the role that will create future objects.\nALTER DEFAULT PRIVILEGES FOR ROLE commerce_owner IN SCHEMA commerce\n  GRANT SELECT, INSERT ON TABLES TO orders_runtime;" },
      { kind: 'table', caption: 'Quarterly access-review evidence', headers: ['Question', 'Evidence', 'Action'], rows: [['Who can log in?', 'principal, owner, last use, credential age', 'disable dormant/shared identities'], ['What is inherited?', 'membership graph and admin options', 'remove transitive or circular elevation'], ['Who owns objects?', 'schema/object/routine owners', 'move ownership from people/apps'], ['How can policy be bypassed?', 'superuser, bypass, definer, extension, replication paths', 'narrow, monitor, or remove'], ['Are grants still justified?', 'service catalog and approved capability', 'renew with expiry or revoke']] },
      { kind: 'pitfall', title: 'Revoking a role but leaving an equivalent path', text: 'Ownership, PUBLIC grants, nested membership, a privileged definer routine, or a leaked migration credential may preserve the same capability. Review effective privileges and escalation paths, not only direct grants.' },
      { kind: 'bestPractice', title: 'Test the authorization matrix', text: 'For each runtime role, execute representative allowed and denied operations in CI or a disposable database. Alert on role, ownership, and grant changes, and rehearse break-glass revocation.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '4.7 — Authorization; 9.8 — Application Security' }, { book: 'postgresql-docs', chapter: 'Ch. 21 — Database Roles; 5.8 — Privileges; ALTER DEFAULT PRIVILEGES' }, { book: 'database-reliability-engineering', chapter: 'Ch. 9 — Security' }],
    related: ['row-level-security-and-data-boundaries', 'sql-injection-and-query-safety', 'schema-migrations-and-database-testing'],
  },
  {
    id: 'row-level-security-and-data-boundaries', domainId: 'db-operations', title: 'Row-Level Security and Data Boundaries',
    summary: 'Row-level security adds a database-enforced predicate to ordinary table access, but safe tenant isolation also requires trustworthy identity propagation, constrained ownership and bypass roles, complete policy coverage, and adversarial tests.',
    keyPoints: [
      { text: 'RLS is defense in depth, not tenant identity discovery.', detail: 'The policy can only be as correct as the session attribute or database role that identifies the tenant; pooled-session leakage or caller-controlled settings defeat the boundary.' },
      { text: 'Visibility and writes need separate reasoning.', detail: 'A USING expression filters existing rows; WITH CHECK constrains rows introduced by INSERT or UPDATE. Test both old-row selection and new-row acceptance.' },
      { text: 'Owners and privileged roles may bypass policy.', detail: 'PostgreSQL table owners normally bypass RLS unless FORCE ROW LEVEL SECURITY is used, while superusers and BYPASSRLS roles bypass it regardless; maintenance paths must be deliberately isolated.' },
      { text: 'Policy composition changes meaning.', detail: 'Permissive policies are OR-combined; restrictive policies are AND-combined with the permissive result. A harmless-looking new permissive policy can broaden access.' },
      { text: 'RLS does not solve every side channel.', detail: 'Constraints, errors, timing, sequences, statistics, definer routines, backups, replicas, and observability systems can reveal or move data outside the row predicate.' },
      { text: 'A denied-case suite is mandatory.', detail: 'Test missing identity, wrong tenant, forged identity, cross-tenant joins, inserts, key-changing updates, deletes, owner/bypass execution, reused sessions, and bulk/maintenance workflows.' },
    ],
    blocks: [
      { kind: 'code', language: 'sql', title: 'PostgreSQL: tenant policy on the canonical orders table', code: "-- This multi-tenant example extends the canonical\n-- commerce.orders(order_id, customer_id, placed_at, status) with tenant_id.\nALTER TABLE commerce.orders ENABLE ROW LEVEL SECURITY;\nALTER TABLE commerce.orders FORCE ROW LEVEL SECURITY;\n\nCREATE POLICY orders_tenant_access ON commerce.orders\n  AS PERMISSIVE FOR ALL TO orders_runtime\n  USING (tenant_id = current_setting('app.tenant_id', true)::uuid)\n  WITH CHECK (tenant_id = current_setting('app.tenant_id', true)::uuid);\n\nCREATE POLICY orders_not_archived ON commerce.orders\n  AS RESTRICTIVE FOR SELECT TO orders_runtime\n  USING (status <> 'ARCHIVED');" },
      { kind: 'table', caption: 'Policy tests', headers: ['Case', 'Expected'], rows: [['Tenant A selects A order', 'row returned'], ['Tenant A selects/updates/deletes B order', 'zero rows or denied'], ['Tenant A inserts/retags row as B', 'WITH CHECK rejects'], ['Identity absent or malformed', 'fail closed; transaction discarded/reset'], ['Table owner or BYPASSRLS role', 'explicitly demonstrates bypass'], ['Pooled connection reused for B', 'transaction-local identity reset; only B visible']] },
      { kind: 'paragraph', text: 'Set tenant context from authenticated server-side data inside the transaction, not from raw request text. Prefer a transaction-local setting and revoke direct table access from roles that should enter through a trusted routine or view. The exact mechanism is vendor-specific; the invariant is a non-forgeable identity-to-session binding.' },
      { kind: 'pitfall', title: 'Testing only as the table owner', text: 'A development connection that owns the table can make a correct policy look empty or an incorrect policy look safe because owner bypass changes behavior. Test the exact runtime principal and a deliberately privileged control principal.' },
      { kind: 'bestPractice', title: 'Treat policy changes like schema changes', text: 'Review policy truth tables and composition, migrate them transactionally where supported, monitor changes, and run cross-tenant negative tests after every role, pool, ORM, and policy change.' },
    ],
    refs: [{ book: 'postgresql-docs', chapter: '5.9 — Row Security Policies; CREATE POLICY' }, { book: 'database-system-concepts', chapter: '4.7 — Authorization; 9.8 — Application Security' }, { book: 'database-reliability-engineering', chapter: 'Ch. 9 — Security' }],
    related: ['roles-privileges-and-least-authority', 'application-schema-patterns', 'connection-pooling'],
  },
  {
    id: 'encryption-secrets-and-auditing', domainId: 'db-operations', title: 'Encryption, Secrets, and Auditing',
    summary: 'Database protection is layered: encryption limits disclosure in particular threat paths, secret management controls who can authenticate or decrypt, auditing preserves accountable evidence, and minimization reduces what an attacker or operator can obtain at all.',
    keyPoints: [
      { text: 'Name the adversary and the boundary for every encryption layer.', detail: 'TLS protects traffic between authenticated endpoints; storage encryption protects lost media or snapshots; application/field encryption can hide values from database operators but changes search, constraints, and key custody.' },
      { text: 'Encryption at rest does not stop an authorized query.', detail: 'A compromised database process, privileged session, vulnerable application, or exported plaintext can read data after transparent decryption.' },
      { text: 'Keys and credentials have lifecycles.', detail: 'Generate in a managed boundary, distribute narrowly, version, rotate without downtime, revoke, destroy after retention, and maintain an independently protected recovery path.' },
      { text: 'Audit logs must resist the subject they audit.', detail: 'Capture authenticated principal, assumed role, action, object, decision, time, source, request/trace ID, and outcome; ship promptly to restricted append-oriented storage with integrity and retention controls.' },
      { text: 'Sensitive data belongs neither in logs nor broad telemetry.', detail: 'Parameter values, full SQL, snapshots, traces, and error payloads can create a second ungoverned database. Redact or tokenize before export and restrict high-cardinality labels.' },
      { text: 'Minimization is the strongest blast-radius reduction.', detail: 'Do not collect unneeded fields; shorten retention, separate duties, tokenize stable identifiers, and delete cryptographic keys only when loss of recovery is intended and verified.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Layered threat model', headers: ['Layer', 'Helps against', 'Does not by itself stop'], rows: [['TLS with endpoint verification', 'network interception and tampering', 'compromised endpoint or stolen credential'], ['Volume/backup encryption', 'lost disks, media, raw snapshots', 'database/app reading plaintext'], ['Application/field encryption', 'selected DB/operator disclosure', 'compromised app/key service; metadata leakage'], ['Secret manager + short-lived identity', 'credential sprawl and long exposure', 'overprivileged issued identity'], ['Tamper-resistant audit sink', 'repudiation and local log deletion', 'the original data access'], ['Minimization/tokenization', 'impact of compromise', 'misuse of retained cleartext']] },
      { kind: 'diagram', title: 'Key and credential lifecycle', code: 'flowchart LR\n  G["generate in KMS/HSM"] --> I["issue versioned key or short-lived credential"]\n  I --> U["use by named workload"]\n  U --> R["rotate: new writes/new sessions first"]\n  R --> M["migrate or rewrap old material"]\n  M --> V["verify decrypt/recovery and audit"]\n  V --> X["revoke old version"]\n  X --> D["destroy after retention + legal hold"]' },
      { kind: 'code', language: 'text', title: 'Audit event contract', code: 'event_id, occurred_at_utc, authenticated_principal, assumed_role\naction, object_type, object_id_or_token, authorization_decision\nsource_service, source_address_class, request_id, trace_id, outcome\npolicy_version, database_instance, immutable_sink_sequence\n\nNever include passwords, private keys, raw payment data, or unrestricted bind values.' },
      { kind: 'pitfall', title: 'One key beside the encrypted database', text: 'If the same administrator, host, or backup contains ciphertext and its unprotected key, encryption adds little separation. Isolate key administration, access policy, logging, backup, and recovery credentials.' },
      { kind: 'bestPractice', title: 'Rehearse rotation and loss', text: 'Practice certificate, credential, data-key, and master-key rotation; test rollback and old-backup recovery; alert on decryption, secret reads, audit gaps, disabled logging, and unusual privileged access.' },
    ],
    refs: [{ book: 'database-reliability-engineering', chapter: 'Ch. 9 — Security; Encryption of Data' }, { book: 'postgresql-docs', chapter: '18.9 — Secure TCP/IP Connections with SSL; Ch. 19 — Server Configuration' }, { book: 'sql-antipatterns', chapter: 'Ch. 20 — Readable Passwords' }],
    related: ['roles-privileges-and-least-authority', 'database-observability', 'backup-restore-and-recovery-objectives'],
  },
  {
    id: 'backup-restore-and-recovery-objectives', domainId: 'db-operations', title: 'Backup, Restore, and Recovery Objectives',
    summary: 'A backup strategy starts from business-defined recovery point and recovery time objectives, maps them to independent copies and restore procedures, and proves through drills that a recovered service is consistent, usable, secure, and recent enough.',
    keyPoints: [
      { text: 'RPO limits tolerable data loss; RTO limits tolerable service disruption.', detail: 'Both are business decisions per workflow and failure class, not properties a backup product can choose. Dependencies and validation time count toward RTO.' },
      { text: 'Backup types solve different restore problems.', detail: 'Logical exports offer selective portability; physical copies restore whole clusters quickly; full, incremental, differential, snapshot, and log archives trade copy time, chain length, storage, and recovery complexity.' },
      { text: 'A replica is not an independent backup.', detail: 'Deletion, corruption, malicious writes, and schema mistakes replicate. Keep copies across failure and administrative boundaries, including off-site and immutable or offline retention.' },
      { text: 'Retention must cover discovery delay and dependencies.', detail: 'Keep every base, incremental, catalog, encryption key, binary/configuration, and log segment needed for the oldest promised recovery point, plus legal and deletion requirements.' },
      { text: 'Checksums prove bytes, not business usability.', detail: 'A successful restore still needs catalog, constraint, application, security, freshness, and representative query checks before declaring recovery.' },
      { text: 'Restore drills produce the only credible RTO evidence.', detail: 'Measure request, transfer, reconstruction, replay, validation, credential/routing, and application restart phases under realistic volume and staff availability.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Recovery-objective worksheet', headers: ['Workflow / failure', 'Impact and objective', 'Protection', 'Restore evidence'], rows: [['Order intake / accidental delete', 'RPO ≤ 5 min; RTO ≤ 60 min', 'daily physical base + continuous log archive; force/archive partially filled logs within a tested bound below 5 min, alert before the bound is consumed, and retain an immutable copy', 'restore to before delete; verify archived-log age/continuity and reconcile operation IDs'], ['Reporting / bad deployment', 'RPO 24 h acceptable; RTO 4 h', 'nightly logical export + schema/version manifest', 'load clean instance; row/count and report checks'], ['Region loss', 'business-set RPO/RTO', 'independent regional copy, keys/config/catalog', 'isolated regional exercise including DNS/clients'], ['Credential/key compromise', 'contain immediately; recovery may need clean-room rebuild', 'separate backup and key administration', 'prove revoked identity cannot read restored system']] },
      { kind: 'diagram', title: 'Evidence-producing restore drill', code: 'flowchart LR\n  O["select failure + target RPO/RTO"] --> C["obtain backup catalog, bytes, keys, binaries, config"]\n  C --> R["restore into isolated environment"]\n  R --> P["replay logs / apply incrementals"]\n  P --> V["verify engine + schema + constraints + app invariants"]\n  V --> S["security, clients, observability"]\n  S --> E["record achieved point/time and remediate gaps"]' },
      { kind: 'code', language: 'text', title: 'Restore acceptance checklist', code: '[ ] requested recovery target is present and unambiguous\n[ ] backup pieces, manifests, checksums, and keys verified\n[ ] engine starts without ignored recovery errors\n[ ] schema/version and required extensions/configuration match\n[ ] constraints and representative business invariants pass\n[ ] newest durable operation IDs/timestamps establish achieved RPO\n[ ] runtime roles, RLS, encryption, and audit export work\n[ ] application smoke/read/write checks pass\n[ ] elapsed time and every manual dependency recorded' },
      { kind: 'pitfall', title: 'A green backup job with no restore owner', text: 'Successful upload only proves that some bytes were written. Missing catalogs, keys, versions, permissions, network paths, or validation can make those bytes unrecoverable inside the objective.' },
      { kind: 'bestPractice', title: 'Make recovery a recurring production capability', text: 'Automate restore into isolation, sample old and recent points, rotate operators, preserve drill evidence, and page on broken backup chains or missed recovery windows rather than only job failure.' },
    ],
    refs: [{ book: 'database-reliability-engineering', chapter: 'Ch. 7 — Backup and Recovery' }, { book: 'postgresql-docs', chapter: 'Ch. 25 — Backup and Restore' }, { book: 'database-system-concepts', chapter: 'Ch. 19 — Recovery System' }],
    related: ['point-in-time-recovery', 'write-ahead-logging-and-checkpoints', 'failover-disaster-recovery-and-reliability'],
  },
  {
    id: 'point-in-time-recovery', domainId: 'db-operations', title: 'Point-in-Time Recovery',
    summary: 'Point-in-time recovery reconstructs database state from a consistent base backup plus an unbroken ordered archive of recovery logs, stopping at a chosen target on a new history and then validating the recovered service.',
    keyPoints: [
      { text: 'A base backup and continuous log archive are one recovery chain.', detail: 'The base supplies pages; archived WAL or equivalent records every required change afterward. Neither component alone provides arbitrary recovery points.' },
      { text: 'The target must be chosen from business evidence.', detail: 'Timestamp, transaction ID, restore point, log sequence, or immediate consistency are technical selectors; correlate them with audit events, deployment markers, time zones, and commit uncertainty.' },
      { text: 'Archive gaps are hard recovery limits.', detail: 'One missing required segment prevents replay beyond the gap. Monitor completeness and restore retrieval, not only archive-command exit status.' },
      { text: 'Recovery creates a new timeline or history.', detail: 'After promotion, later records from the abandoned history must not be confused with the new branch. Preserve history metadata and rebuild descendants from the chosen branch.' },
      { text: 'Retention is coupled across base backups, logs, catalog, and keys.', detail: 'Deleting an old base can orphan logs; deleting one log can invalidate several bases. Expiry must understand whole recoverable chains and the oldest promised target.' },
      { text: 'Stopping is not finishing.', detail: 'Validate integrity, schema, business invariants, security, replication, clients, and achieved recovery point before routing production traffic.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'PITR timeline and branch', code: 'flowchart LR\n  B["base backup 00:00"] --> W1["logs 00:00–09:41"] --> E["bad delete commits 09:42"] --> W2["logs after 09:42"]\n  W1 --> T["restore target: before delete"]\n  T --> N["promote: new timeline 7"]\n  E -. "old timeline 6 retained for evidence" .-> A["archive"]\n  G["missing segment"] -. "would cap replay" .-> W1' },
      { kind: 'table', caption: 'Recovery target choices', headers: ['Selector', 'Useful when', 'Hazard'], rows: [['Named restore point', 'application/deployment emits a marker', 'marker must precede harmful commit'], ['Transaction/log ID', 'exact offending transaction known', 'include/exclude semantics must be rehearsed'], ['Timestamp', 'incident time is bounded', 'clock/time-zone and commit ordering uncertainty'], ['Latest consistent', 'hardware loss without logical corruption', 'replays harmful logical change too'], ['Immediate after base', 'test base independently', 'large RPO by design']] },
      { kind: 'code', language: 'text', title: 'PITR rehearsal runbook', code: '1. Freeze deletion and copy archive/catalog evidence; do not overwrite history.\n2. Choose a compatible base before the target; verify manifest and keys.\n3. Restore into an isolated destination with no production client route.\n4. Retrieve every required log in order; fail closed on a gap.\n5. Stop before/after the selected event according to rehearsed semantics.\n6. Inspect recovery completion and new history/timeline metadata.\n7. Validate business state and achieved point; then rebuild replicas and route clients.' },
      { kind: 'pitfall', title: 'Guessing “one second before”', text: 'Application clocks, database clocks, time zones, concurrent commits, and delayed detection make wall time ambiguous. Prefer a durable restore marker or transaction boundary and inspect neighboring business operations.' },
      { kind: 'bestPractice', title: 'Continuously prove the archive chain', text: 'Restore recent and aged bases in isolation, replay through sampled targets, alert on segment age/gaps and catalog inconsistency, and record measured recovery rate so log volume cannot silently exceed RTO.' },
    ],
    refs: [{ book: 'postgresql-docs', chapter: '25.3 — Continuous Archiving and Point-in-Time Recovery (PITR)' }, { book: 'database-reliability-engineering', chapter: 'Ch. 7 — Backup and Recovery' }, { book: 'database-system-concepts', chapter: '19.9 — ARIES' }],
    related: ['backup-restore-and-recovery-objectives', 'write-ahead-logging-and-checkpoints', 'postgresql-wal-backup-and-replication'],
  },
  {
    id: 'database-observability', domainId: 'db-operations', title: 'Database Observability',
    summary: 'Database observability connects user-visible workload and latency to transactions, waits, plans, replication, recovery, and host resources while controlling metric cardinality and protecting sensitive query data.',
    keyPoints: [
      { text: 'Start from service outcomes.', detail: 'Measure successful and failed operations, latency distributions, throughput, freshness, and correctness indicators by bounded operation class before inspecting internal counters.' },
      { text: 'Saturation is a queue, not just high CPU.', detail: 'Connection waiters, runnable work, lock queues, storage latency/depth, memory pressure, spills, checkpoint pressure, and replica apply queues expose constrained resources.' },
      { text: 'Waits explain where elapsed time goes.', detail: 'Relate sessions and transactions to blocking chains, I/O, locks, client reads/writes, CPU scheduling, and background work; a long query text alone is not a cause.' },
      { text: 'Query fingerprints need plan and data context.', detail: 'Normalize literals, retain bounded samples, correlate deployments and schema/statistics changes, and compare estimate-versus-actual evidence rather than ranking only total time.' },
      { text: 'Protection systems are part of health.', detail: 'Observe backup age and restore tests, archive continuity, replica lag in time and bytes, replay conflicts, recovery headroom, maintenance debt, and credential/audit pipeline status.' },
      { text: 'Telemetry has cost and privacy boundaries.', detail: 'Unbounded SQL, bind values, tenant IDs, plan labels, and stack traces create cardinality, memory, network, and disclosure risks. Aggregate, sample, redact, retain briefly, and restrict access.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Observability map', headers: ['Layer', 'Signals', 'Diagnostic join key'], rows: [['Service/workload', 'rate, errors, latency percentiles, freshness', 'operation + request/trace ID'], ['Admission/session', 'pool wait, connections, state, transaction age', 'service + session/application name'], ['Query/transaction', 'fingerprint, calls, rows, elapsed, errors, plans', 'fingerprint + transaction/request ID'], ['Contention', 'wait class/event, blocker chain, deadlocks', 'session/transaction/resource'], ['Resources', 'CPU/run queue, memory, cache, IOPS/latency, network', 'instance + time'], ['Data/maintenance', 'growth, dead versions, stats age, bloat, jobs', 'database/object + time'], ['Protection', 'backup/restore, archive, replication lag/conflict, audit export', 'instance/stream + timeline']] },
      { kind: 'diagram', title: 'Evidence correlation', code: 'flowchart LR\n  U["user impact"] --> O["operation + trace"]\n  O --> Q["query fingerprint + transaction"]\n  Q --> W["wait / blocker / plan"]\n  W --> R["CPU, memory, storage, network"]\n  Q --> D["data shape + statistics + schema"]\n  Q --> C["deploy/config/recovery events"]\n  P["privacy + cardinality budget"] -. "constrains collection" .-> Q' },
      { kind: 'code', language: 'text', title: 'Alert contract', code: 'symptom: checkout p99 and order-create p99 exceed objective for 10 min\nimpact: affected operations, regions, tenants (bounded categories), error budget burn\ncorroboration: DB active/waiting, wait classes, storage latency, lock graph\ncontext: deploy/schema/config/failover events within 60 min\nrunbook: safe evidence query, containment options, owner, escalation\nauto-close: sustained recovery plus backlog drained\n\nDo not alert on a single internal metric without an actionable hypothesis.' },
      { kind: 'pitfall', title: 'Full SQL and tenant IDs as metric labels', text: 'This creates near-unbounded series and exports sensitive values. Use normalized fingerprints and controlled dimensions; keep redacted samples in a secured, short-lived diagnostic store.' },
      { kind: 'bestPractice', title: 'Design dashboards from incident questions', text: 'Can users succeed? Is work queued? What resource or lock is gating it? What changed? Are replicas and recovery protections healthy? Ensure timestamps and identifiers let an operator answer those questions quickly.' },
    ],
    refs: [{ book: 'database-reliability-engineering', chapter: 'Ch. 4 — Operational Visibility' }, { book: 'postgresql-docs', chapter: 'Ch. 27 — Monitoring Database Activity' }, { book: 'ddia-2', chapter: 'Ch. 2 — Defining Nonfunctional Requirements' }],
    related: ['slow-query-and-incident-diagnosis', 'explain-and-performance-methodology', 'connection-pooling'],
  },
  {
    id: 'slow-query-and-incident-diagnosis', domainId: 'db-operations', title: 'Slow-Query and Incident Diagnosis',
    summary: 'Database incident response begins with user impact and recent change, preserves time-correlated evidence, separates blocking, resource, plan, and data-shape causes, applies reversible containment, and turns the result into tested prevention and detection.',
    keyPoints: [
      { text: 'Establish impact and a timeline before tuning.', detail: 'Identify failed/slow operations, start time, affected scope, error-budget burn, data correctness risk, and recent application, schema, statistics, configuration, topology, or workload changes.' },
      { text: 'Preserve evidence before killing the symptom.', detail: 'Capture bounded session/transaction state, blocker graph, waits, query fingerprints, plans where safe, resource signals, replication state, and change events with synchronized timestamps.' },
      { text: 'Classify the bottleneck.', detail: 'Blocking has holders and waiters; resource saturation has a constrained queue; plan regressions change access/join/spill behavior; data-shape changes alter cardinality, skew, or working set. Several may form a causal chain.' },
      { text: 'Containment must be safer than the incident.', detail: 'Cancel obsolete work before terminating sessions, shed new load, pause a batch, roll back a known change, or reduce concurrency. Consider transaction rollback cost, unknown outcomes, retry storms, and replica impact.' },
      { text: 'Explain evidence, not folklore.', detail: 'Use actual execution only when its side effects and load are safe; distinguish estimated and observed rows/timing, warm and cold cache, parameter values, concurrency, and production data distribution.' },
      { text: 'A post-incident action changes the system.', detail: 'Add a regression test, capacity/admission control, safer migration, index/query correction, alert/runbook improvement, or ownership decision; track it to verification.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Diagnostic flow', code: 'flowchart TD\n  I["impact, scope, timeline, correctness risk"] --> C["recent code/schema/config/load/topology changes"]\n  C --> E["preserve sessions, waits, blockers, fingerprints, plans, resources"]\n  E --> K{"dominant evidence"}\n  K -->|holder/wait graph| L["blocking/contention"]\n  K -->|queue + saturated resource| R["CPU/memory/storage/network/admission"]\n  K -->|estimate/access/join/spill shift| P["plan/statistics"]\n  K -->|volume/skew/working-set shift| D["data shape"]\n  L --> M["bounded reversible containment"]\n  R --> M\n  P --> M\n  D --> M\n  M --> V["verify impact, backlog, correctness, protections"]\n  V --> F["postmortem and tested follow-up"]' },
      { kind: 'table', caption: 'Evidence by hypothesis', headers: ['Hypothesis', 'Look for', 'Safe first move'], rows: [['Blocking', 'wait chain, old transaction, lock/resource', 'stop source or cancel obsolete waiter; preserve graph'], ['Admission overload', 'pool/connection queue, rising tails, DB saturation', 'shed/defer work; prevent retry amplification'], ['Storage/CPU/memory', 'latency/run queue/cache/spills and throughput', 'reduce competing work; protect headroom'], ['Plan/statistics', 'fingerprint plan change, estimate error, spill', 'rollback known change or use tested targeted mitigation'], ['Data shape', 'growth, skew, hot key/partition, changed selectivity', 'isolate hot workload; repair query/index/design'], ['Replica', 'send/replay lag, conflicts, stale-read symptoms', 'route consistency-sensitive reads primary; bound backlog']] },
      { kind: 'code', language: 'text', title: 'Incident log skeleton', code: '13:02 impact: checkout p99 8s; 12% order-create timeouts; no correctness alarm\n13:04 change: catalog import began 12:58; no deploy/schema change\n13:06 evidence: one import transaction age 8m; lock graph + fingerprint saved\n13:08 containment: stop new imports; cancel import statement; no session kill\n13:11 verification: blockers clear, backlog drains, replica/archive healthy\n13:30 cause hypothesis: oversized transaction + conflicting order lock order\nfollow-up: bounded batches, lock-order test, alert on old write transaction; owners/dates' },
      { kind: 'pitfall', title: 'Running EXPLAIN ANALYZE on harmful DML', text: 'Actual execution executes the statement and can add load, locks, or writes. Prefer captured plan telemetry, estimated explain, a rollback-safe isolated copy, or a carefully bounded read-only reproduction.' },
      { kind: 'bestPractice', title: 'Use one incident commander and timestamped evidence', text: 'Separate coordination from diagnosis, record hypotheses and interventions, assign an observer to watch side effects, and define rollback and success conditions before each mitigation.' },
    ],
    refs: [{ book: 'database-reliability-engineering', chapter: 'Ch. 3 — Risk Management; Ch. 4 — Operational Visibility' }, { book: 'postgresql-docs', chapter: '27.1 — Standard Unix Tools; 27.2 — The Cumulative Statistics System' }, { book: 'sql-performance-explained', chapter: 'Execution Plans' }],
    related: ['database-observability', 'explain-and-performance-methodology', 'deadlocks-contention-and-retries'],
  },
  {
    id: 'capacity-maintenance-and-data-lifecycle', domainId: 'db-operations', title: 'Capacity, Maintenance, and Data Lifecycle',
    summary: 'Capacity engineering forecasts data and workload growth against measured ceilings, while maintenance and lifecycle policies keep statistics, versions, indexes, partitions, and retained data within safe operational and legal bounds.',
    keyPoints: [
      { text: 'Forecast demand and each constraining resource.', detail: 'Track rows/bytes and change rate, transactions/queries, concurrency, CPU, memory, I/O, WAL/log volume, replication/archive bandwidth, backup/restore duration, and maintenance work with uncertainty bands and seasonal scenarios.' },
      { text: 'Connections are a workload ceiling, not capacity by themselves.', detail: 'Reserve operator and background headroom, bound pools across maximum instance count, and load-test useful concurrency; more admitted sessions may only deepen queues.' },
      { text: 'MVCC creates maintenance debt.', detail: 'Old snapshots and write churn delay version reclamation; vacuum/purge/compaction, freezing or transaction-age safeguards, statistics collection, and index cleanup need resources before an emergency.' },
      { text: 'Partitions help only with an explicit lifecycle or access boundary.', detail: 'They can make retention drops and pruning efficient, but add catalog/planning/index/constraint complexity and do not cure arbitrary slow queries.' },
      { text: 'Delete, archive, and legal retention are different states.', detail: 'Define event-based eligibility, searchable archive format, deletion propagation to replicas/backups/caches, holds, tombstones or evidence, and tested restore/disposal procedures.' },
      { text: 'Maintenance changes the workload.', detail: 'Reindexing, statistics, vacuum, partition movement, archival, and mass deletion consume locks, I/O, CPU, log and replica bandwidth; schedule, throttle, observe, pause, and rehearse them.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Capacity model', headers: ['Dimension', 'Measure and forecast', 'Trigger / response'], rows: [['Primary data/indexes', 'bytes/day, rows/day, amplification, skew', 'storage and maintenance lead time'], ['Compute/memory', 'CPU/run queue, working set, cache misses, spills', 'optimize/admit/scale before tail collapse'], ['Connections/work', 'active, waiting, transaction age, service time', 'pool budget and workload shedding'], ['Logs/replication', 'bytes/s, retained bytes, send/apply rate', 'bandwidth/storage headroom and lag policy'], ['Backups/restores', 'copy/replay/validation rate vs size', 'objective breach forecast; redesign chain'], ['Maintenance', 'dead space, stats age, transaction age, index health', 'routine threshold plus emergency margin']] },
      { kind: 'diagram', title: 'Lifecycle states for commerce data', code: 'flowchart LR\n  A["active orders"] -->|business close| C["closed online"]\n  C -->|retention eligibility| P["cold partition/archive copy"]\n  P -->|archive verified + cataloged| D["delete from primary"]\n  D -->|backup expiry and cache/replica propagation| X["disposed"]\n  H["legal hold"] -. "blocks transition" .-> P\n  H -. "blocks disposal" .-> X' },
      { kind: 'code', language: 'text', title: 'Maintenance safety envelope', code: 'preconditions: replica/archive/backup healthy; headroom available; no conflicting migration\nlimits: statement/lock timeout, batch rows/bytes, WAL rate, IOPS, lag, runtime\nobserve: user latency/errors, waits, dead space, stats, logs, replica/archive backlog\npause when: objective burn, blocker age, lag/backlog or storage threshold exceeded\nverify: object validity, statistics, query plans, recovered space, backup/PITR chain\nrollback/recovery: documented per operation; mass deletes are not assumed reversible' },
      { kind: 'pitfall', title: 'Deleting millions of rows in one transaction', text: 'The transaction can retain locks and old versions, generate a log/replication spike, delay cleanup, exhaust disk, and make rollback slower than the maintenance window. Use bounded, observable batches or partition lifecycle operations where semantics allow.' },
      { kind: 'bestPractice', title: 'Tie forecasts to procurement and change lead time', text: 'Report date ranges rather than one exhaustion date, include failure/failover and maintenance headroom, define owners and escalation thresholds, and validate the model against actual growth and recovery drills.' },
    ],
    refs: [{ book: 'database-reliability-engineering', chapter: 'Ch. 2 — Service-Level Management; Ch. 10 — Data Storage, Indexing, and Replication' }, { book: 'postgresql-docs', chapter: 'Ch. 24 — Routine Database Maintenance Tasks; 5.12 — Table Partitioning' }, { book: 'ddia-2', chapter: 'Ch. 2 — Defining Nonfunctional Requirements' }],
    related: ['space-reclamation-and-maintenance', 'connection-pooling', 'replication-and-read-scaling'],
  },
  {
    id: 'replication-and-read-scaling', domainId: 'db-operations', title: 'Replication and Read Scaling',
    summary: 'Replication maintains additional copies by shipping physical changes or logical data operations; topology, acknowledgement policy, lag, conflicts, and routing determine durability and read semantics, while independent backups remain necessary.',
    keyPoints: [
      { text: 'Physical and logical replication copy different abstractions.', detail: 'Physical replication follows storage/log changes and is usually close to engine/version internals; logical replication publishes row/data changes and supports selective or transformed topologies with identity, ordering, and DDL constraints.' },
      { text: 'Synchronous and asynchronous describe acknowledgement, not freshness forever.', detail: 'Synchronous commit waits for configured replica evidence and raises write latency/availability tradeoffs; asynchronous commit can lose acknowledged recent writes on failover and permits variable lag.' },
      { text: 'Lag has several stages.', detail: 'Measure generation, network receipt, durable flush, replay/apply, and query-visible position in time and bytes. A zero transport queue can coexist with blocked apply or replay.' },
      { text: 'Read replicas weaken session expectations unless routing repairs them.', detail: 'Read-after-write and monotonic reads need a commit-position token, sticky primary/session routing, an apply wait with a deadline, or a product decision to expose staleness.' },
      { text: 'Replicas have conflicts and backpressure.', detail: 'Long reads can delay replay or be canceled; logical apply can hit keys/constraints; slots or retention horizons can pin logs and fill disks. Bound lag and disconnect or rebuild unhealthy consumers.' },
      { text: 'Replication is not backup.', detail: 'Logical mistakes, compromised credentials, corrupt changes, and destructive DDL can propagate. Retain independently administered, point-in-time recoverable copies.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Read-scaling topology and positions', code: 'flowchart LR\n  C["client"] --> W["write router"]\n  W --> P["primary: log position L120"]\n  P -->|physical async stream| R1["replica A replay L118"]\n  P -->|physical sync acknowledgement policy| R2["replica B flush L120"]\n  P -->|logical publication| A["analytics apply L115"]\n  C --> G{"read needs token ≥ L120?"}\n  G -->|yes; replica behind| P\n  G -->|no / caught up| R1\n  B["independent PITR backups"] -. "not a replica" .-> P' },
      { kind: 'table', caption: 'Topology trade-offs', headers: ['Choice', 'Benefit', 'Failure/semantic cost'], rows: [['Physical async standby', 'low primary wait; close copy', 'recent acknowledged loss and stale reads'], ['Physical sync standby', 'stronger configured durability evidence', 'write latency; unavailable acknowledgement set can stall/fail writes'], ['Logical subscriber', 'selective data/version flexibility', 'DDL/sequence/identity/conflict management'], ['Read routing', 'offloads eligible reads', 'staleness, monotonicity, replica conflict'], ['Delayed replica', 'time to notice destructive writes', 'not immutable; operational lag and promotion complexity']] },
      { kind: 'code', language: 'text', title: 'Read-your-writes routing contract', code: 'commit write on primary -> receive durable commit position token L\nfor a dependent read:\n  if chosen replica visible_position >= L: read replica\n  else wait only within request freshness budget\n  if budget expires: route primary or return explicit stale/unavailable result\nnever infer freshness from wall-clock delay alone\nrecord route, required position, observed position, and wait duration' },
      { kind: 'pitfall', title: 'Retrying every stale read against every replica', text: 'Fan-out or rapid retry amplifies load while apply is already behind. Use a bounded position-aware wait, route to the authoritative primary when allowed, and shed nonessential reads during recovery.' },
      { kind: 'bestPractice', title: 'Test failure at each replication stage', text: 'Pause network, flush, replay, and logical apply independently; fill retention safely; run long replica queries; promote; and verify client-visible consistency, data-loss assessment, rebuild, backups, and alerts.' },
    ],
    refs: [{ book: 'ddia-2', chapter: 'Ch. 6 — Replication' }, { book: 'database-reliability-engineering', chapter: 'Ch. 10 — Data Storage, Indexing, and Replication' }, { book: 'postgresql-docs', chapter: '26.2 — Log-Shipping Standby Servers; 26.3 — Failover; Ch. 29 — Logical Replication' }],
    related: ['failover-disaster-recovery-and-reliability', 'point-in-time-recovery', 'read-committed-and-repeatable-read'],
  },
  {
    id: 'failover-disaster-recovery-and-reliability', domainId: 'db-operations', title: 'Failover, Disaster Recovery, and Reliability',
    summary: 'Reliable failover is a rehearsed control loop that detects real impact, prevents two writers through fencing, promotes a sufficiently current copy, reroutes clients, measures loss, rebuilds redundancy, and learns within an explicit reliability budget.',
    keyPoints: [
      { text: 'Detection must balance speed and false promotion.', detail: 'Use independent signals from clients, database health, replication position, and infrastructure; define who declares the incident and when automation may act.' },
      { text: 'Fence before promoting.', detail: 'Prove the old writer cannot accept writes through power, lease/quorum, storage, network, credential, or proxy controls. Promotion without fencing risks split brain and irreconcilable histories.' },
      { text: 'Promotion chooses a data-loss boundary.', detail: 'Identify candidate receive/flush/replay positions and acknowledgement policy, preserve the old primary, and report which acknowledged operations may be absent or uncertain.' },
      { text: 'Client routing is part of recovery.', detail: 'DNS, proxies, pools, credentials, certificates, session state, retries, caches, and write fencing must converge; stale connections can keep reaching an unsafe endpoint.' },
      { text: 'Failover ends only after redundancy and protections return.', detail: 'Rebuild replicas from the new history, restore backup/archive/monitoring capacity, resolve or reconcile uncertain writes, and plan failback as another controlled migration rather than an automatic reversal.' },
      { text: 'DR includes regional dependencies and human operation.', detail: 'Identity, secrets/KMS, object storage, network/DNS, observability, artifact/config repositories, staff access, vendors, and data residency can prevent an otherwise healthy database restore.' },
      { text: 'Reliability consumes a budget.', detail: 'Set service objectives and error budget, reserve failover and maintenance headroom, use game days to measure detection/recovery, and prioritize work from observed risk rather than claiming “zero downtime.”' },
    ],
    blocks: [
      { kind: 'code', language: 'text', title: 'Failover and DR runbook', code: '1 DETECT: declare impact, commander, timeline; freeze risky automation.\n2 ASSESS: primary reachability, candidate receive/flush/replay positions, archive health.\n3 FENCE: revoke route/lease/storage/credential or power off old writer; verify from independent path.\n4 PROMOTE: choose most suitable eligible candidate; record expected RPO/data-loss window.\n5 ROUTE: update proxy/DNS/service discovery; drain stale pools; verify writes/read-your-writes.\n6 VERIFY: business invariants, unknown commits, audit/security, backup/archive, lag and objectives.\n7 REBUILD: create replicas from new timeline; restore capacity and monitoring.\n8 FAILBACK: only as a planned migration after evidence review.\n9 LEARN: postmortem, owners, tested actions, game-day update.' },
      { kind: 'diagram', title: 'Fencing prevents split brain', code: 'flowchart TD\n  D["failure detected"] --> Q{"old primary conclusively fenced?"}\n  Q -->|no| H["contain writes; escalate; do not create second writer"]\n  Q -->|yes| P["promote eligible replica"]\n  P --> R["route and drain clients"]\n  R --> V["assess loss + verify invariants"]\n  V --> B["rebuild redundancy and backups"]\n  O["old primary"] -. "preserve for evidence; never rejoin as writer" .-> V' },
      { kind: 'table', caption: 'Regional dependency analysis', headers: ['Dependency', 'Failure question', 'Exercise evidence'], rows: [['Identity/secrets/KMS', 'Can responders and apps authenticate/decrypt?', 'clean-room issuance and rotation'], ['Network/DNS/proxy', 'Can clients converge without stale writer paths?', 'TTL/pool drain and fence checks'], ['Backup/archive/object store', 'Are bytes, catalog, keys independently reachable?', 'isolated restore to selected target'], ['Artifacts/config/extensions', 'Can compatible database/app be rebuilt?', 'versioned manifests and bootstrap'], ['Observability/audit', 'Can responders see and preserve evidence?', 'out-of-region dashboards and immutable sink'], ['People/vendors', 'Who has authority and access off-hours?', 'game-day contacts, approvals, handoff']] },
      { kind: 'pitfall', title: 'Automatic promotion on one failed health check', text: 'A network partition can isolate the monitor while the primary still serves writes. Without independent evidence and fencing, automation converts an availability problem into divergent data.' },
      { kind: 'bestPractice', title: 'Run game days against explicit hypotheses', text: 'Exercise primary loss, stale candidate, lost region, expired credential, archive gap, and unavailable operator. Measure detection, fence, promote, route, validation, rebuild, and decision time; spend the reliability budget on the largest observed gaps.' },
    ],
    refs: [{ book: 'database-reliability-engineering', chapter: 'Ch. 2 — Service-Level Management; Ch. 3 — Risk Management; Ch. 7 — Backup and Recovery; Ch. 10 — Data Storage, Indexing, and Replication' }, { book: 'ddia-2', chapter: 'Ch. 2 — Defining Nonfunctional Requirements; Ch. 6 — Replication' }, { book: 'postgresql-docs', chapter: '26.3 — Failover; 26.2.2 — Standby Server Operation' }],
    related: ['replication-and-read-scaling', 'backup-restore-and-recovery-objectives', 'database-observability'],
  },
]

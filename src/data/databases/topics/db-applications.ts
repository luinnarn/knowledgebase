import type { Topic } from '../../../types/content'

export const dbApplicationsTopics: Topic[] = [
  {
    id: 'connections-sessions-and-protocols', domainId: 'db-applications', title: 'Connections, Sessions, and Protocols',
    summary: 'A connection is a transport carrying a database protocol; a session is server-side identity and mutable context; a transaction is a bounded atomic history within that session. Correct clients establish, use, reset, and discard each layer deliberately.',
    keyPoints: [
      { text: 'Transport, protocol, session, and transaction are distinct.', detail: 'A socket or encrypted channel carries framed messages; authentication creates session identity and settings; BEGIN starts transaction state. Losing one can invalidate the layers above it.' },
      { text: 'Authentication is not authorization.', detail: 'Authentication proves a principal at connection establishment; every statement is still checked against current privileges, role, schema path, and row-security context.' },
      { text: 'Session state survives transaction boundaries.', detail: 'Role changes, temporary objects, prepared statements, advisory locks, and non-local settings can outlive COMMIT or ROLLBACK and contaminate the next borrower.' },
      { text: 'Protocol phases constrain legal recovery.', detail: 'Parse/bind/execute and transaction-status messages are ordered. After an error, a driver may need to drain responses or roll back before the session is usable.' },
      { text: 'Cancellation and connection loss have different meanings.', detail: 'A cancellation asks the server to stop work; a broken channel only removes observation and can leave the outcome of an in-flight commit unknown.' },
      { text: 'Reset is a correctness operation.', detail: 'Return a session only after transactions, cursors, locks, temporary state, settings, and driver bookkeeping satisfy an explicit clean-session contract; otherwise discard it.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Layered lifecycle and failure boundary', code: 'flowchart LR\n  A["DNS / route / TCP / TLS"] --> P["protocol startup"]\n  P --> U["authenticate principal"]\n  U --> S["session: role + settings + prepared state"]\n  S --> T["transaction: idle / active / failed"]\n  T --> Q["parse → bind → execute → results"]\n  Q --> R{"return session?"}\n  R -->|"reset verified"| S\n  R -->|"unknown or broken"| D["discard connection"]' },
      { kind: 'table', caption: 'State ownership and cleanup', headers: ['Layer', 'Representative state', 'End/reset signal'], rows: [['Transport', 'socket, TLS keys, liveness', 'close/discard'], ['Protocol', 'message sequence, portals/cursors', 'responses drained; driver ready'], ['Session', 'principal, role, settings, temporary/prepared objects', 'vendor/driver reset contract'], ['Transaction', 'snapshot, locks, writes, failed flag', 'COMMIT or ROLLBACK acknowledged']] },
      { kind: 'code', language: 'text', title: 'A defensive checkout contract', code: 'acquire(deadline)\n  verify connection is live and driver says IDLE\n  establish required role, schema, time zone, and statement timeout\n  execute work; consume or close every result\nfinally\n  if transaction active/failed: ROLLBACK and verify acknowledgement\n  restore session contract; otherwise discard\n  return only a known-clean connection' },
      { kind: 'note', title: 'PostgreSQL protocol example', text: 'PostgreSQL reports transaction status in `ReadyForQuery`: idle, inside a transaction, or inside a failed transaction. That concrete signal is PostgreSQL-specific; portable code should use the driver’s documented state/reset API rather than parse protocol bytes itself.' },
      { kind: 'pitfall', title: 'Treating COMMIT as a socket write', text: 'A successful local write does not prove durable commit. If the connection fails before the acknowledgement, do not blindly replay a non-idempotent operation; reconcile by a durable operation key or expose an unknown outcome.' },
      { kind: 'bestPractice', title: 'Specify session invariants', text: 'Document required principal, role, schema resolution, locale/time zone, isolation default, statement timeout, and allowed persistent objects. Test checkout after errors and cancellation, not only the happy path.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 9 — Application Development' }, { book: 'postgresql-docs', chapter: 'Ch. 54 — Frontend/Backend Protocol; 54.2 Message Flow' }, { book: 'database-reliability-engineering', chapter: 'Ch. 4 — Operational Visibility' }],
    related: ['connection-pooling', 'prepared-statements-and-parameter-binding', 'application-transaction-design'],
  },
  {
    id: 'connection-pooling', domainId: 'db-applications', title: 'Connection Pooling',
    summary: 'A pool amortizes connection setup and caps concurrent database sessions; it does not create capacity. Its queue, deadlines, reset rules, and admission policy must turn finite database capacity into bounded, observable application behavior.',
    keyPoints: [
      { text: 'Pool size is an admission limit.', detail: 'More connections can increase useful concurrency until CPU, memory, locks, storage, or downstream limits saturate; beyond that they usually add queueing and contention.' },
      { text: 'Size from an end-to-end budget.', detail: 'Reserve administrative and background capacity, divide the remaining session budget across application instances, then validate with service time, throughput, utilization, and tail latency under representative load.' },
      { text: 'A bounded queue makes overload visible.', detail: 'An unbounded waiter list converts a database bottleneck into memory growth and stale work. Bound acquisition by both queue capacity and the request deadline.' },
      { text: 'Timeouts form one shrinking budget.', detail: 'Connect, acquire, statement/lock, and request deadlines must compose; a statement should not begin when too little request budget remains to finish and return a response.' },
      { text: 'Leaks and long holds are capacity loss.', detail: 'Measure checkout duration, active/idle/waiting counts, acquisition latency, timeouts, and owner traces; reclamation by age alone can corrupt in-flight work.' },
      { text: 'Pool mode constrains session features.', detail: 'Transaction-level proxies can move transactions between server sessions, so session-scoped prepared statements, temporary tables, locks, and settings may be unsafe or require explicit support.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Bounded admission and load shedding', code: 'flowchart LR\n  R["requests"] --> G{"request deadline valid?"}\n  G -->|no| X["reject"]\n  G -->|yes| Q["bounded FIFO/fair queue"]\n  Q -->|acquire before deadline| P["pool: at most N checked out"]\n  Q -->|full / expired| X\n  P --> D["database finite capacity"]\n  D --> M["release + reset or discard"]' },
      { kind: 'table', caption: 'Quantitative signals, not universal constants', headers: ['Question', 'Measure', 'Adjustment hypothesis'], rows: [['Is N too small?', 'DB headroom with sustained pool wait', 'Increase gradually within total session budget'], ['Is N too large?', 'DB saturated; tail latency/lock waits rise with concurrency', 'Reduce admission and shed earlier'], ['Are queries holding capacity?', 'checkout and statement duration by operation', 'Fix query/transaction boundary'], ['Is demand bursty?', 'queue depth/age and timeout rate', 'Small bounded queue; smooth or reject'], ['Are connections unhealthy?', 'creation/discard/reset failures', 'repair lifecycle/network; avoid retry storm']] },
      { kind: 'code', language: 'text', title: 'Budget calculation to validate with load tests', code: 'database session ceiling                  240\n- administration / migrations / monitoring  30\n- safety and failover reserve                50\n= application budget                        160\n/ maximum simultaneously live app instances  20\n= initial cap per instance                     8\n\nThis is an example calculation, not a recommended pool size. Recalculate at peak instance count and test it.' },
      { kind: 'paragraph', text: 'Apply Little’s Law as a measurement check: average in-flight work is approximately throughput × average time in the system. It does not prescribe the cap, and averages conceal tails; inspect distributions and saturation resources.' },
      { kind: 'pitfall', title: 'Increasing the pool to cure timeouts', text: 'If the database is saturated, a larger pool admits more simultaneous work, often worsening cache pressure, lock waits, and p99 latency. Pooling amortizes setup; it cannot repair overload.' },
      { kind: 'bestPractice', title: 'Fail within the caller’s deadline', text: 'Propagate one monotonic deadline. Reject when queue capacity is exhausted, stop waiting before the deadline, cancel obsolete database work where supported, and retry only classified transient failures with jitter and a bounded attempt budget.' },
    ],
    refs: [{ book: 'database-reliability-engineering', chapter: 'Ch. 3 — Service-Level Management; Ch. 4 — Operational Visibility' }, { book: 'ddia-2', chapter: 'Ch. 2 — Defining Nonfunctional Requirements' }, { book: 'postgresql-docs', chapter: '19.3 — Connections and Authentication' }],
    related: ['connections-sessions-and-protocols', 'database-observability', 'capacity-maintenance-and-data-lifecycle'],
  },
  {
    id: 'prepared-statements-and-parameter-binding', domainId: 'db-applications', title: 'Prepared Statements and Parameter Binding',
    summary: 'A prepared statement separates fixed SQL structure from typed data values. Binding protects the value boundary and can reuse protocol or planning work, but it neither validates dynamic SQL structure nor guarantees one optimal cached plan.',
    keyPoints: [
      { text: 'Placeholders represent values, not grammar.', detail: 'A parameter can stand for `customer_id` or a timestamp, but not a table name, column, keyword, operator, or sort direction.' },
      { text: 'Binding preserves the code/data boundary.', detail: 'The driver encodes a value according to protocol and type rules, so quotes and comment markers remain data rather than becoming SQL syntax.' },
      { text: 'Type choice is part of correctness.', detail: 'Bind decimal, temporal, binary, and null values with appropriate types; ambiguous nulls, lossy strings, time zones, and implicit casts can change semantics and index use.' },
      { text: 'Preparation and plan caching differ.', detail: 'A driver may bind without a named server-side prepared statement, prepare after a threshold, or emulate preparation. Server plan reuse and generic/custom plan selection are separate concerns.' },
      { text: 'Lifetime follows a session unless documented otherwise.', detail: 'Named prepared state commonly disappears when its server session ends and may conflict with transaction-pooling proxies or schema changes.' },
      { text: 'Dynamic identifiers require an allowlist.', detail: 'Map a small external token to a constant SQL fragment or use a vendor identifier-quoting API after validation; never send an identifier through a value placeholder.' },
    ],
    blocks: [
      { kind: 'code', language: 'java', title: 'JDBC value binding on the commerce schema', code: 'String sql = """\n    SELECT order_id, status, placed_at\n    FROM orders\n    WHERE customer_id = ? AND placed_at >= ?\n    ORDER BY placed_at DESC, order_id DESC\n    """;\ntry (PreparedStatement ps = connection.prepareStatement(sql)) {\n  ps.setLong(1, customerId);\n  ps.setTimestamp(2, Timestamp.from(fromInstant), UTC_CALENDAR);\n  try (ResultSet rows = ps.executeQuery()) {\n    while (rows.next()) consume(rows);\n  }\n}' },
      { kind: 'code', language: 'java', title: 'Allowlisted dynamic order', code: 'String orderBy = switch (requestedSort) {\n  case "newest" -> "placed_at DESC, order_id DESC";\n  case "oldest" -> "placed_at ASC, order_id ASC";\n  default -> throw new IllegalArgumentException("unsupported sort");\n};\nString sql = "SELECT order_id, status, placed_at FROM orders "\n           + "WHERE customer_id = ? ORDER BY " + orderBy;\n// Only the constant fragment is concatenated; customer_id remains bound.' },
      { kind: 'table', caption: 'Three distinct mechanisms', headers: ['Mechanism', 'Primary purpose', 'Does not guarantee'], rows: [['Value binding', 'preserve type and code/data boundary', 'safe identifiers or optimal plan'], ['Server preparation', 'name parsed statement in one session', 'cross-session lifetime'], ['Plan caching', 'reuse some planning artifacts', 'same plan or best plan for every value']] },
      { kind: 'note', title: 'PostgreSQL parse/bind example', text: 'In PostgreSQL’s extended query protocol, Parse creates a prepared statement and Bind supplies parameter values to create a portal before Execute. `PREPARE`/`EXECUTE` expose related SQL-level behavior. Drivers may choose other paths, so inspect driver documentation and telemetry.' },
      { kind: 'pitfall', title: 'Converting everything to text', text: 'Stringifying timestamps, decimals, arrays, or nulls reintroduces parsing ambiguity and can provoke implicit casts. Use the driver’s typed API and verify server parameter types for performance-sensitive statements.' },
      { kind: 'bestPractice', title: 'Observe statement shape and parameter class separately', text: 'Log a safe query identifier, duration, rows, and parameter-type/selectivity class without secrets. Investigate generic versus value-sensitive plans rather than disabling preparation globally.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 9 — Application Development' }, { book: 'postgresql-docs', chapter: '54.2.3 — Extended Query; PREPARE; EXECUTE' }, { book: 'sql-antipatterns', chapter: 'Ch. 21 — SQL Injection' }],
    related: ['sql-injection-and-query-safety', 'cost-based-query-planning', 'connections-sessions-and-protocols'],
  },
  {
    id: 'sql-injection-and-query-safety', domainId: 'db-applications', title: 'SQL Injection and Query Safety',
    summary: 'SQL injection occurs when untrusted data changes SQL structure. Bind every value, allowlist the few dynamic structural choices, minimize database authority, and treat stored data as untrusted again whenever it later enters a command.',
    keyPoints: [
      { text: 'Binding is the primary value defense.', detail: 'Do not concatenate, interpolate, or “escape” values into SQL. Escaping is dialect, encoding, and mode sensitive and does not replace a parameter API.' },
      { text: 'Identifiers need structural validation.', detail: 'Placeholders do not represent table or column names. Prefer fixed queries; otherwise map allowed external tokens to constant identifiers.' },
      { text: 'Sort grammar must be closed.', detail: 'Allowlist complete `ORDER BY` fragments including direction and deterministic tie-breaker; checking only for `ASC`/`DESC` while concatenating a column is insufficient.' },
      { text: 'Variable lists need generated placeholders.', detail: 'Generate exactly one value placeholder per element, use a supported array/table parameter, or stage validated values; never join raw list elements into SQL.' },
      { text: 'Second-order injection crosses time.', detail: 'A string safely stored today can become syntax when a report, migration, admin tool, or dynamic routine later concatenates it. Re-establish the boundary at every interpreter.' },
      { text: 'Least privilege limits blast radius.', detail: 'Application credentials should not own schemas or read password verifiers/secrets. Store passwords only as slow, salted password hashes in an identity boundary, never reversible or readable plaintext.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Safe construction by input kind', headers: ['Input', 'Unsafe', 'Safe'], rows: [['Value', '`... email = \'` + input', '`email = ?` plus bind'], ['Column', '`ORDER BY ` + column', 'token → constant fragment map'], ['Direction', 'append request text', 'closed enum → `ASC` or `DESC`'], ['IN list', 'join raw strings', 'N placeholders / array / staging table'], ['Stored text', 'concatenate value read from DB', 'bind again at the new SQL boundary']] },
      { kind: 'code', language: 'java', title: 'Values, list, identifier, and direction kept separate', code: 'List<Long> ids = requireAtMost(requestedIds, 100);\nif (ids.isEmpty()) return List.of();\nString sort = switch (request.sort()) {\n  case NEWEST -> "o.placed_at DESC, o.order_id DESC";\n  case OLDEST -> "o.placed_at ASC, o.order_id ASC";\n};\nString marks = String.join(",", Collections.nCopies(ids.size(), "?"));\nString sql = "SELECT o.order_id, o.status FROM orders o "\n           + "WHERE o.customer_id = ? AND o.order_id IN (" + marks + ") "\n           + "ORDER BY " + sort;\n// Bind customerId, then each validated Long. No request text enters SQL.' },
      { kind: 'code', language: 'text', title: 'Second-order risk', code: '1. A customer name is safely bound and stored as:  Acme"; DROP ...\n2. A later export job reads that name.\n3. The job concatenates it into a dynamically generated SQL statement.\n4. Stored data becomes syntax at the second interpreter.\n\nFix step 3 with binding/structural APIs; “it came from our database” is not trust.' },
      { kind: 'note', title: 'Logging is another interpreter boundary', text: 'Do not log raw SQL with credentials or sensitive parameters. Structured logging should keep a stable statement identifier and separately redacted metadata; terminal, HTML, shell, CSV, and log viewers each have their own injection/escaping rules.' },
      { kind: 'pitfall', title: 'A home-grown quote replacer', text: 'Replacing apostrophes misses identifiers, encodings, backslashes, comments, numeric contexts, and configuration differences. It also normalizes concatenation as an acceptable design. Bind values instead.' },
      { kind: 'bestPractice', title: 'Test the boundary, not attack strings alone', text: 'Property-test that arbitrary values never alter statement shape; unit-test every structural token mapping; integration-test with the real driver and engine; scan code for raw query concatenation and run the application under a non-owner role.' },
    ],
    refs: [{ book: 'sql-antipatterns', chapter: 'Ch. 20 — Readable Passwords; Ch. 21 — SQL Injection' }, { book: 'database-system-concepts', chapter: 'Ch. 9 — Application Development; SQL Injection' }, { book: 'postgresql-docs', chapter: '34.3 — Command Execution Functions; 34.4 — Asynchronous Command Processing' }],
    related: ['prepared-statements-and-parameter-binding', 'roles-privileges-and-least-authority', 'encryption-secrets-and-auditing'],
  },
  {
    id: 'batching-bulk-loading-and-backpressure', domainId: 'db-applications', title: 'Batching, Bulk Loading, and Backpressure',
    summary: 'Batching amortizes round trips and bulk paths reduce per-row protocol work, but larger units consume memory, locks, log, and recovery time. A safe pipeline bounds both batch size and queued work, defines partial-failure semantics, and slows producers when the database cannot keep up.',
    keyPoints: [
      { text: 'Batching and bulk loading optimize different layers.', detail: 'Multirow/batched statements reduce round trips; COPY-style or loader APIs stream a specialized representation with lower parsing/protocol overhead.' },
      { text: 'Transaction size is a tradeoff.', detail: 'Larger commits amortize fixed costs but lengthen locks/snapshots, increase WAL/log bursts and retry work, and delay visibility. Choose with measurements and atomicity requirements.' },
      { text: 'Generated-key semantics are API-specific.', detail: 'For multirow operations, returned rows, order, partial availability, and connection-scoped identity functions differ; correlate with an explicit client import key rather than assumptions.' },
      { text: 'Partial failure needs a contract.', detail: 'Decide whether one bad row aborts the batch, is isolated into a reject stream, or is retried. Savepoints, driver batch counts, and bulk error facilities vary.' },
      { text: 'Backpressure bounds unfinished work.', detail: 'Use a bounded channel measured in rows and bytes, a small number of consumers, and deadline-aware enqueue; never let a fast parser create an unbounded database backlog.' },
      { text: 'Idempotency survives retries.', detail: 'A unique import or operation key makes duplicate delivery observable and lets workers reconcile ambiguous outcomes before resubmitting.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Bounded producer-to-database flow', code: 'flowchart LR\n  S["stream source"] --> V["parse + validate"]\n  V -->|"offer with deadline"| Q["bounded rows + bytes queue"]\n  Q --> B["batcher: max rows/bytes/time"]\n  B --> W["limited DB workers"]\n  W --> C{"commit acknowledged?"}\n  C -->|yes| K["checkpoint source offset"]\n  C -->|row rejected| E["durable reject record"]\n  C -->|unknown| R["reconcile import key"]\n  Q -->|"full"| SLOW["pause producer"]' },
      { kind: 'code', language: 'sql', title: 'Define an idempotent staging object before use', code: 'CREATE TABLE order_import_lines (\n  import_id VARCHAR(80) NOT NULL,\n  source_line_no BIGINT NOT NULL,\n  order_id BIGINT NOT NULL,\n  line_no INTEGER NOT NULL,\n  product_id BIGINT NOT NULL,\n  quantity INTEGER NOT NULL CHECK (quantity > 0),\n  unit_price DECIMAL(19, 4) NOT NULL CHECK (unit_price >= 0),\n  PRIMARY KEY (import_id, source_line_no),\n  UNIQUE (order_id, line_no),\n  FOREIGN KEY (order_id) REFERENCES orders(order_id),\n  FOREIGN KEY (product_id) REFERENCES products(product_id)\n);\n\n-- Bind every value; commit bounded chunks only when cross-chunk atomicity is not required.\nINSERT INTO order_items (order_id, line_no, product_id, quantity, unit_price)\nSELECT order_id, line_no, product_id, quantity, unit_price\nFROM order_import_lines WHERE import_id = :import_id;' },
      { kind: 'code', language: 'sql', title: 'PostgreSQL streaming bulk path', code: 'COPY order_import_lines\n  (import_id, source_line_no, order_id, line_no, product_id, quantity, unit_price)\nFROM STDIN WITH (FORMAT csv, HEADER true);\n-- PostgreSQL-specific. Use the driver COPY API; do not interpolate a client filename or raw SQL.\n-- Validate and merge from staging under the intended transaction/failure policy.' },
      { kind: 'table', caption: 'Batch control dimensions', headers: ['Bound', 'Why it matters', 'Observe'], rows: [['rows', 'statement/lock/retry work', 'rows/s and error position'], ['encoded bytes', 'memory, packets, parser limits', 'queue and payload bytes'], ['elapsed time', 'freshness and request deadline', 'batch age and commit p99'], ['workers', 'database concurrency', 'pool wait and saturation'], ['retries', 'prevents replay storms', 'attempt count and reconciliations']] },
      { kind: 'pitfall', title: 'Collecting the entire file before writing', text: 'Memory grows with input and the database sees a burst after validation. Stream through a bounded buffer, stop reading when consumers lag, and persist a checkpoint only after the corresponding commit is known.' },
      { kind: 'bestPractice', title: 'Benchmark failure as well as throughput', text: 'Measure normal rows/s, commit latency, WAL/log and replica lag, then inject duplicates, constraint errors, cancellation, and lost commit acknowledgements. Verify restart does not duplicate or silently skip rows.' },
    ],
    refs: [{ book: 'postgresql-docs', chapter: 'COPY; 14.4 — Populating a Database' }, { book: 'database-system-concepts', chapter: 'Ch. 9 — Application Development' }, { book: 'ddia-2', chapter: 'Ch. 5 — Encoding and Evolution; Dataflow Through Services' }],
    related: ['application-transaction-design', 'merge-upsert-and-returning', 'write-ahead-logging-and-checkpoints'],
  },
  {
    id: 'application-transaction-design', domainId: 'db-applications', title: 'Application Transaction Design',
    summary: 'An application transaction should protect one explicit invariant with a short, retryable unit of database work. Deadlines, cancellation, ambiguous commit, and external side effects require outcome protocols beyond a convenient transaction annotation.',
    keyPoints: [
      { text: 'Start from the invariant and unit of work.', detail: 'Identify reads whose results justify writes, constraints that arbitrate races, the isolation needed, and the exact point at which the database outcome becomes durable.' },
      { text: 'Keep remote calls outside database transactions.', detail: 'Network latency and failure extend lock/snapshot lifetime and pool occupancy. Fetch stable prerequisites first or use a state machine; publish after commit through a durable handoff.' },
      { text: 'Deadlines must reach the server.', detail: 'A request timeout that only abandons the client leaves database work running. Configure statement/lock limits, propagate cancellation, and still drain/reset or discard the session.' },
      { text: 'Commit acknowledgement can be lost.', detail: 'After connection failure during COMMIT, the application may not know whether effects committed. Retrying without an operation key can duplicate them.' },
      { text: 'Retries replay the whole unit.', detail: 'Classify transient serialization/deadlock failures, roll back, reacquire clean state, reread, and retry with bounded backoff. Do not retry arbitrary SQL exceptions.' },
      { text: 'Atomicity stops at the database boundary.', detail: 'Use compensation for reversible business steps and an outbox/inbox or equivalent durable protocol for messages; neither creates a magical global transaction.' },
    ],
    blocks: [
      { kind: 'code', language: 'sql', title: 'Define the durable operation and outbox objects', code: 'CREATE TABLE checkout_operations (\n  operation_id VARCHAR(80) PRIMARY KEY,\n  order_id BIGINT NOT NULL UNIQUE REFERENCES orders(order_id),\n  completed_at TIMESTAMP NOT NULL\n);\n\nCREATE TABLE application_outbox (\n  event_id VARCHAR(80) PRIMARY KEY,\n  aggregate_type VARCHAR(40) NOT NULL,\n  aggregate_id BIGINT NOT NULL,\n  event_type VARCHAR(80) NOT NULL,\n  payload_json VARCHAR(8000) NOT NULL,\n  occurred_at TIMESTAMP NOT NULL,\n  published_at TIMESTAMP NULL\n);' },
      { kind: 'code', language: 'text', title: 'Checkout unit of work', code: 'validate request and obtain operation_id\nBEGIN\n  if checkout_operations contains operation_id: return recorded outcome\n  lock/read the pending order and verify invariant\n  update orders from PENDING to PAID with a guarded predicate\n  insert checkout_operations(operation_id, order_id, completed_at)\n  insert application_outbox(event_id, ..., "OrderPaid", ...)\nCOMMIT\nAfter commit, an independent relay publishes; consumers deduplicate event_id.\nOn lost COMMIT acknowledgement, query operation_id before retrying.' },
      { kind: 'diagram', title: 'External effect after durable handoff', code: 'sequenceDiagram\n  participant A as Application\n  participant D as Database\n  participant R as Outbox relay\n  participant M as Message broker\n  A->>D: transaction: order + operation + outbox\n  D-->>A: commit acknowledgement\n  R->>D: claim unpublished event\n  R->>M: publish(event_id)\n  M-->>R: acknowledgement\n  R->>D: mark published\n  Note over R,M: delivery may repeat; consumer deduplicates event_id' },
      { kind: 'table', caption: 'Outcome handling', headers: ['Observation', 'Meaning', 'Action'], rows: [['statement rejected before commit', 'transaction failed/not committed', 'rollback; classify'], ['serialization/deadlock abort', 'unit did not commit', 'bounded whole-unit retry'], ['client deadline/cancel', 'server outcome may lag', 'drain and inspect; rollback/reset'], ['connection lost during COMMIT', 'commit unknown', 'reconcile durable operation key'], ['message publish failed', 'database may be committed', 'relay retries; do not roll back history']] },
      { kind: 'pitfall', title: 'Calling payment while holding an order lock', text: 'A slow or timed-out payment call prolongs locks and creates an unclear cross-system outcome. Model authorization and capture as explicit states with idempotent external requests and compensation where the business permits.' },
      { kind: 'bestPractice', title: 'Test every boundary outcome', text: 'Inject failure before write, after write/before commit, during commit acknowledgement, and after commit/before publish. Assert invariant preservation, bounded resource lifetime, replay behavior, and operator-visible reconciliation.' },
    ],
    refs: [{ book: 'ddia-2', chapter: 'Ch. 8 — Transactions' }, { book: 'database-system-concepts', chapter: 'Ch. 17 — Transactions; Ch. 18 — Concurrency Control' }, { book: 'database-reliability-engineering', chapter: 'Ch. 6 — Infrastructure Engineering' }, { book: 'postgresql-docs', chapter: '3.4 — Transactions; ROLLBACK; COMMIT' }],
    related: ['application-concurrency-and-transaction-boundaries', 'deadlocks-contention-and-retries', 'connections-sessions-and-protocols'],
  },
  {
    id: 'n-plus-one-and-data-access-shape', domainId: 'db-applications', title: 'N+1 and Data-Access Shape',
    summary: 'N+1 is a dataflow mismatch: one query discovers N parents and application traversal issues another query per parent. Fix the access shape for the use case with a join, batched key lookup, aggregate, or deliberate streaming—not a universal eager-loading switch.',
    keyPoints: [
      { text: 'Trace round trips, not just SQL duration.', detail: 'Many individually fast indexed queries can dominate through network latency, parsing, scheduling, pool holds, and inconsistent snapshots.' },
      { text: 'Object traversal can hide I/O.', detail: 'Lazy association access in a loop, serializer, template, or debugger may execute SQL far from the repository method that appears to load data.' },
      { text: 'A join changes cardinality.', detail: 'Joining orders to lines repeats order columns and can multiply rows further when several to-many associations are fetched together; reconstruct and paginate at the correct grain.' },
      { text: 'Batch fetching bounds query count.', detail: 'Fetch parent keys, then retrieve children with bounded `IN` lists, array/table parameters, or a staging relation; preserve empty parents and deterministic association order.' },
      { text: 'Projection often beats entity loading.', detail: 'For counts, totals, and screens, select exactly the scalar result or DTO shape instead of materializing a navigable graph.' },
      { text: 'Eager loading is not universally safe.', detail: 'It can overfetch large graphs, break pagination, amplify duplicates, and move the problem to another endpoint. Specify per-use-case fetch plans and budgets.' },
    ],
    blocks: [
      { kind: 'code', language: 'text', title: 'N+1 trace for five orders', code: 'Q1 SELECT order_id, customer_id, placed_at FROM orders\n   WHERE customer_id = ? ORDER BY placed_at DESC FETCH FIRST 5 ROWS ONLY\nQ2 SELECT ... FROM order_items WHERE order_id = 501\nQ3 SELECT ... FROM order_items WHERE order_id = 498\nQ4 SELECT ... FROM order_items WHERE order_id = 490\nQ5 SELECT ... FROM order_items WHERE order_id = 477\nQ6 SELECT ... FROM order_items WHERE order_id = 462\n\nOne request, six round trips; N=500 means 501 unless another strategy intervenes.' },
      { kind: 'code', language: 'sql', title: 'Set-oriented detail for an already bounded parent page', code: 'WITH selected_orders AS (\n  SELECT order_id, customer_id, placed_at, status\n  FROM orders\n  WHERE customer_id = :customer_id\n  ORDER BY placed_at DESC, order_id DESC\n  FETCH FIRST 20 ROWS ONLY\n)\nSELECT o.order_id, o.placed_at, o.status,\n       i.line_no, i.product_id, i.quantity, i.unit_price\nFROM selected_orders AS o\nLEFT JOIN order_items AS i ON i.order_id = o.order_id\nORDER BY o.placed_at DESC, o.order_id DESC, i.line_no;' },
      { kind: 'code', language: 'sql', title: 'Project an aggregate when lines are not needed', code: 'SELECT o.order_id, o.placed_at, o.status,\n       COUNT(i.line_no) AS line_count,\n       COALESCE(SUM(i.quantity * i.unit_price), 0) AS order_total\nFROM orders AS o\nLEFT JOIN order_items AS i ON i.order_id = o.order_id\nWHERE o.customer_id = :customer_id\nGROUP BY o.order_id, o.placed_at, o.status\nORDER BY o.placed_at DESC, o.order_id DESC\nFETCH FIRST 20 ROWS ONLY;' },
      { kind: 'table', caption: 'Choose shape by required result', headers: ['Need', 'Candidate', 'Watch'], rows: [['small parent+child page', 'bounded parent CTE then join', 'row multiplication'], ['many parents, selective children', 'parent query + batched child query', 'list limits and snapshot'], ['counts/totals', 'database aggregate projection', 'correct grouping grain'], ['huge export', 'ordered streaming/set query', 'fetch size and backpressure'], ['occasionally used association', 'explicit lazy load', 'query budget and closed session']] },
      { kind: 'pitfall', title: 'Fixing N+1 with fetch-all', text: 'Global eager loading can produce a Cartesian explosion or transfer unused columns. It also hides the endpoint’s actual data contract. Make query count, rows, bytes, and latency visible per operation.' },
      { kind: 'bestPractice', title: 'Assert an access budget', text: 'Integration-test representative cardinalities with the real ORM/driver, capture the SQL trace, and assert a bounded query count plus rows/bytes. Test pagination and empty associations explicitly.' },
    ],
    refs: [{ book: 'sql-antipatterns', chapter: 'Ch. 18 — Spaghetti Query' }, { book: 'database-system-concepts', chapter: 'Ch. 9 — Application Development' }, { book: 'ddia-2', chapter: 'Ch. 6 — Replication and Other Distributed State' }],
    related: ['orm-impedance-mismatch', 'joins', 'batching-bulk-loading-and-backpressure'],
  },
  {
    id: 'orm-impedance-mismatch', domainId: 'db-applications', title: 'ORM Impedance Mismatch',
    summary: 'An ORM coordinates objects, identity, relationships, and changes over relational data, but object graphs and relations have different semantics. Use the abstraction deliberately, keep database constraints authoritative, and inspect the SQL and transaction behavior it emits.',
    keyPoints: [
      { text: 'Object identity and relational keys differ.', detail: 'A persistence context may guarantee one object per key, while equality/hash behavior, detached copies, generated identifiers, and composite keys still require explicit design.' },
      { text: 'Associations are queries and constraints.', detail: 'An object reference or collection maps to foreign keys and join tables; ownership, nullability, ordering, cascades, and orphan removal encode different lifecycle rules.' },
      { text: 'Inheritance has no single relational mapping.', detail: 'Single-table, joined-table, and concrete-table strategies trade nulls, joins, duplication, constraints, and polymorphic query cost.' },
      { text: 'Fetching is part of the API contract.', detail: 'Lazy proxies can fail outside a session or cause N+1; eager graphs can explode. Prefer explicit projections/fetch plans for each operation.' },
      { text: 'Change tracking emits concurrency behavior.', detail: 'Dirty checking, flush timing, write ordering, optimistic version columns, and bulk updates affect lost-update protection and whether in-memory objects become stale.' },
      { text: 'Abstractions leak at semantic boundaries.', detail: 'SQL nulls, three-valued logic, collation, decimal/time types, isolation, locking, generated values, and vendor features remain observable and must be tested.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Mismatch ledger', headers: ['Concern', 'Object view', 'Relational/database reality', 'Control'], rows: [['Identity', 'reference / `equals`', 'primary/candidate key', 'stable key policy + identity map'], ['Association', 'field/collection', 'FK or join relation', 'constraints + explicit ownership'], ['Inheritance', 'subtype polymorphism', 'one/many tables', 'choose per query/constraint tradeoff'], ['Fetching', 'navigation', 'round trips and row sets', 'projection/fetch plan + budgets'], ['Change tracking', 'mutated fields', 'ordered DML under concurrency', 'version predicate + flush tests'], ['Query leak', 'repository method', 'SQL types/plans/isolation', 'inspect SQL and real-engine behavior']] },
      { kind: 'code', language: 'sql', title: 'Define optimistic concurrency explicitly', code: 'ALTER TABLE orders ADD COLUMN row_version BIGINT NOT NULL DEFAULT 0;\n\nUPDATE orders\nSET status = :new_status, row_version = row_version + 1\nWHERE order_id = :order_id\n  AND row_version = :expected_version;\n-- Exactly one affected row means the expected version won.\n-- Zero means missing row or concurrent change; classify by the application contract.' },
      { kind: 'code', language: 'text', title: 'One aggregate boundary', code: 'Order\n  identity: order_id\n  invariant: allowed status transition\n  children: OrderItem identified by (order_id, line_no)\n  concurrency token: row_version\n\nCustomer and Product are references by key, not automatically cascaded children.\nDeletion, orphan removal, and loading are explicit use-case decisions.' },
      { kind: 'note', title: 'Bulk DML bypasses many unit-of-work guarantees', text: 'ORM bulk update/delete commonly operates directly in the database and can bypass entity callbacks, per-object version checks, and the persistence context. Clear or refresh affected objects and state the concurrency policy.' },
      { kind: 'pitfall', title: 'Letting generated SQL define the model', text: 'Automatic schema generation can omit business candidate keys, checks, precise types, online-migration sequencing, and least-privilege ownership. Keep reviewed migrations and database constraints as the deployed contract.' },
      { kind: 'bestPractice', title: 'Test both mapping semantics and emitted SQL', text: 'Cover identity, cascades, orphan behavior, nulls, optimistic conflicts, flush/rollback, and detached objects. Then trace query count, parameter types, row shape, and plans at representative scale.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 9 — Application Development; Object-Relational Mapping' }, { book: 'sql-antipatterns', chapter: 'Ch. 6 — Entity-Attribute-Value; Ch. 16 — Random Selection' }, { book: 'ddia-2', chapter: 'Ch. 3 — Data Models and Query Languages' }],
    related: ['n-plus-one-and-data-access-shape', 'application-concurrency-and-transaction-boundaries', 'schema-migrations-and-database-testing'],
  },
  {
    id: 'schema-migrations-and-database-testing', domainId: 'db-applications', title: 'Schema Migrations and Database Testing',
    summary: 'Schema evolution is a compatibility rollout across old and new application versions, data, replicas, and operational tooling. Versioned, observable expand–migrate–contract steps plus real-engine tests make that rollout reviewable and recoverable.',
    keyPoints: [
      { text: 'Migrations are immutable ordered history.', detail: 'Give each change a unique version, checksum, owner, precondition, and completion record. Never silently edit a migration already applied to any shared environment.' },
      { text: 'Expand before contract.', detail: 'Add backward-compatible structures, deploy code that can tolerate both representations, backfill in bounded resumable chunks, switch reads/writes, then remove the old contract only after every consumer has moved.' },
      { text: 'Online DDL is conditional.', detail: '“Online” can still take metadata locks, scan/rewrite rows, build indexes, generate log/replica lag, or fail on old versions. Verify the exact engine/version, table size, and lock behavior.' },
      { text: 'Roll-forward is often safer than rollback.', detail: 'Code rollback cannot restore dropped data or undo a partially applied nontransactional DDL. Prefer additive corrective migrations; define backups/reconciliation for destructive steps.' },
      { text: 'Tests need layers.', detail: 'Fast query/unit tests, migration-from-supported-version tests, real-engine integration tests, and property/model tests answer different questions; mocks do not reproduce constraints, types, isolation, or planner behavior.' },
      { text: 'Fixtures are explicit valid states.', detail: 'Keep small deterministic baseline data plus generated edge cases for nulls, Unicode, decimals, time boundaries, skew, duplicates, and concurrency. Avoid order-dependent shared databases.' },
    ],
    blocks: [
      { kind: 'code', language: 'sql', title: 'Define migration metadata and the expanded object', code: 'CREATE TABLE schema_migrations (\n  version VARCHAR(80) PRIMARY KEY,\n  checksum VARCHAR(128) NOT NULL,\n  applied_at TIMESTAMP NOT NULL,\n  applied_by VARCHAR(120) NOT NULL\n);\n\n-- V20260711_01: expand; nullable keeps old writers compatible.\nALTER TABLE customers ADD COLUMN email_normalized VARCHAR(320);\nCREATE INDEX customers_email_normalized_idx ON customers (email_normalized);\n-- Product-specific online/concurrent index syntax has different transaction rules;\n-- choose it only after verifying the deployed engine and version.' },
      { kind: 'diagram', title: 'Expand–migrate–contract deployment', code: 'flowchart LR\n  E["expand: nullable column/index"] --> C1["deploy code: old-read + dual/new write"]\n  C1 --> B["bounded resumable backfill"]\n  B --> V["validate counts, uniqueness, lag, errors"]\n  V --> C2["switch reads to new column"]\n  C2 --> O["observe through rollback window"]\n  O --> K["contract: enforce constraint/remove old path"]' },
      { kind: 'code', language: 'sql', title: 'Backfill and validate the explicitly added column', code: '-- Repeat bounded primary-key windows; checkpoint the last customer_id.\nUPDATE customers\nSET email_normalized = LOWER(email)\nWHERE customer_id >= :first_id\n  AND customer_id < :next_id\n  AND email_normalized IS NULL;\n\nSELECT COUNT(*) AS missing FROM customers WHERE email_normalized IS NULL;\nSELECT email_normalized, COUNT(*) AS duplicates\nFROM customers\nWHERE email_normalized IS NOT NULL\nGROUP BY email_normalized HAVING COUNT(*) > 1;\n-- Resolve collisions before a later migration adds NOT NULL / UNIQUE semantics.' },
      { kind: 'table', caption: 'Database test portfolio', headers: ['Layer', 'Proves', 'Must include'], rows: [['query/unit', 'mapping and boundary logic', 'null/empty/error cases'], ['real-engine integration', 'actual types, constraints, SQL, transactions', 'production engine/version where feasible'], ['migration path', 'every supported old schema reaches expected new schema', 'data-preservation and rerun/failure checks'], ['property/model', 'invariants across generated operations', 'sequences, concurrency, shrinking/replay'], ['representative performance', 'operationally viable rollout/query', 'scale, skew, locks, log and replica lag']] },
      { kind: 'pitfall', title: 'Renaming or dropping in one release', text: 'Old application instances, queued jobs, replicas, reports, and rollback binaries may still depend on the old column. Treat contract removal as a separately approved deployment after consumer evidence reaches zero.' },
      { kind: 'bestPractice', title: 'Rehearse production-shaped failure', text: 'Restore a realistic snapshot into an isolated real engine, run the complete migration, interrupt the backfill, retry it, measure locks/log/lag, validate invariants, and exercise both old and new application versions across the compatibility window.' },
    ],
    refs: [{ book: 'ddia-2', chapter: 'Ch. 5 — Encoding and Evolution' }, { book: 'database-reliability-engineering', chapter: 'Ch. 8 — Release Management; Ch. 10 — Data Storage, Indexing, and Replication' }, { book: 'sql-antipatterns', chapter: 'Ch. 19 — Implicit Columns' }, { book: 'postgresql-docs', chapter: 'ALTER TABLE; CREATE INDEX; 14.4 — Populating a Database' }],
    related: ['orm-impedance-mismatch', 'primary-unique-and-check-constraints', 'replication-and-read-scaling'],
  },
]

import type { Topic } from '../../../types/content'

// content-pass: deepened

export const dbAdvancedSqlTopics: Topic[] = [
  {
    id: 'common-table-expressions',
    domainId: 'db-advanced-sql',
    title: 'Common Table Expressions',
    summary:
      'A common table expression gives a statement-local name to a query result, making a multi-stage relational transformation readable without implying that every engine stores the intermediate rows.',
    keyPoints: [
      { text: 'A nonrecursive CTE is a named table expression scoped to one statement.', detail: 'Later clauses can read its named columns as they would a derived table. The name is not a table, view, cache, or reusable result after the statement ends.' },
      { text: 'CTEs clarify data flow by naming semantic stages.', detail: 'A useful stage has one job—qualifying paid lines, aggregating customer revenue, or ranking totals—and a name that describes the resulting relation.' },
      { text: 'A CTE does not create an ordering guarantee.', detail: 'Rows remain unordered unless the final query has an `ORDER BY`; ordering inside an intermediate expression is meaningful only when paired with an operation whose semantics require it.' },
      { text: 'Materialization is an optimizer and dialect question.', detail: 'Some engines inline eligible CTEs, some materialize them, and some expose hints. Treat neither behavior as universal; inspect the target engine and plan when repeated work or predicate pushdown matters.' },
      { text: 'Multiple references preserve query semantics, not a single-evaluation promise.', detail: 'A CTE referenced twice denotes the same query expression, but whether its work is shared is implementation-dependent.' },
      { text: 'CTEs compose but do not repair wrong cardinality.', detail: 'Name and validate each stage’s key and expected row count so a many-to-many join does not become harder to see behind pleasant names.' },
    ],
    blocks: [
      { kind: 'subheading', text: 'Build customer value in explicit stages' },
      { kind: 'code', language: 'sql', title: 'Filter, aggregate, then classify', code: "WITH paid_lines AS (\n  SELECT o.customer_id, o.order_id,\n         i.quantity * i.unit_price AS line_total\n  FROM orders AS o\n  JOIN order_items AS i ON i.order_id = o.order_id\n  WHERE o.status = 'PAID'\n), customer_totals AS (\n  SELECT customer_id, COUNT(DISTINCT order_id) AS order_count,\n         SUM(line_total) AS revenue\n  FROM paid_lines\n  GROUP BY customer_id\n)\nSELECT customer_id, order_count, revenue,\n       CASE WHEN revenue >= 1000 THEN 'high-value' ELSE 'standard' END AS segment\nFROM customer_totals\nORDER BY revenue DESC, customer_id;" },
      { kind: 'table', caption: 'Stage contracts', headers: ['Stage', 'Grain', 'Invariant'], rows: [['`paid_lines`', 'One row per order item', 'Only paid orders; line total is derived once'], ['`customer_totals`', 'One row per customer', 'Revenue is the sum of paid lines'], ['Final result', 'One row per customer', 'Segment derives from the completed total']] },
      { kind: 'paragraph', text: 'For a customer with paid line totals 40, 60, and 900, the first stage emits three rows, the second emits one row with revenue 1000, and the final stage labels it high-value. This progression makes both multiplicity and business rules inspectable.' },
      { kind: 'pitfall', title: 'Assuming WITH means “compute once”', text: 'Do not use a CTE as a portable performance hint. Repeated references can be shared or recomputed, and forced materialization can either help repeated work or block useful pushdown. Compare the actual plan on the target vendor.' },
      { kind: 'bestPractice', title: 'Name relations, document grain', text: 'Give each CTE a result-oriented name, list columns explicitly when names are not self-evident, and keep the final `ORDER BY` where the ordering contract is visible.' },
    ],
    refs: [
      { book: 'database-system-concepts', chapter: '§3.8.6 — The With Clause' },
      { book: 'sql-relational-theory', chapter: 'Ch. 6 — Formulating expressions one step at a time' },
      { book: 'postgresql-docs', chapter: '7.8 — WITH Queries: materialization controls' },
    ],
    related: ['subqueries-and-correlation', 'recursive-queries', 'cost-based-query-planning'],
  },
  {
    id: 'recursive-queries', domainId: 'db-advanced-sql', title: 'Recursive Queries',
    summary: 'A recursive CTE grows a result from an anchor through repeated recursive steps until no new step rows appear; safe traversal makes depth, path, cycles, duplicates, and output ordering explicit.',
    keyPoints: [
      { text: 'The anchor seeds the working relation.', detail: 'It selects roots and initializes traversal state such as depth and path. Its column types must be compatible with the recursive term.' },
      { text: 'The recursive term advances exactly one relationship step.', detail: 'It reads rows produced by the prior iteration and joins children; evaluation stops when an iteration produces no rows.' },
      { text: '`UNION ALL` preserves distinct paths; `UNION` removes duplicate result rows.', detail: 'Duplicate elimination is not general cycle protection when depth or path changes on every visit. Choose based on whether repeated reachable paths are meaningful.' },
      { text: 'Cycle prevention belongs in the traversal state.', detail: 'Track visited IDs in a path and reject a child already present. Vendor `CYCLE` syntax can express this, but an explicit path also teaches and displays the invariant.' },
      { text: 'Depth limits are safety bounds, not cycle detection.', detail: 'A maximum depth prevents runaway work and can encode a domain limit, but it can truncate valid deep trees and does not identify corrupt edges.' },
      { text: 'Traversal order and display order are separate.', detail: 'Recursive production order is not guaranteed. Carry a path or search key and apply a final `ORDER BY` for deterministic depth-first or breadth-first presentation.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Working-table flow', code: 'flowchart LR\n  A["Anchor: root category"] --> W["Working rows"]\n  W --> R["Recursive term: join children"]\n  R --> C{"Rows produced?"}\n  C -- Yes --> O["Append to result"]\n  O --> W\n  C -- No --> E["Stop"]' },
      { kind: 'code', variants: [{ id: 'postgresql', label: 'PostgreSQL', language: 'sql', code: "WITH RECURSIVE category_tree AS (\n  SELECT c.category_id, c.parent_category_id, c.name,\n         0 AS depth, ARRAY[c.category_id] AS path\n  FROM categories AS c\n  WHERE c.category_id = :root_id\n  UNION ALL\n  SELECT child.category_id, child.parent_category_id, child.name,\n         parent.depth + 1, parent.path || child.category_id\n  FROM category_tree AS parent\n  JOIN categories AS child\n    ON child.parent_category_id = parent.category_id\n  WHERE parent.depth < :max_depth\n    AND NOT child.category_id = ANY(parent.path)\n)\nSELECT category_id, parent_category_id, name, depth, path\nFROM category_tree\nORDER BY path;" }, { id: 'sql-server', label: 'SQL Server', language: 'sql', code: "WITH category_tree AS (\n  SELECT category_id, parent_category_id, name, 0 AS depth,\n         '/' + CONVERT(varchar(max), category_id) + '/' AS path\n  FROM categories WHERE category_id = @root_id\n  UNION ALL\n  SELECT c.category_id, c.parent_category_id, c.name, p.depth + 1,\n         p.path + CONVERT(varchar(20), c.category_id) + '/'\n  FROM category_tree AS p\n  JOIN categories AS c ON c.parent_category_id = p.category_id\n  WHERE p.depth < @max_depth\n    AND p.path NOT LIKE '%/' + CONVERT(varchar(20), c.category_id) + '/%'\n)\nSELECT * FROM category_tree\nOPTION (MAXRECURSION 100);" }] },
      { kind: 'table', caption: 'Traversal state for A → B → C with corrupt C → A', headers: ['Iteration', 'Candidate', 'Path', 'Decision'], rows: [['Anchor', 'A', '`[A]`', 'Emit depth 0'], ['1', 'B', '`[A,B]`', 'Emit depth 1'], ['2', 'C', '`[A,B,C]`', 'Emit depth 2'], ['3', 'A', 'Already in path', 'Reject cycle edge']] },
      { kind: 'pitfall', title: 'A changing column defeats UNION cycle suppression', text: 'If each visit increments `depth`, revisiting the same node produces a different row. `UNION` cannot recognize that as a duplicate; test membership by node identity in the path.' },
      { kind: 'bestPractice', title: 'Constrain, diagnose, then order', text: 'Validate parent keys where the model is a tree, retain a diagnostic path, impose a defensible depth guard, and sort only in the outer query.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 5.4 — Recursive Queries' }, { book: 'postgresql-docs', chapter: '7.8.2 — Recursive Queries; SEARCH and CYCLE' }, { book: 'sqlserver-docs', chapter: 'Recursive queries using common table expressions; MAXRECURSION' }],
    related: ['common-table-expressions', 'modeling-hierarchies-and-graphs', 'query-lifecycle-and-iterator-execution'],
  },
  {
    id: 'window-functions', domainId: 'db-advanced-sql', title: 'Window Functions',
    summary: 'Window functions calculate across related rows while retaining each row; partition, ordering, and frame are independent parts of the semantic contract and ties require deliberate treatment.',
    keyPoints: [
      { text: '`PARTITION BY` divides the input into independent analytic groups.', detail: 'Without it, all rows form one partition. Unlike `GROUP BY`, partitioning does not collapse rows.' },
      { text: 'Window `ORDER BY` defines analytic sequence and peer groups.', detail: 'It does not order the final result. Equal ordering values are peers; add a stable unique key when row-by-row order must be deterministic.' },
      { text: 'The frame is a subset around the current row.', detail: 'Aggregate window functions read the frame, while ranking functions are defined by the partition ordering. State the frame instead of relying on a subtle default.' },
      { text: '`ROWS` counts physical ordered rows; `RANGE` groups by ordering values.', detail: 'With peers, a `RANGE ... CURRENT ROW` boundary includes the peer group, whereas a `ROWS` boundary can end at one physical row.' },
      { text: 'The ordered default commonly ends at the current row’s last peer.', detail: 'This produces running aggregates but makes `last_value` surprising. Use an explicit whole-partition frame when the desired last value is the partition’s last row.' },
      { text: 'Null ordering and null inputs need explicit intent.', detail: 'Aggregates generally ignore null inputs; ranking still positions rows. Vendor defaults for null placement vary, so specify a portable discriminator when it matters.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Partition, order, and frame', code: 'flowchart TB\n  P["Partition: customer 7"] --> R1["Jan 2 · order 10 · 40"]\n  P --> R2["Jan 2 · order 11 · 60"]\n  P --> R3["Jan 5 · order 12 · 30"]\n  R1 --> F["ROWS frame at order 11: rows 10..11"]\n  R2 --> F\n  R1 -. "RANGE peers at Jan 2" .-> G["both Jan 2 rows"]\n  R2 -.-> G' },
      { kind: 'code', language: 'sql', title: 'Explicit running total and ranking', code: "WITH order_totals AS (\n  SELECT o.customer_id, o.order_id, o.placed_at,\n         SUM(i.quantity * i.unit_price) AS order_total\n  FROM orders AS o\n  JOIN order_items AS i ON i.order_id = o.order_id\n  WHERE o.status = 'PAID'\n  GROUP BY o.customer_id, o.order_id, o.placed_at\n)\nSELECT customer_id, order_id, placed_at, order_total,\n       SUM(order_total) OVER (\n         PARTITION BY customer_id\n         ORDER BY placed_at, order_id\n         ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n       ) AS running_amount,\n       ROW_NUMBER() OVER (\n         PARTITION BY customer_id ORDER BY placed_at, order_id\n       ) AS sequence_number\nFROM order_totals\nORDER BY customer_id, placed_at, order_id;" },
      { kind: 'table', caption: 'Frame behavior when two orders share Jan 2', headers: ['Frame', 'At first Jan 2 row', 'At second Jan 2 row'], rows: [['`ROWS ... CURRENT ROW`', 'Only first physical row', 'Both physical rows'], ['`RANGE ... CURRENT ROW`', 'Both Jan 2 peers', 'Both Jan 2 peers'], ['Whole partition', 'Every customer row', 'Every customer row']] },
      { kind: 'paragraph', text: 'With amounts 40 and 60 on the same timestamp, deterministic `ROWS` plus `order_id` yields running totals 40 then 100. Ordering only by the timestamp leaves the physical order tied; a peer-aware `RANGE` total is 100 for both.' },
      { kind: 'pitfall', title: 'Confusing analytic order with presentation order', text: 'An `ORDER BY` inside `OVER` defines a calculation. Only the outer `ORDER BY` guarantees returned row order, and it too needs a total tie-breaker for stable pagination or display.' },
      { kind: 'bestPractice', title: 'Write the complete window contract', text: 'Choose partition, total order, and explicit frame independently. Name reusable windows when supported and test duplicate order values and nulls.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§5.5.2 — Windowing' }, { book: 'postgresql-docs', chapter: '4.2.8 and 9.22 — Window function calls, frames, and peers' }, { book: 'sql-standard', chapter: 'ISO/IEC 9075-2 — Windowed table functions' }],
    related: ['advanced-window-patterns', 'ordering-and-pagination', 'grouping-and-aggregation'],
  },
  {
    id: 'advanced-window-patterns', domainId: 'db-advanced-sql', title: 'Advanced Window Patterns',
    summary: 'Layered window queries solve deterministic top-N, running and moving measures, session boundaries, and cohort retention by first establishing the correct row grain and total order.',
    keyPoints: [
      { text: 'Top-N-per-group ranks inside each group and filters outside.', detail: 'Window results are not normally available to `WHERE` at the same level, so compute rank in a CTE and retain ranks up to N.' },
      { text: 'Running and moving windows are different contracts.', detail: 'A running total begins at the partition start; a moving total covers a bounded neighborhood. `ROWS 6 PRECEDING` means seven rows, not seven calendar days.' },
      { text: 'Sessionization marks gaps, then cumulatively numbers starts.', detail: 'Use `LAG` on a deterministic event sequence, flag the first event or a gap beyond the threshold, then take a running sum of the flag.' },
      { text: 'Retention needs one explicit grain per cohort and period.', detail: 'Deduplicate customer-period activity before counting, or multiple orders inflate retained-customer measures.' },
      { text: 'Ties require a business rule.', detail: '`ROW_NUMBER` chooses exactly N rows only with a total order; `RANK` or `DENSE_RANK` deliberately includes tied positions and can return more than N rows.' },
      { text: 'Window pipelines often require multiple query levels.', detail: 'One window result generally cannot feed another window at the same logical stage. Name intermediate relations and verify their keys.' },
    ],
    blocks: [
      { kind: 'code', language: 'sql', title: 'Deterministic top three orders per customer', code: "WITH order_totals AS (\n  SELECT o.customer_id, o.order_id, o.placed_at,\n         SUM(i.quantity * i.unit_price) AS order_total\n  FROM orders AS o\n  JOIN order_items AS i ON i.order_id = o.order_id\n  WHERE o.status = 'PAID'\n  GROUP BY o.customer_id, o.order_id, o.placed_at\n), ranked AS (\n  SELECT order_totals.*, ROW_NUMBER() OVER (\n    PARTITION BY customer_id\n    ORDER BY order_total DESC, placed_at DESC, order_id\n  ) AS rn\n  FROM order_totals\n)\nSELECT customer_id, order_id, order_total, placed_at\nFROM ranked WHERE rn <= 3\nORDER BY customer_id, rn;" },
      { kind: 'code', language: 'sql', title: 'Seven-order moving average', code: "WITH order_totals AS (\n  SELECT o.customer_id, o.order_id, o.placed_at,\n         SUM(i.quantity * i.unit_price) AS order_total\n  FROM orders AS o\n  JOIN order_items AS i ON i.order_id = o.order_id\n  WHERE o.status = 'PAID'\n  GROUP BY o.customer_id, o.order_id, o.placed_at\n)\nSELECT customer_id, order_id, placed_at, order_total,\n       AVG(order_total) OVER (\n         PARTITION BY customer_id\n         ORDER BY placed_at, order_id\n         ROWS BETWEEN 6 PRECEDING AND CURRENT ROW\n       ) AS last_7_order_average\nFROM order_totals\nORDER BY customer_id, placed_at, order_id;", caption: 'First establish one row per paid order. For each row, the frame then contains that order and at most six predecessors; early rows average the smaller frame that actually exists.' },
      { kind: 'code', language: 'sql', title: 'Sessionize paid-order events', code: "WITH order_events AS (\n  SELECT o.customer_id, o.order_id AS event_id, o.placed_at AS occurred_at\n  FROM orders AS o\n  WHERE o.status = 'PAID'\n), previous AS (\n  SELECT order_events.*, LAG(occurred_at) OVER (\n    PARTITION BY customer_id ORDER BY occurred_at, event_id\n  ) AS previous_at\n  FROM order_events\n), boundaries AS (\n  SELECT previous.*, CASE\n    WHEN previous_at IS NULL\n      OR occurred_at > previous_at + INTERVAL '30 minutes' THEN 1 ELSE 0\n  END AS session_start\n  FROM previous\n)\nSELECT boundaries.*, SUM(session_start) OVER (\n  PARTITION BY customer_id ORDER BY occurred_at, event_id\n  ROWS UNBOUNDED PRECEDING\n) AS session_number\nFROM boundaries;" },
      { kind: 'code', language: 'sql', title: 'Monthly cohort retention', code: "WITH customer_months AS (\n  SELECT DISTINCT o.customer_id,\n         EXTRACT(YEAR FROM o.placed_at) AS activity_year,\n         EXTRACT(MONTH FROM o.placed_at) AS activity_month\n  FROM orders AS o\n  WHERE o.status = 'PAID'\n), first_month AS (\n  SELECT customer_id, MIN(activity_year * 12 + activity_month) AS cohort_month\n  FROM customer_months\n  GROUP BY customer_id\n), retention AS (\n  SELECT f.cohort_month,\n         (m.activity_year * 12 + m.activity_month) - f.cohort_month AS month_number,\n         COUNT(*) AS retained_customers\n  FROM customer_months AS m\n  JOIN first_month AS f ON f.customer_id = m.customer_id\n  GROUP BY f.cohort_month,\n           (m.activity_year * 12 + m.activity_month) - f.cohort_month\n)\nSELECT cohort_month, month_number, retained_customers\nFROM retention\nORDER BY cohort_month, month_number;", caption: '`customer_months` removes repeated orders in a month; `first_month` fixes one cohort per customer; the final grouping counts each customer once in each elapsed month.' },
      { kind: 'table', caption: 'Choosing a pattern', headers: ['Question', 'Window contract', 'Precondition'], rows: [['Lifetime spend', 'Unbounded preceding to current row', 'One row per intended transaction'], ['Last seven orders', 'Six rows preceding to current row', 'Deterministic order'], ['Top three including ties', '`RANK() <= 3`', 'Tie behavior accepted'], ['Retention by month', 'Cohort plus activity-period grouping', 'Customer-period deduplicated']] },
      { kind: 'pitfall', title: 'Calling seven rows “seven days”', text: 'Sparse days and multiple same-day orders make a row-count frame diverge from elapsed time. Aggregate to one row per day first or use a supported value-based time frame with precisely tested boundaries.' },
      { kind: 'bestPractice', title: 'Stabilize grain and order first', text: 'Deduplicate or aggregate to the intended event grain, add an immutable tie-breaker, and expose boundary flags in a separate stage so session and retention logic can be audited.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§5.5.1–5.5.2 — Ranking; Windowing' }, { book: 'postgresql-docs', chapter: '9.22 — General-purpose window functions' }],
    related: ['window-functions', 'lateral-joins-and-apply', 'ordering-and-pagination'],
  },
  {
    id: 'lateral-joins-and-apply', domainId: 'db-advanced-sql', title: 'Lateral Joins and APPLY',
    summary: 'A lateral table expression is parameterized by columns from preceding FROM items, enabling per-row sets such as an index-friendly top-N; PostgreSQL spells this `LATERAL`, while SQL Server uses `APPLY`.',
    keyPoints: [
      { text: 'The right-hand expression can reference the current left row.', detail: 'Conceptually it is evaluated for each left row and the resulting rowsets are combined, although the optimizer may choose an equivalent physical plan.' },
      { text: 'Inner and preserving forms differ on an empty right result.', detail: 'PostgreSQL `CROSS JOIN LATERAL` and SQL Server `CROSS APPLY` drop that left row; `LEFT JOIN LATERAL ... ON TRUE` and `OUTER APPLY` retain it with null-extended right columns.' },
      { text: 'Top-N per parent is a natural use.', detail: 'A correlated ordered subquery with a stable limit can seek into an index beginning with parent key and ordering columns, avoiding a global ranking pass for selective workloads.' },
      { text: 'Ordering must be total before limiting.', detail: 'Add an immutable unique tie-breaker after business sort keys or repeated executions may choose different tied rows.' },
      { text: 'Lateral is not automatically faster than a window.', detail: 'Many parents or weak indexes can make repeated probes costly. Compare plans and cardinalities against `ROW_NUMBER`.' },
    ],
    blocks: [
      { kind: 'code', variants: [{ id: 'postgresql', label: 'PostgreSQL', language: 'sql', code: "SELECT c.customer_id, recent.order_id, recent.placed_at\nFROM customers AS c\nLEFT JOIN LATERAL (\n  SELECT o.order_id, o.placed_at\n  FROM orders AS o\n  WHERE o.customer_id = c.customer_id\n    AND o.status = 'PAID'\n  ORDER BY o.placed_at DESC, o.order_id DESC\n  LIMIT 3\n) AS recent ON TRUE\nORDER BY c.customer_id, recent.placed_at DESC, recent.order_id DESC;" }, { id: 'sql-server', label: 'SQL Server', language: 'sql', code: "SELECT c.customer_id, recent.order_id, recent.placed_at\nFROM customers AS c\nOUTER APPLY (\n  SELECT TOP (3) o.order_id, o.placed_at\n  FROM orders AS o\n  WHERE o.customer_id = c.customer_id\n    AND o.status = 'PAID'\n  ORDER BY o.placed_at DESC, o.order_id DESC\n) AS recent\nORDER BY c.customer_id, recent.placed_at DESC, recent.order_id DESC;" }] },
      { kind: 'table', caption: 'Dialect mapping', headers: ['Intent', 'PostgreSQL', 'SQL Server'], rows: [['Drop parent when right side is empty', '`CROSS JOIN LATERAL`', '`CROSS APPLY`'], ['Preserve parent when right side is empty', '`LEFT JOIN LATERAL ... ON TRUE`', '`OUTER APPLY`'], ['Limit correlated rows', '`ORDER BY ... LIMIT n`', '`SELECT TOP (n) ... ORDER BY`']] },
      { kind: 'paragraph', text: 'A customer with five paid orders contributes three recent rows; a customer with none contributes one null-extended row in the preserving form. That output grain differs from a scalar correlated subquery and must be expected by downstream aggregation.' },
      { kind: 'pitfall', title: 'Limiting before establishing a stable order', text: '`LIMIT 3` or `TOP (3)` without a total order asks for any three qualifying rows. A timestamp alone is insufficient when simultaneous orders are possible.' },
      { kind: 'bestPractice', title: 'Support the parameterized access path', text: 'For this pattern, consider an index beginning with the correlation key and continuing with sort keys, including the filter and projected data as appropriate for the target engine; verify with its plan.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§3.8.5 — The Lateral Clause' }, { book: 'sql-relational-theory', chapter: 'Ch. 12 — Subqueries' }, { book: 'postgresql-docs', chapter: '7.2.1.5 — LATERAL subqueries' }, { book: 'sqlserver-docs', chapter: 'FROM clause — CROSS APPLY and OUTER APPLY' }],
    related: ['subqueries-and-correlation', 'advanced-window-patterns', 'index-mental-model'],
  },
  {
    id: 'grouping-sets-rollup-and-cube', domainId: 'db-advanced-sql', title: 'GROUPING SETS, ROLLUP, and CUBE',
    summary: 'Advanced grouping produces detail and multiple subtotal levels in one result: `GROUPING SETS` lists exact levels, `ROLLUP` builds hierarchical prefixes, and `CUBE` builds every dimension combination.',
    keyPoints: [
      { text: '`GROUPING SETS` states the exact grouping keys required.', detail: 'It is semantically like combining compatible aggregate queries, including the empty grouping set `()` for a grand total.' },
      { text: '`ROLLUP(a,b)` produces hierarchical prefixes.', detail: 'It yields `(a,b)`, `(a)`, and `()`. Column order therefore expresses the hierarchy.' },
      { text: '`CUBE(a,b)` produces every combination.', detail: 'It yields `(a,b)`, `(a)`, `(b)`, and `()`. The number of combinations grows exponentially with dimensions.' },
      { text: '`GROUPING(column)` distinguishes subtotal placeholders from data nulls.', detail: 'A null stored in the source and a null inserted because a dimension is rolled up are different facts and must not share an ambiguous label.' },
      { text: 'Filters apply before subtotal construction; `HAVING` applies to generated groups.', detail: 'A threshold can remove subtotal rows too, so include grouping level in the predicate when level-specific rules are intended.' },
    ],
    blocks: [
      { kind: 'code', language: 'sql', title: 'Customer/category detail, subtotals, and grand total', code: "SELECT CASE WHEN GROUPING(o.customer_id) = 1 THEN 'ALL CUSTOMERS'\n            ELSE CAST(o.customer_id AS varchar(20)) END AS customer_label,\n       CASE WHEN GROUPING(pc.category_id) = 1 THEN 'ALL CATEGORIES'\n            ELSE CAST(pc.category_id AS varchar(20)) END AS category_label,\n       SUM(i.quantity * i.unit_price) AS revenue\nFROM orders AS o\nJOIN order_items AS i ON i.order_id = o.order_id\nJOIN product_categories AS pc ON pc.product_id = i.product_id\nWHERE o.status = 'PAID'\nGROUP BY ROLLUP (o.customer_id, pc.category_id)\nORDER BY GROUPING(o.customer_id), o.customer_id,\n         GROUPING(pc.category_id), pc.category_id;" },
      { kind: 'table', caption: 'Grouping expansion', headers: ['Construct', 'Generated grouping sets for `(customer, category)`', 'Rows'], rows: [['Explicit', '`(customer,category), (customer), ()`', 'Chosen levels'], ['`ROLLUP`', '`(customer,category), (customer), ()`', 'Hierarchy prefixes'], ['`CUBE`', '`(customer,category), (customer), (category), ()`', 'All combinations']] },
      { kind: 'paragraph', text: 'The explicit joins establish every grouping column: customers come from `orders`, categories from the canonical `product_categories` bridge, and revenue from order-line quantity times transaction price. `GROUPING(pc.category_id)=1` identifies a generated subtotal placeholder rather than a stored category.' },
      { kind: 'pitfall', title: 'Using COALESCE to label every subtotal', text: '`COALESCE(dimension, \'ALL\')` mislabels a real null dimension value. Use `GROUPING()` first, then handle stored null according to its domain meaning.' },
      { kind: 'bestPractice', title: 'Request only useful levels', text: 'Prefer explicit grouping sets when consumers need a few totals. Use `CUBE` only when every cross-dimensional subtotal is required and the resulting cardinality is acceptable.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§5.5.4 — Rollup and Cube' }, { book: 'postgresql-docs', chapter: '7.2.4 — GROUPING SETS, CUBE, and ROLLUP' }, { book: 'sql-standard', chapter: 'ISO/IEC 9075-2 — Grouping operations' }],
    related: ['grouping-and-aggregation', 'pivot-unpivot-and-conditional-aggregation', 'row-column-and-compressed-storage'],
  },
  {
    id: 'pivot-unpivot-and-conditional-aggregation', domainId: 'db-advanced-sql', title: 'Pivot, Unpivot, and Conditional Aggregation',
    summary: 'Pivoting turns category values into columns and unpivoting turns columns into rows; conditional aggregation is the portable, explicit core, while vendor operators have distinct static-column and null behavior.',
    keyPoints: [
      { text: 'Conditional aggregation expresses pivot semantics portably.', detail: 'Group by the row dimensions and aggregate a `CASE` expression per desired output category.' },
      { text: 'A pivot requires a duplicate policy.', detail: 'Multiple source rows can map to one output cell, so an aggregate such as `SUM`, `MAX`, or `COUNT` is part of the meaning, not syntactic ceremony.' },
      { text: 'Static pivot syntax needs a known output column list.', detail: 'Data-driven categories require dynamic SQL or a tall result consumed by an application/reporting layer; dynamic identifiers must be safely quoted.' },
      { text: 'Null and zero are different results.', detail: 'No matching value may yield null, while `ELSE 0` can make an additive measure zero. Choose based on whether absence means zero or unknown.' },
      { text: 'Unpivot is not generally an inverse.', detail: 'A prior pivot may aggregate duplicates, discard distinctions, and represent missing cells as null. The original rows cannot necessarily be reconstructed.' },
    ],
    blocks: [
      { kind: 'code', language: 'sql', title: 'Portable quarterly revenue for an explicit year', code: "SELECT o.customer_id,\n       SUM(CASE WHEN o.placed_at >= :year_start\n                 AND o.placed_at < :q2_start THEN i.quantity * i.unit_price ELSE 0 END) AS q1,\n       SUM(CASE WHEN o.placed_at >= :q2_start\n                 AND o.placed_at < :q3_start THEN i.quantity * i.unit_price ELSE 0 END) AS q2,\n       SUM(CASE WHEN o.placed_at >= :q3_start\n                 AND o.placed_at < :q4_start THEN i.quantity * i.unit_price ELSE 0 END) AS q3,\n       SUM(CASE WHEN o.placed_at >= :q4_start\n                 AND o.placed_at < :next_year_start THEN i.quantity * i.unit_price ELSE 0 END) AS q4\nFROM orders AS o\nJOIN order_items AS i ON i.order_id = o.order_id\nWHERE o.status = 'PAID'\n  AND o.placed_at >= :year_start\n  AND o.placed_at < :next_year_start\nGROUP BY o.customer_id;" },
      { kind: 'code', variants: [{ id: 'postgresql', label: 'PostgreSQL', language: 'sql', code: "WITH order_quarters AS (\n  SELECT o.customer_id, EXTRACT(QUARTER FROM o.placed_at) AS quarter_no,\n         i.quantity * i.unit_price AS line_total\n  FROM orders AS o\n  JOIN order_items AS i ON i.order_id = o.order_id\n  WHERE o.status = 'PAID'\n    AND o.placed_at >= :year_start AND o.placed_at < :next_year_start\n)\nSELECT customer_id,\n       SUM(line_total) FILTER (WHERE quarter_no = 1) AS q1,\n       SUM(line_total) FILTER (WHERE quarter_no = 2) AS q2\nFROM order_quarters GROUP BY customer_id;" }, { id: 'sql-server', label: 'SQL Server', language: 'sql', code: "WITH order_quarters AS (\n  SELECT o.customer_id, DATEPART(quarter, o.placed_at) AS quarter_no,\n         i.quantity * i.unit_price AS line_total\n  FROM orders AS o\n  JOIN order_items AS i ON i.order_id = o.order_id\n  WHERE o.status = 'PAID'\n    AND o.placed_at >= @year_start AND o.placed_at < @next_year_start\n), quarterly_totals AS (\n  SELECT customer_id, [1] AS q1, [2] AS q2\n  FROM order_quarters\n  PIVOT (SUM(line_total) FOR quarter_no IN ([1], [2])) AS p\n)\nSELECT customer_id, [1] AS q1, [2] AS q2 FROM (\n  SELECT customer_id, quarter_no, line_total FROM order_quarters\n) AS source\nPIVOT (SUM(line_total) FOR quarter_no IN ([1], [2])) AS p;\n\nSELECT customer_id, quarter_no, quarter_total\nFROM quarterly_totals\nUNPIVOT (quarter_total FOR quarter_no IN (q1, q2)) AS u;" }] },
      { kind: 'table', caption: 'Technique trade-offs', headers: ['Technique', 'Strength', 'Constraint'], rows: [['Conditional aggregation', 'Portable and explicit', 'One expression per output column'], ['PostgreSQL aggregate `FILTER`', 'Concise conditional aggregate', 'PostgreSQL-specific syntax'], ['SQL Server `PIVOT`', 'Dedicated row-to-column operator', 'Static `IN` column list'], ['SQL Server `UNPIVOT`', 'Dedicated column-to-row operator', 'Null/aggregation loss prevents general inversion']] },
      { kind: 'pitfall', title: 'Pivoting before fixing the source grain', text: 'Joining orders to both items and promotions can multiply each cell before aggregation. First produce one row per intended cell contribution, then reshape.' },
      { kind: 'bestPractice', title: 'Keep analytics tall until the boundary', text: 'Tall `(dimension, category, measure)` results compose and accommodate new categories. Pivot only for a consumer that truly requires a fixed wide schema.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§5.5.3 — Pivoting' }, { book: 'sql-relational-theory', chapter: 'Ch. 7 — Summarization' }, { book: 'postgresql-docs', chapter: '9.21 — Aggregate FILTER clause' }, { book: 'sqlserver-docs', chapter: 'FROM and Using PIVOT and UNPIVOT' }],
    related: ['grouping-sets-rollup-and-cube', 'grouping-and-aggregation', 'row-column-and-compressed-storage'],
  },
  {
    id: 'gaps-islands-and-relational-division', domainId: 'db-advanced-sql', title: 'Gaps, Islands, and Relational Division',
    summary: 'Gaps-and-islands finds consecutive runs by comparing each value with its predecessor, while relational division answers universal “has every required item” questions with nested nonexistence or exact counts.',
    keyPoints: [
      { text: 'An island begins where no qualifying predecessor connects.', detail: 'Use `LAG` to expose the prior value, flag a boundary, then cumulatively sum flags into stable island identifiers.' },
      { text: 'Consecutiveness needs an exact domain rule.', detail: 'Adjacent dates, sequence numbers, and time intervals use different predicates. Deduplicate at that domain grain before detecting boundaries.' },
      { text: 'Relational division expresses universal quantification.', detail: '“Customer bought every required product” means there does not exist a required product for which no qualifying customer purchase exists.' },
      { text: 'The empty divisor is a deliberate edge case.', detail: 'Nested `NOT EXISTS` returns every candidate when there are no requirements, matching vacuous truth. A count solution must be checked for the same policy.' },
      { text: 'Duplicates must not inflate count-based division.', detail: 'Count distinct requirement keys on both sides or enforce set-like uniqueness before comparing counts.' },
      { text: 'Null requirement keys make equality ambiguous.', detail: 'Use non-null keys for identity. If a nullable attribute is the business requirement, define null matching explicitly rather than relying on `=`.' },
    ],
    blocks: [
      { kind: 'code', language: 'sql', title: 'Exact consecutive paid-order date islands', code: "WITH days AS (\n  SELECT DISTINCT customer_id, CAST(placed_at AS DATE) AS order_day\n  FROM orders WHERE status = 'PAID'\n), marked AS (\n  SELECT days.*, CASE\n    WHEN LAG(order_day) OVER (PARTITION BY customer_id ORDER BY order_day)\n         = order_day - INTERVAL '1 day' THEN 0 ELSE 1 END AS island_start\n  FROM days\n), numbered AS (\n  SELECT marked.*, SUM(island_start) OVER (\n    PARTITION BY customer_id ORDER BY order_day ROWS UNBOUNDED PRECEDING\n  ) AS island_id\n  FROM marked\n)\nSELECT customer_id, MIN(order_day) AS start_day, MAX(order_day) AS end_day, COUNT(*) AS day_count\nFROM numbered GROUP BY customer_id, island_id\nORDER BY customer_id, start_day;" },
      { kind: 'code', language: 'sql', title: 'Customers who bought every product in a required set', code: "SELECT c.customer_id\nFROM customers AS c\nWHERE NOT EXISTS (\n  SELECT 1 FROM required_products AS r\n  WHERE NOT EXISTS (\n    SELECT 1\n    FROM orders AS o\n    JOIN order_items AS i ON i.order_id = o.order_id\n    WHERE o.customer_id = c.customer_id\n      AND o.status = 'PAID'\n      AND i.product_id = r.product_id\n  )\n);" },
      { kind: 'table', caption: 'Worked division: requirements {A, B}', headers: ['Customer purchases', 'Missing requirement exists?', 'Qualifies?'], rows: [['7: A, A, B', 'No', 'Yes; duplicate A is irrelevant'], ['8: A, C', 'Yes: B', 'No'], ['9: none', 'Yes: A and B', 'No'], ['Any customer; requirements empty', 'No', 'Yes under vacuous truth']] },
      { kind: 'pitfall', title: 'Applying row-number subtraction to duplicate dates', text: 'Two orders on one date shift later row numbers and split or merge islands incorrectly. Reduce to one customer-date row before the consecutive-day calculation.' },
      { kind: 'bestPractice', title: 'State the set predicates first', text: 'Write down candidate, required set, qualifying relationship, duplicate rule, and empty-set policy before choosing nested `NOT EXISTS` or count equality.' },
    ],
    refs: [{ book: 'sql-relational-theory', chapter: 'Ch. 7 — Divide; Ch. 11 — Example 2: Universal quantification' }, { book: 'database-system-concepts', chapter: '§3.8.2 — The EXISTS Test' }, { book: 'postgresql-docs', chapter: '9.22 and 9.24 — Window and subquery expressions' }],
    related: ['relational-algebra', 'exists-in-and-null-traps', 'window-functions'],
  },
  {
    id: 'temporal-and-versioned-queries', domainId: 'db-advanced-sql', title: 'Temporal and Versioned Queries',
    summary: 'Temporal querying models facts over explicit validity intervals; half-open boundaries make adjacency unambiguous, while as-of, overlap, and history-integrity queries preserve the difference between valid time and recorded system time.',
    keyPoints: [
      { text: 'Use half-open intervals `[valid_from, valid_to)`.', detail: 'The start is included and the end excluded, so one version can end exactly when the next begins without overlap or double matching.' },
      { text: 'An as-of predicate contains one instant.', detail: '`valid_from <= :as_of AND (valid_to IS NULL OR :as_of < valid_to)` returns versions active at the instant when null means an unbounded upper end. An integrity rule should guarantee at most one version per entity.' },
      { text: 'Two half-open intervals overlap when each starts before the other ends.', detail: 'With nullable open ends, test each finite end or treat null as positive infinity; adjacent finite intervals do not overlap.' },
      { text: 'Valid time and system time answer different questions.', detail: 'Valid time says when a fact applies in the modeled world; system time says when the database recorded a version. Bitemporal data retains both.' },
      { text: 'Open-ended history needs a consistent representation.', detail: 'This model uses null to mean positive infinity. Every scalar predicate therefore handles null explicitly, while PostgreSQL range construction maps the null upper bound to an unbounded range.' },
      { text: 'Version integrity is a write-time concern.', detail: 'An as-of query cannot correct overlapping versions. Use target-engine constraints, exclusion rules, or serialized validation to prevent conflicting history.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Half-open price history', code: 'timeline\n  title Product 42 validity\n  2026-01-01 : 10.00 starts\n  2026-03-01 : 10.00 ends; 12.00 starts\n  2026-06-01 : 12.00 ends; 15.00 starts' },
      { kind: 'code', language: 'sql', title: 'As-of and overlap checks with null as infinity', code: "-- Version active at the instant\nSELECT product_id, price\nFROM product_price_history\nWHERE product_id = :product_id\n  AND valid_from <= :as_of\n  AND (valid_to IS NULL OR :as_of < valid_to);\n\n-- Conflicting pairs; adjacency is allowed\nSELECT a.product_id, a.valid_from, a.valid_to, b.valid_from, b.valid_to\nFROM product_price_history AS a\nJOIN product_price_history AS b\n  ON b.product_id = a.product_id\n AND (b.valid_from, b.version_id) > (a.valid_from, a.version_id)\n AND (b.valid_to IS NULL OR a.valid_from < b.valid_to)\n AND (a.valid_to IS NULL OR b.valid_from < a.valid_to);" },
      { kind: 'code', variants: [{ id: 'postgresql', label: 'PostgreSQL', language: 'sql', code: "-- PostgreSQL interprets a NULL upper bound as unbounded here.\nSELECT * FROM product_price_history\nWHERE product_id = :product_id\n  AND tstzrange(valid_from, valid_to, '[)') @> :as_of;" }, { id: 'sql-server', label: 'SQL Server', language: 'sql', code: "SELECT *\nFROM product_prices FOR SYSTEM_TIME AS OF @as_of\nWHERE product_id = @product_id;" }] },
      { kind: 'table', caption: 'Boundary truth table', headers: ['A', 'B', 'Overlap?'], rows: [['`[Jan 1, Mar 1)`', '`[Mar 1, Jun 1)`', 'No; exactly adjacent'], ['`[Jan 1, Apr 1)`', '`[Mar 1, Jun 1)`', 'Yes; Mar 1–Apr 1'], ['`[Jan 1, NULL)`', '`[Jun 1, NULL)`', 'Yes; both are open-ended']] },
      { kind: 'pitfall', title: 'Using BETWEEN for half-open validity', text: '`BETWEEN` includes both endpoints, so an instant at a handoff can match the ending and starting versions. Write the two comparisons explicitly.' },
      { kind: 'bestPractice', title: 'Make time semantics part of the schema', text: 'Store one time zone convention, constrain nonempty intervals, index entity plus boundaries, prevent overlap where required, and document whether corrections rewrite valid history or append system-time versions.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§7.10 — Modeling Temporal Data' }, { book: 'postgresql-docs', chapter: '8.17 — Range types and exclusion constraints' }, { book: 'sqlserver-docs', chapter: 'FROM clause — FOR SYSTEM_TIME temporal querying' }],
    related: ['temporal-data-modeling', 'dates-times-and-time-zones', 'primary-unique-and-check-constraints'],
  },
]

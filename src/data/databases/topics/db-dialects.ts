import type { Topic } from '../../../types/content'

const dialectIds = ['postgresql', 'mysql', 'sqlite', 'sql-server', 'oracle'] as const

export const dbDialectsTopics: Topic[] = [
  {
    id: 'sql-portability-strategy', domainId: 'db-dialects', title: 'SQL Portability Strategy',
    summary: 'Portability is the ability to move or support a workload with known, tested costs—not the absence of vendor features. Keep invariants and a useful relational/query core portable, isolate consequential differences behind explicit adapters, and specialize deliberately when the operational or correctness gain pays for the boundary.',
    keyPoints: [
      { text: 'Choose a portability target, not an aspiration.', detail: 'Name the supported products and versions, deployment modes, drivers, extensions, migration direction, acceptable downtime, and which behaviors must remain identical. “ANSI SQL” alone does not define types, plans, error classification, or operations.' },
      { text: 'Portable does not mean lowest common denominator.', detail: 'A deliberately small core should preserve relational constraints, parameterized queries, transactions, and precise types. A capability module can use stronger vendor facilities without scattering conditional SQL through business logic.' },
      { text: 'Separate syntax from semantics.', detail: 'A token rewrite can fix pagination spelling but cannot equalize nulls, collations, isolation, generated-key scope, lock behavior, affected-row counts, or DDL atomicity.' },
      { text: 'Make the database boundary observable.', detail: 'Centralize SQL ownership, typed parameter/result conversion, error classification, transaction policy, and capability discovery. Log the chosen adapter and server version without exposing values.' },
      { text: 'Migrations are part of the portability contract.', detail: 'Maintain per-dialect migration renderings or capability-gated steps, record schema versions, and rehearse forward, rollback/roll-forward, and data conversion on real engines.' },
      { text: 'Cross-dialect tests need semantic oracles.', detail: 'Run the same invariant, concurrency, null/collation, overflow, time-zone, and round-trip cases against every supported engine; snapshotting SQL strings proves only rendering.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Layered portability boundary', code: 'flowchart TD\n  B["business invariants + domain types"] --> C["portable relational model + query core"]\n  C --> A["typed, tested dialect adapters"]\n  A --> P["PostgreSQL"]\n  A --> M["MySQL"]\n  A --> S["SQLite"]\n  A --> Q["SQL Server"]\n  A --> O["Oracle"]\n  C --> V["vendor capability modules"]\n  V --> P\n  V --> M\n  X["versioned migration strategy"] --> A\n  T["cross-dialect contract tests"] -. "verify semantics" .-> C\n  T -. "verify specialization" .-> V' },
      { kind: 'table', caption: 'Portability decision framework', headers: ['Decision', 'Portable core', 'Adapter/capability boundary', 'Evidence'], rows: [['Integrity', 'PK/FK/unique/check and explicit nullability', 'deferral, exclusion/predicate features', 'race tests on real engines'], ['Queries', 'joins, predicates, aggregates, stable ordering', 'pagination, upsert, returning, JSON/search', 'result + concurrency contracts'], ['Types', 'bounded semantic domain and conversion rules', 'native UUID/JSON/time-zone/binary types', 'boundary/overflow round trips'], ['Transactions', 'short unit, rollback, retry protocol', 'isolation mapping, lock hints/modes', 'anomaly and deadlock tests'], ['Evolution', 'logical target schema', 'online-DDL and migration steps', 'upgrade rehearsal and timing'], ['Operations', 'RPO/RTO and telemetry requirements', 'backup, replication, failover tooling', 'restore/failover drills']] },
      { kind: 'code', language: 'text', title: 'Capability contract', code: 'DatabaseCapabilities {\n  serverProduct, serverVersion, driverVersion\n  generatedKeyStrategy\n  paginationStrategy\n  conflictWriteStrategy\n  ddlAtomicityClass\n  supportedIsolationContracts\n}\n\nAt startup: detect and reject unsupported combinations.\nIn CI: run the same contract suite on every supported version.\nIn production: expose chosen capabilities as bounded telemetry.' },
      { kind: 'pitfall', title: 'A universal repository that leaks dialect strings', text: 'Conditionals in every query create pseudo-portability: no owner, incomplete tests, and accidental mixed semantics. Put a meaningful operation behind one typed interface with one implementation and migration path per supported dialect.' },
      { kind: 'bestPractice', title: 'Keep an exit-cost ledger', text: 'For each specialization record its value, dependent schema/data, replacement strategy, export format, test coverage, and estimated migration/operational cost. Revisit it when versions or business constraints change.' },
    ],
    refs: [{ book: 'sql-antipatterns', chapter: 'Ch. 25 — Magic Beans' }, { book: 'sql-relational-theory', chapter: 'Ch. 4 — No Duplicates, No Nulls, and No Ordering' }, { book: 'sql-standard', chapter: 'ISO/IEC 9075-2:2023 — SQL/Foundation' }, { book: 'ddia-2', chapter: 'Ch. 5 — Encoding and Evolution' }],
    related: ['schema-migrations-and-database-testing', 'orm-impedance-mismatch', 'choosing-a-relational-database'],
  },
  {
    id: 'identifiers-quoting-and-name-resolution', domainId: 'db-dialects', title: 'Identifiers, Quoting, and Name Resolution',
    summary: 'Identifier portability depends on lexical rules, quoting, case treatment, qualification, and the namespaces searched—not merely changing quote characters. Prefer simple lowercase unquoted names, avoid reserved words, qualify security-sensitive objects, and test metadata round trips.',
    keyPoints: [
      { text: 'Quoted and unquoted identifiers are different contracts.', detail: 'Quoting permits reserved words and preserves otherwise significant spelling, but then every reference and tool must reproduce it correctly. String literal quotes are never identifier quotes.' },
      { text: 'Case behavior is product and configuration dependent.', detail: 'PostgreSQL folds unquoted names to lowercase; Oracle treats nonquoted names as uppercase; SQL Server comparison follows database/identifier collation; MySQL table-name behavior depends on platform and `lower_case_table_names`; SQLite resolves names case-insensitively in ordinary use.' },
      { text: 'Namespace vocabularies do not align.', detail: 'PostgreSQL and Oracle schemas, MySQL databases, SQLite attached database names, and SQL Server database/schema pairs have different ownership and resolution semantics. Do not map terms by spelling alone.' },
      { text: 'Qualification is a correctness and security tool.', detail: 'Qualify migration, administrative, and security-definer references. Application code should use a controlled default namespace only when the connection contract fixes it.' },
      { text: 'PostgreSQL `search_path` can select hostile objects.', detail: 'The first matching object wins; adding a schema where untrusted users can create objects effectively trusts them. Secure functions and migrations should pin trusted schemas and qualify references.' },
      { text: 'Naming policy should survive future keywords.', detail: 'Use one conservative case, ASCII letters/digits/underscores, non-keyword nouns, explicit length limits, and a metadata test. Quote externally imposed legacy names at one boundary.' },
    ],
    blocks: [
      { kind: 'code', title: 'Five identifier and qualification forms', variants: [
        { id: dialectIds[0], label: 'PostgreSQL', language: 'sql', code: 'CREATE SCHEMA commerce;\nCREATE TABLE commerce.orders (order_id bigint PRIMARY KEY, "status" text);\nSET search_path = commerce, pg_catalog;\nSELECT o.order_id FROM commerce.orders AS o;\n-- Unquoted Order_ID resolves as order_id; quoted spelling is exact.' },
        { id: dialectIds[1], label: 'MySQL', language: 'sql', code: 'CREATE DATABASE commerce;\nCREATE TABLE commerce.orders (order_id bigint PRIMARY KEY, `status` varchar(30));\nUSE commerce;\nSELECT o.order_id FROM commerce.orders AS o;\n-- Table-name case depends on platform/lower_case_table_names.' },
        { id: dialectIds[2], label: 'SQLite', language: 'sql', code: "ATTACH DATABASE 'commerce.db' AS commerce;\nCREATE TABLE commerce.orders (order_id INTEGER PRIMARY KEY, \"status\" TEXT);\nSELECT o.order_id FROM commerce.orders AS o;\n-- commerce is an attached-database name; SQLite has no schema layer." },
        { id: dialectIds[3], label: 'SQL Server', language: 'sql', code: 'CREATE SCHEMA commerce;\nGO\nCREATE TABLE commerce.orders (order_id bigint PRIMARY KEY, [status] varchar(30));\nSELECT o.order_id FROM commerce.orders AS o;\n-- Qualify database.schema.object when crossing databases; case follows collation.' },
        { id: dialectIds[4], label: 'Oracle', language: 'sql', code: 'CREATE TABLE commerce.orders (order_id NUMBER(19) PRIMARY KEY, "status" VARCHAR2(30));\nSELECT o.order_id FROM commerce.orders o;\n-- Schema normally names an owning user; nonquoted ORDER_ID is treated as uppercase.' },
      ] },
      { kind: 'table', caption: 'Resolution distinctions', headers: ['Dialect', 'Delimited identifier', 'Ordinary-name behavior', 'Qualification model'], rows: [['PostgreSQL', '"name"', 'unquoted folds lowercase', 'database → schema; `search_path` for unqualified objects'], ['MySQL', '`name` (`"name"` only with ANSI_QUOTES)', 'object-specific; table/database case is configured/platform-sensitive', 'server → database → object'], ['SQLite', '"name" (also compatibility forms)', 'ASCII case-insensitive lookup in normal use', 'connection → attached database → object'], ['SQL Server', '[name] or "name" with QUOTED_IDENTIFIER', 'identifier collation determines comparison', 'server → database → schema → object'], ['Oracle', '"name"', 'nonquoted names uppercase', 'database/container → schema(owner) → object; synonyms may participate']] },
      { kind: 'pitfall', title: 'Projecting `search_path` onto every product', text: 'Other engines have different namespace and default-schema mechanisms. Model each resolver explicitly; a generic “schema path” setting can conceal which principal owns or can shadow an object.' },
      { kind: 'bestPractice', title: 'Test names through the driver and catalog', text: 'Create, migrate, query, and introspect representative names on every supported platform/configuration. Assert the exact catalog name and qualification emitted by migration and ORM tooling.' },
    ],
    refs: [{ book: 'postgresql-docs', chapter: '4.1.1 — Identifiers and Key Words; 5.10.3 — The Schema Search Path' }, { book: 'mysql-docs', chapter: '11.2 — Schema Object Names; 11.2.3 — Identifier Case Sensitivity' }, { book: 'sqlite-docs', chapter: 'SQL Language Keywords; Database Object Name Resolution' }, { book: 'sqlserver-docs', chapter: 'Database Identifiers; Multipart Names' }, { book: 'oracle-docs', chapter: 'Database Object Names and Qualifiers' }],
    related: ['sql-portability-strategy', 'roles-privileges-and-least-authority', 'sql-injection-and-query-safety'],
  },
  {
    id: 'type-and-generated-value-differences', domainId: 'db-dialects', title: 'Type and Generated-Value Differences',
    summary: 'Type names are not semantic guarantees: range, precision, time-zone handling, coercion, storage, comparison, and driver conversion differ. Define application domains and round-trip tests first, then map each dialect explicitly; generated values still require uniqueness constraints and safe retrieval.',
    keyPoints: [
      { text: 'Map domains, not spellings.', detail: '`DECIMAL`, timestamps, booleans, JSON, UUIDs, and large values can share a name while differing in range, normalization, indexing, or returned driver type.' },
      { text: 'Exact numbers need an overflow policy.', detail: 'Specify precision/scale, rounding point and mode, maximum intermediate values, and client representation. Never substitute binary floating point for money merely because a driver makes it convenient.' },
      { text: 'Separate instants from civil time.', detail: 'Persist an instant with a defined UTC conversion and retain zone ID separately when future civil rules matter. Verify daylight-saving gaps/overlaps and driver/session time zones.' },
      { text: 'Generated values are allocation, not identity.', detail: 'Sequences, identity, auto-increment, and rowid mechanisms can leave gaps and may be nontransactional. Add PK/unique constraints and never infer insertion order from generated numbers.' },
      { text: 'Retrieval must be statement- and scope-safe.', detail: 'Prefer a statement result (`RETURNING`/`OUTPUT`) or the driver generated-keys API with documented multirow ordering. Connection/global identity functions and follow-up max queries are race-prone.' },
      { text: 'Round-trip at boundaries.', detail: 'Test min/max, scale, Unicode, zero bytes, large payloads, nulls, timestamps around DST, JSON numeric fidelity, UUID byte/text order, and migration conversions through the production driver.' },
    ],
    blocks: [
      { kind: 'code', title: 'Generated integer key and safe insert result', variants: [
        { id: dialectIds[0], label: 'PostgreSQL', language: 'sql', code: "CREATE TABLE customers (customer_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY, email varchar(320) NOT NULL UNIQUE);\nINSERT INTO customers (email) VALUES ('ada@example.test') RETURNING customer_id;" },
        { id: dialectIds[1], label: 'MySQL', language: 'sql', code: "CREATE TABLE customers (customer_id bigint NOT NULL AUTO_INCREMENT PRIMARY KEY, email varchar(320) NOT NULL UNIQUE) ENGINE=InnoDB;\nINSERT INTO customers (email) VALUES ('ada@example.test');\n-- Retrieve via the same connection's driver generated-keys API/LAST_INSERT_ID()." },
        { id: dialectIds[2], label: 'SQLite', language: 'sql', code: "CREATE TABLE customers (customer_id INTEGER PRIMARY KEY, email TEXT NOT NULL UNIQUE);\nINSERT INTO customers (email) VALUES ('ada@example.test') RETURNING customer_id;\n-- INTEGER PRIMARY KEY aliases rowid; AUTOINCREMENT is usually unnecessary." },
        { id: dialectIds[3], label: 'SQL Server', language: 'sql', code: "CREATE TABLE customers (customer_id bigint IDENTITY(1,1) PRIMARY KEY, email varchar(320) NOT NULL UNIQUE);\nINSERT INTO customers (email) OUTPUT inserted.customer_id VALUES ('ada@example.test');" },
        { id: dialectIds[4], label: 'Oracle', language: 'sql', code: "CREATE TABLE customers (customer_id NUMBER(19) GENERATED ALWAYS AS IDENTITY PRIMARY KEY, email VARCHAR2(320) NOT NULL UNIQUE);\nINSERT INTO customers (email) VALUES ('ada@example.test') RETURNING customer_id INTO :customer_id;" },
      ] },
      { kind: 'table', caption: 'Type-mapping questions (verify chosen versions and drivers)', headers: ['Domain', 'PostgreSQL', 'MySQL', 'SQLite', 'SQL Server', 'Oracle'], rows: [['Text', 'varchar/text, database encoding', 'varchar/text + charset/collation', 'TEXT, dynamic typing/affinity', 'varchar or nvarchar', 'VARCHAR2/NVARCHAR2/CLOB; empty string becomes null'], ['Exact number', 'numeric(p,s)', 'decimal(p,s)', 'NUMERIC affinity; representation can vary', 'decimal(p,s)', 'NUMBER(p,s)'], ['Instant', 'timestamp with time zone normalizes instant', 'timestamp has UTC/session conversion and limited range', 'no dedicated storage class; convention required', 'datetimeoffset', 'TIMESTAMP WITH TIME ZONE'], ['Boolean', 'boolean', 'BOOLEAN synonym maps to integer type', '0/1 convention; TRUE/FALSE literals', 'bit', 'BOOLEAN in 26ai; qualify older-version mapping'], ['Binary/large', 'bytea/large objects', 'binary/blob families', 'BLOB', 'varbinary(max)', 'RAW/BLOB'], ['JSON', 'json/jsonb', 'JSON type', 'text/blob plus JSON functions', 'JSON representation/features depend version', 'JSON type/features depend version'], ['UUID', 'uuid', 'binary(16) or char mapping', 'BLOB(16)/TEXT convention', 'uniqueidentifier', 'RAW(16) or character mapping']] },
      { kind: 'note', title: 'SQLite type affinity is not a five-engine type system', text: 'Except for STRICT tables and special cases, SQLite associates storage classes with values and applies column affinity. A declaration that looks like another engine’s type does not import its range or validation semantics.' },
      { kind: 'pitfall', title: 'Assuming a generated number cannot be skipped', text: 'Rollbacks, cache allocation, failed inserts, conflict paths, and restarts can consume values. Use it as an opaque key, not a gap-free invoice or event sequence.' },
      { kind: 'bestPractice', title: 'Publish a type compatibility matrix', text: 'For every field record the semantic domain, database declaration, bind/result API type, normalization, bounds, indexes, export representation, and destructive conversion policy. Execute boundary fixtures during every driver/server upgrade.' },
    ],
    refs: [{ book: 'postgresql-docs', chapter: '5.3 — Identity Columns; Ch. 8 — Data Types' }, { book: 'mysql-docs', chapter: '13 — Data Types; 15.6.1.6 — AUTO_INCREMENT Handling in InnoDB' }, { book: 'sqlite-docs', chapter: 'Datatypes In SQLite; SQLite Autoincrement' }, { book: 'sqlserver-docs', chapter: 'Data types (Transact-SQL); CREATE TABLE IDENTITY' }, { book: 'oracle-docs', chapter: 'Data Types; identity_clause' }],
    related: ['numeric-types-and-precision', 'dates-times-and-time-zones', 'boolean-enum-domain-and-uuid-types'],
  },
  {
    id: 'pagination-upsert-and-returning-differences', domainId: 'db-dialects', title: 'Pagination, Upsert, and Returning Differences',
    summary: 'Pagination, conflict-aware writes, and modified-row retrieval differ in both syntax and concurrency contract. Require a unique stable order, identify the exact uniqueness arbiter and state transition, and consume statement-produced results rather than reconstructing them afterward.',
    keyPoints: [
      { text: 'Pagination without a total order is undefined.', detail: 'Add a unique tiebreaker and keep direction/null placement explicit. Offset pagination can repeat or omit rows under concurrent changes and grows more expensive with deep offsets.' },
      { text: 'Keyset predicates encode the order.', detail: 'The cursor must carry every ordering key and the predicate must expand lexicographically with matching direction, collation, null policy, and snapshot/freshness contract.' },
      { text: 'Upsert is not one portable operation.', detail: 'PostgreSQL/SQLite conflict clauses select uniqueness arbiters; MySQL reacts to any violated unique key; SQL Server and Oracle expose `MERGE` with different restrictions and histories. Define whether insert, update, no-op, or conflict is valid.' },
      { text: 'Concurrency semantics need a constraint.', detail: 'A prior existence check races. Let a unique constraint arbitrate and use guarded updates/version predicates; test simultaneous insert and update cases on every engine.' },
      { text: 'Affected rows are not a universal outcome API.', detail: 'Drivers and products differ on whether matched-but-unchanged, insert-via-upsert, and triggers count as affected. Return or query an explicit business state when branching depends on it.' },
      { text: 'Return values in the modifying statement.', detail: 'PostgreSQL and SQLite use `RETURNING`, SQL Server uses `OUTPUT`, Oracle supports `RETURNING INTO`, while MySQL 8.4 uses generated-key/identity mechanisms for inserts and has no general DML `RETURNING` clause.' },
    ],
    blocks: [
      { kind: 'code', title: 'Stable first page', variants: [
        { id: dialectIds[0], label: 'PostgreSQL', language: 'sql', code: 'SELECT order_id, placed_at, status FROM orders\nORDER BY placed_at DESC, order_id DESC LIMIT 20;' },
        { id: dialectIds[1], label: 'MySQL', language: 'sql', code: 'SELECT order_id, placed_at, status FROM orders\nORDER BY placed_at DESC, order_id DESC LIMIT 20;' },
        { id: dialectIds[2], label: 'SQLite', language: 'sql', code: 'SELECT order_id, placed_at, status FROM orders\nORDER BY placed_at DESC, order_id DESC LIMIT 20;' },
        { id: dialectIds[3], label: 'SQL Server', language: 'sql', code: 'SELECT order_id, placed_at, status FROM orders\nORDER BY placed_at DESC, order_id DESC\nOFFSET 0 ROWS FETCH NEXT 20 ROWS ONLY;' },
        { id: dialectIds[4], label: 'Oracle', language: 'sql', code: 'SELECT order_id, placed_at, status FROM orders\nORDER BY placed_at DESC, order_id DESC\nFETCH FIRST 20 ROWS ONLY;' },
      ] },
      { kind: 'code', title: 'Conflict-aware customer write and result path', variants: [
        { id: dialectIds[0], label: 'PostgreSQL', language: 'sql', code: "INSERT INTO customers (email, display_name) VALUES (:email, :name)\nON CONFLICT (email) DO UPDATE SET display_name = EXCLUDED.display_name\nRETURNING customer_id, email, display_name;" },
        { id: dialectIds[1], label: 'MySQL', language: 'sql', code: "INSERT INTO customers (email, display_name) VALUES (:email, :name) AS incoming\nON DUPLICATE KEY UPDATE display_name = incoming.display_name;\n-- Re-read by the declared UNIQUE email in this transaction when the full row is required; affected-row counts vary by path/client flag." },
        { id: dialectIds[2], label: 'SQLite', language: 'sql', code: "INSERT INTO customers (email, display_name) VALUES (:email, :name)\nON CONFLICT (email) DO UPDATE SET display_name = excluded.display_name\nRETURNING customer_id, email, display_name;" },
        { id: dialectIds[3], label: 'SQL Server', language: 'sql', code: "-- Prefer an explicit transaction plus guarded UPDATE/INSERT for critical races; MERGE needs product-specific concurrency testing.\nMERGE customers WITH (HOLDLOCK) AS t\nUSING (VALUES (:email, :name)) AS s(email, display_name) ON t.email = s.email\nWHEN MATCHED THEN UPDATE SET display_name = s.display_name\nWHEN NOT MATCHED THEN INSERT (email, display_name) VALUES (s.email, s.display_name)\nOUTPUT inserted.customer_id, inserted.email, inserted.display_name;" },
        { id: dialectIds[4], label: 'Oracle', language: 'sql', code: "MERGE INTO customers t USING (SELECT :email email, :name display_name FROM dual) s\nON (t.email = s.email)\nWHEN MATCHED THEN UPDATE SET t.display_name = s.display_name\nWHEN NOT MATCHED THEN INSERT (email, display_name) VALUES (s.email, s.display_name);\n-- MERGE has no RETURNING clause; select by the unique email when the row is needed." },
      ] },
      { kind: 'table', caption: 'Outcome contract to test', headers: ['Case', 'Required assertion'], rows: [['two sessions insert same email', 'one logical customer; loser follows declared conflict path'], ['existing value unchanged', 'business result independent of affected-row convention'], ['several unique constraints conflict', 'declared arbiter or explicit rejection; no arbitrary target'], ['trigger/generated column changes row', 'returned/re-read result reflects stored row'], ['page boundary receives new row', 'document snapshot or freshness behavior; no claim of offset stability'], ['cursor has duplicate sort key', 'unique tiebreaker prevents omission']] },
      { kind: 'pitfall', title: 'Emulating upsert with SELECT then INSERT', text: 'Both sessions can observe absence. Without a unique constraint and conflict protocol this is a race; with them, the preliminary read adds latency but does not remove the need to handle conflict.' },
      { kind: 'bestPractice', title: 'Name the business transition', text: 'Instead of a generic save(), define operations such as create-customer-if-absent or rename-customer-if-version. State the uniqueness arbiter, permitted update, returned state, isolation, and retry behavior per adapter.' },
    ],
    refs: [{ book: 'postgresql-docs', chapter: 'INSERT; 6.4 — Returning Data from Modified Rows' }, { book: 'mysql-docs', chapter: '15.2.7.2 — INSERT ... ON DUPLICATE KEY UPDATE Statement' }, { book: 'sqlite-docs', chapter: 'UPSERT; RETURNING' }, { book: 'sqlserver-docs', chapter: 'MERGE (Transact-SQL); OUTPUT Clause (Transact-SQL)' }, { book: 'oracle-docs', chapter: 'MERGE; DML returning_clause' }],
    related: ['ordering-and-pagination', 'merge-upsert-and-returning', 'application-concurrency-and-transaction-boundaries'],
  },
  {
    id: 'null-boolean-collation-and-expression-differences', domainId: 'db-dialects', title: 'NULL, Boolean, Collation, and Expression Differences',
    summary: 'Expression portability fails at semantic edges: null propagation, empty strings, boolean storage, implicit coercion, uniqueness, and locale-aware comparison can change rows and order. State these rules in schema/query contracts and test with adversarial values under the deployed configuration.',
    keyPoints: [
      { text: 'SQL null uses three-valued logic, but extensions differ.', detail: 'Use `IS NULL`, explicit null-safe comparisons where supported, and deliberate `COALESCE`; never translate unknown into false without checking the business meaning.' },
      { text: 'Concatenation is not uniform.', detail: 'PostgreSQL/SQLite/Oracle `||` and SQL Server `+` have product/configuration rules for null; MySQL normally uses `CONCAT` because `||` is logical OR unless a SQL mode changes it. Express the desired null policy explicitly.' },
      { text: 'Oracle collapses zero-length character values to null.', detail: 'This differs from the other four and affects required fields, concatenation, equality, length, uniqueness, and round trips. Do not promise empty-string preservation across Oracle.' },
      { text: 'Boolean domains vary.', detail: 'PostgreSQL has boolean; MySQL BOOLEAN is a synonym for a small integer type; SQLite uses integer convention; SQL Server uses bit; current Oracle 26ai has BOOLEAN but older supported releases need another mapping.' },
      { text: 'Unique plus null is vendor-specific.', detail: 'Multiple null-containing keys and composite-null behavior differ. If the business means “at most one absent value,” model and test it explicitly rather than relying on a generic UNIQUE declaration.' },
      { text: 'Collation is data behavior, not decoration.', detail: 'Case/accent/kana sensitivity, Unicode/locale versions, normalization, coercibility, and default inheritance affect equality, uniqueness, joins, LIKE, grouping, and sort order. Add a stable tiebreaker even after a collation key.' },
    ],
    blocks: [
      { kind: 'table', caption: 'High-risk semantic edges', headers: ['Concern', 'PostgreSQL', 'MySQL', 'SQLite', 'SQL Server', 'Oracle'], rows: [['Empty string', 'distinct from null', 'distinct from null', 'distinct from null', 'distinct from null', 'currently treated as null for character values'], ['Boolean', 'native boolean', 'BOOLEAN synonym for TINYINT(1)', '0/1 convention', 'bit', 'native BOOLEAN in 26ai; version-gate'], ['Concatenation', '`||`; null operand yields null', '`CONCAT`; null argument yields null (`||` mode-sensitive)', '`||`; null operand yields null', '`+`; CONCAT_NULL_YIELDS_NULL is always ON from SQL Server 2017 onward', '`||`; empty string/null behavior is Oracle-specific'], ['Null-safe equality', '`IS NOT DISTINCT FROM`', '`<=>`', '`IS` / `IS NOT`', '`IS [NOT] DISTINCT FROM` in SQL Server 2022+', 'explicit equality-or-both-null predicate'], ['UNIQUE nullable key', 'multiple nulls by default; `NULLS NOT DISTINCT` available', 'multiple nulls generally permitted', 'distinct nulls permitted', 'single-column unique index normally permits one null', 'multiple nulls generally permitted; empty strings become null']] },
      { kind: 'code', title: 'Explicit null-safe email comparison', variants: [
        { id: dialectIds[0], label: 'PostgreSQL', language: 'sql', code: 'SELECT customer_id FROM customers WHERE email IS NOT DISTINCT FROM :email;' },
        { id: dialectIds[1], label: 'MySQL', language: 'sql', code: 'SELECT customer_id FROM customers WHERE email <=> :email;' },
        { id: dialectIds[2], label: 'SQLite', language: 'sql', code: 'SELECT customer_id FROM customers WHERE email IS :email;' },
        { id: dialectIds[3], label: 'SQL Server', language: 'sql', code: 'SELECT customer_id FROM customers WHERE email IS NOT DISTINCT FROM :email;\n-- SQL Server 2022+; use an explicit equality/NULL disjunction on older versions.' },
        { id: dialectIds[4], label: 'Oracle', language: 'sql', code: 'SELECT customer_id FROM customers\nWHERE email = :email OR (email IS NULL AND :email IS NULL);\n-- A bound empty character string is currently treated as NULL.' },
      ] },
      { kind: 'code', language: 'sql', title: 'Portable explicit null ordering', code: 'SELECT order_id, shipped_at\nFROM orders\nORDER BY CASE WHEN shipped_at IS NULL THEN 1 ELSE 0 END,\n         shipped_at ASC,\n         order_id ASC;\n-- The unique order_id tiebreaker makes pagination deterministic.\n-- Verify expression indexes/plans if this is performance critical.' },
      { kind: 'pitfall', title: 'Lowercasing both sides as universal case-insensitivity', text: 'Case conversion is locale- and Unicode-sensitive, may change index use, and does not define accent or normalization behavior. Choose a documented collation or normalized search key with explicit versioning and collision tests.' },
      { kind: 'bestPractice', title: 'Maintain a semantic edge corpus', text: 'Test null, empty, whitespace, composed/decomposed Unicode, case pairs, accents, supplementary characters, booleans outside 0/1, numeric-looking text, and collation ties through constraints, joins, ordering, export, and upgrades.' },
    ],
    refs: [{ book: 'postgresql-docs', chapter: '9.2 — Comparison Functions and Operators; 23.2 — Collation Support' }, { book: 'mysql-docs', chapter: '11.1.6 — Boolean Literals; 12.8 — Collation Issues' }, { book: 'sqlite-docs', chapter: 'Datatypes In SQLite; NULL Handling in SQLite' }, { book: 'sqlserver-docs', chapter: 'NULL and UNKNOWN (Transact-SQL); Collation and Unicode support' }, { book: 'oracle-docs', chapter: 'Nulls; Data Type Comparison Rules; Collation' }],
    related: ['nulls-and-three-valued-logic', 'text-types-collations-and-encoding', 'primary-unique-and-check-constraints'],
  },
  {
    id: 'transaction-and-ddl-differences', domainId: 'db-dialects', title: 'Transaction and DDL Differences',
    summary: 'Transaction labels do not guarantee identical anomalies, lock behavior, or failure handling, and DDL ranges from transactional to implicitly committed. Specify observable invariants, map them to each engine/version, retry complete units, and treat every migration as a dialect-specific operational program.',
    keyPoints: [
      { text: 'Isolation names are not equivalence proofs.', detail: 'Defaults differ—PostgreSQL READ COMMITTED, MySQL InnoDB REPEATABLE READ, SQLite SERIALIZABLE except shared-cache read-uncommitted mode, SQL Server READ COMMITTED, Oracle READ COMMITTED—and implementation guarantees differ within names.' },
      { text: 'Savepoints limit rollback, not isolation.', detail: 'They do not release every kind of resource uniformly or make external effects atomic. After a statement error, the legal recovery sequence is product/driver specific.' },
      { text: 'Deadlocks and serialization failures are expected outcomes.', detail: 'Keep transactions short and ordered, classify vendor error codes, roll back, reacquire state, and retry the complete idempotent unit with bounds and jitter.' },
      { text: 'Locks differ in granularity and lifecycle.', detail: 'MVCC readers, writer serialization, escalation, gap/key-range/predicate locks, lock hints, and DDL metadata locks vary. Verify the actual invariant under concurrent schedules.' },
      { text: 'DDL atomicity is a migration property.', detail: 'PostgreSQL and SQL Server support much transactional DDL with exceptions; SQLite schema changes can participate in explicit transactions; MySQL and Oracle implicitly commit around many DDL statements. Online/instant labels do not imply rollback or no blocking.' },
      { text: 'Deferred constraints are not uniformly available.', detail: 'PostgreSQL and Oracle support deferrable constraints in defined cases; SQLite foreign keys can be deferred when enforcement is enabled; MySQL and SQL Server generally require different sequencing. Never emit `SET CONSTRAINTS` unconditionally.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Transaction and migration comparison', headers: ['Dialect', 'Documented default isolation', 'DDL transaction boundary', 'Migration consequence'], rows: [['PostgreSQL', 'READ COMMITTED', 'most catalog DDL rolls back; commands such as CREATE DATABASE and concurrent index operations have restrictions', 'group compatible steps; isolate nontransactional commands'], ['MySQL/InnoDB', 'REPEATABLE READ', 'many DDL statements implicitly commit; atomic DDL is crash consistency, not user rollback', 'checkpoint and compensate stepwise; inspect algorithm/lock'], ['SQLite', 'SERIALIZABLE by serializing writes (with documented exceptions/modes)', 'schema changes participate in transactions; ALTER surface is deliberately limited', 'use documented table-rebuild procedure and test file/version'], ['SQL Server', 'READ COMMITTED', 'many DDL statements can roll back in an explicit transaction, with documented exceptions and lock/log costs', 'test transactionability, blocking, and recovery per statement'], ['Oracle', 'READ COMMITTED', 'implicit commit before and after DDL', 'assume each DDL is a committed boundary; design restartable stages']] },
      { kind: 'code', title: 'Begin a deliberate transaction', variants: [
        { id: dialectIds[0], label: 'PostgreSQL', language: 'sql', code: 'BEGIN TRANSACTION ISOLATION LEVEL SERIALIZABLE;\n-- read invariant, write guarded state\nCOMMIT; -- retry whole transaction on SQLSTATE 40001 or deadlock 40P01' },
        { id: dialectIds[1], label: 'MySQL', language: 'sql', code: 'SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;\nSTART TRANSACTION;\n-- InnoDB transactional tables only; classify deadlock/timeout separately\nCOMMIT;' },
        { id: dialectIds[2], label: 'SQLite', language: 'sql', code: 'BEGIN IMMEDIATE;\n-- reserves the write transaction early; busy handling needs a bounded policy\nCOMMIT;' },
        { id: dialectIds[3], label: 'SQL Server', language: 'sql', code: 'SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;\nBEGIN TRANSACTION;\n-- keep TRY/CATCH + XACT_STATE-aware rollback in application/procedure\nCOMMIT TRANSACTION;' },
        { id: dialectIds[4], label: 'Oracle', language: 'sql', code: 'SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;\n-- transaction begins with first statement; no BEGIN TRANSACTION SQL statement\n-- read invariant, write guarded state\nCOMMIT;' },
      ] },
      { kind: 'code', language: 'text', title: 'Migration atomicity checklist', code: 'For every step and supported version record:\n- implicit commit / may-run-in-transaction / must-run-alone\n- metadata and data locks; reader/writer blocking\n- rewrite, log, temporary-space, replica and backup impact\n- online/instant/concurrent prerequisites and fallback\n- failure state and idempotent resume/compensation\n- constraint-validation timing and deferred support\n- rehearsal duration on production-shaped data\n\nNever wrap a script in BEGIN and infer that it became atomic.' },
      { kind: 'pitfall', title: 'Equating “atomic DDL” with rollbackable migrations', text: 'A vendor may mean crash-safe dictionary/storage changes while still committing the user transaction. Read the precise product definition and inject failure between migration steps.' },
      { kind: 'bestPractice', title: 'Test histories, not level names', text: 'Use two or more real connections to exercise write skew, lost update, phantoms/ranges, deadlocks, savepoint recovery, DDL blocking, and retry. Assert the business invariant and exact error/outcome contract.' },
    ],
    refs: [{ book: 'postgresql-docs', chapter: '13.2 — Transaction Isolation; 3.4 — Transactions' }, { book: 'mysql-docs', chapter: '17.7.2.1 — Transaction Isolation Levels; 15.3.3 — Statements That Cause an Implicit Commit' }, { book: 'sqlite-docs', chapter: 'Isolation In SQLite; Transaction' }, { book: 'sqlserver-docs', chapter: 'SET TRANSACTION ISOLATION LEVEL (Transact-SQL); Transactions (Database Engine)' }, { book: 'oracle-docs', chapter: 'Data Concurrency and Consistency; Types of SQL Statements' }],
    related: ['isolation-levels-and-anomalies', 'deadlocks-contention-and-retries', 'schema-migrations-and-database-testing'],
  },
  {
    id: 'choosing-a-relational-database', domainId: 'db-dialects', title: 'Choosing a Relational Database',
    summary: 'Database selection is an evidence-backed fit decision under explicit assumptions. Start with correctness and workload, then test operability, recovery, ecosystem, deployment constraints, staffing, lifecycle cost, and acceptable specialization instead of applying a universal product ranking.',
    keyPoints: [
      { text: 'Correctness requirements are gates.', detail: 'Model invariants, transaction histories, type/collation needs, durability, recovery objectives, and required query features. A product that cannot meet them in the intended deployment mode does not advance on convenience.' },
      { text: 'Use representative workload shapes.', detail: 'Capture reads/writes, joins and aggregates, hot keys, skew, working set, concurrency, growth, batch/analytics overlap, latency objectives, and failure conditions—not a synthetic single-row benchmark.' },
      { text: 'The deployment is part of the product.', detail: 'Self-managed, embedded, appliance, and managed offerings differ in versions, extensions, privileges, replication, backup control, observability, maintenance windows, limits, and exit paths.' },
      { text: 'Operability must be demonstrated.', detail: 'Measure schema changes, backup/restore, PITR, failover, replica lag, upgrades, capacity response, incident evidence, and automation under the team’s actual access model.' },
      { text: 'Ecosystem includes people.', detail: 'Evaluate drivers, migration/observability tools, vendor/community support, security response, hiring and training, on-call experience, and ownership boundaries.' },
      { text: 'Cost is lifecycle risk-adjusted cost.', detail: 'Include licenses/service tiers, compute/storage/I/O/network/backups, standby capacity, engineering/on-call time, migration, outage exposure, growth, and lock-in—not only the first monthly estimate.' },
      { text: 'Revisit the decision.', detail: 'Record assumptions, disqualifiers, weights, measurements, uncertainties, and expiry triggers. Changes in workload, regulation, team, offering, or product version can alter the answer.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Weighted scorecard—weights and gates come from the project', headers: ['Dimension', 'Evidence to collect', 'Gate or scored question'], rows: [['Correctness', 'invariant/concurrency/type tests', 'all required histories preserved?'], ['Query/workload', 'production-shaped plans and p95/p99', 'objectives at peak plus headroom?'], ['Evolution', 'large-table migration rehearsals', 'blocking/failure/resume acceptable?'], ['Recovery/reliability', 'restore, PITR, failover drills', 'measured RPO/RTO and data loss?'], ['Observability/security', 'least privilege, audit, waits/plans', 'operators can detect and diagnose safely?'], ['Deployment/lifecycle', 'upgrade, patch, topology, limits', 'fits platform and ownership model?'], ['Ecosystem/skills', 'driver/tool support and staffing', 'supportable on call for years?'], ['Economics/exit', '3–5 year scenarios and export', 'risk-adjusted cost and exit acceptable?']] },
      { kind: 'diagram', title: 'Selection protocol', code: 'flowchart LR\n  R["requirements + assumptions"] --> G{"correctness / compliance gates"}\n  G -->|fail| X["reject or redesign"]\n  G -->|pass| P["representative proof of concept"]\n  P --> F["failure, recovery, migration, operations drills"]\n  F --> S["weighted score + uncertainty"]\n  S --> D["decision record + specialization ledger"]\n  D --> E["expiry triggers and reassessment"]' },
      { kind: 'code', language: 'text', title: 'Proof-of-concept protocol', code: '1. Load production-shaped volume, skew, Unicode/time/null edge corpus.\n2. Enforce canonical commerce constraints and run concurrent checkout/inventory histories.\n3. Run critical joins, search, pagination, reports, imports, and maintenance together.\n4. Capture plans, waits, lock chains, resource curves, tail latency, and cost drivers.\n5. Exercise required extensions and driver/ORM/migration tooling.\n6. Rehearse expand-contract migration, backup restore, PITR, replica lag, failover, and upgrade.\n7. Test least privilege, audit evidence, observability, deployment automation, and support escalation.\n8. Export schema/data to the stated exit format and estimate conversion loss/time.\n9. Publish raw results, assumptions, uncertainty, owners, and rejection criteria.' },
      { kind: 'note', title: 'Five products represent different deployment shapes', text: 'PostgreSQL, MySQL, SQL Server, and Oracle commonly serve client/server workloads with broad but different ecosystems; SQLite is an embedded library with a single-file database and serialized writer model. Compare only deployments that can satisfy the application boundary—do not award or subtract points for irrelevant features.' },
      { kind: 'pitfall', title: 'Choosing from feature checklists or popularity', text: 'A checked feature can differ in semantics, limits, managed-service availability, and operational maturity. Popularity can improve staffing and tooling but does not prove correctness or workload fit.' },
      { kind: 'bestPractice', title: 'Make the decision falsifiable', text: 'Write measurable acceptance thresholds, disqualifiers, confidence levels, and conditions that trigger reassessment. Keep benchmark harnesses and recovery/migration runbooks so later versions can be compared with the same evidence.' },
    ],
    refs: [{ book: 'ddia-2', chapter: 'Ch. 2 — Defining Nonfunctional Requirements; Ch. 3 — Data Models and Query Languages' }, { book: 'database-reliability-engineering', chapter: 'Ch. 2 — Service-Level Management; Ch. 3 — Risk Management' }, { book: 'database-system-concepts', chapter: 'Ch. 20 — Database-System Architectures' }, { book: 'sql-antipatterns', chapter: 'Ch. 25 — Magic Beans' }],
    related: ['sql-portability-strategy', 'database-observability', 'backup-restore-and-recovery-objectives'],
  },
]

import type { Topic } from '../../../types/content'

/** PostgreSQL 18 is the behavioral baseline; older internals sources explain stable mechanisms only. */
export const dbPostgresqlTopics: Topic[] = [
  {
    id: 'postgresql-architecture', domainId: 'db-postgresql', title: 'PostgreSQL Architecture',
    summary: 'PostgreSQL 18 uses a process-per-connection server around shared memory, durable relation files, catalogs, and WAL. That architecture makes session count, cache behavior, checkpoints, and background maintenance application-visible operational concerns.',
    keyPoints: [
      { text: 'The postmaster accepts connections and supervises child processes.', detail: 'A dedicated backend executes each admitted session; pooling controls the memory, process, catalog-cache, and scheduling cost of that model.' },
      { text: 'Shared buffers mediate database-page access.', detail: 'Backends pin and lock shared buffers while PostgreSQL and the operating-system page cache cooperate; a cache hit avoids a PostgreSQL read, not necessarily physical storage.' },
      { text: 'Catalogs are ordinary, transactionally visible relations.', detail: 'Parsing, name resolution, privileges, type lookup, and planning consult catalog data cached by each backend and invalidated across processes.' },
      { text: 'Relation forks separate concerns.', detail: 'Heap and index relations use numbered segment files plus main, free-space-map, visibility-map, and initialization forks where applicable; filenames are not a supported data API.' },
      { text: 'WAL decouples commit durability from heap-page flushing.', detail: 'WAL must reach durable storage before a changed page; checkpointer and background writer spread data-file writes while WAL writer advances log durability.' },
      { text: 'Auxiliary processes protect liveness and operability.', detail: 'Autovacuum, checkpointer, WAL writer, archiver, startup, replication, and logical workers have distinct responsibilities and observable failure modes.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Connection, cache, storage, and durability flow', code: 'flowchart LR\n  C["client / pool"] --> P["postmaster"]\n  P --> B["client backend process"]\n  P --> AV["autovacuum worker processes"]\n  B --> CAT["catalog + backend-local caches"]\n  AV --> CAT\n  B --> SB["shared buffers"]\n  AV --> SB\n  SB <--> OS["OS cache / relation files"]\n  B --> WB["WAL buffers"] --> WAL["WAL files"]\n  AV --> WB\n  CP["checkpointer + background writer"] --> SB\n  WAL --> AR["archive / replicas"]' },
      { kind: 'table', caption: 'Layer and consequence', headers: ['Layer', 'Responsibility', 'Operational consequence'], rows: [['Backend', 'parse, plan, execute, session state', 'pool sessions; avoid unbounded connection admission'], ['Shared buffers', 'shared page cache and dirty state', 'inspect workload and I/O before changing size'], ['Relation files/forks', 'heap, index, FSM, VM, unlogged initialization', 'use SQL/catalog tools, never manipulate files'], ['WAL/checkpoint', 'durability and restart frontier', 'observe WAL rate, checkpoint I/O, flush latency'], ['Workers', 'vacuum, archive, replication, parallel work', 'reserve capacity and alert on stalled work']] },
      { kind: 'code', language: 'sql', title: 'Inspect the running PostgreSQL 18 instance', code: "SELECT version();\nSELECT name, setting, unit, source, sourcefile\nFROM pg_settings\nWHERE name IN ('shared_buffers','max_connections','checkpoint_timeout');\n\nSELECT backend_type, count(*)\nFROM pg_stat_activity\nGROUP BY backend_type ORDER BY backend_type;" },
      { kind: 'pitfall', title: 'Treating every cache miss as a disk read', text: 'PostgreSQL buffer statistics and OS/device statistics describe different layers. Correlate buffer hits/reads with I/O timing, waits, device latency, and working-set changes.' },
      { kind: 'bestPractice', title: 'Control admission before tuning memory', text: 'Bound active database work in the application pool, budget per-backend and per-query memory, preserve worker headroom, and load-test connection churn and failover reconnects.' },
    ],
    refs: [{ book: 'postgresql-internals', chapter: 'Ch. 1 — Introduction; §1.1 — Data Organization; §1.2 — Processes and Memory; Ch. 9 — Buffer Cache; Ch. 10 — Write-Ahead Log' }, { book: 'postgresql-docs', chapter: 'Ch. 18 — Server Setup and Operation; 27.2 — The Cumulative Statistics System; Ch. 66 — Database Physical Storage' }, { book: 'database-internals', chapter: 'Ch. 3 — File Formats; Ch. 6 — B-Tree Variants' }],
    related: ['database-system-architecture', 'buffer-pools-and-caching', 'connection-pooling'],
  },
  {
    id: 'postgresql-types-jsonb-and-extensions', domainId: 'db-postgresql', title: 'PostgreSQL Types, JSONB, and Extensions',
    summary: 'PostgreSQL’s rich type and extension systems can encode domain meaning and unlock specialized operators, but every choice also fixes comparison, indexing, portability, privilege, upgrade, and recovery behavior.',
    keyPoints: [
      { text: 'Choose the narrowest semantic type, not the shortest syntax.', detail: 'UUID, inet/cidr, numeric, timestamptz, ranges, arrays, domains, and enums carry different validation, ordering, operator, and evolution contracts.' },
      { text: 'JSONB is queryable binary structure, not a schema exemption.', detail: 'Use it for sparse or evolving attributes; keep identities, relationships, money, and invariants relational when constraints and joins matter.' },
      { text: 'Operator classes connect operators to indexes.', detail: 'A type being indexable does not mean every operator uses every index; choose GIN jsonb_ops/jsonb_path_ops or array/range classes from actual predicates.' },
      { text: 'Arrays suit atomic bounded collections.', detail: 'If elements need keys, independent lifecycle, foreign keys, frequent updates, or joins, a child relation is usually clearer and cheaper to govern.' },
      { text: 'Enums and domains have distinct change boundaries.', detail: 'Enums provide an ordered label set with operational DDL constraints; domains centralize scalar checks but do not replace cross-row or lifecycle modeling.' },
      { text: 'Extensions are executable dependencies.', detail: 'Trusted status, owner, schema, control/update scripts, binary compatibility, replication, dump/restore order, supply chain, and upgrade tests belong in governance.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Native-type decision table', headers: ['Type', 'Good fit', 'Index/operator consequence', 'Watch'], rows: [['uuid', 'opaque distributed identity', 'B-tree equality/order; hash equality', 'random locality and textual ingestion'], ['numeric', 'exact money/measurements', 'B-tree; exact arithmetic', 'precision and CPU versus integer minor units'], ['timestamptz', 'instants', 'B-tree and range predicates', 'render in explicit zone; not civil schedule'], ['range/multirange', 'interval sets', 'GiST/SP-GiST overlap/containment', 'bounds and exclusion semantics'], ['inet/cidr', 'network addresses/prefixes', 'B-tree plus GiST/SP-GiST operator classes', 'address versus network intent'], ['array', 'bounded atomic collection', 'GIN containment/overlap', 'updates rewrite row; limited integrity'], ['jsonb', 'sparse/evolving document fragment', 'GIN or expression indexes by operator', 'weak relational constraints and broad indexes'], ['enum/domain', 'stable labels / reusable scalar constraint', 'underlying comparison semantics', 'migration and cast behavior']] },
      { kind: 'code', language: 'sql', title: 'Extend the canonical commerce products safely', code: "-- Existing commerce.products(product_id, sku, name, current_price) remains authoritative.\nCREATE DOMAIN commerce.positive_money AS numeric(19,4) CHECK (VALUE >= 0);\nALTER TABLE commerce.products\n  ADD COLUMN attributes jsonb NOT NULL DEFAULT '{}'::jsonb,\n  ADD COLUMN available_during tstzmultirange;\nCREATE TABLE commerce.product_price_quotes (\n  quote_id uuid PRIMARY KEY,\n  product_id bigint NOT NULL REFERENCES commerce.products(product_id),\n  quoted_price commerce.positive_money NOT NULL,\n  valid_during tstzrange NOT NULL\n);\nCREATE INDEX products_attributes_gin\n  ON commerce.products USING gin (attributes jsonb_path_ops);\nSELECT product_id, sku, current_price\nFROM commerce.products\nWHERE attributes @> '{\"color\":\"blue\"}'::jsonb;" },
      { kind: 'code', language: 'sql', title: 'Inventory extension provenance before installation', code: "SELECT name, version, installed, superuser, trusted, relocatable, schema, requires\nFROM pg_available_extension_versions ORDER BY name, version;\n-- Install only a pinned, reviewed package available on every server image.\nCREATE EXTENSION IF NOT EXISTS pg_stat_statements WITH SCHEMA public;\nSELECT extname, extversion, extowner::regrole FROM pg_extension;" },
      { kind: 'pitfall', title: 'One giant JSONB GIN index', text: 'It can amplify writes and WAL while serving few queries. Index stable scalar expressions or a narrower operator class after measuring predicates, selectivity, size, and rechecks.' },
      { kind: 'bestPractice', title: 'Exercise extensions through the recovery lifecycle', text: 'Pin package sources and versions; test install/update/rollback, physical and logical replication behavior, pg_dump/restore, standby promotion, and removal in an environment matching production.' },
    ],
    refs: [{ book: 'postgresql-docs', chapter: 'Ch. 8 — Data Types; 11.9 — Index-Only Scans and Covering Indexes; Ch. 36 — Extending SQL; Appendix F — Additional Supplied Modules and Extensions' }, { book: 'postgresql-internals', chapter: 'Ch. 1 — Introduction; §1.1 — Data Organization; Ch. 19 — Index Access Methods; Ch. 20 — Index Scans' }, { book: 'sql-antipatterns', chapter: 'Ch. 2 — Jaywalking; Ch. 6 — Entity-Attribute-Value' }],
    related: ['json-arrays-and-composite-data', 'boolean-enum-domain-and-uuid-types', 'schema-migrations-and-database-testing'],
  },
  {
    id: 'postgresql-mvcc-and-snapshots', domainId: 'db-postgresql', title: 'PostgreSQL MVCC and Snapshots',
    summary: 'PostgreSQL stores tuple versions whose transaction and command metadata are interpreted against a snapshot. Visibility changes by isolation level, while long-lived snapshots retain heap versions and can turn application transaction duration into maintenance debt.',
    keyPoints: [
      { text: 'xmin and xmax describe version history, not business ownership.', detail: 'A tuple header records inserting and deleting/updating transaction IDs plus infomask state; visibility also depends on transaction status, snapshot horizons, and command ID.' },
      { text: 'READ COMMITTED normally takes a new snapshot per statement.', detail: 'Two statements in one transaction may see different committed worlds; each command still sees a coherent statement snapshot.' },
      { text: 'REPEATABLE READ and SERIALIZABLE retain a transaction snapshot.', detail: 'PostgreSQL repeatable read prevents ordinary phantoms but can still require conflict handling; serializable adds SSI and mandates whole-transaction retry on serialization failure.' },
      { text: 'Command IDs order effects inside one transaction.', detail: 'They let a command distinguish earlier effects from later/current command work without exposing tuple headers as an application API.' },
      { text: 'HOT can avoid new index entries.', detail: 'When indexed columns are unchanged and the page has room, an update can form a heap-only chain that indexes reach through the root; pruning later shortens it.' },
      { text: 'Old snapshots pin cleanup horizons.', detail: 'Idle transactions, cursors, prepared transactions, and replication consumers can retain dead tuples or WAL and increase bloat, wraparound pressure, and replica lag.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Tuple versions and a snapshot', code: 'flowchart LR\n  I["T10 INSERT v1: xmin=10"] --> U["T20 UPDATE: v1 xmax=20"]\n  U --> V2["v2 xmin=20, xmax=0"]\n  S1["snapshot before T20 commits"] -->|visible| I\n  S1 -. "not visible" .-> V2\n  S2["later statement snapshot after commit"] -->|visible| V2\n  IDX["unchanged index key"] --> I\n  I -->|"HOT chain on same page"| V2' },
      { kind: 'table', caption: 'Snapshot scope in PostgreSQL 18', headers: ['Isolation', 'Snapshot', 'Application consequence'], rows: [['READ COMMITTED', 'new snapshot at command start', 'recheck assumptions across statements or lock/encode invariant'], ['REPEATABLE READ', 'first non-control statement for transaction', 'stable reads; update conflicts can abort'], ['SERIALIZABLE', 'transaction snapshot plus SSI dependencies', 'retry SQLSTATE 40001 as a complete unit'], ['READ UNCOMMITTED', 'behaves as READ COMMITTED', 'do not expect dirty reads']] },
      { kind: 'code', language: 'sql', title: 'Observe supported transaction metadata', code: "BEGIN ISOLATION LEVEL REPEATABLE READ;\nSELECT txid_current_if_assigned(), pg_current_snapshot();\nSELECT pg_snapshot_xmin(pg_current_snapshot()), pg_snapshot_xmax(pg_current_snapshot());\n-- Inspect xmin only for diagnosis; never build business logic on tuple headers.\nSELECT order_id, xmin FROM commerce.orders WHERE order_id = $1;\nCOMMIT;" },
      { kind: 'pitfall', title: 'Using xmin as an application version forever', text: 'Transaction IDs wrap, freezing changes historical meaning, dump/restore rewrites metadata, and tuple headers are implementation state. Store an explicit revision or timestamp for optimistic concurrency.' },
      { kind: 'bestPractice', title: 'Bound transaction age, not just query time', text: 'Set statement and idle-in-transaction limits appropriate to the workload; monitor oldest backend, prepared transaction, slot, and standby feedback horizons; retry serialization/deadlock failures with idempotency and jitter.' },
    ],
    refs: [{ book: 'postgresql-internals', chapter: 'Ch. 2 — Isolation; Ch. 3 — Pages and Tuples; Ch. 4 — Snapshots; Ch. 5 — Page Pruning and HOT Updates' }, { book: 'postgresql-docs', chapter: '13.2 — Transaction Isolation; 13.3 — Explicit Locking; 66.6 — Database Page Layout' }, { book: 'postgresql-ssi', chapter: '§2 — Snapshot Isolation; §3 — Serializable Snapshot Isolation' }],
    related: ['mvcc', 'snapshot-isolation-and-write-skew', 'postgresql-vacuum-freezing-and-bloat'],
  },
  {
    id: 'postgresql-vacuum-freezing-and-bloat', domainId: 'db-postgresql', title: 'PostgreSQL Vacuum, Freezing, and Bloat',
    summary: 'Vacuum turns versions no snapshot can see into reusable space, maintains visibility and planner metadata, and freezes old transaction IDs before wraparound. Safe operation manages cleanup horizons and debt continuously instead of scheduling emergency rewrites.',
    keyPoints: [
      { text: 'Pruning, vacuuming, truncating, and rewriting reclaim different things.', detail: 'Page pruning removes dead line-pointer chains; ordinary vacuum enables internal reuse and may truncate empty tail pages; VACUUM FULL rewrites under ACCESS EXCLUSIVE lock.' },
      { text: 'The visibility map enables index-only scans and vacuum skipping.', detail: 'All-visible and all-frozen bits are conservative hints cleared by writes; vacuum proves and sets them, while the free-space map guides insertion.' },
      { text: 'Autovacuum is threshold plus scale factor.', detail: 'Vacuum and analyze trigger from per-table base thresholds plus fractions of live tuples; insert thresholds, worker capacity, cost throttling, and per-table storage parameters matter on large/hot tables.' },
      { text: 'Freezing prevents transaction-ID and multixact wraparound.', detail: 'Aggressive vacuum advances relation frozen horizons; anti-wraparound work can override normal scheduling and must not be disabled.' },
      { text: 'Analyze and vacuum solve different feedback loops.', detail: 'ANALYZE samples value distributions for plans; VACUUM reclaims versions and maintains maps. Autovacuum may perform either or both based on separate counters.' },
      { text: 'Bloat is an evidence question.', detail: 'Dead tuple estimates, relation/index bytes, scan cost, free space, workload churn, and horizon age determine whether tuning, reindexing, repacking, or rewriting is justified.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Cleanup and freeze lifecycle', code: 'flowchart LR\n  W["UPDATE/DELETE"] --> D["dead version"] --> H["wait for global visibility horizon"] --> P["heap prune / VACUUM"]\n  P --> R["space reusable; indexes cleaned"]\n  P --> VM["visibility map all-visible/all-frozen"]\n  OLD["old live xmin"] --> F["aggressive vacuum freezes"] --> SAFE["wraparound-safe horizon"]\n  L["long txn / slot / standby feedback"] -. "holds horizon" .-> H' },
      { kind: 'table', caption: 'Intervention ladder', headers: ['Signal', 'First action', 'Escalation'], rows: [['oldest xmin/slot horizon', 'find owner and business need', 'cancel/terminate or advance/drop only with approved data-loss analysis'], ['dead tuples rising', 'check autovacuum runs, locks, worker saturation', 'per-table thresholds/cost; manual VACUUM'], ['stale estimates', 'ANALYZE and inspect modifications', 'raise statistics target selectively'], ['index growth/poor density', 'verify workload and index use', 'REINDEX CONCURRENTLY with capacity plan'], ['heap file must shrink', 'confirm disk/RTO/lock budget', 'online repack tool governance or VACUUM FULL rewrite']] },
      { kind: 'code', language: 'sql', title: 'Monitor debt with PostgreSQL 18 views', code: "SELECT relid::regclass, n_live_tup, n_dead_tup,\n       last_autovacuum, autovacuum_count, last_autoanalyze, autoanalyze_count\nFROM pg_stat_user_tables ORDER BY n_dead_tup DESC LIMIT 20;\n\nSELECT relname, age(relfrozenxid), mxid_age(relminmxid)\nFROM pg_class WHERE relkind IN ('r','m') ORDER BY age(relfrozenxid) DESC LIMIT 20;\n\nSELECT pid, datname, backend_xmin, state, xact_start\nFROM pg_stat_activity WHERE backend_xmin IS NOT NULL ORDER BY xact_start;" },
      { kind: 'pitfall', title: 'Running VACUUM FULL as routine maintenance', text: 'It rewrites the table, requires extra disk/WAL/replica capacity, changes physical locality, and takes a strong lock. Fix horizon and autovacuum causes before selecting a disruptive shrink operation.' },
      { kind: 'bestPractice', title: 'Tune hot tables by garbage rate', text: 'Measure modifications, dead-tuple creation, vacuum duration/throughput, freeze age, map coverage, index growth, blocking, and replica/slot retention; then set per-table thresholds early enough that workers keep up.' },
    ],
    refs: [{ book: 'postgresql-internals', chapter: 'Ch. 6 — Vacuum and Autovacuum; Ch. 7 — Freezing; Ch. 8 — Rebuilding Tables and Indexes' }, { book: 'postgresql-docs', chapter: '24.1 — Routine Vacuuming; VACUUM; 27.4 — Progress Reporting' }, { book: 'database-reliability-engineering', chapter: 'Ch. 10 — Data Storage, Indexing, and Replication' }],
    related: ['space-reclamation-and-maintenance', 'postgresql-mvcc-and-snapshots', 'capacity-maintenance-and-data-lifecycle'],
  },
  {
    id: 'postgresql-index-families', domainId: 'db-postgresql', title: 'PostgreSQL Index Families',
    summary: 'PostgreSQL index access methods implement different operator strategies: B-tree, hash, GIN, GiST, SP-GiST, and BRIN are not interchangeable. Selection begins with predicates and operator classes, then weighs false positives, heap access, write cost, WAL, and maintenance.',
    keyPoints: [
      { text: 'B-tree is the default for ordered scalar predicates.', detail: 'It supports equality, ranges, ordering, prefix use of multicolumn keys, and many uniqueness constraints.' },
      { text: 'Hash is intentionally narrow.', detail: 'It serves equality operators and is WAL-logged, but lacks ordering and usually offers less flexibility than B-tree.' },
      { text: 'GIN inverts multivalued content.', detail: 'It excels for arrays, JSONB and text search; pending lists and many entries per row make writes and vacuum behavior central.' },
      { text: 'GiST and SP-GiST are frameworks.', detail: 'Operator classes define strategies for geometric, range, nearest-neighbor or partitioned search; behavior follows the class, not the access-method name alone.' },
      { text: 'BRIN summarizes block ranges.', detail: 'Tiny indexes can prune naturally clustered large tables, but correlation, pages_per_range, summarization lag, and lossy heap rechecks control effectiveness.' },
      { text: 'Index refinements still have semantic gates.', detail: 'Partial predicates must be implied by the query; expression matching is structural; INCLUDE helps index-only scans only when visibility permits and increases tuple size.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Access-method and operator-class matrix', headers: ['Method', 'Typical predicates/data', 'Loss/recheck', 'Primary cost'], rows: [['B-tree', '=, range, ORDER BY, scalar uniqueness', 'normally exact', 'page splits, random insert locality, duplicate maintenance'], ['Hash', 'equality only', 'exact match then visibility', 'single-purpose index'], ['GIN', 'array/JSONB/tsvector membership', 'class/operator dependent; bitmap may be lossy', 'many entries, pending list, write/WAL'], ['GiST', 'ranges, geometry, KNN, exclusion', 'often consistent-function recheck', 'overlap and split quality'], ['SP-GiST', 'tries/quadtrees/radix partitioning', 'class dependent', 'data distribution fit'], ['BRIN', 'correlated values over large heaps', 'lossy range recheck', 'false-positive blocks, summarization']] },
      { kind: 'code', language: 'sql', title: 'Indexes for measured commerce predicates', code: "CREATE INDEX orders_open_customer_placed\n  ON commerce.orders (customer_id, placed_at DESC) INCLUDE (status)\n  WHERE status IN ('NEW','PAID');\n-- products_attributes_gin was defined with jsonb_path_ops in the types topic.\nCREATE INDEX orders_placed_brin\n  ON commerce.orders USING brin (placed_at) WITH (pages_per_range = 64);\n\nSELECT * FROM pg_stat_user_indexes\nWHERE schemaname = 'commerce' ORDER BY idx_scan DESC;" },
      { kind: 'note', title: 'Read plans and rechecks together', text: 'Bitmap heap scans can report exact and lossy heap blocks plus rows removed by index recheck. A lossy access method is not incorrect: it returns candidates that the executor verifies against the original condition.' },
      { kind: 'pitfall', title: 'Creating one index per visible column', text: 'Every index competes for cache, extends writes and WAL, lengthens vacuum/recovery, and may never serve a complete predicate/order. Consolidate from workload evidence while preserving constraint-backed indexes.' },
      { kind: 'bestPractice', title: 'Prove operator support and lifecycle cost', text: 'Record query predicate, operator/class, estimated and actual rows, heap fetch/recheck behavior, index bytes, write latency, WAL, build lock/capacity, and replica impact before and after deployment.' },
    ],
    refs: [{ book: 'postgresql-internals', chapter: 'Ch. 19 — Index Access Methods; Ch. 20 — Index Scans; Ch. 5 — Page Pruning and HOT Updates' }, { book: 'postgresql-docs', chapter: 'Ch. 11 — Indexes; Ch. 63 — Index Access Method Interface Definition' }, { book: 'sql-performance-explained', chapter: 'Anatomy of an Index; Index-Only Scan; Partial Indexes' }],
    related: ['b-tree-indexes', 'non-btree-indexes', 'postgresql-planner-and-explain'],
  },
  {
    id: 'postgresql-planner-and-explain', domainId: 'db-postgresql', title: 'PostgreSQL Planner and EXPLAIN',
    summary: 'PostgreSQL’s planner searches access paths, join orders, and algorithms using sampled statistics and a configurable cost model. EXPLAIN work is a controlled experiment: locate the first estimate divergence, account for loops, buffers, I/O and spills, change one cause, and remeasure.',
    keyPoints: [
      { text: 'Cardinality estimates dominate downstream choices.', detail: 'Per-column statistics miss cross-column dependence, skew and expression distributions; extended statistics can capture dependencies, MCVs and ndistinct for a table.' },
      { text: 'Costs are comparative estimates, not milliseconds.', detail: 'seq_page_cost, random_page_cost, CPU costs, effective_cache_size, parallel costs, row width, and work_mem influence ranking; calibrate globally from evidence rather than forcing one plan.' },
      { text: 'Execution nodes form a demand tree.', detail: 'Read leaves to root and multiply per-loop rows/time/buffers. Sorts, hashes, materialization, rescans and parallel workers explain where work expands or blocks.' },
      { text: 'Prepared statements may choose custom or generic plans.', detail: 'After repeated execution PostgreSQL compares planning/estimated costs; parameter skew can make one generic plan poor, and plan_cache_mode is a diagnostic lever, not a default cure.' },
      { text: 'EXPLAIN ANALYZE executes the statement.', detail: 'On writes use BEGIN/ROLLBACK in a disposable or carefully controlled context; volatile functions, locks, sequences and external effects may not roll back as expected.' },
      { text: 'Buffers and I/O timings need context.', detail: 'Shared hit/read/dirtied/written, temp blocks, WAL, memory/disk sort data, cache warmth and concurrent waits must be interpreted with timing overhead and system telemetry.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Evidence-driven remediation loop', code: 'flowchart LR\n  Q["capture query + binds + context"] --> E["EXPLAIN ANALYZE, BUFFERS, WAL, SETTINGS"] --> D["first estimates/actual divergence"]\n  D --> C["cause: stats, predicate, index, memory, contention"] --> X["one controlled change"] --> R["representative rerun + regression set"] --> Q' },
      { kind: 'code', language: 'sql', title: 'Diagnose a commerce query safely', code: "EXPLAIN (ANALYZE, BUFFERS, WAL, SETTINGS, FORMAT TEXT)\nSELECT o.order_id, sum(oi.quantity * oi.unit_price)\nFROM commerce.orders AS o\nJOIN commerce.order_items AS oi ON oi.order_id = o.order_id\nWHERE o.customer_id = $1 AND o.status = 'PAID'\nGROUP BY o.order_id;\n\nCREATE STATISTICS orders_customer_status (dependencies, mcv)\nON customer_id, status FROM commerce.orders;\nANALYZE commerce.orders;" },
      { kind: 'table', caption: 'Plan symptom to next evidence', headers: ['Symptom', 'Inspect', 'Possible response'], rows: [['estimate diverges at scan', 'pg_stats, parameter value, predicate/type', 'ANALYZE, statistics target, expression/extended stats'], ['loops multiply inner work', 'loops × rows/buffers', 'join/index/query-shape correction'], ['hash batches or sort disk', 'rows/width, memory and temp I/O', 'reduce input; bounded session-local work_mem test'], ['many heap fetches', 'visibility map and churn', 'vacuum health; narrower covering index only if justified'], ['good plan, slow runtime', 'wait events, I/O latency, concurrency', 'treat saturation/blocking, not planner knobs']] },
      { kind: 'pitfall', title: 'Disabling a plan node as the fix', text: 'enable_* settings are useful experiments but can hide bad estimates, missing access paths, or a workload shift. Preserve the evidence and repair the model/query/index before adopting a broad setting.' },
      { kind: 'bestPractice', title: 'Test distributions, not one lucky bind', text: 'Compare cold/warm and selective/common parameter values, concurrency levels, generic/custom behavior, plan shape, latency percentiles, WAL/temp I/O, and semantic results; keep a regression corpus.' },
    ],
    refs: [{ book: 'postgresql-internals', chapter: 'Ch. 16 — Query Execution Stages; Ch. 17 — Statistics; Ch. 20 — Index Scans' }, { book: 'postgresql-docs', chapter: '14.1 — Using EXPLAIN; 14.2 — Statistics Used by the Planner; 19.7 — Query Planning' }, { book: 'sql-performance-explained', chapter: 'Execution Plans; Bind Variables' }, { book: 'selinger-access-path', chapter: 'Access Path Selection in a Relational Database Management System' }],
    related: ['cost-based-query-planning', 'explain-and-performance-methodology', 'postgresql-index-families'],
  },
  {
    id: 'postgresql-wal-backup-and-replication', domainId: 'db-postgresql', title: 'PostgreSQL WAL, Backup, and Replication',
    summary: 'PostgreSQL WAL orders durable changes by LSN and feeds crash recovery, physical backup/PITR, streaming replication, and logical decoding. A reliable design treats archive continuity, slot retention, timeline history, lag semantics, and restore/failover drills as one lifecycle.',
    keyPoints: [
      { text: 'WAL-before-data is the core durability rule.', detail: 'A changed page cannot reach durable data storage before WAL covering that change; full-page images after checkpoints protect against torn-page recovery hazards.' },
      { text: 'Checkpoints trade restart distance for foreground I/O and WAL.', detail: 'Frequent checkpoints can increase write pressure and full-page images; long distance increases recovery work and retained WAL requirements.' },
      { text: 'PITR needs a consistent base backup plus an unbroken WAL chain.', detail: 'Archive_command/archive_library success, segment completeness, backup manifests, keys/configuration, recovery target, and timeline history must all survive independently.' },
      { text: 'Physical streaming replays cluster-level WAL.', detail: 'Standbys follow receive, write, flush and replay positions; asynchronous commit can be acknowledged before standby durability, while synchronous settings trade latency and availability for stronger placement.' },
      { text: 'Slots protect consumers by retaining resources.', detail: 'Physical slots retain WAL; logical slots also retain catalog/row history. max_slot_wal_keep_size can bound WAL retention, but invalidation then requires consumer repair/reseed.' },
      { text: 'Logical replication transfers decoded changes, not a complete cluster.', detail: 'Publications/subscriptions require table identity and separately managed schema, sequences, large objects, DDL, privileges, extensions, and conflict/initial-copy procedures.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'One WAL stream, several recovery consumers', code: 'flowchart LR\n  TX["commit record at LSN"] --> WAL["WAL buffers / pg_wal"]\n  WAL --> ARCH["archive"] --> PITR["base backup + replay + target + timeline"]\n  WAL --> SEND["walsender"] --> PHYS["physical standby receive/write/flush/replay"]\n  WAL --> DEC["logical slot decoding"] --> SUB["publication/subscription apply"]\n  CP["checkpoint + full-page-write cycle"] --> WAL\n  SLOT["slot restart_lsn / confirmed_flush_lsn"] -. "retention" .-> WAL' },
      { kind: 'table', caption: 'Replication position and risk', headers: ['Mechanism', 'Observe', 'Risk/action'], rows: [['physical sender', 'sent_lsn, write_lsn, flush_lsn, replay_lsn in pg_stat_replication', 'separate network, storage and replay lag'], ['receiver', 'written/flushed LSN and latest_end_lsn in pg_stat_wal_receiver', 'connection/config drift'], ['slot', 'restart_lsn, confirmed_flush_lsn, wal_status, safe_wal_size in pg_replication_slots', 'retained WAL/history or invalidation'], ['archiver', 'archived_count, failed_count, last_* in pg_stat_archiver', 'archive RPO gap despite healthy replica'], ['logical subscription', 'pg_stat_subscription worker/LSN timestamps', 'schema/identity/conflict and initial-copy state']] },
      { kind: 'code', language: 'sql', title: 'Inspect WAL retention and replication in PostgreSQL 18', code: "SELECT application_name, state, sync_state,\n       sent_lsn, write_lsn, flush_lsn, replay_lsn,\n       pg_wal_lsn_diff(sent_lsn, replay_lsn) AS replay_gap_bytes\nFROM pg_stat_replication;\nSELECT slot_name, slot_type, active, restart_lsn, confirmed_flush_lsn,\n       wal_status, safe_wal_size, invalidation_reason\nFROM pg_replication_slots;\nSELECT archived_count, failed_count, last_archived_wal, last_failed_wal\nFROM pg_stat_archiver;" },
      { kind: 'pitfall', title: 'Calling byte lag a time RPO', text: 'LSN gaps express retained work, not business time; replay can pause, workload WAL rates vary, and an archive gap may coexist with a current standby. Track timestamps, LSN stages, archive continuity, and recoverable business operations.' },
      { kind: 'bestPractice', title: 'Validate recovery and promotion end to end', text: 'Restore a selected target into isolation, verify manifest and WAL continuity, application invariants and security, promote, record the new timeline, reconnect clients, fence the old primary, and prove replicas/backups resume from the new history.' },
    ],
    refs: [{ book: 'postgresql-internals', chapter: 'Ch. 10 — Write-Ahead Log; Ch. 11 — WAL Modes' }, { book: 'postgresql-docs', chapter: 'Ch. 25 — Backup and Restore; Ch. 26 — High Availability, Load Balancing, and Replication; Ch. 28 — Reliability and the Write-Ahead Log; Ch. 29 — Logical Replication' }, { book: 'database-reliability-engineering', chapter: 'Ch. 7 — Backup and Recovery; Ch. 10 — Data Storage, Indexing, and Replication' }, { book: 'ddia-2', chapter: 'Ch. 7 — Replication' }],
    related: ['write-ahead-logging-and-checkpoints', 'point-in-time-recovery', 'replication-and-read-scaling'],
  },
  {
    id: 'postgresql-locks-monitoring-and-production-diagnostics', domainId: 'db-postgresql', title: 'PostgreSQL Locks, Monitoring, and Production Diagnostics',
    summary: 'PostgreSQL incident diagnosis correlates sessions, waits, locks, query fingerprints, WAL/replication, checkpoint and vacuum signals with configuration provenance. The runbook preserves evidence, contains harm with the least destructive action, and verifies recovery rather than stopping at symptom disappearance.',
    keyPoints: [
      { text: 'A wait event names the current wait, not the root cause.', detail: 'Join pg_stat_activity context with blockers, transaction age, query/application identity, resource telemetry, and a timeline before acting.' },
      { text: 'pg_locks shows held and awaited lockable objects.', detail: 'Use granted=false and pg_blocking_pids(pid) to build blocker edges; row-lock waits commonly appear as transactionid waits rather than one row per tuple.' },
      { text: 'Statistics views are cumulative and version-sensitive.', detail: 'Record PostgreSQL version, stats reset time, sample interval and columns; PostgreSQL 18 additionally exposes I/O, WAL, checkpointer, replication-slot and subscription views whose availability/shape must be checked.' },
      { text: 'pg_stat_statements is an optional, normalized workload view.', detail: 'It requires shared_preload_libraries, extension creation, query-ID computation and storage budget; fingerprints aggregate executions and normally do not preserve bind values.' },
      { text: 'Settings need provenance.', detail: 'pg_settings source/sourcefile/pending_restart and ALTER SYSTEM/configuration includes explain why a value is active; SHOW alone cannot distinguish defaults, role/database overrides, or restart debt.' },
      { text: 'Escalation should preserve correctness and evidence.', detail: 'Prefer application shedding and pg_cancel_backend for a query; terminate a session only after assessing rollback, locks, retries and owner, and never delete database process/files as incident response.' },
    ],
    blocks: [
      { kind: 'code', language: 'sql', title: 'Snapshot activity and blocking edges', code: "SELECT clock_timestamp() AS captured_at, pid, usename, application_name,\n       state, wait_event_type, wait_event, xact_start, query_start,\n       backend_xmin, pg_blocking_pids(pid) AS blockers, query_id, left(query, 500) AS query\nFROM pg_stat_activity WHERE pid <> pg_backend_pid();\n\nSELECT pid, locktype, relation::regclass, transactionid, mode, granted, waitstart\nFROM pg_locks WHERE NOT granted ORDER BY waitstart;\n\nSELECT name, setting, unit, source, sourcefile, pending_restart\nFROM pg_settings WHERE name IN ('max_connections','shared_buffers','work_mem','synchronous_commit');" },
      { kind: 'diagram', title: 'Evidence-preserving production runbook', code: 'flowchart LR\n  A["declare incident + timestamp"] --> S["snapshot activity, waits, locks, settings, host/I/O"] --> T["build blocker/resource timeline"]\n  T --> C["contain: shed, throttle, cancel"] --> E["terminate/fail over only with owner + impact"] --> V["verify latency, errors, invariants, replicas, jobs"] --> P["preserve evidence + corrective action"]' },
      { kind: 'table', caption: 'Signal bundle', headers: ['Question', 'PostgreSQL evidence', 'Interpret with'], rows: [['Who is blocked?', 'pg_stat_activity, pg_locks, pg_blocking_pids', 'transaction age, application owner, DDL/job timeline'], ['Which workload changed?', 'pg_stat_statements if installed; logs/query_id', 'stats reset, deploy, binds unavailable'], ['Is storage/checkpoint saturated?', 'pg_stat_io, pg_stat_bgwriter, pg_stat_checkpointer, pg_stat_wal', 'OS/device latency and sample deltas'], ['Is maintenance pinned?', 'backend_xmin, progress views, pg_stat_user_tables', 'slots, prepared xacts, standby feedback'], ['Is recovery path healthy?', 'pg_stat_replication, pg_replication_slots, pg_stat_archiver', 'archive continuity and business freshness']] },
      { kind: 'pitfall', title: 'Terminating the visible waiter', text: 'The slow query may be a victim behind an idle transaction or DDL blocker. Killing waiters wastes work while preserving the cause; reconstruct the blocking tree and coordinate with the root owner.' },
      { kind: 'bestPractice', title: 'Capture deltas before resetting statistics', text: 'Export timestamped view snapshots, logs, plans, settings provenance, host metrics and change events. After containment, verify error rate, latency, queues, locks, oldest horizons, replication/archive health, business invariants and recurrence monitors.' },
    ],
    refs: [{ book: 'postgresql-internals', chapter: 'Ch. 12 — Relation-Level Locks; Ch. 13 — Row-Level Locks; Ch. 14 — Miscellaneous Locks; Ch. 17 — Statistics' }, { book: 'postgresql-docs', chapter: '13.3 — Explicit Locking; Ch. 27 — Monitoring Database Activity; pg_stat_statements' }, { book: 'database-reliability-engineering', chapter: 'Ch. 4 — Operational Visibility; Ch. 5 — Infrastructure Engineering' }],
    related: ['database-observability', 'slow-query-and-incident-diagnosis', 'deadlocks-contention-and-retries'],
  },
]

import type { Topic } from '../../../types/content'

export const dbSchemaObjectsTopics: Topic[] = [
  {
    id: 'numeric-types-and-precision', domainId: 'db-schema-objects', title: 'Numeric Types and Precision',
    summary: 'Choose numeric types from the quantity’s mathematical domain: exact integers and fixed-scale decimals preserve money and counts, while binary floating point trades exact decimal representation for range and speed.',
    keyPoints: [
      { text: 'Integer types model exact whole quantities.', detail: 'Choose a range that covers the business lifetime, not merely today’s sample; narrowing a key or accumulated count later is costly.' },
      { text: '`DECIMAL(p,s)` is exact decimal arithmetic.', detail: '`p` is total significant decimal digits and `s` is digits after the decimal point; the representable magnitude is bounded by `p - s` integer digits.' },
      { text: 'Binary floating point is approximate.', detail: 'Values such as 0.1 usually have no finite binary representation, so equality and repeated addition can expose rounding error.' },
      { text: 'Money needs an explicit currency and rounding policy.', detail: 'A decimal amount alone does not say USD versus JPY, tax rounding stage, or midpoint rule. Store a currency code and define when rounding occurs.' },
      { text: 'Arithmetic can widen or overflow.', detail: 'Result precision and scale rules differ by engine. Cast deliberately at durable boundaries and test maximum quantities, prices, multiplication, sums, and division.' },
      { text: 'Logical exactness is separate from physical encoding.', detail: 'Vendors may use packed decimal, variable-length numeric, or native integers internally; schema choice should first preserve the domain’s required values and operations.' },
    ],
    blocks: [
      { kind: 'subheading', text: 'The classic money failure' },
      { kind: 'code', language: 'sql', title: 'Exact line totals in the commerce schema', code: "CREATE TABLE currencies (\n  currency_code CHAR(3) PRIMARY KEY,\n  minor_units SMALLINT NOT NULL CHECK (minor_units BETWEEN 0 AND 6)\n);\n\nALTER TABLE order_items\n  ADD COLUMN currency_code CHAR(3) NOT NULL REFERENCES currencies(currency_code);\n\nSELECT order_id, SUM(CAST(quantity AS DECIMAL(12,0)) * unit_price) AS order_total\nFROM order_items GROUP BY order_id;" },
      { kind: 'table', caption: 'Precision and scale examples', headers: ['Declaration', 'Largest positive value', 'Good fit'], rows: [['`DECIMAL(9,2)`', '`9,999,999.99`', 'Moderate currency amount'], ['`DECIMAL(19,4)`', '`999,999,999,999,999.9999`', 'Money with sub-minor precision'], ['`BIGINT` minor units', 'Implementation-defined signed 64-bit maximum', 'One fixed scale and checked arithmetic'], ['`DOUBLE PRECISION`', 'Very wide, approximate', 'Measurements and statistical calculations']] },
      { kind: 'paragraph', text: 'Adding approximate 0.1 three times need not compare equal to exact 0.3 because each value is rounded to a nearby binary fraction. `DECIMAL(10,2)` stores the decimal cents exactly, although multiplication and division still require an agreed result scale and rounding rule.' },
      { kind: 'pitfall', title: 'Using a tolerance to repair financial storage', text: 'A tolerance is useful for scientific floating-point comparisons, but it does not establish a ledger’s legal amount. Use exact decimal or checked minor units, currency metadata, and deterministic rounding.' },
      { kind: 'bestPractice', title: 'Budget the whole expression', text: 'Derive precision from maximum unit price × quantity × lines, include tax and currency conversion headroom, then test boundary values in the target engine.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§4.5.1 — SQL Data Types and Schemas' }, { book: 'sql-relational-theory', chapter: 'Ch. 2 — Types and Domains' }, { book: 'sql-antipatterns', chapter: 'Ch. 10 — Rounding Errors' }, { book: 'postgresql-docs', chapter: '8.1 — Numeric Types' }],
    related: ['boolean-enum-domain-and-uuid-types', 'schema-design-antipatterns', 'grouping-and-aggregation'],
  },
  {
    id: 'text-types-collations-and-encoding', domainId: 'db-schema-objects', title: 'Text Types, Collations, and Encoding',
    summary: 'Text correctness spans character encoding, length semantics, normalization, and collation; comparison rules affect ordering, equality, indexes, and whether a unique constraint rejects two spellings.',
    keyPoints: [
      { text: 'Unicode is a character repertoire, while UTF-8 is an encoding.', detail: 'A character may occupy several bytes; byte limits and character limits are not interchangeable.' },
      { text: '`CHAR`, `VARCHAR`, and large text differ in constraints more than meaning.', detail: 'Fixed padding, maximum length, and indexability vary. Do not use fixed-width text merely because a code currently has a fixed presentation.' },
      { text: 'A collation defines comparison and sort behavior.', detail: 'Locale, case sensitivity, accent sensitivity, and version determine whether strings compare equal and their order.' },
      { text: 'Canonical-equivalent Unicode can have different code sequences.', detail: 'For example, a precomposed accented letter and a base letter plus combining mark may require normalization before stable identity comparisons.' },
      { text: 'Uniqueness inherits comparison semantics.', detail: 'Under a case-insensitive or accent-insensitive collation, spellings that binary comparison distinguishes may conflict in a unique index.' },
      { text: 'Changing collation can require rebuilding indexes.', detail: 'Index ordering records the comparison rules. Pin intentional collations and plan for provider or collation-version upgrades.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Comparison policy and consequence', headers: ['Policy', 'May compare equal', 'Unique-key consequence'], rows: [['Binary/code-unit', '`Résumé` only to identical encoding', 'Case/accent variants coexist'], ['Case-insensitive', '`SKU-7`, `sku-7`', 'Only one spelling can be inserted'], ['Case- and accent-insensitive', '`resume`, `résumé`', 'Both collapse to one key'], ['Normalized application key', 'Canonical-equivalent sequences', 'Stable only if every write uses the same normalization']] },
      { kind: 'code', variants: [{ id: 'postgresql', label: 'PostgreSQL', language: 'sql', code: "CREATE TABLE product_aliases (\n  alias_id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  product_id BIGINT NOT NULL REFERENCES products(product_id),\n  display_name TEXT NOT NULL,\n  search_name TEXT COLLATE \"und-x-icu\" NOT NULL,\n  UNIQUE (search_name)\n);" }, { id: 'mysql', label: 'MySQL', language: 'sql', code: "CREATE TABLE product_aliases (\n  alias_id BIGINT AUTO_INCREMENT PRIMARY KEY,\n  product_id BIGINT NOT NULL,\n  display_name VARCHAR(255) NOT NULL,\n  search_name VARCHAR(255) COLLATE utf8mb4_0900_ai_ci NOT NULL UNIQUE,\n  FOREIGN KEY (product_id) REFERENCES products(product_id)\n);" }] },
      { kind: 'paragraph', text: 'The added `product_aliases` object separates preserved display text from an identity/search key. The shown collations illustrate vendor syntax, not a portable promise: test actual case, accent, punctuation, normalization, and language behavior for the chosen collation.' },
      { kind: 'pitfall', title: 'Assuming lowercase is universal identity', text: 'Case mapping can expand characters and is locale-sensitive. A function-based key also needs the same expression and collation in queries to use its index reliably.' },
      { kind: 'bestPractice', title: 'Test the comparison contract', text: 'Choose UTF-8 where supported, state character versus byte limits, preserve display input, normalize at a controlled boundary, and test representative multilingual pairs before enforcing uniqueness.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§4.5.1 — Character types' }, { book: 'sql-relational-theory', chapter: 'Ch. 2 — Types and Domains' }, { book: 'postgresql-docs', chapter: '23.2 and 24.2 — Collation support; character sets' }, { book: 'mysql-docs', chapter: '12 — Character Sets, Collations, Unicode' }],
    related: ['primary-unique-and-check-constraints', 'schema-migrations-and-database-testing', 'select-expressions-and-filtering'],
  },
  {
    id: 'dates-times-and-time-zones', domainId: 'db-schema-objects', title: 'Dates, Times, and Time Zones',
    summary: 'Temporal schemas must distinguish instants on the global timeline, civil date-times interpreted under a zone’s rules, dates, and elapsed or calendar intervals—especially across daylight-saving transitions.',
    keyPoints: [
      { text: 'An instant identifies one point on the global timeline.', detail: 'Store an instant-capable type or normalized UTC representation for events such as payment authorization, while retaining zone context when later civil rendering matters.' },
      { text: 'A local date-time is not an instant.', detail: '`2026-10-25 02:30` can be ambiguous in an overlap; `2026-03-29 02:30` may not exist in a DST gap, depending on the zone.' },
      { text: 'A time-zone ID is a rule set, not merely an offset.', detail: '`Europe/Belgrade` includes historical and future transition rules; `+01:00` says only one offset.' },
      { text: 'Dates and recurring civil schedules deserve civil types.', detail: 'A birthday or “store opens at 09:00 local time” should not drift merely because it was converted prematurely to an instant.' },
      { text: 'Elapsed duration and calendar interval differ.', detail: 'Adding 24 hours follows the timeline; adding one civil day aims for the same local clock time and can span 23 or 25 hours.' },
      { text: 'Vendor type names conceal different semantics.', detail: 'PostgreSQL `timestamp with time zone` stores an instant and renders in the session zone; SQL Server `datetimeoffset` retains an offset, not a named-zone rule set.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Temporal value model', code: 'flowchart LR\n  L["Civil date-time + zone ID"] --> R["Resolve using zone rules"]\n  R --> I["Instant"]\n  I --> Z["Render in a chosen zone"]\n  D["Date or recurring local time"] --> L\n  V["Duration / calendar interval"] --> I' },
      { kind: 'table', caption: 'DST boundary example for Europe/Belgrade', headers: ['Civil input', 'Condition', 'Required policy'], rows: [['Spring `02:30` on transition day', 'Gap: clock skips forward', 'Reject, or explicitly shift'], ['Autumn `02:30` on transition day', 'Overlap: two candidate instants', 'Require earlier/later offset'], ['`2026-07-10 09:00 Europe/Belgrade`', 'One candidate', 'Resolve to instant; preserve zone if recurrence matters']] },
      { kind: 'code', language: 'sql', title: 'Define a scheduled fulfillment before using it', code: "CREATE TABLE fulfillment_windows (\n  window_id BIGINT PRIMARY KEY,\n  order_id BIGINT NOT NULL REFERENCES orders(order_id),\n  local_start TIMESTAMP NOT NULL,\n  time_zone_id VARCHAR(64) NOT NULL,\n  resolved_start TIMESTAMP WITH TIME ZONE NOT NULL,\n  CHECK (time_zone_id <> '')\n);" },
      { kind: 'pitfall', title: 'Letting the session zone reinterpret input', text: 'Implicit casts and zone defaults make the same literal denote different instants in different sessions. Bind typed values and resolve ambiguous or nonexistent civil times in an explicitly chosen zone.' },
      { kind: 'bestPractice', title: 'Name the temporal question', text: 'For each column state whether it is an instant, civil value, date, duration, or calendar interval; define precision, zone source, DST policy, and half-open range boundaries.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§4.5.1 — Date and time types' }, { book: 'sql-relational-theory', chapter: 'Ch. 2 — Types and Domains' }, { book: 'postgresql-docs', chapter: '8.5 — Date/Time Types' }, { book: 'sqlserver-docs', chapter: 'Date and time data types and functions; datetimeoffset' }],
    related: ['temporal-data-modeling', 'temporal-and-versioned-queries', 'schema-migrations-and-database-testing'],
  },
  {
    id: 'boolean-enum-domain-and-uuid-types', domainId: 'db-schema-objects', title: 'Boolean, Enum, Domain, and UUID Types',
    summary: 'Specialized scalar types can communicate and enforce a domain, but portability, evolution, ordering, generation, and nullability determine whether a native type, reusable domain, lookup relation, or constraint is the durable choice.',
    keyPoints: [
      { text: 'Boolean is a two-value domain only when `NULL` is excluded.', detail: 'Nullable SQL boolean also has UNKNOWN; predicates such as `WHERE flag` select TRUE, not FALSE or UNKNOWN.' },
      { text: 'Enums close a value set into the type definition.', detail: 'They are concise for small stable sets, but adding, renaming, ordering, and removing labels are vendor-specific migration operations.' },
      { text: 'A domain packages a base type with reusable constraints.', detail: 'Domains express semantic reuse such as nonnegative quantity, but engine support and treatment of null/domain constraints differ.' },
      { text: 'Lookup relations handle evolving reference data.', detail: 'Use a referenced table when values carry metadata, lifecycle, translations, or administrator-managed additions.' },
      { text: 'UUIDs decentralize generation but do not prove business identity.', detail: 'Validate a natural candidate key separately; random UUID locality can increase index fragmentation, while time-ordered variants reveal ordering information.' },
      { text: 'Native syntax is not the logical model.', detail: 'A UUID is a 128-bit identifier and an enum is a finite domain whether represented natively, by text plus CHECK, or by a referenced key.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Integrity and portability trade-offs', headers: ['Choice', 'Strength', 'Cost / portability'], rows: [['Boolean + NOT NULL', 'Clear binary fact', 'Some engines expose integer-like syntax'], ['Native enum', 'Compact closed vocabulary', 'Evolution and ordering are vendor-specific'], ['Domain', 'Reusable semantic constraint', 'Not uniformly supported'], ['Lookup relation + FK', 'Metadata and controlled evolution', 'Join and lifecycle management'], ['UUID', 'Distributed key generation', 'Larger indexes; generation/version choices']] },
      { kind: 'code', variants: [{ id: 'postgresql', label: 'PostgreSQL', language: 'sql', code: "CREATE DOMAIN positive_quantity AS INTEGER CHECK (VALUE > 0);\nCREATE TYPE fulfillment_state AS ENUM ('PENDING', 'PACKED', 'SHIPPED');\nCREATE TABLE shipments (\n  shipment_id UUID PRIMARY KEY,\n  order_id BIGINT NOT NULL REFERENCES orders(order_id),\n  quantity positive_quantity NOT NULL,\n  state fulfillment_state NOT NULL,\n  insured BOOLEAN NOT NULL DEFAULT FALSE\n);" }, { id: 'sqlite', label: 'SQLite', language: 'sql', code: "CREATE TABLE shipments (\n  shipment_id TEXT PRIMARY KEY,\n  order_id INTEGER NOT NULL REFERENCES orders(order_id),\n  quantity INTEGER NOT NULL CHECK (quantity > 0),\n  state TEXT NOT NULL CHECK (state IN ('PENDING','PACKED','SHIPPED')),\n  insured INTEGER NOT NULL DEFAULT 0 CHECK (insured IN (0,1))\n);" }] },
      { kind: 'pitfall', title: 'Treating a generated key as a candidate key', text: 'Two retries can receive two UUIDs for the same external purchase. Preserve an idempotency or order-number uniqueness constraint independently of the surrogate identifier.' },
      { kind: 'bestPractice', title: 'Choose for change frequency', text: 'Use a constraint or enum for genuinely closed, metadata-free values; a lookup relation for governed values that evolve; and a UUID generation strategy whose collision, locality, privacy, and coordination properties fit the system.' },
    ],
    refs: [{ book: 'sql-relational-theory', chapter: 'Ch. 2 — Types and Domains; Ch. 8 — SQL and Constraints' }, { book: 'database-system-concepts', chapter: '§4.5.1 — Built-in and user-defined types' }, { book: 'sql-antipatterns', chapter: 'Ch. 2 — Jaywalking; Ch. 4 — ID Required' }, { book: 'postgresql-docs', chapter: '8.3, 8.7, 8.12, and CREATE DOMAIN — Boolean, enum, UUID, domains' }],
    related: ['numeric-types-and-precision', 'primary-unique-and-check-constraints', 'schema-design-antipatterns'],
  },
  {
    id: 'json-arrays-and-composite-data', domainId: 'db-schema-objects', title: 'JSON, Arrays, and Composite Data',
    summary: 'Nested types are useful when a value is naturally retrieved and changed as one bounded aggregate, but relational decomposition is superior when elements need independent identity, constraints, joins, lifecycle, or selective updates.',
    keyPoints: [
      { text: 'JSON preserves a document shape, not a relational contract by itself.', detail: 'Schema validation, required properties, scalar types, and references need explicit constraints or controlled application boundaries.' },
      { text: 'Arrays model ordered or repeated values inside one row.', detail: 'They are poor substitutes for an association relation when elements need foreign keys, attributes, uniqueness, or individual updates.' },
      { text: 'Composite values group named fields.', detail: 'They can clarify a cohesive value such as an address snapshot, but vendor syntax, null semantics, indexing, and field evolution differ.' },
      { text: 'Relational decomposition exposes keys and dependencies.', detail: 'Rows can reference products, enforce one item per order/product, and be indexed or joined without parsing a document.' },
      { text: 'Duplication can be intentional for immutable snapshots.', detail: 'An order may preserve the delivery address used at purchase even after a customer changes the current address; name the snapshot semantics.' },
      { text: 'Query workload cannot override integrity requirements silently.', detail: 'Document reads may be convenient, but frequent partial writes and cross-document invariants create contention and fragile application validation.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Decision guide', headers: ['Need', 'Prefer', 'Why'], rows: [['Independent element identity or FK', 'Child relation', 'Keys and referential integrity'], ['Many joins/filters on elements', 'Child relation', 'Direct statistics and indexes'], ['Bounded immutable snapshot', 'JSON/composite', 'Read and version as one value'], ['Ordered primitive list with no element metadata', 'Array, if portable enough', 'Order is intrinsic and ownership is singular'], ['Unbounded or concurrently edited collection', 'Child relation', 'Avoid rewriting/locking one large value']] },
      { kind: 'code', language: 'sql', title: 'Relational items, JSON address snapshot', code: "CREATE TABLE order_delivery_snapshots (\n  order_id BIGINT PRIMARY KEY REFERENCES orders(order_id),\n  address_document JSON NOT NULL,\n  captured_at TIMESTAMP WITH TIME ZONE NOT NULL\n);\n\n-- Keep product-bearing lines relational: order_items already provides\n-- order_id, product_id, quantity, unit_price and their constraints.\nSELECT i.order_id, i.product_id, i.quantity\nFROM order_items AS i\nWHERE i.product_id = :product_id;" },
      { kind: 'paragraph', text: 'The added snapshot is explicitly defined before use and represents the address as purchased, not the customer’s current address. A JSON type can verify syntactic JSON; stronger shape validation is a separate contract. Product-bearing line items remain decomposed because products have identity and line-level constraints.' },
      { kind: 'pitfall', title: 'Serializing an association to avoid a join', text: 'Comma-separated IDs, arrays of foreign keys, or JSON line items hide cardinality and integrity. They complicate updates, statistics, cascading lifecycle rules, and queries such as “all orders containing product 42.”' },
      { kind: 'bestPractice', title: 'Define the aggregate boundary', text: 'Document ownership, maximum size, validation, partial-update frequency, indexing expressions, versioning, and whether consumers tolerate vendor-specific operators.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§8.1–8.3 — Complex data types and semi-structured data' }, { book: 'sql-relational-theory', chapter: 'Ch. 2 — Types and Domains; Ch. 5 — Base Relvars, Base Tables' }, { book: 'sql-antipatterns', chapter: 'Ch. 2 — Jaywalking; Ch. 12 — Phantom Files' }, { book: 'postgresql-docs', chapter: '8.14, 8.15, 8.16 — JSON, arrays, composite types' }],
    related: ['application-schema-patterns', 'schema-design-antipatterns', 'modeling-hierarchies-and-graphs'],
  },
  {
    id: 'primary-unique-and-check-constraints', domainId: 'db-schema-objects', title: 'Primary, Unique, and Check Constraints',
    summary: 'Candidate keys identify rows, primary and unique constraints declare chosen keys, and CHECK constrains each row—but SQL nulls make uniqueness vendor-sensitive and a CHECK rejects FALSE while allowing UNKNOWN.',
    keyPoints: [
      { text: 'A candidate key is minimal and unique by business meaning.', detail: 'A primary key selects one candidate key as the principal row identifier; alternate candidate keys still deserve UNIQUE and usually NOT NULL.' },
      { text: 'A surrogate key does not replace natural integrity.', detail: 'A generated `order_id` supports references, while a merchant’s order number or idempotency key may independently prevent duplicate business events.' },
      { text: 'Null behavior in unique constraints is vendor-sensitive.', detail: 'SQL commonly permits multiple nulls because nulls are distinct for uniqueness; SQL Server unique indexes commonly allow only one null for a single nullable key unless filtered. PostgreSQL can request `NULLS NOT DISTINCT`.' },
      { text: 'A CHECK succeeds on TRUE or UNKNOWN and fails only on FALSE.', detail: '`CHECK (quantity > 0)` does not reject NULL because the comparison is UNKNOWN. Add `NOT NULL` when absence is forbidden.' },
      { text: 'CHECK is best for row-local predicates.', detail: 'Cross-row totals and mutable reference lookups need other mechanisms; subqueries in CHECK are unsupported by major engines and would be difficult to keep valid.' },
      { text: 'Constraint names and lifecycle matter operationally.', detail: 'Name constraints for actionable errors, validate existing data during rollout, and understand vendor facilities for deferred or not-yet-validated enforcement.' },
    ],
    blocks: [
      { kind: 'code', language: 'sql', title: 'Declare every candidate-key rule', code: "CREATE TABLE order_external_refs (\n  source_system VARCHAR(40) NOT NULL,\n  external_order_id VARCHAR(120) NOT NULL,\n  order_id BIGINT NOT NULL REFERENCES orders(order_id),\n  CONSTRAINT pk_order_external_refs\n    PRIMARY KEY (source_system, external_order_id),\n  CONSTRAINT uq_order_external_refs_order UNIQUE (source_system, order_id),\n  CONSTRAINT ck_order_external_refs_source CHECK (source_system <> '')\n);\n\nALTER TABLE order_items\n  ADD CONSTRAINT uq_order_items_product UNIQUE (order_id, product_id);" },
      { kind: 'table', caption: 'CHECK three-valued evaluation', headers: ['`quantity`', '`quantity > 0`', 'CHECK result'], rows: [['`2`', 'TRUE', 'Accept'], ['`0`', 'FALSE', 'Reject'], ['`NULL`', 'UNKNOWN', 'Accept; NOT NULL must reject absence']] },
      { kind: 'table', caption: 'Nullable uniqueness caveat', headers: ['Engine / declaration', 'Two rows with NULL key?'], rows: [['PostgreSQL default UNIQUE', 'Allowed'], ['PostgreSQL `UNIQUE NULLS NOT DISTINCT`', 'Rejected'], ['MySQL UNIQUE', 'Allowed'], ['SQLite UNIQUE', 'Allowed'], ['SQL Server ordinary unique single-column index', 'Only one NULL; use a filtered index to ignore NULLs'], ['Oracle single nullable unique column', 'Multiple all-null key rows because entirely null index entries are absent']] },
      { kind: 'pitfall', title: 'Encoding mandatory input only in CHECK', text: '`CHECK (email <> \'\')` allows NULL. Combine type, `NOT NULL`, uniqueness, and a predicate so each expresses one part of the domain.' },
      { kind: 'bestPractice', title: 'Test nulls and concurrency', text: 'Let constraints arbitrate competing inserts; do not rely on “check then insert.” Test null, empty, boundary, duplicate, and concurrent cases on every supported vendor.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§4.4 — Integrity Constraints' }, { book: 'sql-relational-theory', chapter: 'Ch. 5 — Base Relvars, Base Tables; Ch. 8 — SQL and Constraints; Ch. 10 — SQL and Logic' }, { book: 'postgresql-docs', chapter: '5.5 — Constraints; CREATE TABLE NULLS NOT DISTINCT' }, { book: 'sqlserver-docs', chapter: 'Create unique indexes; filtered indexes' }, { book: 'oracle-docs', chapter: 'Data integrity — unique constraints and nulls' }],
    related: ['keys-and-identity', 'nulls-and-three-valued-logic', 'foreign-keys-and-referential-actions'],
  },
  {
    id: 'foreign-keys-and-referential-actions', domainId: 'db-schema-objects', title: 'Foreign Keys and Referential Actions',
    summary: 'A foreign key requires each non-null referencing value to match a referenced candidate key; update/delete actions define lifecycle consequences, while action timing and constraint deferral are distinct and vendor-dependent.',
    keyPoints: [
      { text: 'A foreign key targets a primary or alternate candidate key.', detail: 'Column count, order, and compatible domains must match. Index the referencing columns when parent deletion or child lookup must scale.' },
      { text: '`CASCADE` propagates identity or lifecycle changes.', detail: 'Delete cascade is appropriate for truly owned rows such as order items, but dangerous when the child has an independent retention obligation.' },
      { text: '`SET NULL` preserves the child by removing the relationship.', detail: 'Every affected referencing column must permit null, and null must have a defined domain meaning such as “former referrer unavailable.”' },
      { text: '`NO ACTION` and `RESTRICT` are not universally synonymous.', detail: 'In PostgreSQL, `RESTRICT` prevents the referenced change immediately and is not deferrable as an action; `NO ACTION` permits the check to occur at the constraint’s configured time. Other vendors can treat them equivalently.' },
      { text: 'Deferral controls when a constraint is checked.', detail: 'PostgreSQL and Oracle support deferrable constraints; MySQL, SQL Server, and SQLite differ—SQLite requires foreign keys enabled and supports declaration-time deferral, not PostgreSQL’s runtime mode model.' },
      { text: 'Composite nullable foreign keys need a match policy.', detail: 'Default `MATCH SIMPLE` exempts a row if any referencing column is null; `MATCH FULL` requires all null or all matching where supported.' },
    ],
    blocks: [
      { kind: 'code', variants: [{ id: 'postgresql', label: 'PostgreSQL', language: 'sql', code: "CREATE TABLE shipment_items (\n  shipment_id UUID NOT NULL REFERENCES shipments(shipment_id) ON DELETE CASCADE,\n  order_id BIGINT NOT NULL,\n  line_no INTEGER NOT NULL,\n  PRIMARY KEY (shipment_id, order_id, line_no),\n  CONSTRAINT fk_shipment_item_order_line FOREIGN KEY (order_id, line_no)\n    REFERENCES order_items(order_id, line_no) ON DELETE NO ACTION\n    DEFERRABLE INITIALLY IMMEDIATE\n);\n\nBEGIN;\nSET CONSTRAINTS fk_shipment_item_order_line DEFERRED;\nUPDATE order_items SET line_no = line_no + 1000 WHERE order_id = $1;\n-- References are temporarily inconsistent until the child keys follow.\nUPDATE shipment_items SET line_no = line_no + 1000 WHERE order_id = $1;\nCOMMIT;\n\n-- Use ON DELETE RESTRICT on a separate FK when the parent change must\n-- be rejected immediately; PostgreSQL does not defer that action." }, { id: 'mysql', label: 'MySQL', language: 'sql', code: "CREATE TABLE shipment_items (\n  shipment_id CHAR(36) NOT NULL, order_id BIGINT NOT NULL, line_no INT NOT NULL,\n  PRIMARY KEY (shipment_id, order_id, line_no),\n  FOREIGN KEY (shipment_id) REFERENCES shipments(shipment_id) ON DELETE CASCADE,\n  FOREIGN KEY (order_id, line_no) REFERENCES order_items(order_id, line_no)\n    ON DELETE RESTRICT\n);\n-- InnoDB checks foreign keys immediately; it does not support deferred checking." }] },
      { kind: 'table', caption: 'Parent lifecycle actions', headers: ['Action', 'When parent is deleted', 'Suitable example'], rows: [['`CASCADE`', 'Delete matching children', 'Order owns order items'], ['`RESTRICT`', 'Refuse referenced change at action time', 'Product with retained dependent rows'], ['`NO ACTION`', 'Refuse if violation exists at check time', 'Default protective relationship; may be deferred where supported'], ['`SET NULL`', 'Null the child reference', 'Optional referrer/product link'], ['`SET DEFAULT`', 'Write declared default, which must still satisfy FK', 'Rare sentinel policy']] },
      { kind: 'paragraph', text: 'Immediate versus deferred answers “when is consistency tested?”; the action answers “what transformation follows a parent change?” A deferred `NO ACTION` constraint can allow a transaction to reorder keys temporarily, but it must be valid at its deferred check or commit.' },
      { kind: 'pitfall', title: 'Cascading across an aggregate boundary', text: 'A delete cascade can erase audit, payment, or shipment history long after it became independently meaningful. Map ownership and retention rules before choosing an action.' },
      { kind: 'bestPractice', title: 'Exercise the whole lifecycle', text: 'Test parent update/delete, nullable references, bulk load, cyclic relationships, rollback, and commit-time failures on the exact engine; monitor orphan cleanup before adding enforcement to legacy data.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§4.4.3 — Referential Integrity' }, { book: 'sql-relational-theory', chapter: 'Ch. 8 — SQL and Constraints' }, { book: 'postgresql-docs', chapter: '5.5.5 and CREATE TABLE — Foreign keys, actions, deferrability' }, { book: 'mysql-docs', chapter: '17.1.20.5 — InnoDB Foreign Key Constraints' }, { book: 'sqlite-docs', chapter: 'Foreign Key Support — Deferred foreign key constraints' }],
    related: ['primary-unique-and-check-constraints', 'transactions-and-acid', 'schema-migrations-and-database-testing'],
  },
  {
    id: 'views-and-materialized-views', domainId: 'db-schema-objects', title: 'Views and Materialized Views',
    summary: 'A view stores a query interface evaluated against current base data, while a materialized view stores a result snapshot that must be refreshed; both can provide abstraction, but security, freshness, updateability, and refresh concurrency are separate contracts.',
    keyPoints: [
      { text: 'A regular view stores a query definition.', detail: 'It presents current data under transaction visibility and can stabilize names or hide complexity, but it does not inherently cache the result.' },
      { text: 'A materialized view stores rows from a query.', detail: 'Reads can be fast and indexed, but results are stale between refreshes and storage/write maintenance must be budgeted.' },
      { text: 'Refresh semantics are vendor-specific.', detail: 'PostgreSQL refresh replaces contents and `CONCURRENTLY` requires an eligible unique index and a populated view; Oracle offers complete/fast and on-demand/on-commit choices; SQL Server uses indexed views maintained with base writes.' },
      { text: 'A view is not automatically a security boundary.', detail: 'Privileges, owner/invoker execution, row-level security, leakproof predicates, and access to base tables vary. Grant least privilege and test information leakage.' },
      { text: 'Updateability depends on mapping writes unambiguously.', detail: 'Simple single-table views may be updatable; joins and aggregates generally need vendor mechanisms such as INSTEAD OF triggers or explicit routines.' },
      { text: 'Schema abstraction has limits.', detail: 'Renaming, type changes, `SELECT *`, and changed semantics can still break consumers. Treat a view’s columns and meaning as a versioned API.' },
    ],
    blocks: [
      { kind: 'code', variants: [{ id: 'postgresql', label: 'PostgreSQL', language: 'sql', code: "CREATE VIEW customer_public AS\nSELECT customer_id, name\nFROM customers;\n\nCREATE MATERIALIZED VIEW customer_lifetime_value AS\nSELECT o.customer_id, SUM(i.quantity * i.unit_price) AS paid_total\nFROM orders o JOIN order_items i ON i.order_id = o.order_id\nWHERE o.status = 'PAID' GROUP BY o.customer_id;\n\nCREATE UNIQUE INDEX uq_customer_ltv\n  ON customer_lifetime_value(customer_id);\nREFRESH MATERIALIZED VIEW CONCURRENTLY customer_lifetime_value;" }, { id: 'oracle', label: 'Oracle', language: 'sql', code: "CREATE MATERIALIZED VIEW customer_lifetime_value\n  REFRESH COMPLETE ON DEMAND\nAS SELECT o.customer_id, SUM(i.quantity * i.unit_price) AS paid_total\nFROM orders o JOIN order_items i ON i.order_id = o.order_id\nWHERE o.status = 'PAID' GROUP BY o.customer_id;" }] },
      { kind: 'table', caption: 'View choices', headers: ['Property', 'View', 'Materialized view'], rows: [['Storage', 'Definition; result not inherently stored', 'Result snapshot plus indexes'], ['Freshness', 'Current under query visibility', 'As of last successful refresh/maintenance'], ['Read cost', 'Underlying query cost', 'Snapshot scan/index cost'], ['Write cost', 'Normally base-table cost', 'Refresh or incremental maintenance'], ['Security', 'Possible interface, engine-specific execution rules', 'Separate stored object still requiring grants'], ['Failure mode', 'Slow or invalid dependency', 'Stale, blocked, or failed refresh']] },
      { kind: 'paragraph', text: 'A refresh is a data pipeline: define its timestamp, acceptable staleness, transaction snapshot, failure alert, and reader behavior. PostgreSQL `CONCURRENTLY` reduces reader blocking but does not mean “free,” instantaneous, or incremental.' },
      { kind: 'pitfall', title: 'Calling a view a performance optimization', text: 'An ordinary view can be expanded into the same expensive query. Confirm plans; materialize only with a refresh and freshness contract.' },
      { kind: 'bestPractice', title: 'Publish an explicit interface', text: 'List columns rather than `SELECT *`, document semantics and security context, test grants as the application role, and monitor refresh duration, last success, staleness, and index growth.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§4.2 — Views; §17.2.2 — Materialized Views' }, { book: 'sql-relational-theory', chapter: 'Ch. 9 — SQL and Views' }, { book: 'postgresql-docs', chapter: '39.3 and REFRESH MATERIALIZED VIEW — Materialized views and concurrent refresh' }, { book: 'oracle-docs', chapter: 'Data Warehousing Guide — Basic materialized views; refresh methods' }, { book: 'sqlserver-docs', chapter: 'Create indexed views' }],
    related: ['schema-migrations-and-database-testing', 'cost-based-query-planning', 'roles-privileges-and-least-authority'],
  },
  {
    id: 'sequences-generated-columns-triggers-and-routines', domainId: 'db-schema-objects', title: 'Sequences, Generated Columns, Triggers, and Routines',
    summary: 'Database-generated values and executable objects centralize useful invariants, but identity allocation, derived-value timing, side effects, privileges, recursion, and concurrency must remain explicit and observable.',
    keyPoints: [
      { text: 'Identity and sequences allocate surrogate values safely under concurrency.', detail: 'Use the engine’s atomic allocator and retrieve the value from the INSERT. Gaps are normal after rollback, caching, crashes, or failed statements; sequence values do not encode commit order.' },
      { text: 'Generated columns derive a row value from an expression.', detail: 'Virtual versus stored support and allowed expressions differ. They fit deterministic same-row derivations, not aggregates or cross-row business state.' },
      { text: 'Triggers react implicitly to data events.', detail: 'Row versus statement, BEFORE/AFTER/INSTEAD OF timing, transition data, recursion, and ordering differ by vendor. Multi-row statements and concurrent transactions must be correct.' },
      { text: 'Routines expose an explicit database operation.', detail: 'Procedures/functions can centralize set-based work and privilege boundaries, but transaction control, volatility, overloading, result sets, and deployment are vendor-specific.' },
      { text: 'Triggers do not serialize arbitrary read-then-write logic.', detail: 'Two transactions can both observe “available” stock and then update. Use atomic conditional DML, locks, constraints, or serializable transactions; a trigger must follow the same concurrency discipline.' },
      { text: 'Hidden work requires operational controls.', detail: 'Trigger cascades and routines affect latency, locks, replication, retry safety, and error surfaces. Keep them small, deterministic where possible, and documented beside the schema.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Choosing a database object', headers: ['Mechanism', 'Best fit', 'Main caution'], rows: [['Identity / sequence', 'Concurrent surrogate allocation', 'Gaps; not business chronology'], ['Generated column', 'Deterministic same-row expression', 'Vendor expression/storage limits'], ['Constraint', 'Declarative invariant', 'Only supported predicate scope'], ['Trigger', 'Unavoidable event reaction/audit', 'Implicit multi-row and concurrency behavior'], ['Routine', 'Explicit set-based operation or privilege API', 'Vendor coupling and transaction semantics']] },
      { kind: 'code', variants: [{ id: 'postgresql', label: 'PostgreSQL', language: 'sql', code: "CREATE TABLE inventory (\n  product_id BIGINT PRIMARY KEY REFERENCES products(product_id),\n  on_hand INTEGER NOT NULL CHECK (on_hand >= 0),\n  reserved INTEGER NOT NULL DEFAULT 0 CHECK (reserved >= 0),\n  available INTEGER GENERATED ALWAYS AS (on_hand - reserved) STORED,\n  CHECK (reserved <= on_hand)\n);\n\n-- One atomic statement: competing writers cannot both reserve the same units.\nUPDATE inventory SET reserved = reserved + :quantity\nWHERE product_id = :product_id\n  AND on_hand - reserved >= :quantity\nRETURNING product_id, available;" }, { id: 'sql-server', label: 'SQL Server', language: 'sql', code: "CREATE TABLE inventory (\n  product_id BIGINT PRIMARY KEY REFERENCES products(product_id),\n  on_hand INT NOT NULL CHECK (on_hand >= 0),\n  reserved INT NOT NULL DEFAULT 0 CHECK (reserved >= 0),\n  available AS (on_hand - reserved) PERSISTED,\n  CHECK (reserved <= on_hand)\n);\n\nUPDATE inventory WITH (UPDLOCK)\nSET reserved = reserved + @quantity\nOUTPUT inserted.product_id, inserted.available\nWHERE product_id = @product_id\n  AND on_hand - reserved >= @quantity;" }] },
      { kind: 'code', title: 'Isolated allocator demonstration: retrieve the generated key', variants: [{ id: 'postgresql', label: 'PostgreSQL', language: 'sql', code: "CREATE TABLE generated_key_allocator_demo (\n  allocation_id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  payload TEXT NOT NULL\n);\n\nINSERT INTO generated_key_allocator_demo (payload)\nVALUES ($1)\nRETURNING allocation_id;" }, { id: 'sql-server', label: 'SQL Server', language: 'sql', code: "CREATE TABLE generated_key_allocator_demo (\n  allocation_id BIGINT IDENTITY(1,1) PRIMARY KEY,\n  payload NVARCHAR(200) NOT NULL\n);\n\nINSERT INTO generated_key_allocator_demo (payload)\nOUTPUT inserted.allocation_id\nVALUES (@payload);" }, { id: 'sqlite', label: 'SQLite', language: 'sql', code: "CREATE TABLE generated_key_allocator_demo (\n  allocation_id INTEGER PRIMARY KEY,\n  payload TEXT NOT NULL\n);\n\nINSERT INTO generated_key_allocator_demo (payload)\nVALUES (?1)\nRETURNING allocation_id;" }, { id: 'oracle', label: 'Oracle', language: 'sql', code: "CREATE TABLE generated_key_allocator_demo (\n  allocation_id NUMBER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  payload VARCHAR2(200) NOT NULL\n);\n\nINSERT INTO generated_key_allocator_demo (payload)\nVALUES (:payload)\nRETURNING allocation_id INTO :new_allocation_id;" }, { id: 'mysql', label: 'MySQL', language: 'sql', code: "CREATE TABLE generated_key_allocator_demo (\n  allocation_id BIGINT AUTO_INCREMENT PRIMARY KEY,\n  payload VARCHAR(200) NOT NULL\n);\n\nINSERT INTO generated_key_allocator_demo (payload)\nVALUES (?);\nSELECT LAST_INSERT_ID(); -- same physical connection" }] },
      { kind: 'paragraph', text: 'This is an isolated allocator demonstration: it defines a separate table instead of changing how the canonical `orders` table allocates identifiers. The mechanisms are not interchangeable APIs. PostgreSQL and SQLite `RETURNING` and SQL Server `OUTPUT` produce rows; Oracle writes the value into an output bind; MySQL clients normally use their driver’s generated-keys API, whose connection and multirow semantics must be checked. `LAST_INSERT_ID()` is connection-specific and must run on the same physical connection; for a multirow insert it reports the first generated value. Never use `SELECT MAX(allocation_id)`, because another session can insert concurrently.' },
      { kind: 'paragraph', text: 'The added `inventory` object is defined before use. Its checks protect row-local bounds, while the conditional update makes availability testing and reservation one write. A trigger that first selects availability and later updates would still require locking or serialization and must handle a statement affecting many rows.' },
      { kind: 'pitfall', title: 'Assuming trigger execution is a queue', text: 'Triggers normally execute within the modifying transaction, can fire for multi-row statements, and can deadlock or recurse. They do not provide global event order or exactly-once external side effects.' },
      { kind: 'bestPractice', title: 'Prefer the least implicit mechanism', text: 'Start with types and constraints, use generated columns for deterministic row expressions, explicit routines for operations, and triggers only when every write path must react. Load-test concurrent writers and inspect locks, retries, and replication behavior.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: '§4.5.2 — Default values, generated identity and generated columns; §5.2 and §5.3 — Functions, procedures, triggers' }, { book: 'sql-antipatterns', chapter: 'Ch. 4 — ID Required; Ch. 23 — See No Evil' }, { book: 'postgresql-docs', chapter: '5.3 — Identity Columns; 5.4 — Generated Columns; 9.17 — Sequence Manipulation Functions; 36.3 — User-Defined Functions; 37.1 — Overview of Trigger Behavior' }, { book: 'sqlserver-docs', chapter: 'Specify computed columns; DML triggers; OUTPUT clause' }],
    related: ['primary-unique-and-check-constraints', 'transactions-and-acid', 'isolation-levels-and-anomalies'],
  },
]

import type { Topic } from '../../../types/content'

export const dbPerformanceTopics: Topic[] = [
  {
    id: 'index-mental-model', domainId: 'db-performance', title: 'The Index Mental Model',
    summary: 'An index is a maintained, redundant access path from searchable keys to rows. It can replace broad inspection with targeted navigation, but every extra structure consumes space and write work, and the planner may rationally choose another path.',
    keyPoints: [
      { text: 'Logical promise and physical structure differ.', detail: 'SQL declares keys, expressions, predicates, and uniqueness; an engine maps those semantics to B-trees, hashes, inverted structures, or other access methods.' },
      { text: 'An index maps keys to row locators or stored rows.', detail: 'A secondary leaf commonly contains a key plus a tuple/primary-key locator; clustered or index-organized layouts can store the base row at the leaf.' },
      { text: 'Access has navigation and fetch phases.', detail: 'Finding leaf entries can be cheap while scattered base-row lookups dominate; a broad sequential read can therefore be preferable.' },
      { text: 'Every write maintains applicable indexes.', detail: 'Insert, delete, and key-changing update work includes searching, logging, latching, splitting or merging, and later cleanup according to the engine.' },
      { text: 'An index is useful only through supported operators.', detail: 'Operator classes, collation, expression identity, column order, and predicate implication determine whether a query can use a structure.' },
      { text: 'Benefit belongs to a workload, not a table.', detail: 'Read frequency, selectivity, ordering, projected columns, write rate, cache state, and concurrency jointly determine value.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Commerce lookup and maintenance paths', code: 'flowchart LR\n  Q["Query: tenant 7, order A9"] --> P{"Planner compares paths"}\n  P --> S["scan orders pages"]\n  P --> I["seek index key"]\n  I --> L["leaf: key → row locator"]\n  L --> H["fetch visible order row"]\n  W["INSERT / key UPDATE / DELETE"] --> T["maintain table"]\n  W --> M["maintain every applicable index"]\n  M --> G["log + page work + possible split/cleanup"]' },
      { kind: 'table', caption: 'Cost ledger for `(tenant_id, external_id)`', headers: ['Operation', 'Potential gain', 'Potential cost'], rows: [['Point lookup', 'Navigate to a narrow key interval', 'Tree pages plus possible heap fetch'], ['Tenant range', 'Read adjacent matching entries', 'Many locators can mean scattered fetches'], ['Insert order', 'Uniqueness can be enforced during lookup', 'Extra search, WAL/logging, page modification'], ['Change indexed status', 'New searches become possible', 'Old entry removal and new entry insertion'], ['Delete', 'Future lookup avoided', 'Logical deletion, vacuum/purge or merge work varies']] },
      { kind: 'code', language: 'sql', title: 'One canonical commerce access path', code: 'CREATE UNIQUE INDEX orders_tenant_external_uq\n  ON orders (tenant_id, external_id);\n\nSELECT order_id, status, total_cents\nFROM orders\nWHERE tenant_id = 7 AND external_id = \'A9\';' },
      { kind: 'paragraph', text: 'The optimizer compares alternatives rather than obeying the mere existence of this index. If tenant 7 owns most rows, pages are already cached, or the query returns most columns and rows, scanning may cost less than navigating and fetching many scattered tuples.' },
      { kind: 'pitfall', title: 'Indexing every predicate independently', text: 'Redundant structures multiply write, cache, backup, replication, and maintenance cost. Separate single-column indexes also do not automatically provide the ordering or contiguous key interval of a matching composite index.' },
      { kind: 'bestPractice', title: 'Write a per-query access-path hypothesis', text: 'Record predicate, expected rows, ordering, projection, write rate, candidate path, and rejected alternatives; validate it with representative plans and runtime evidence.' },
    ],
    refs: [{ book: 'sql-performance-explained', chapter: 'The Anatomy of an Index; Slow Indexes, Part I' }, { book: 'database-system-concepts', chapter: 'Ch. 14 — Indexing' }, { book: 'database-internals', chapter: 'Ch. 2 — B-Tree Basics' }, { book: 'postgresql-docs', chapter: '11.1 — Introduction; 11.12 — Examining Index Usage' }],
    related: ['b-tree-indexes', 'cost-based-query-planning', 'explain-and-performance-methodology'],
  },
  {
    id: 'b-tree-indexes', domainId: 'db-performance', title: 'B-Tree Indexes',
    summary: 'A B-tree keeps sorted separator keys in a shallow, balanced page hierarchy. Navigation finds a leaf boundary; linked or successive leaves support ranges and order, while splits, occupancy, concurrency, and row fetches shape real cost.',
    keyPoints: [
      { text: 'Fan-out keeps trees shallow.', detail: 'A page stores many key/pointer pairs, so each internal level eliminates many subtrees; height depends on page size, key width, occupancy, and implementation.' },
      { text: 'Leaves hold the searchable order.', detail: 'Point lookup descends to one key interval; a range finds its lower bound and advances through leaf entries until the upper bound.' },
      { text: 'Balanced means bounded path length, not constant I/O.', detail: 'Root and upper pages are often cached, while leaf and heap locality, compression, and concurrency determine physical work.' },
      { text: 'Splits preserve order under growth.', detail: 'When a target page lacks room, entries are redistributed and a separator propagates upward; engines differ in fill factors, right-edge handling, and merge policy.' },
      { text: 'B-trees can satisfy ordering.', detail: 'A compatible forward or backward traversal may avoid an explicit sort; mixed directions and NULL ordering require a matching definition.' },
      { text: 'Wide or random keys change economics.', detail: 'They reduce fan-out and locality and can increase page churn; this is a workload tendency, not a universal prohibition.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Seek, range scan, and split', code: 'flowchart TD\n  R["root: [2025-01 | 2025-06]"] --> A["internal: earlier"]\n  R --> B["internal: 2025 H1"]\n  R --> C["internal: later"]\n  B --> L1["leaf: Apr keys"]\n  B --> L2["leaf: May keys"]\n  L1 -->|"next leaf: range continues"| L2\n  N["insert into full May leaf"] --> SP["split into May-low / May-high"]\n  SP --> UP["install separator in parent"]' },
      { kind: 'code', language: 'sql', title: 'Seek to a boundary, then walk the range', code: 'CREATE INDEX orders_created_idx ON orders (created_at);\n\nSELECT order_id, created_at\nFROM orders\nWHERE created_at >= TIMESTAMPTZ \'2026-05-01 00:00+00\'\n  AND created_at <  TIMESTAMPTZ \'2026-06-01 00:00+00\'\nORDER BY created_at;' },
      { kind: 'table', caption: 'Physical stages', headers: ['Stage', 'Mechanic', 'Cost drivers'], rows: [['Root/internal descent', 'Compare separator keys and choose child', 'Height, cache, key width'], ['Leaf positioning', 'Binary/search-method lookup within page', 'Page format and compression'], ['Range advance', 'Consume ordered leaf entries', 'Matching entries and leaf pages'], ['Row retrieval', 'Follow locator unless covered', 'Clustering, visibility, random access'], ['Insert/split', 'Place key; redistribute if full', 'Free space, key pattern, contention, logging']] },
      { kind: 'note', title: 'Tree height is not the whole query', text: 'A three-level descent does not mean three total reads: a range can visit many leaf pages and many base rows, while cached internal pages may require no storage I/O.' },
      { kind: 'pitfall', title: 'Calling B-tree lookup O(log n) and stopping', text: 'That abstraction hides page fan-out, range cardinality, cache state, tuple visibility, and fetch locality—the terms that often decide between index and scan.' },
      { kind: 'bestPractice', title: 'Keep ranges half-open', text: 'Use `>= start AND < next_boundary` for timestamps; it avoids precision-dependent end-of-day literals and maps cleanly to an ordered interval.' },
    ],
    refs: [{ book: 'database-internals', chapter: 'Ch. 2 — B-Tree Basics; B-Tree Node Splits' }, { book: 'database-system-concepts', chapter: 'Ch. 14 — Ordered Indices; B+-Tree Index Files' }, { book: 'sql-performance-explained', chapter: 'The Anatomy of an Index; Index Range Scan' }, { book: 'postgresql-docs', chapter: '11.2.1 — B-Tree; 11.4 — Indexes and ORDER BY' }],
    related: ['index-mental-model', 'composite-indexes', 'pages-records-and-file-layout'],
  },
  {
    id: 'non-btree-indexes', domainId: 'db-performance', title: 'Non-B-Tree Indexes',
    summary: 'Non-B-tree families encode different questions: exact equality, set membership, geometric relations, text containment, or value summaries over page ranges. Choose by operator semantics and workload, not by a generic “faster index” hierarchy.',
    keyPoints: [
      { text: 'Hash indexes target equality.', detail: 'Hashing routes equal keys to buckets but does not naturally preserve range or sort order; collision and growth mechanics are engine-specific.' },
      { text: 'Bitmaps represent sets compactly.', detail: 'Bitwise AND/OR combines predicates effectively for some low-to-moderate-cardinality analytical workloads; mutable row-oriented engines may materialize bitmaps per scan rather than store bitmap indexes.' },
      { text: 'Inverted indexes map terms to rows.', detail: 'GIN-like structures support arrays, documents, and full text by storing postings; writes and pending-list/merge maintenance can be substantial.' },
      { text: 'Spatial trees preserve spatial predicates.', detail: 'R-tree/GiST/SP-GiST families prune by bounding regions or partitions; lossy matches can require exact rechecks.' },
      { text: 'Block-range indexes summarize storage.', detail: 'BRIN stores min/max or other summaries for physical ranges, becoming tiny and useful when values correlate with row placement, but returns candidate ranges for recheck.' },
      { text: 'Operator support is the contract.', detail: 'An access method is usable only for operators supplied by its class/family; similar-looking types or predicates do not guarantee compatibility.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Workload-fit comparison', headers: ['Family', 'Natural question', 'Strong fit', 'Tradeoff / recheck'], rows: [['Hash', '`key = ?`', 'Exact-key lookup', 'No natural range/order; collisions'], ['Bitmap', 'set A AND/OR set B', 'Warehouse filters and index combination', 'Memory/maintenance; order lost'], ['Inverted / GIN', 'contains term/element', 'JSON, arrays, full text', 'Large postings and write amplification'], ['Spatial / GiST / SP-GiST', 'overlaps, contains, nearest', 'Geometry and partitionable spaces', 'May be lossy; operator-class dependent'], ['Block-range / BRIN', 'which page ranges may match?', 'Huge append-correlated tables', 'False positives and heap recheck']] },
      { kind: 'code', language: 'sql', title: 'PostgreSQL workload-specific structures', code: 'CREATE INDEX products_attrs_gin ON products USING gin (attributes);\nCREATE INDEX deliveries_area_gist ON deliveries USING gist (service_area);\nCREATE INDEX events_created_brin ON events USING brin (created_at);\n\nSELECT product_id FROM products\nWHERE attributes @> \'{"color":"blue"}\'::jsonb;' },
      { kind: 'paragraph', text: 'The GIN index answers containment by postings; it is not a substitute for a B-tree ordering of a scalar extracted from JSON. The BRIN example is most promising when creation time follows physical append order; correlation loss increases candidate page ranges.' },
      { kind: 'note', title: 'Bitmap scan is not necessarily a persistent bitmap index', text: 'PostgreSQL can combine ordinary index results into an in-memory bitmap, then visit heap pages in physical order. Other products expose persistent bitmap indexes, with different concurrency and maintenance properties.' },
      { kind: 'pitfall', title: 'Selecting by data type alone', text: 'JSON, text, and geometry each support multiple operators. Start with actual predicates—equality, containment, prefix, distance, overlap—and confirm the access method/operator class implements them.' },
      { kind: 'bestPractice', title: 'Measure false positives and write cost', text: 'For lossy or summary indexes inspect rechecks and heap blocks; for inverted structures observe build size, insertion latency, merge/pending work, and read latency under the real update mix.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 14 — Hash Indices; Bitmap Indices' }, { book: 'postgresql-docs', chapter: '11.2 — Index Types; 11.5 — Combining Multiple Indexes' }],
    related: ['b-tree-indexes', 'partial-functional-and-specialized-indexes', 'selectivity-cardinality-and-statistics'],
  },
  {
    id: 'composite-indexes', domainId: 'db-performance', title: 'Composite Indexes',
    summary: 'A composite B-tree sorts lexicographically: later columns refine equal prefixes. Column order therefore defines searchable intervals and useful output order; equality predicates usually extend the prefix, while the first inequality commonly bounds contiguous scanning.',
    keyPoints: [
      { text: 'Permutations are different indexes.', detail: '`(tenant_id, status, created_at)` and `(status, tenant_id, created_at)` have different global order and serve different leading-prefix workloads.' },
      { text: 'Equality extends a contiguous prefix.', detail: 'With equalities on tenant and status, a date range becomes one bounded interval in the three-column order.' },
      { text: 'The first range is a boundary.', detail: 'Columns after an inequality can sometimes be checked in-index or via skip-scan-like optimizations, but generally do not narrow the single contiguous interval in the same way.' },
      { text: 'Sort support follows key order.', detail: 'After equality-constrained leading columns, remaining key order may satisfy `ORDER BY`; direction and NULL placement must match engine rules.' },
      { text: 'Most selective first is not a law.', detail: 'Workload predicates, equality/range shape, ordering, grouping, reuse by other queries, compression, and engine capabilities matter more than a slogan.' },
      { text: 'Separate indexes are a different option.', detail: 'Bitmap/index intersection can combine predicates, but adds scans, loses index order, and may cost more than one matching composite path.' },
    ],
    blocks: [
      { kind: 'code', language: 'text', title: 'Lexicographic permutations', code: '(tenant, status, created):\n(7, PAID, 09:00) (7, PAID, 10:00) (7, SHIPPED, 08:00) (8, PAID, 07:00)\nQuery tenant=7 AND status=PAID AND created>=09:30 → one interval starting at (7,PAID,09:30).\n\n(status, tenant, created):\n(PAID,7,09:00) (PAID,7,10:00) (PAID,8,07:00) (SHIPPED,7,08:00)\nThis leads efficiently by status, but tenant-only rows are dispersed among every status.' },
      { kind: 'code', language: 'sql', title: 'Equality prefix, range, and order', code: 'CREATE INDEX orders_tenant_status_created_idx\n  ON orders (tenant_id, status, created_at DESC);\n\nSELECT order_id, created_at\nFROM orders\nWHERE tenant_id = 7\n  AND status = \'PAID\'\n  AND created_at >= TIMESTAMPTZ \'2026-05-01 00:00+00\'\nORDER BY created_at DESC\nLIMIT 50;' },
      { kind: 'table', caption: 'How predicates map to `(tenant_id, status, created_at)`', headers: ['Predicate', 'Typical B-tree consequence'], rows: [['tenant = 7', 'Leading prefix; contiguous tenant interval'], ['tenant = 7 AND status = PAID', 'Longer equality prefix'], ['tenant = 7 AND status = PAID AND created >= t', 'Bounded ordered range'], ['tenant = 7 AND status > PAID AND created = t', 'Status range bounds interval; created may filter entries'], ['status = PAID', 'No leading tenant equality; full/skip scan may or may not win'], ['tenant = 7 ORDER BY status, created', 'Remaining order is compatible']] },
      { kind: 'note', title: 'Skip scan is an optimization, not a portable design premise', text: 'Some engines can repeatedly probe later columns across few distinct leading values. Its availability and profitability depend on implementation and statistics; design the common leading-prefix path explicitly.' },
      { kind: 'pitfall', title: 'Adding every projected column to the key', text: 'Wider keys reduce fan-out and increase maintenance. Use included payload where supported and justified, and remember coverage still does not guarantee an index-only execution.' },
      { kind: 'bestPractice', title: 'Enumerate the query family', text: 'List each predicate combination and required order, then compare a small candidate set against representative reads and writes; retain an index only when its measured workload value exceeds its cost.' },
    ],
    refs: [{ book: 'sql-performance-explained', chapter: 'Concatenated Indexes; Indexing SQL WHERE Clauses' }, { book: 'database-system-concepts', chapter: 'Ch. 14 — Multiple-Key Access' }, { book: 'postgresql-docs', chapter: '11.3 — Multicolumn Indexes; 11.4 — Indexes and ORDER BY; 11.5 — Combining Multiple Indexes' }],
    related: ['b-tree-indexes', 'covering-and-index-only-scans', 'sargability-and-query-shape'],
  },
  {
    id: 'covering-and-index-only-scans', domainId: 'db-performance', title: 'Covering and Index-Only Scans',
    summary: 'A covering index contains every value needed by a query, making an index-only path possible. Whether it avoids base storage depends on visibility and implementation: PostgreSQL may still visit heap pages unless its visibility map proves all tuples on a page visible.',
    keyPoints: [
      { text: 'Coverage is query-relative.', detail: 'All predicate, join, projection, and sometimes ordering values required by one plan must be available from the index.' },
      { text: 'Key and included columns have different roles.', detail: 'Key columns determine search/order; included payload can cover output without participating in B-tree search semantics.' },
      { text: 'Index-only is a physical execution property.', detail: 'A covering definition enables but does not guarantee it; planner cost, access-method support, visibility, locking, and engine layout decide.' },
      { text: 'PostgreSQL visibility lives outside index tuples.', detail: 'The visibility map can certify an all-visible heap page; otherwise an index-only scan fetches the heap tuple to check MVCC visibility.' },
      { text: 'Payload width has a price.', detail: 'Larger leaves reduce fan-out and cache density, increase write/WAL volume, and can exceed entry-size limits.' },
      { text: 'Frequently changing columns weaken the case.', detail: 'Updates maintain payload and can clear all-visible state, producing both write amplification and heap fetches.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'PostgreSQL index-only decision', code: 'flowchart LR\n  Q["Need tenant,status,created,total"] --> IX["covering index entry"]\n  IX --> VM{"heap page all-visible?"}\n  VM -->|yes| OUT["return values from index"]\n  VM -->|no| HEAP["fetch heap tuple + visibility check"]\n  HEAP --> OUT' },
      { kind: 'code', language: 'sql', title: 'Search keys plus payload', code: 'CREATE INDEX orders_recent_lookup_idx\n  ON orders (tenant_id, status, created_at DESC)\n  INCLUDE (order_id, total_cents);\n\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT order_id, created_at, total_cents\nFROM orders\nWHERE tenant_id = 7 AND status = \'PAID\'\nORDER BY created_at DESC\nLIMIT 50;' },
      { kind: 'table', caption: 'Read the resulting evidence', headers: ['Observation', 'Interpretation'], rows: [['Index Only Scan', 'Plan can obtain query values from index'], ['Heap Fetches: 0', 'Visibility checks avoided heap for this run'], ['Heap Fetches > 0', 'Coverage exists, but some heap visibility checks occurred'], ['High buffers / wide index', 'Coverage may cost cache capacity'], ['Frequent updates', 'Payload maintenance and all-visible churn may offset read gain']] },
      { kind: 'paragraph', text: 'Other engines may store visibility or clustered rows differently, so PostgreSQL’s visibility-map caveat is not vendor-universal. Conversely, the generic term “covering” must not be read as a promise of zero base-table work everywhere.' },
      { kind: 'pitfall', title: 'Covering `SELECT *`', text: 'Replicating a wide row in many secondary indexes bloats storage and write work. Project the response contract narrowly and cover only stable, high-value query shapes.' },
      { kind: 'bestPractice', title: 'Compare total workload cost', text: 'Measure heap fetches, buffers, index bytes, update throughput, vacuum behavior, and latency percentiles before and after; remove the covering variant if its saved reads do not justify maintenance.' },
    ],
    refs: [{ book: 'sql-performance-explained', chapter: 'Index-Only Scan; Clustering Data' }, { book: 'database-system-concepts', chapter: 'Ch. 14 — Secondary Indices; Multilevel Indices' }, { book: 'postgresql-docs', chapter: '11.9 — Index-Only Scans and Covering Indexes' }, { book: 'postgresql-internals', chapter: 'Ch. 4 — Indexes; Ch. 6 — Vacuum and Autovacuum' }],
    related: ['composite-indexes', 'mvcc', 'explain-and-performance-methodology'],
  },
  {
    id: 'partial-functional-and-specialized-indexes', domainId: 'db-performance', title: 'Partial, Functional, and Specialized Indexes',
    summary: 'Specialized indexes encode a narrower population, a computed search key, uniqueness, or type-specific operator semantics. They are powerful when query text and invariants match their contract, but deliberately serve fewer shapes.',
    keyPoints: [
      { text: 'Partial indexes store only qualifying rows.', detail: 'They reduce size and maintenance for a stable subset; the planner must be able to prove the query predicate implies the index predicate.' },
      { text: 'Expression indexes store computed keys.', detail: 'Queries must use a matching expression under engine rules; function volatility/determinism, collation, casts, and session semantics matter.' },
      { text: 'Unique indexes enforce an invariant.', detail: 'They are both access path and concurrency-safe constraint mechanism; NULL and partial uniqueness semantics vary by product.' },
      { text: 'Partial uniqueness expresses scoped identity.', detail: 'For example, one active cart per customer can be enforced only for rows where status is active.' },
      { text: 'Specialized operator classes define meaning.', detail: 'A trigram, JSON containment, geometry, or full-text index supports specific operators, not arbitrary functions over the same column.' },
      { text: 'Prepared parameters can obstruct implication.', detail: 'A generic parameterized predicate may not prove a fixed partial-index condition at planning time; plan caching behavior can change applicability.' },
    ],
    blocks: [
      { kind: 'code', language: 'sql', title: 'PostgreSQL specialized contracts', code: '-- Small queue index: query must imply shipped_at IS NULL.\nCREATE INDEX orders_unshipped_idx\n  ON orders (tenant_id, created_at) WHERE shipped_at IS NULL;\n\n-- Case-folded identity.\nCREATE UNIQUE INDEX customers_email_ci_uq\n  ON customers (lower(email));\n\n-- One open cart per customer.\nCREATE UNIQUE INDEX carts_one_open_uq\n  ON carts (customer_id) WHERE status = \'OPEN\';\n\nSELECT order_id FROM orders\nWHERE tenant_id = 7 AND shipped_at IS NULL\nORDER BY created_at;' },
      { kind: 'table', caption: 'Contract and failure mode', headers: ['Kind', 'Contract', 'Common mismatch'], rows: [['Partial / filtered', 'Query implies stored-row predicate', 'Parameterized or logically different predicate'], ['Expression / function-based', 'Query expression matches indexed expression', 'Implicit cast, collation, different function'], ['Unique', 'No duplicate index keys under engine NULL rules', 'Assuming business normalization not encoded'], ['GIN/GiST/trigram/etc.', 'Operator belongs to configured class/family', 'Using unsupported operator or function']] },
      { kind: 'paragraph', text: 'The partial queue index omits shipped orders, so their writes avoid this structure and queue scans touch fewer entries. It cannot answer historical shipped-order queries. The unique cart index enforces the scoped invariant atomically under concurrency, which an application “check then insert” cannot.' },
      { kind: 'note', title: 'Normalize deliberately', text: 'An index on `lower(email)` enforces equality of that expression, not every notion of email equivalence. Decide trimming, Unicode normalization, collation, and domain-specific rules before declaring it the identity.' },
      { kind: 'pitfall', title: 'Hiding a mismatch behind an expression index', text: 'Indexing `date(created_at)` may serve that exact expression but can sacrifice timestamp range flexibility. A half-open base-column range is often clearer and reusable.' },
      { kind: 'bestPractice', title: 'Test applicability as part of the contract', text: 'Keep representative literal and prepared query forms, inspect plans after upgrades, and test duplicate/concurrent writes for every specialized uniqueness rule.' },
    ],
    refs: [{ book: 'sql-performance-explained', chapter: 'Functions; Partial Indexes' }, { book: 'database-system-concepts', chapter: 'Ch. 14 — Indices on Multiple Keys' }, { book: 'postgresql-docs', chapter: '11.6 — Unique Indexes; 11.7 — Indexes on Expressions; 11.8 — Partial Indexes; 11.10 — Operator Classes and Operator Families' }],
    related: ['non-btree-indexes', 'sargability-and-query-shape', 'primary-unique-and-check-constraints'],
  },
  {
    id: 'selectivity-cardinality-and-statistics', domainId: 'db-performance', title: 'Selectivity, Cardinality, and Statistics',
    summary: 'Cardinality estimates predict row counts at each plan node; selectivity is the retained fraction. Planners derive them from sampled summaries and assumptions, so skew, correlation, cross-column dependence, parameters, and stale data can compound into wrong plan choices.',
    keyPoints: [
      { text: 'Cardinality propagates through the plan.', detail: 'Estimated rows influence scan, join, memory, aggregation, and parallelism choices; an early multiplicative error can magnify above it.' },
      { text: 'Distinct counts and frequencies model equality.', detail: 'Most-common-value lists capture skewed heavy hitters; remaining values are often modeled more uniformly.' },
      { text: 'Histograms model ranges.', detail: 'Bucket boundaries approximate cumulative distribution; they cannot preserve every local spike and depend on sample and target size.' },
      { text: 'Correlation has multiple meanings.', detail: 'Physical-order correlation affects fetch cost, while statistical dependence between predicates invalidates naïve independence multiplication.' },
      { text: 'Multicolumn statistics repair selected dependencies.', detail: 'PostgreSQL extended statistics can record combinations such as dependencies, distinct counts, and multivariate most-common values, but only when created and analyzed.' },
      { text: 'Statistics age with the data.', detail: 'Bulk loads, changing tenant mix, new hot values, and ascending keys can make a previously reasonable summary misleading until analyze refreshes it.' },
    ],
    blocks: [
      { kind: 'code', language: 'text', title: 'Independence error in commerce data', code: '1,000,000 orders. status=RETURNED: 20,000 (2%). region=EU: 300,000 (30%).\nIndependence estimate: 1,000,000 × 0.02 × 0.30 = 6,000 rows.\nObserved EU returns after a recall: 18,000 rows.\nThe 3× miss can change join build side, memory grant, scan choice, and spill risk.' },
      { kind: 'code', language: 'sql', title: 'Teach PostgreSQL a cross-column relationship', code: 'CREATE STATISTICS orders_region_status_stats\n  (dependencies, mcv, ndistinct)\n  ON region, status FROM orders;\nANALYZE orders;\n\nSELECT attname, n_distinct, most_common_vals, histogram_bounds, correlation\nFROM pg_stats\nWHERE schemaname = \'public\' AND tablename = \'orders\';' },
      { kind: 'table', caption: 'Summary, question, blind spot', headers: ['Statistic', 'Helps estimate', 'Blind spot'], rows: [['`n_distinct`', 'Equality/group counts', 'Individual skew'], ['MCV + frequencies', 'Hot equality values', 'Long tail'], ['Histogram bounds', 'Range fraction', 'Within-bucket spikes'], ['Physical correlation', 'Ordered index fetch locality', 'Predicate dependence'], ['Extended dependencies/MCV', 'Cross-column combinations', 'Untracked columns/expressions']] },
      { kind: 'paragraph', text: 'Selectivity does not by itself determine index usefulness. Ten thousand physically adjacent rows can be cheaper than one thousand scattered heap fetches; coverage, clustering, cache, row width, and limit/order behavior belong in the cost model.' },
      { kind: 'pitfall', title: 'Reading one top-level estimate', text: 'Compare estimated versus actual rows at every node and account for `loops`; the first major divergence usually explains downstream algorithm and memory mistakes better than the final count.' },
      { kind: 'bestPractice', title: 'Preserve production-shaped distributions', text: 'Benchmark with realistic scale, tenant sizes, hot values, null rates, correlations, and recent growth. Refresh statistics after representative loading before diagnosing a plan.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 16 — Estimation of Statistics of Expression Results' }, { book: 'sql-performance-explained', chapter: 'The Where Clause; Cardinality and Selectivity' }, { book: 'selinger-access-path', chapter: 'Access Path for Single Relations; Join Cardinality' }, { book: 'postgresql-docs', chapter: '14.2 — Statistics Used by the Planner; 69.2 — Multivariate Statistics Examples' }],
    related: ['cost-based-query-planning', 'explain-and-performance-methodology', 'composite-indexes'],
  },
  {
    id: 'sargability-and-query-shape', domainId: 'db-performance', title: 'Sargability and Query Shape',
    summary: 'A predicate is sargable when it can become a search argument for an access method, rather than merely filtering rows after retrieval. Preserve indexed expressions, compatible types, bounded ranges, and indexable Boolean branches without changing query semantics.',
    keyPoints: [
      { text: 'Functions on the indexed column can hide its order.', detail: 'Rewrite to a base-column range when equivalent, or deliberately index the exact immutable/deterministic expression.' },
      { text: 'Implicit conversion direction matters.', detail: 'Casting every stored value can prevent search; use a parameter of the column type and verify collation/time-zone semantics.' },
      { text: 'Leading wildcards lack a B-tree prefix.', detail: '`LIKE \'shoe%\'` can map to a prefix range under suitable operator/collation rules; `%shoe` needs another structure such as trigram/full text.' },
      { text: 'Arithmetic should isolate the column.', detail: '`price * 1.2 < x` can sometimes become `price < x/1.2`, but rounding, overflow, NULL, and numeric type rules must remain equivalent.' },
      { text: 'OR requires usable branches.', detail: 'An optimizer may combine indexes or transform to unions, but one broad/nonindexable branch can dominate; preserve duplicate and NULL semantics when rewriting.' },
      { text: 'Sargable does not mean faster.', detail: 'It only makes an access path eligible. Cardinality and cost can still favor a scan.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Before and semantics-preserving direction', headers: ['Shape that can block search', 'Candidate rewrite / structure'], rows: [['`date(created_at) = DATE \'2026-05-01\'`', '`created_at >= start AND created_at < next_day` using explicit zone'], ['`CAST(order_id AS text) = :text_id`', 'Bind `:id` as the column type'], ['`sku LIKE \'%ABC\'`', 'Trigram/reverse-key/suffix design if truly required'], ['`total_cents + 500 > 10000`', '`total_cents > 9500` after overflow/type check'], ['`tenant_id=7 OR lower(email)=:e`', 'Indexes for both branches or justified `UNION`; retain set semantics']] },
      { kind: 'code', language: 'sql', title: 'Timestamp and prefix rewrites', code: '-- Before: applies a function to every candidate timestamp.\nWHERE date(created_at AT TIME ZONE \'Europe/Belgrade\') = DATE \'2026-05-01\'\n\n-- After: compute these UTC boundaries once in the application.\nWHERE created_at >= TIMESTAMPTZ \'2026-04-30 22:00+00\'\n  AND created_at <  TIMESTAMPTZ \'2026-05-01 22:00+00\'\n\n-- Prefix, subject to matching collation/operator class.\nWHERE sku LIKE \'ABC%\';' },
      { kind: 'code', language: 'sql', title: 'OR shape with explicit set semantics', code: '-- UNION removes duplicates like this OR result; UNION ALL would not.\nSELECT order_id FROM orders WHERE tenant_id = 7\nUNION\nSELECT order_id FROM orders WHERE customer_email = :email;\n-- Compare with the original plan: PostgreSQL may already BitmapOr indexes.' },
      { kind: 'note', title: 'Compute time boundaries with domain semantics', text: 'A civil day is not always 24 hours. Resolve the requested local zone and daylight-saving rules, then bind half-open instants; do not blindly add 86,400 seconds.' },
      { kind: 'pitfall', title: 'Rewriting until an index appears', text: 'A changed plan is not evidence of improvement, and algebraic rewrites can change rounding, collation, timezone, NULL, or duplicate behavior. Test result equivalence before performance.' },
      { kind: 'bestPractice', title: 'Separate eligibility from profitability', text: 'First verify the condition appears as `Index Cond` rather than residual `Filter`; then compare rows, buffers, time, and write cost against the original on representative data.' },
    ],
    refs: [{ book: 'sql-performance-explained', chapter: 'Obfuscated Conditions; Searching for Ranges; LIKE Filters' }, { book: 'database-system-concepts', chapter: 'Ch. 15 — Selection Operation' }, { book: 'postgresql-docs', chapter: '11.2 — Index Types; 11.5 — Combining Multiple Indexes; 11.7 — Indexes on Expressions' }],
    related: ['partial-functional-and-specialized-indexes', 'composite-indexes', 'explain-and-performance-methodology'],
  },
  {
    id: 'cost-based-query-planning', domainId: 'db-performance', title: 'Cost-Based Query Planning',
    summary: 'A cost-based optimizer enumerates semantically valid physical alternatives, estimates intermediate cardinalities and resource work, and chooses the lowest modeled cost. Selinger’s System R work established dynamic-programming join-order search and “interesting orders”; modern engines extend and prune this space.',
    keyPoints: [
      { text: 'Optimization maps logical algebra to physical operators.', detail: 'Equivalent rewrites, access paths, join orders, and algorithms form a search space; legality constraints from outer joins, correlation, and volatility restrict it.' },
      { text: 'Selinger search builds plans by relation subsets.', detail: 'System R retained cheapest paths for successively larger join subsets, including alternatives whose useful output ordering could pay off later.' },
      { text: 'Cardinality drives cost.', detail: 'Estimated intermediate rows multiply CPU, I/O, network, and memory terms; estimation errors can make a bad alternative look cheapest.' },
      { text: 'Cost units are model units, not elapsed time.', detail: 'PostgreSQL combines configurable relative page and CPU costs; values compare plans for one query and are not milliseconds.' },
      { text: 'Search is deliberately bounded.', detail: 'Exhaustive join permutations grow rapidly, so optimizers use dynamic programming limits, heuristics, transformations, and sometimes randomized search.' },
      { text: 'Parameters can cross plan regimes.', detail: 'A hot tenant may favor a scan while a rare tenant favors an index; generic/custom plan caching, parameter sniffing, and changing statistics can cause instability.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Selinger-style plan construction', code: 'flowchart TD\n  L["logical query: O ⋈ I ⋈ P"] --> S1["size 1: cheapest scan + interesting orders"]\n  S1 --> S2["size 2: legal joins and algorithms"]\n  S2 --> S3["size 3: extend retained subset plans"]\n  S3 --> C["compare estimated total costs"]\n  C --> P["chosen executable plan"]\n  ST["statistics + parameters"] --> S1\n  CM["CPU/I/O/memory cost model"] --> C' },
      { kind: 'table', caption: 'Candidate commerce plans', headers: ['Alternative', 'Looks attractive when', 'Can lose when'], rows: [['Index orders → nested-loop items', 'Few orders; indexed item lookup', 'Popular tenant returns many orders'], ['Hash items, scan orders', 'Large unsorted equijoin; hash fits', 'Build spills or input is tiny'], ['Ordered scans → merge join', 'Inputs already ordered / order reused', 'Sorting both inputs dominates'], ['Scan + filter', 'Large fraction needed; sequential locality', 'Predicate is rare and covering index exists']] },
      { kind: 'code', language: 'sql', title: 'Expose parameter sensitivity safely', code: 'EXPLAIN (ANALYZE, BUFFERS, SETTINGS)\nSELECT o.order_id, sum(i.quantity * i.unit_cents)\nFROM orders o JOIN order_items i USING (order_id)\nWHERE o.tenant_id = 7 AND o.created_at >= :from_time\nGROUP BY o.order_id;\n-- Repeat for representative small, median, and hot tenants; do not infer from one literal.' },
      { kind: 'paragraph', text: 'An estimated cost of `1200` is not 1.2 seconds. It is the optimizer’s weighted prediction under its configuration. Actual elapsed time also includes cache state, contention, parallel scheduling, client effects, storage, and estimation error.' },
      { kind: 'pitfall', title: 'Freezing one observed plan', text: 'Hints or forced plans can mask stale statistics or model problems and become wrong as distributions change. If used, treat them as versioned, monitored operational controls with regression tests.' },
      { kind: 'bestPractice', title: 'Find the first estimation divergence', text: 'Compare estimated and actual rows bottom-up across parameter classes. Repair data/statistics/query-shape causes before changing global cost constants or pinning a plan.' },
    ],
    refs: [{ book: 'selinger-access-path', chapter: 'Access Path Selection in a Relational Database Management System — Access Path for Joins' }, { book: 'database-system-concepts', chapter: 'Ch. 16 — Query Optimization' }, { book: 'sql-performance-explained', chapter: 'Execution Plans; Bind Parameters' }, { book: 'postgresql-docs', chapter: '14.1 — Using EXPLAIN; 19.7 — Query Planning' }],
    related: ['selectivity-cardinality-and-statistics', 'join-sort-and-aggregation-algorithms', 'explain-and-performance-methodology'],
  },
  {
    id: 'join-sort-and-aggregation-algorithms', domainId: 'db-performance', title: 'Join, Sort, and Aggregation Algorithms',
    summary: 'Physical operators trade startup, ordering, memory, repeated lookup, and spill behavior. Nested loop, hash, and merge joins—and hash versus sort/stream aggregation—are alternatives selected from input size, predicates, order, indexes, memory, and estimates.',
    keyPoints: [
      { text: 'Nested loop repeats inner work.', detail: 'It excels with few outer rows and a cheap indexed inner probe; without that, repeated scans can multiply cost.' },
      { text: 'Hash join targets equijoins.', detail: 'One input builds a hash table and the other probes it; build-side size, skew, memory, batching, and spill determine behavior.' },
      { text: 'Merge join consumes compatible order.', detail: 'Sorted inputs advance together and can handle duplicates in groups; preexisting index order helps, otherwise sort startup may dominate.' },
      { text: 'Sort is blocking but reusable.', detail: 'It must consume input before ordinary output, uses bounded memory, and spills to external runs; order can also serve merge join, grouping, windowing, or final output.' },
      { text: 'Hash aggregation stores groups.', detail: 'It avoids ordering when groups fit; underestimated groups or wide states can batch/spill. Ordered/stream aggregation uses sorted input with a small current state.' },
      { text: 'Memory is per active operator/worker.', detail: 'A setting is not necessarily a query-wide cap; concurrent nodes and parallel workers can multiply allocation and spill pressure.' },
    ],
    blocks: [
      { kind: 'table', caption: 'Algorithm comparison', headers: ['Operator', 'Input requirement', 'Strength', 'Failure mode'], rows: [['Nested loop', 'Any join; efficient inner access helps', 'Low startup for tiny outer input', 'Repeated inner work'], ['Hash join', 'Hashable equality', 'Linear-style build/probe when fit', 'Skew, batches, disk spill'], ['Merge join', 'Merge-compatible equality/order', 'Streaming through sorted inputs', 'Sort cost and duplicate groups'], ['Hash aggregate', 'Hashable group keys', 'No pre-sort', 'Too many/wide groups spill'], ['Sort/stream aggregate', 'Grouped order', 'Small streaming state after order', 'Sort startup/spill if order absent']] },
      { kind: 'diagram', title: 'Same join, three physical shapes', code: 'flowchart LR\n  O["filtered orders"] --> NL["Nested Loop"]\n  IX["probe items(order_id)"] --> NL\n  O --> HJ["Hash Join"]\n  HB["hash one input"] --> HJ\n  SO["orders sorted by id"] --> MJ["Merge Join"]\n  SI["items sorted by id"] --> MJ' },
      { kind: 'code', language: 'sql', title: 'Inspect memory and spill evidence', code: 'EXPLAIN (ANALYZE, BUFFERS)\nSELECT o.tenant_id, p.category_id, sum(i.quantity * i.unit_cents) AS revenue\nFROM orders o\nJOIN order_items i USING (order_id)\nJOIN products p USING (product_id)\nWHERE o.created_at >= :from_time\nGROUP BY o.tenant_id, p.category_id;\n-- Look for loops, Sort Method/Disk, hash Buckets/Batches/Memory Usage, and temp I/O.' },
      { kind: 'paragraph', text: '“Small build side” means the side’s actual filtered width and rows, not its base-table size. Projection and early filters matter. A cardinality miss can reverse the intended build/probe economics and cause an unexpected multi-batch hash.' },
      { kind: 'pitfall', title: 'Globally raising memory after one spill', text: 'Concurrent sorts, hashes, nodes, and workers may multiply the setting into unsafe aggregate memory. First fix estimates/query shape, reduce rows or width, and quantify concurrency.' },
      { kind: 'bestPractice', title: 'Read operator evidence with loops', text: 'Multiply per-loop rows/time where appropriate, identify ordering provenance, record peak memory and temporary I/O, and compare warm/cold representative runs rather than declaring one algorithm universally superior.' },
    ],
    refs: [{ book: 'database-system-concepts', chapter: 'Ch. 15 — Join Operation; Sorting; Aggregation' }, { book: 'sql-performance-explained', chapter: 'Nested Loops; Hash Join; Sort Merge Join' }, { book: 'selinger-access-path', chapter: 'Access Path for Joins' }, { book: 'postgresql-docs', chapter: '14.1 — Using EXPLAIN; 19.7 — Query Planning' }],
    related: ['cost-based-query-planning', 'selectivity-cardinality-and-statistics', 'explain-and-performance-methodology'],
  },
  {
    id: 'explain-and-performance-methodology', domainId: 'db-performance', title: 'EXPLAIN and Performance Methodology',
    summary: 'Performance work is a controlled evidence loop: define the workload and correctness, capture a plan with estimates and safe runtime metrics, locate the first divergence or dominant work, change one hypothesis, and regression-test latency, resources, and writes.',
    keyPoints: [
      { text: '`EXPLAIN` predicts; `ANALYZE` executes.', detail: 'Plain explain is safer for mutations and expensive statements. Runtime explain adds actual rows/timing and can have real side effects unless wrapped and rolled back appropriately.' },
      { text: 'Plan trees flow bottom-up.', detail: 'Indentation shows children; compare estimates with actual rows and loops, then inspect filters, buffers, heap fetches, sorts, hash batches, temp I/O, and parallel workers.' },
      { text: 'Estimated cost is not time.', detail: 'Startup/total cost are relative model units. Actual time is environment-dependent and instrumentation itself adds overhead.' },
      { text: 'Cache state is an experimental variable.', detail: 'Cold OS/storage, warm database buffers, and hot repeated execution answer different production questions; record rather than obscure them.' },
      { text: 'Data and parameters must be representative.', detail: 'Scale, skew, correlation, row width, dead tuples, statistics age, hot tenants, and concurrency distinguish a useful benchmark from a toy.' },
      { text: 'Regression controls include the write path.', detail: 'Track plan/latency distributions, buffers/temp bytes, index size, write throughput, maintenance, and lock impact across deployments and data growth.' },
    ],
    blocks: [
      { kind: 'diagram', title: 'Evidence-driven tuning loop', code: 'flowchart LR\n  B["Baseline: query + data + params + concurrency"] --> E["EXPLAIN / safe ANALYZE evidence"]\n  E --> D["First estimate miss or dominant work"]\n  D --> H["One falsifiable hypothesis"]\n  H --> C["One controlled change"]\n  C --> V["Correctness + read/write benchmark"]\n  V --> R{"Improves target without unacceptable regression?"}\n  R -->|yes| G["Regression guard + monitor"]\n  R -->|no| B' },
      { kind: 'code', language: 'sql', title: 'PostgreSQL read-only evidence capture', code: 'BEGIN READ ONLY;\nSET LOCAL statement_timeout = \'5s\';\nEXPLAIN (ANALYZE, BUFFERS, WAL, SETTINGS, FORMAT JSON)\nSELECT order_id, status, total_cents\nFROM orders\nWHERE tenant_id = :tenant_id\n  AND created_at >= :from_time\nORDER BY created_at DESC\nLIMIT 100;\nROLLBACK;\n-- READ ONLY protects against accidental writes, not expensive reads or user-defined side effects.' },
      { kind: 'table', caption: 'Plan-reading checklist', headers: ['Evidence', 'Question'], rows: [['Actual vs estimated rows × loops', 'Where does the first large divergence begin?'], ['Index Cond vs Filter / Rows Removed', 'Which predicate navigates and which only rejects?'], ['Buffers read/hit/dirtied', 'What page work occurred in this cache state?'], ['Heap Fetches', 'Did an index-only path still visit heap?'], ['Sort Method / Disk; Hash Batches', 'Did bounded memory spill?'], ['Planning vs execution time', 'Is compilation or execution the target?']] },
      { kind: 'paragraph', text: 'Run multiple iterations and report distributions, not the fastest sample. Separate client/network time from server execution; control connection/session settings; refresh statistics; preserve both selective and hot parameter classes; and benchmark at production-like concurrency when contention matters.' },
      { kind: 'pitfall', title: 'Running `EXPLAIN ANALYZE DELETE` on production', text: '`ANALYZE` executes the statement. A surrounding rollback can protect transactional DML, but sequences, external functions, locks, triggers, and resource consumption can still have effects. Reproduce safely or use plain `EXPLAIN`.' },
      { kind: 'bestPractice', title: 'Ship a measurable performance contract', text: 'Store query fingerprint, schema/index version, representative parameter buckets, plan JSON, latency percentiles, rows, buffers, temp bytes, and write/maintenance deltas; alert on sustained regression, not harmless plan text changes.' },
    ],
    refs: [{ book: 'sql-performance-explained', chapter: 'Execution Plans; Statistics' }, { book: 'database-system-concepts', chapter: 'Ch. 15 — Measures of Query Cost; Ch. 16 — Query Optimization' }, { book: 'postgresql-docs', chapter: '14.1 — Using EXPLAIN; 11.12 — Examining Index Usage; 27.2 — The Cumulative Statistics System' }, { book: 'database-reliability-engineering', chapter: 'Ch. 5 — Infrastructure Engineering; Ch. 6 — Infrastructure Management' }],
    related: ['cost-based-query-planning', 'selectivity-cardinality-and-statistics', 'join-sort-and-aggregation-algorithms'],
  },
]
